# Closed scenarios

Every scenario that has reached 🟢 Done or ⚪ Blocked / Out of Scope,
moved here **verbatim** by `UX-232` when the backlog passed 234 rows.
The open ones live in [README.md](README.md); a row moves here in the
same commit that flips its marker.

History is preserved, not rewritten: these rows carry the full
what-was-built narrative they were written with. The index job — what
is open, on what topic, for whom — is the other file's.

| ID | Scenario | Priority | Depends on | Status | Task File |
|---|---|---|---|---|---|
| UX-01 | No built-in way to compare two runs (baseline vs. after-a-change) - real friction for the iterative-optimization workflow | High | — | 🟢 Done — new `bga compare` subcommand | [UX-01](UX-0001-run-comparison-command.md) |
| UX-02 | No composite "how efficient is this build, is it good enough" signal - only fragmented raw numbers (certified_headroom in absolute time, confidence separately) | High | — | 🟢 Done — new `efficiency_score` floor, banded + confidence-gated | [UX-02](UX-0002-composite-efficiency-score.md) |
| UX-03 | No CI-friendly gate to fail a pipeline when a build genuinely regressed | Medium | UX-01, UX-02 | 🟢 Done — `bga compare --fail-on-regression`, distinct exit code 4, fails open on low confidence | [UX-03](UX-0003-ci-regression-gate.md) |
| UX-04 | "Biggest Opportunity" names an attribution category but not what it means or what to do about it | Medium | — | 🟢 Done — static per-category hint shown under Biggest Opportunity, additive `attribution_hints` in JSON | [UX-04](UX-0004-attribution-category-next-step-hints.md) |
| UX-05 | No worked, narrative "iteratively optimize a real project" tutorial - only command-reference-style docs | Medium | UX-01, UX-02 (content), not blocking to start | 🟢 Done — real 2-iteration walkthrough, 48.1% improvement | [UX-05](UX-0005-optimization-walkthrough-tutorial.md) |
| UX-06 | `--format raw` corrupts cross-task ordering on real multi-task logs (BuildStream's elapsed prefix is per-activity, not session-wide) - likely affects examples 01-03's historical CI numbers too | High | — | 🟢 Done — watermark+depth-anchor reconstruction, validated against real dual-captured ground truth | [UX-06](UX-0006-raw-log-timestamp-corruption.md) |
| UX-07 | `run_identity.manifest_hash` collides for two different projects living in the same git repo/commit with the same target name | Medium | — (see Recommended order for a real, non-blocking sequencing note vs. UX-18) | 🟢 Done — manifest gained `project_identity` (path relative to the project's git repo root, or resolved absolute path outside one); real sibling-project repro no longer collides | [UX-07](UX-0007-run-identity-collides-across-sibling-projects.md) |
| UX-09 | Does `--builders` and native `max-jobs` genuinely compete for the same CPU cores? (real question, real evidence gathered) | High | — | 🟢 Done — confirmed with source citations + a real 6-configuration timing table | [UX-09](UX-0009-builders-max-jobs-joint-optimization.md) |
| UX-10 | `Total Duration`/`bga compare`'s verdict is computed from tracked-task span only, not real wall-clock - can miss real pre-task overhead (sandbox staging, cache query) entirely | High | — | 🟢 Done — `total_duration_us` now prefers real wall-clock per spec Part 4.3 | [UX-10](UX-0010-total-duration-excludes-pre-task-overhead.md) |
| UX-11 | A tool to observe native-build-system behavior *inside* a single element (no jobserver, no remote-execution visibility today) | Medium | UX-09 | 🟢 Done — new standalone `tools/bst_native_build_tracer.py` (+ `tools/native_trace/`), the validated `LD_PRELOAD`+`bwrap`-shim mechanism; real CLI run against `examples/05-cmake-cpp-toolchain` traced 98 real processes incl. reproduced `-j4` concurrency evidence; 28 new tests | [UX-11](UX-0011-native-build-system-profiler-tool.md) |
| UX-12 | `bga` captures neither the real native `--max-jobs` value nor host CPU core count - can't even show "you're oversubscribed" per UX-09's own finding | High | UX-09 | 🟢 Done — `--native-max-jobs`/`host_cpu_count` captured, real oversubscription/undersubscription caveat | [UX-12](UX-0012-capture-native-max-jobs-and-host-cores.md) |
| UX-13 | `LB`/Certified Headroom report text never says it certifies against BuildStream's own dispatch capacity, not real host CPU cores | Medium | UX-09 | 🟢 Done — Certified Floors block/JSON now carry a real capacity-model caveat, enriched with UX-12's numbers when available | [UX-13](UX-0013-lb-report-conflates-scheduling-and-cpu-capacity.md) |
| UX-14 | `bga sweep --resource PROCESS`/`bga replay` use fixed per-task durations - structurally can't represent the real contention-driven slowdown UX-09 measured (8×8 slower than 4×4); no caveat shown to the user | High | UX-09 | 🟢 Done — tier-1 caveat + tier-2 `--calibration-dir` contention model both shipped; real re-verification against `examples/05` at 4×8/8×8 shows real predicted degradation (6.21s→6.82s), not a plateau | [UX-14](UX-0014-sweep-replay-blind-to-contention-slowdown.md) |
| UX-15 | A `bga` user may want the tool's capacity checks to respect their own declared CPU budget (e.g. a cgroup CFS quota `os.sched_getaffinity` can't see), not raw detected host cores | Medium | UX-12 | 🟢 Done — `--cpu-budget` governs oversubscription checks over detected `host_cpu_count` when declared | [UX-15](UX-0015-declared-cpu-budget-overrides-host-detection.md) |
| UX-16 | `native_max_jobs`/`cpu_budget`/`host_cpu_count` of `0` silently treated as "missing" by `_check_process_oversubscription`'s truthiness gate - BuildStream's own real `max-jobs=0` sentinel value goes undetected | High | UX-12, UX-15 | 🟢 Done — explicit `is None` checks + real `0`→`min(cores,8)` resolution, both violation dicts and report text carry a `native_max_jobs_was_auto` provenance flag | [UX-16](UX-0016-max-jobs-zero-treated-as-missing.md) |
| UX-17 | `UtilizationAnalyzer`'s own Part 30.3 oversubscription check is dead code - `builders` is never wired in, would compute `builders x builders` if fixed naively, and is *also* gated inert by `P1-33`'s own real discipline (no `cpu_accounting` measurement source exists) - resolved: delegate to `UX-12`'s already-correct check, wiring `effective_cpus` from `host_cpu_count`/`cpu_budget` | High | UX-12, UX-15 | 🟢 Done — evidence source 1 now delegates to `_check_process_oversubscription`'s own verdict; `effective_cpus` gained a `host_cpu_count`/`cpu_budget` fallback tier with a new `effective_cpus_source` provenance field | [UX-17](UX-0017-utilization-analyzer-oversubscription-dead-code.md) |
| UX-18 | `tools/bst_run_context.py` (the standalone, documented run-context.json producer) doesn't capture `native_max_jobs`/`cpu_budget`/`host_cpu_count` at all - silently incomplete relative to `bst_extract_run.py` | Medium | UX-12, UX-15 | 🟢 Done — shared `tools/_run_context_common.py` (`host_cpu_count()` + `add_cpu_capacity_fields()`) now used by both producers; `bst_run_context.py` gained matching `--native-max-jobs`/`--cpu-budget` flags | [UX-18](UX-0018-standalone-run-context-producer-missing-fields.md) |
| UX-19 | Resource/scheduler-wait attribution's known "re-saturation within a gap" and "retry gap has no real predecessor" limitations (`P1-31`/`P1-39`/`P1-30`'s own documented, deliberately-deferred gaps) - independently reconfirmed by external review | Medium | P1-31, P1-39, P1-30 (all done) | 🟢 Done — `_classify_wait_gap` now runs a real multi-cycle sweep; both classifiers accept a real window instead of hardcoding `task.ready_us`/`start_us` | [UX-19](UX-0019-resource-scheduler-wait-known-gap-shapes-reconfirmed.md) |
| UX-20 | Single-critical-path report framing forces many small iterations on large graphs - a real `sensitivity.top_opportunities` signal already exists but is JSON-only and never batches independent findings into one combined "fix these together" prediction | Medium | — | 🟢 Done — top opportunities now in text report; new `bga/structural/batching.py` groups independent elements and simulates their combined effect via `ReplayScheduler`'s new `duration_overrides` | [UX-20](UX-0020-batch-optimization-map-reduce-reporting.md) |
| UX-21 | Oversubscription guard (`UX-12`/`UX-16`) only models CPU contention - no memory/swap dimension at all, despite swap thrashing being a far worse real failure mode than CPU slowdown | High | UX-12, UX-16 | 🟢 Done — new `memory_budget_mb`/`estimated_job_memory_mb` declared fields, `_check_memory_oversubscription` fires its own independent `memory_oversubscription` violation | [UX-21](UX-0021-oversubscription-memory-swap-guard.md) |
| UX-22 | Per-element `max-jobs` variance (a real BuildStream possibility, e.g. giving one large synchronization-point element like an LLVM build the full core count) isn't captured or checked for concurrent-dispatch oversubscription risk - a known `UX-12` simplification, elaborated with a real motivating scenario | Medium | UX-12 | 🟢 Done — real per-element capture via `%{public}` (`bst show`), new `serialization_point_risks` diagnostic flags near-full-core, long-duration, concurrently-dispatchable element pairs | [UX-22](UX-0022-per-element-max-jobs-serialization-point-detection.md) |
| UX-23 | Plane 2 (`UX-11`)'s native trace has no notion of which BuildStream element a traced process belongs to - blocks detecting redundant operations repeated across elements' own sandboxes (a real, confirmed finding: `examples/05`'s own CMake compiler-ABI probe re-ran independently in all 6 cmake elements) | Medium | UX-11 | 🟢 Done — real `--dir`-based element tagging, cross-element PID-namespace pairing bug fixed, real detector found 37 redundant-operation findings across all 6 real elements | [UX-23](UX-0023-cross-element-native-trace-tagging-and-redundant-operation-detection.md) |
| UX-24 | Plane 2 has no Chrome Trace export (Plane 1 already does, and it's the user's own real `perfetto.dev` workflow) - no standalone or combined two-plane visualization exists | Medium | UX-11, UX-23 (combined mode only) | 🟢 Done — real single-invocation dual-plane capture (`--wrapped-log`), standalone + combined Chrome Trace export, real correlation confirmed (Plane 2's earliest ts landed exactly on Plane 1's real build-start ts) | [UX-24](UX-0024-cross-plane-chrome-trace-visualization.md) |
| UX-25 | `critical_path_coverage`/`dominator_coverage` hard-gate violations report a bare ratio with no diagnostic detail, even though the tool already computes and displays the explaining fact elsewhere in the same report (real repro: `examples/05`'s `all.bst` stack element) | Medium | — | 🟢 Done — violations now carry a `detail` list naming each missing element, tagged structural (`STRUCTURAL_ELEMENT_KINDS`, P4-12) vs. genuine gap (real evidence: `examples/05` run reports `toolchain.bst (kind: import, structural - may not have a real compute task)`) | [UX-25](UX-0025-coverage-hard-gate-violations-lack-diagnostic-detail.md) |
| UX-26 | `bga`'s batch/map-reduce opportunity report (`UX-20`) includes groups with zero real predicted savings, adding noise instead of only surfacing genuinely worthwhile combos (real repro: `examples/05`'s `lib-a.bst`/`lib-b.bst`) | Low | — | 🟢 Done — zero-savings groups moved to a new `omitted_zero_savings_groups` JSON list and out of the default text report, replaced by a one-line "(N further group(s) had no measurable combined effect, omitted)" summary (real evidence: the doc's own `lib-a.bst`/`lib-b.bst` case, replayed against a fresh `examples/05` capture, now reports `{'groups': [], 'omitted_zero_savings_groups': [{'elements': ['lib-a.bst', 'lib-b.bst']}]}`) | [UX-26](UX-0026-batch-opportunities-report-zero-savings-groups.md) |
| UX-27 | `efficiency_score`/`certified_headroom` certify the graph the run was given, so a deliberately-serialized build scores 1.00 "very efficient" with 0.00s headroom - and a real 30.5% optimization moves both numbers the wrong way | High | — (UX-02 defines the score; UX-39 is the CI consequence) | 🟢 Done — new `floors.occupancy_ratio` (work-vs-span, blind to the graph by construction) shown beside `Efficiency Score` in the report and in `bga compare`; the top efficiency band no longer reads "very efficient" but names its own scope. Real pair: +35.2pp where every other metric was flat or backwards | [UX-27](UX-0027-efficiency-score-certifies-the-graph-it-was-given.md) |
| UX-28 | The oversubscription bar is BuildStream's *own defaults* (`4 x min(cores,8)`), which stop growing at 8 cores while the host does not - so the check's sensitivity depends on host size (4x the cores at 4 cores, 0.5x at 64), and above 8 cores it flags configurations that are *below* one process per core. **Originally filed on different, over-claimed evidence; Motivation corrected in place after re-verification against UX-09's own timing table** | High | UX-12, UX-15, UX-16 (all done) | 🟢 Done — bar re-based onto a documented multiple of the real governing core count (calibrated against UX-09's measurements), plus a new, sharper `dispatch_oversubscription` check on `builders` alone | [UX-28](UX-0028-oversubscription-threshold-is-self-referential.md) |
| UX-29 | `native_max_jobs` is never auto-extracted, though line 1 of the wrapped log the extractor already parses reads `Executing command: bst --builders 4 --max-jobs 4 ...` - so the whole UX-12/15/16/17/21 capacity-guard chain is inert on runs made by the documented pipeline | High | UX-12, UX-18 (both done) | 🟢 Done — parsed from the wrapped log's own invocation line in both producers, with a `native_max_jobs_source` provenance field; the capacity note now says explicitly when the guards did not run | [UX-29](UX-0029-native-max-jobs-is-never-auto-extracted.md) |
| UX-30 | `bga sweep`'s knee point is first-match-wins on a staircase curve, so it recommends capacity 2 while its own printed table shows capacity 4 is a further 35.1% faster | High | — | 🟢 Done — knee is now the last capacity that bought a real gain, computed over the whole curve; asserted defensible against the table printed beside it (the filing's secondary monotonicity claim was checked and found already implemented) | [UX-30](UX-0030-sweep-knee-point-stops-at-the-first-flat-step.md) |
| UX-31 | `UX-22` captures `public: bst: max-jobs`, which BuildStream 2.7.0 never consults when computing `-jN`; the real per-element control is `variables: notparallel: True` (real repro: `core.bst` ran `make -j1`, `serialization_point_risks: []`) | High | UX-22 (done) | 🟢 Done — `%{vars}` turned out to carry both the *resolved* per-element `max-jobs` and `notparallel` (correcting UX-22's second conclusion too); detector re-pointed from the unreachable raising case to the real pinning case, now naming `core.bst` in Plane 1's own text report | [UX-31](UX-0031-notparallel-is-the-real-per-element-parallelism-control.md) |
| UX-32 | Plane 2 reports counts and one inflated global concurrency number, never per-element achieved parallelism - the question it exists to answer (real repro: `core.bst` peak 1 vs siblings' 3-4, computable from the JSON it already writes) | High | UX-11, UX-23 (both done) | 🟢 Done — per-element peak/mean work concurrency, requested `-jN`, and two real findings (`pinned_to_one_job`, `underachieved_requested_jobs`); work-vs-orchestration split with an explicit unclassified bucket. Real evidence: `core.bst` named outright on the baseline trace, unflagged on the optimized one | [UX-32](UX-0032-plane-2-has-no-per-element-achieved-parallelism.md) |
| UX-33 | The text report suppresses the critical path above 5 elements and prints `Bottlenecks Identified: 5` without naming them - both are in the JSON, and on a 10-element artificial chain they are the entire answer | Medium | — | 🟢 Done — path always printed (per-element duration/share above 5 elements), choke points named | [UX-33](UX-0033-text-report-withholds-critical-path-and-choke-point-names.md) |
| UX-34 | `Top Improvement Opportunities` ranks `stack`/`import` elements at sensitivity 1.00 above every element that does real work, despite `STRUCTURAL_ELEMENT_KINDS`/`UX-25`'s tagging already existing | Medium | UX-20, UX-25, P4-12 (all done) | 🟢 Done — structural kinds filtered out of the ranked list (surfacing the next real candidate) and named in a new `omitted_structural_opportunities`; `serialized_pairs` fixed transitively | [UX-34](UX-0034-structural-elements-dominate-improvement-opportunities.md) |
| UX-35 | `UX-04`'s `RESOURCE WAIT` hint tells an already-4x-oversubscribed run to raise `--capacity`, because the hints are constant strings that consult none of the capacity facts the tool has | Medium | UX-04 (done), UX-28/UX-29 (the verdict it should consume) | 🟢 Done — `RESOURCE_WAIT`'s hint is conditioned on a new `AnalysisResult.capacity_verdict` (consumed from `UX-28`'s check, never re-derived), with a distinct branch for "the checks could not run"; the other seven hints re-read and left unchanged | [UX-35](UX-0035-attribution-hints-are-capacity-blind.md) |
| UX-36 | The `CPU Utilisation` block prints task-occupancy seconds under a CPU heading - `P1-33` established the honest meaning internally and it never reached the report (real repro: `Useful` grew 40.25s->61.45s across an optimization that did identical work) | Medium | P1-33, UX-13 (both done) | 🟢 Done — section titled for what it measures, capacity shown with provenance, vacuous reconciliation line replaced, buckets labelled as occupancy under both source tiers | [UX-36](UX-0036-cpu-utilisation-block-reports-occupancy-not-cpu.md) |
| UX-37 | Plane 2's redundant-operation findings sum process time across elements that ran *concurrently* and rank by it, unfiltered down to `uname -r` at 0.001s - the same category error `UX-26` fixed on Plane 1 | Medium | UX-23, UX-26 (both done) | 🟢 Done — findings now carry and rank by recoverable wall-clock (worst affected element) alongside total machine time, sub-threshold ones counted and omitted, commands elided in the middle rather than truncated, and each element's own build driver excluded (identical by construction, different work) | [UX-37](UX-0037-plane-2-redundancy-savings-are-summed-cpu-time.md) |
| UX-38 | `bst_native_build_tracer report` accepts the JSON report `run` just wrote and prints `Processes traced: 0` with exit 0; there is also no way to re-render a saved report, and a misplaced flag yields a raw traceback | Low | UX-11 (done) | 🟢 Done — `report` detects and re-renders a saved JSON report (closing the no-way-to-re-render gap too), an unparseable file is an error rather than a zero-process result, and a misplaced option is a usage error rather than a traceback | [UX-38](UX-0038-tracer-report-accepts-the-wrong-artifact-silently.md) |
| UX-39 | `--fail-on-regression` gates on wall-clock alone at a 1% threshold: it fires on a +4.3% capacity experiment, cannot express "new work is fine but new inefficiency is not", and the tool's own efficiency numbers point the wrong way to gate on instead | High | UX-01, UX-02, UX-03 (done), UX-27, UX-40 | 🟢 Done — independent efficiency gate on `occupancy_ratio` with its own exit code 5, a relative `--max-efficiency-drop` (default 5.0pp, derived from 1.0pp of measured run-to-run noise) and an absolute `--min-efficiency` floor. Real evidence: two well-parallelized elements added fail the duration gate and pass the efficiency gate | [UX-39](UX-0039-ci-gate-cannot-express-inefficiency-regression.md) |
| UX-40 | Real captures land at ~0.69 confidence because BuildStream's own measured startup counts against `attribution_score` - and `--fail-on-regression` fails *open* below 0.8, silently, so the CI gate is off on exactly the small/fast projects most likely to use it | High | UX-03, UX-10 (both done) | 🟢 Done — untracked head explained by measured pipeline overhead no longer penalizes `attribution_score` (real capture: 0.694 -> 0.869, gate now live), plus a new `--fail-on-low-confidence` so a pipeline can refuse a gate that did not run | [UX-40](UX-0040-real-runs-systematically-fail-the-confidence-gate.md) |
| UX-41 | The parallelism profile decomposes levels by BFS *shortest* path from a root, so every element under a common base collapses into one level - a 1202-element, 14-level graph reports 3 levels of width `[1, 1200, 1]`, contradicting `max_depth: 13` in the same block | High | — | 🟢 Done — topological longest-path decomposition, reusing the same recurrence `max_depth` already used so the two agree by construction. The scale fixture now reports 14 levels of width 100; `examples/06` baseline vs `optimized/` now read `max=2.0x` vs `max=6.0x` where both previously read `max=9.0x`, so the macro optimization is visible in the metric for the first time | [UX-41](UX-0041-level-decomposition-uses-shortest-not-longest-path.md) |
| UX-42 | Attribution re-derives the whole run's resource-saturation timeline from scratch once per wait gap, so a 1202-element build takes 68s to analyze - 98% of it in one `O(gaps x tasks x boundaries)` function performing 112M enum hashes | High | — | 🟢 Done — per-resource occupancy timeline built once and sliced by binary search; **95.5s -> 3.2s (30x)**, byte-identical on all five fixtures. An oracle test against a naive transcription of the original caught two segmentation bugs that byte-identity on real fixtures had missed | [UX-42](UX-0042-attribution-is-quadratic-per-gap-at-scale.md) |
| UX-43 | "Choke point" is the placeholder `fan-in >= 2 and fan-out >= 2`, so 520 of 1202 elements (43.3%) are reported as bottlenecks; the code's own comment names the dominator-based definition that was never implemented | Medium | UX-33 (done - which made the names visible and the breakage legible) | 🟢 Done — a choke point is now an element nothing else can overlap with (`\|ancestors\| + \|descendants\| == N-1`), ranked by descendants. Scale fixture 606 -> 1; `examples/06` baseline shows the whole six-library chain and `optimized/` shows none of it, where the old heuristic reported 5 for both. The dominator definition the old comment proposed was implemented, measured and **rejected**: BuildStream deps are conjunctive, so 1201 of 1202 nodes dominate nothing | [UX-43](UX-0043-choke-point-detection-degenerates-at-scale.md) |
| UX-44 | "Slack" is never computed - it is the placeholder `duration x 0.5` - so the improvement ranking is strictly *inverted* by duration (it omits the 14s element and names five 3s ones), and `best_case_speedup` is a near-constant ~2.0x carrying no information | High | UX-34 (done) | 🟢 Done — real CPM forward/backward slack; elements with slack now score 0 (they cannot move the finish), and a zero-slack element's saving is capped where the next path becomes critical. `core.bst` is now the top opportunity at 10.00s, independently reproducing UX-09's hand-measured headroom; aggregates are makespan-based and rendered in seconds beside a stated contrast with `Certified Headroom` | [UX-44](UX-0044-sensitivity-ranks-improvement-opportunities-backwards.md) |
| UX-45 | The Plane 2 hook already runs at every traced process's exit but never calls `getrusage`, so `bga` has no CPU-time measurement anywhere - which is why I9 reconciliation is disabled on every real run and three separate "this is occupancy, not CPU" caveats have to be carried | High | UX-11, UX-23, UX-36 (done), UX-27 | 🟢 Done — real per-element CPU time from `getrusage` in the hook's destructor, with coverage always reported. A real 822-process capture answers the question the walkthrough could not: `core.bst` (the `notparallel` element) runs at **0.87 cores busy** against every sibling's ~1.7 - it was waiting, not compute-bound. The three occupancy-vs-CPU caveats are deliberately **not** weakened; cross-plane wiring is separate | [UX-45](UX-0045-capture-real-cpu-time-per-traced-process.md) |
| UX-46 | No signal finds a declared-but-unused build dependency - the last macro-level gap in `examples/06` - and the cheap cmdline-matching shortcut is refuted by real trace data: BuildStream stages every dep into one shared sandroot, so paths carry no element identity | High | UX-11, UX-23 (both done) | 🟢 Done — opt-in `open`/`openat` interposition in the hook, matched against `bst artifact list-contents` (which supplied the "half that does not exist yet" for free). A real 822-process build finds **24 unused declared edges** in `examples/06` while correctly confirming `toolchain.bst` as used by all nine elements. Refuses rather than guesses: uncovered elements and truncated read sets never yield an "unused" verdict | [UX-46](UX-0046-detect-declared-but-unused-build-dependencies.md) |
| UX-47 | Every section subcommand is a thin alias over the full pipeline, so `bga graph` spends 67s computing attribution it never renders - the same cost as `bga analyze` - while the README tells users to reach for the narrow subcommands instead | Medium | UX-42 (independent; either fix helps alone) | 🟢 Done — `analyze(section=...)` runs only the stages that section renders, derived by reading both formatters; `P1-14`'s one-shared-pipeline resolution is kept intact. `bga graph` **117s -> 0.99s** across both fixes, `analyze` unchanged. All 20 section/format/fixture outputs byte-identical | [UX-47](UX-0047-narrow-subcommands-pay-for-the-full-analysis.md) |
| UX-48 | `IDLE_UNDERPARALLEL` is never assigned, so all idle capacity books to `IDLE_NO_TASKS`: a deliberately builder-starved run with four tasks ready and unscheduled reports 72.30s of "nothing was ready to run" and sends the user to restructure an already-optimal graph | High | UX-36 (done) | 🟢 Done — split by a boundary sweep over each task's real `[ready_us, start_us)` pending window; no new capture needed. The starved run now books 18.00s underparallel (hand-checked against its own timings) while the chained baseline and the *saturated* 4-builder run both correctly report 0. `IDLE_NO_TASKS` is the remainder, so I9 reconciliation stays exact | [UX-48](UX-0048-idle-underparallel-bucket-is-always-zero.md) |
| UX-49 | `parallelism_efficiency` is `mean_width / max_width` - level-width *uniformity*, not parallelism - so a perfectly serial chain scores 1.000 and `examples/06`'s optimized fan-out scores worse than the chained baseline it improves on | Medium | UX-41 (done - which made the widths correct, and this visible) | 🟢 Done — renamed to `width_uniformity`, **not** redefined: measurement showed the parallelism question already has a published answer in `mean_width`/`avg_parallelism` (1.1 chained vs 2.2 fanned out), so redefining would have left two names for one number. The report's `Parallelism Profile` line now shows that `avg`, making the macro optimization visible outside the JSON | [UX-49](UX-0049-parallelism-efficiency-measures-uniformity-not-parallelism.md) |
| UX-50 | The structural analyzer's element table is `{t.task_key.element_uid: t}`, so an element whose FETCH task sorts after its BUILD is seen as **zero-duration** - dropping the build's heaviest elements from the improvement ranking and understating the critical path by 9s on a real capture. Data-order dependent, so it struck some runs and not others | High | — (pre-existing; UX-44 made it consequential and visible) | 🟢 Done — explicit per-element summed durations passed to `StructuralAnalyzer`; one duration source instead of two. All 24 cross-checks now agree (was 22/24), and `core.bst` is back at the top of the ranking | [UX-50](UX-0050-structural-analyzer-collapses-multiple-tasks-per-element.md) |
| UX-51 | Nothing joins Plane 1's "which elements matter" to Plane 2's "what happened inside them" - the loop `design-directions` describes is supported at every step except the arrows, so a user assembles the answer from two JSON files by hand | High | UX-23, UX-24, UX-32/45/46, UX-44 (all done) | 🟢 Done — new `bga correlate RUN NATIVE_REPORT` joins the planes on element UID, the **only** contract between them. Design chosen by measurement, not argument: one capture already yields both artifacts (UX-24), the join key matched 9/9 with zero mismatches on a real dual capture, and the horizons provably cannot be merged. Produces the sentence neither plane can: *core.bst holds 25% of the critical path but runs at 0.85 cores busy and asked for -j1* | [UX-51](UX-0051-join-the-two-planes-on-element-uid.md) |
| UX-52 | The structural plane builds its graph from **all** dependency edges, so `runtime`-only edges - which do not constrain build scheduling - inflate its critical path, depth, levels, choke points and improvement ranking. `edg.py`'s own docstring states the rule and `build_edg` is the one caller that never applied it | High | — (pre-existing; UX-50 fixed the durations on this path, this is the edges) | 🟢 Done — `build_edg` now builds a **gating** graph (runtime excluded) for scheduling questions and a **full** graph for reachability. On a real freedesktop-sdk graph the structural critical path drops 32 -> 28 elements, matching `t_infinity_observed` exactly; `blast_radius` unchanged. Found only because it is the first round to use a real project: **every fixture in this repo had zero runtime edges** | [UX-52](UX-0052-structural-plane-counts-runtime-edges-as-gating.md) |
| UX-53 | Two different per-element duration definitions coexist: `analyze_graph` takes the **max** over an element's tasks (feeding `t_infinity_observed`, `critical_path`, `slack`), `_compute_structural_analysis` takes the **sum** (feeding `sensitivity`, levels, choke points, the improvement ranking) - so the two numbers `UX-52` wrote down as "must be equal" are **22% apart** on any element with more than one task, in the unsafe direction for a certified floor | High | — (introduced by UX-50; violates the invariant UX-52 stated) | 🟢 Done — one `compute_element_durations` in `edg.py`, used by both, keeping **max**: `T∞` must never overstate, and under unlimited capacity a FETCH is not forced to be sequential with its own BUILD on the chain. Found by pointing the sweep at `synthetic_multi_subproject` - a fixture present since before round 1 and the only one whose elements have several task kinds. Every fixture that *pinned* this invariant gives each element exactly one task, where max and sum coincide | [UX-53](UX-0053-two-element-duration-definitions.md) |
| UX-54 | A build in which **every attempted element failed** is scored `Efficiency Score 1.00` and the word "failed" appears nowhere in the report - and because `confidence` is then low, `UX-03`/`UX-39`'s CI gate *fails open* and reports green. A broken build passes the gate on scheduling grounds | High | — (pre-existing; UX-39/UX-03 made it consequential by putting a gate on the score) | 🟢 Done — the status was already in the log and in the chrome trace (`args.Status: FAILURE`) and was dropped at the last hop. Now recorded as `build_outcome` in run-context, raised as a `build_failed` violation, said **first** in Key Findings, and failed **closed** in the gate *before* the low-confidence fail-open. No fixture here contained a failed task; it took a real project on a machine where the sandbox did not work | [UX-54](UX-0054-a-failed-build-scores-perfectly.md) |
| UX-55 | An element that was **cached** is reported as "no matching task found - genuine coverage gap, worth investigating" and fails the `critical_path_coverage_full` hard gate. That drives confidence down, which makes `UX-03`/`UX-39`'s regression gate **fail open** - so the better the cache works, the less `bga` gates. Measured on a real 25-built/101-cached `freedesktop-sdk` build, the exact shape of every incremental CI run | High | UX-54 (done - which supplies the build-outcome signal the fix needs) | 🟢 Done — the two CI scenarios are now a first-class distinction: `queue_summary` parsed from BuildStream's own Pipeline Summary, `run_mode` (`full`/`incremental`/`unknown`, never guessed), cached elements excluded from coverage **only** when the log says elements were skipped *and* the build succeeded *and* `processed` matches the task count, and `bga compare` flags a nightly-vs-pre-commit comparison. On the real capture: coverage 0.818 -> 1.00, confidence 0.82 -> 1.00, violations 1 -> 0 | [UX-55](UX-0055-cached-elements-are-reported-as-coverage-gaps.md) |
| UX-56 | Plane 2 tags processes with bwrap's `--dir` last segment, which is the element only under BuildStream's **default** build root. `freedesktop-sdk` sets its own, so **126,871 of 127,630 processes (99.4%)** landed in one bucket named `buildstream-build` - and every per-element figure became a whole-build figure wearing an element's name (`achieved_vs_requested` 254.75 against 4 jobs; 44,145s "recoverable" inside a 2,796s build). `bga correlate`'s join key was not an element UID | High | UX-23 (introduced the tag), UX-51 (made it a join key) | 🟢 Done — guard, then a **correlation** after measurement ruled out every lookup (the argv carries the element only via the build root, three times over; the shim's ancestry is `buildbox-run` -> `bst` with no per-element job). A layout-independent sandbox id groups processes per element *build*, and constraint propagation against Plane 1's BUILD spans resolves only what is forced, reporting ambiguous/conflicting/unmatched rather than guessing. On a local reproduction of the real override: **0 -> 616 of 822** processes correctly named, 1 -> 8 distinct elements | [UX-56](UX-0056-plane-2-element-attribution-collapses-on-a-real-project.md) |
| UX-57 | The hook's fixed per-process path buffer dropped **149,053 opens against 65,101 recorded** (70%) on a real build. Not a partial result: `UX-46` refuses to judge a truncated read set, so **every element heavy enough to fill the buffer was excluded from declared-vs-used analysis entirely** - the signal inverted against exactly the elements it exists to serve | High | UX-46 (which introduced the buffer and the `dropped` counter that made this answerable) | 🟢 Done — flush the window and continue instead of dropping, so the ceiling is gone; budgets also raised 4x, which is near-free since both buffers are `.bss` pages faulted in only when written. Compression was **measured and rejected**: front-coding in insertion order buys 1.41x (2.88x only if sorted, which a streaming hook cannot do), moving the ceiling below `OPEN_SLOTS` anyway for the price of a wire-format change | [UX-57](UX-0057-the-open-path-buffer-drops-70-percent-of-a-real-build.md) |
| UX-58 | The Plane 2 shim receives BuildStream's complete bwrap argv, rewrites it, and `execv`s it **without recording it anywhere** - so the one artifact needed to fix `UX-56` has never existed in any capture. Also why declared-vs-used came back entirely empty on the real project: it is handed Plane 2's collapsed tags, and `buildstream-build` is not an element path | High | — (blocks UX-56's real fix) | 🟢 Done — `--argv-log PATH`, opt-in and bounded (default 32), recorded *before* the rewrite and never able to fail a build. The first real capture already answers half of `UX-56`: in a real 349-token argv the element appears three times (`--dir`, `--chdir`, `PWD`) as the **same** build-root-relative path, so a project overriding `build-root` loses all three at once - `UX-56` likely needs a mechanism outside the argv | [UX-58](UX-0058-plane-2-never-records-the-bwrap-argv-it-rewrites.md) |
| UX-59 | The regression gate compares exactly **two** runs against a fixed `_SIGNIFICANCE_PCT = 1`. That is 28s on a 46-minute nightly and 1.2s on a two-minute pre-commit run - less than one element's compile variance. In the scenario that runs most often the gate is most trigger-happy and its signal weakest; `bga` has no cross-run statistic anywhere | High | UX-55 (done - which made the two scenarios explicit) | 🟢 Done — `--baseline-run` (repeatable) derives median ± k·1.4826·MAD from a baseline **set**; `UX-55`'s guard refuses a mixed-mode band. Measured on six real repeated builds of one unchanged commit: sd is 1.8% of the mean, the fixed rule flags **3 of 6 identical runs**, and comparing fastest to slowest reports `REGRESSED (+5.2%)` - with a band, `NO SIGNIFICANT CHANGE`, while a real +15% still fires. The skew justification this task was **filed** with turned out to be wrong; the real argument is that one contaminated baseline run widens a σ band from 3.00s to 40.64s and it then misses that +15% | [UX-59](UX-0059-no-noise-model-for-the-pre-commit-scenario.md) |
| UX-60 | Whether `FETCH` time belongs in **any** efficiency signal - deferred in identical terms by `UX-50` and again by `UX-53`, which chose the per-element maximum because it is *safe* rather than because it is correct. A long tarball fetch currently inflates a *build* chain's certified floor | Medium | UX-53 (done) | 🟢 Done — the two-stage model applied: `finish(E) = max(fetch(E), max finish(deps)) + build(E)`, wired at `analyze_graph` and threaded into the structural traversal so `sensitivity.critical_path_us == t_infinity_observed` still holds. Real captures unchanged (fdsdk 3401.9s, `examples/06` 28.2s); the one fixture with real FETCH durations moves 118s → 122s, which is its head element's own fetch. Applying it **found a real under-constraint**: a BUILD task had no dependency on its own FETCH, so replay could start any build at t=0 — `T_C (118000000) < LB (122000000)`, the exact failure `clamp_task_starts`'s own comment warns about, invisible while no floor modelled the ordering either | [UX-60](UX-0060-fetch-time-in-efficiency-signals.md) |
| UX-61 | `max_concurrency` reports **5,268** concurrent processes on a **4-core** runner. The cause is documented (`_exit()` bypasses the hook's destructor, so 8,040 processes have no END line) and the metric claims to exclude them - the measured number says the exclusion is not achieving that | Medium | UX-11 | 🟢 Done — the cause was **not** the unterminated processes this task assumed. `pair_events` keyed START/END on `(element, pid)`, and sandbox pids are namespaced, so the element name was doing all the disambiguating - once it collapsed (`UX-56`), a START in one sandbox paired with an END in another. Real collapsed capture: longest "duration" 23.48s -> 8.63s in a ~30s build, concurrency 34 -> 20 | [UX-61](UX-0061-max-concurrency-counts-processes-that-never-exited.md) |
| UX-62 | A task's terminal status is known at extraction and discarded at the span level, so attribution cannot tell work that succeeded from work that was thrown away - four failed builds contributed 5.3s of `EXECUTION_ON_CHAIN` on the real failed capture. Retry detection also still infers from ordinal position what a status field would state | Medium | UX-54 (done - which recorded it at run level) | 🟢 Done — `status` on `TaskSpan`/`NormalizedTask`, omitted rather than defaulted when unrecorded. Attribution deliberately still counts failed time as `EXECUTION_ON_CHAIN` (moving it changes `I4`'s identity - a proof obligation, not a re-bucketing); the report now states the waste instead, with `failed_task_us` published for CI | [UX-62](UX-0062-per-span-terminal-status-in-trace-v9.md) |
| UX-63 | The memory oversubscription guard runs entirely on operator-*declared* estimates, deferred by `UX-21` on the grounds that measurement "would need intra-sandbox visibility ... a separate, large, future tool". That tool shipped as `UX-11`, and `UX-45` already reads `struct rusage` in the hook - `ru_maxrss` is one field away | Medium | UX-21, UX-45 (both done) | 🟢 Done — `ru_maxrss` from the `struct rusage` the hook already read, aggregated per element as a **maximum, never a sum** (two processes peaking at different moments never held the sum between them - the `UX-36`/`UX-37` error class). Real build: 20.6 MB peak per element, 63 of 78 processes measured | [UX-63](UX-0063-measured-per-task-memory.md) |
| UX-64 | `UX-56`'s correlation matches a sandbox's start *instant*, so with 4 overlapping builders **18 of 25 real sandboxes are ambiguous** and `deduced: 0` - the elimination never cascades. 85% of a real build's processes still carry no element name, so `bga correlate` rightly refuses the join | High | UX-56 (done) | 🟢 Done — matched on the sandbox's **end**, which is measured rather than assumed: whole-interval containment is *worse* (2 of 9 resolved, 7 unmatched) because Plane 1 timestamps when the wrapper *reads* a line, so every sandbox begins before its element's logged START. The `conflicting` case also disproved `UX-56`'s elimination premise - `bison.bst` hosted two sandboxes - so the elimination is **removed** as unsound, not merely weak. Local reproduction: 75% -> **88.7%** of processes correctly named | [UX-64](UX-0064-correlate-on-the-sandbox-interval-not-its-start.md) |
| UX-65 | **The MVP blocker.** On the only real build we have, `bga` leads with *"Biggest Opportunity: 0.1% of wall-clock time is UNTRACKED HEAD (3.47s)"* and ranks what to fix by blast radius, topped by two elements it simultaneously labels structural. Meanwhile its own critical-path block shows **four elements are 94% of a 3583.90s build**, `cmake-stage1.bst` alone 43.5% - and none of them reach either headline | High | — (blocks calling the tool an MVP) | 🟢 Done — leads with where the time is: `4 element(s) are 94.0% of the 3610.5s critical path` against round 7's `0.1% of wall-clock time is UNTRACKED HEAD (3.47s)`. All four acceptance tests re-checked and holding; Required Fix 4 (join the agreeing signals) was delivered by `UX-72`, which cannot be done in `analyze` at all since it has no Plane 2 data. `UX-70`/`UX-76`/`UX-74` finished what this started | [UX-65](UX-0065-the-headline-findings-point-away-from-the-real-build.md) |
| UX-66 | `reliable = recognized_processes == total` - **100% or nothing**. Round 8 took real attribution to **86.1%** with all 8 resolved names valid, and `bga correlate` still refuses, citing `components/bison.bst` as evidence of failure - which *is* an element. Also: a **cancelled** capture run published over a good one (`if: always()` on the publish step), overwriting round 7 with a 36 KB partial | High | UX-64 (done) | 🟢 Done — the guard judges validity rather than completeness, and both workflow defects are fixed. **One acceptance test had never been met**: a Plane 2 bucket named `flit_core.bst` still entered the join and pushed the build's only real element into 'not traced'. The declared-element check is a Plane 1 fact, so it lives in the join now - on the real capture it names exactly `buildstream-build`, `flit_core`, `unknown` and keeps all 8 real rows | [UX-66](UX-0066-the-reliability-guard-is-all-or-nothing.md) |
| UX-67 | The workflow alternates between `bga <cmd>` and `python3 -m tools.<module>` at nearly every step - **74 occurrences** across docs and CI. The separation of a stable analyzer library from small independent producer programs is right as *code layout*; it is not an argument about what a user types | Medium | — | 🟢 Done — `bga` now dispatches to the tools as well as its own subcommands, and every tool stays runnable directly. Dispatched *before* argparse so a tool's flags reach it untouched, imported lazily so `bga analyze` does not pay for the tracer, and `sys.argv[0]` rewritten so usage names what the user typed | [UX-67](UX-0067-one-entry-point-for-the-workflow.md) |
| UX-68 | `declared_vs_used`'s own note says "**this is evidence, not a verdict**: runtime-only dependencies ... all look the same from here". `bga correlate` renders it as "**removing the edge is free**" - and repeats it for `public-stacks/runtime-minimal.bst`, which accounts for **8 of 10** candidates and is a runtime stack nearly every component declares | High | UX-46, UX-66 | 🟢 Done — and worse than filed: every `stack` stages exactly **1** marker file, so a stack dependency scores 0-of-1 *by construction*. 9 of 10 candidates were stacks, including `zstd.bst`; `m4.bst` (321 staged, 0 opened) was the only true one. **10 candidates -> 1, a 90% false-positive rate removed.** The user's pattern hypothesis also tested and confirmed: 110 of 663 build edges (16.6%) have a stack as dependency, and narrowing them to `runtime` would cut the longest path 3610.5s -> 2241.9s (**-38%**) - an upper bound on a hypothetical, not advice, since narrowing `runtime-minimal` would break every compile | [UX-68](UX-0068-the-recommendation-drops-the-producers-caveat.md) |
| UX-69 | Plane 2 ranks binaries by **invocation count**, so on the element that is 43.5% of the critical path `cc1plus` - **885 processes, 4,352 CPU seconds, 10x the next binary** - does not appear in the top five at all. `dwz` is worse: **one** process holding 137 CPU seconds. The heavy-C++-template and serialization-point signals are both invisible | High | UX-45, UX-64 | 🟢 Done — ranks by measured CPU with count beside it, and names the single-process case. Real capture: **`cc1plus` 81.3%** of `cmake-stage1`'s CPU, `dwz` flagged as one process holding 138.6s. Also surfaced **`lto1` at 48.8%** of another bucket - nobody was looking for it | [UX-69](UX-0069-plane-2-ranks-by-count-not-time.md) |
| UX-70 | The report ranks what to optimize by **share of the critical path**, but **97 of 126 elements have zero slack**. `python3.bst` is ranked 3rd at 17.7% of the path, and making it *instant* saves **114s of 3610s = 3.2%** - the other 82% is masked by a near-tie chain. A user would spend a week for a minute | High | UX-65 | 🟢 Done — recomputes the longest path with each candidate zeroed (bounded to 8). **`python3.bst` displaced from the top three by `doxygen.bst`** - shorter, worth 4.5x more. `zero_slack_share` published and stated above 50%, so a reader knows chain from mesh before acting | [UX-70](UX-0070-critical-path-share-is-not-realizable-saving.md) |
| UX-71 | `bga correlate` ranks and gates on `sensitivity.top_opportunities`, which on the real capture **saturates to one tied value** (0.0316 / 114.1s for all five candidates), so the ordering is alphabetical and the 5% gate never opens. The join's own headline product - "not compute-bound, so fix how it is built" - therefore never fires: `bison.bst` at **0.91 cores busy** was measured and not mentioned. `UX-70`'s `realizable_saving_us` (a 5x spread) sits unread in the same artifact | High | UX-70 | 🟢 Done — ranks and gates on `UX-70`'s realizable saving, so `analyze` and `correlate` name the same element first. The verdict fires for the first time on real data: **`bison.bst`, 0.91 cores busy, worth 144.2s** - and, one correction to the task as filed, it takes a cheap-fix exemption below the 5% gate, since worth is only half of low-hanging fruit. A saturated metric is now declared rather than broken by name | [UX-71](UX-0071-the-join-ranks-on-a-saturated-score.md) |
| UX-72 | `_plane2_view` reads 3 of Plane 2's findings and drops the rest, so all 8 rows of "What to do next" are the *same*, explicitly-hedged declared-vs-used sentence. `binary_cost` (`cc1plus` = **81.3%** of the element that is 43.5% of the build), `peak_memory` (**1902 MB** x 4 builders) and `redundant_operations` never reach the join; `aggregating_dependencies` has **no consumer at all** | High | UX-63, UX-68, UX-69, UX-73 | 🟢 Done — `cmake-stage1.bst`'s row went from one hedged sentence to **five findings from four measurements**: `cc1plus` at 81% of its CPU, `dwz` as a single process holding 138.6s, a 1902 MB peak, and the hedged candidate *last*. One correction: the redundancy bar is **relative** (1% of what fixing that element is worth), since a flat floor put a 2.2s shared `rm -rf` beside 1569.8s of realizable saving. `aggregating_dependencies` finally has a consumer | [UX-72](UX-0072-the-join-reads-three-of-plane-2s-nine-findings.md) |
| UX-73 | `detect_redundant_operations` excludes `unknown` but not the *unresolved bucket* `UX-64`/`UX-66` introduced, so it counts `buildstream-build` as an element: **79 of 93 findings involve it, 87% of the claimed recoverable time (3588s of 4129s)**, and the headline finding is `lto-wrapper` "up to 1932.9s recoverable" against a bucket. Plus BuildStream's own `sh -c -e` wrapper slipping past the build-driver exclusion, and per-finding upper bounds that sum to more than the build | High | UX-64, UX-66 | 🟢 Done — **4129s → 91s** of claimed recoverable wall-clock, 0 findings involving the bucket, and every survivor is a real cross-element repeat (`m4`, autoconf probes, `automake`). Two corrections: the unresolved bucket is excluded by the `.bst` identity `assess_element_attribution` already uses (not by its name, which covers only the largest bucket), and the `sh -c -e` wrapper needed **no string heuristic** - the element's command block is structurally pid 2/ppid 1 plus its shell children, measured as exactly 25 roots across 25 sandboxes | [UX-73](UX-0073-redundancy-findings-treat-the-unresolved-bucket-as-an-element.md) |
| UX-74 | One ~60-minute capture yields **one** finding, on a graph where 77% of elements have zero slack. Five longest-path recomputes at **0.40 ms each** produce the whole horizon: `git-minimal.bst` (547.7s, **4th heaviest in the build**) and `icu.bst` (430.8s, 6th) are invisible today and binding two fixes from now. The top three are worth **2605.8s = 72% together, exactly additive** - never stated. `UX-20`'s grouping is backwards: it refuses to group same-chain elements, whose savings *add*, and would group parallel-chain ones, whose savings take a *maximum* (measured both ways) | High | UX-70, UX-20 | 🟢 Done — `optimization_horizon`, `joint_saving` and `latent_heavies`, all from one capture in **17 ms**. The report now states that the top three are worth **2605.8s (72%) together and that this equals their sum**, projects what the build drops to after each fix, and names `git-minimal.bst` (547.7s, **4th heaviest in the build**) and `icu.bst` (430.8s), which appeared nowhere before. Two corrections: greedy-by-saving never routes through `python3.bst`, so latent heavies had to become their own signal; and feeding `UX-20` the *horizon* reintroduced a `UX-44` bug (a later step cannot move today's finish) - it is fed the baseline-realizable set instead, **0 groups → 1** | [UX-74](UX-0074-one-capture-one-finding.md) |
| UX-75 | The two formats carry disjoint halves: JSON has every number and **none of the conclusions** (a CI consumer must re-implement `_heaviest_on_path` and four thresholds from `bga/report/text.py` to reach what a human reads for free), while text has the conclusions and only part of the data. Proposal: one `findings[]` array with stable ids, rendered by text and published by JSON | Medium | UX-71..UX-74 (settle the conclusions first) | 🟢 Done — new `bga/findings.py` decides what is worth saying **once**; text renders it and JSON publishes it, so a CI gate reads `.findings[] \| select(.id == "time-concentration") \| .evidence.chain_bound` instead of re-deriving `_CHAIN_BOUND_RATIO` out of the renderer. Text output is **byte-identical** across the refactor and the 1100-test suite passed unmodified. `correlate`'s rows carry ids and severities too; `binary_cost`'s text cap now says what it capped and `aggregating_dependencies` finally has a renderer | [UX-75](UX-0075-json-publishes-data-text-publishes-conclusions.md) |
| UX-76 | `Key Findings` ranks the same five elements **three times** in 13 lines, and one ranking regressed: `UX-70` re-sorted the shared `_heaviest_on_path` helper, so "Where the time is" now reports **80.3%** and omits `python3.bst` (17.7% of the path, 3rd largest) in favour of `bison.bst` (4.0%) - it said 94.0% with the four largest one round ago | Medium | UX-70 | 🟢 Done — three rankings became one table: **94.0% restored**, `python3.bst` back in third place *and* shown to be worth only 3.2% to fix, which is the most useful disagreement in the report and was previously invisible. 13 lines → 8; the degenerate criticality list (every entry 100%, one of them a `stack`) is dropped | [UX-76](UX-0076-three-rankings-of-the-same-elements.md) |
| UX-77 | The installed CLI cannot reach its own capture tools: packaging includes only `bga*`, so every `bga wrap/extract/capture/...` alias dies with a raw `ModuleNotFoundError: No module named 'tools'` — **even from the repo root** in a modern-pip venv. The entire documented real-project workflow is broken for a pip-installed user; CI never sees it because it calls `python3 -m tools.*` with `PYTHONPATH` | High | — | 🟢 Done — `tools*` is packaged and `pyyaml` moved to base dependencies (a plain install produced a `bga capture` that could not start, found by the new handled error). All **15** aliases verified from a built wheel in a clean venv from an empty cwd, guarded by a new `packaging` CI job. Deviation recorded: `tools*` is packaged rather than moved under `bga/` — one line vs 49 test import sites and 41 documented `python3 -m tools.<module>` invocations; the tradeoff is written into `pyproject.toml` | [UX-77](UX-0077-installed-cli-cannot-reach-its-own-tools.md) |
| UX-78 | README/guide promise `bga compare` "refuses" cross-project / cross-cache-scenario comparisons; it actually warns and verdicts anyway — a golden fixture vs a real run yields `REGRESSED (+105668.8%)`, exit 0, and exit **4** under `--fail-on-regression`, so a CI artifact-path bug reads as "your build got slower" | High | — | 🟢 Done — refuses with **exit 6**, naming the failing check and printing no comparison; `--allow-mismatch` restores the old warn-and-compare. Exit 6 is deliberately not 4/5, so `--fail-on-regression` on a mismatched pair no longer reads as a regression. `mismatches[]` published structurally; README, guide and `docs/guides/cli.md` now quote the same behaviour | [UX-78](UX-0078-compare-refusal-is-actually-a-warning.md) |
| UX-79 | The efficiency gate is a whole-build average, so a bad diff dilutes with project size: two maximally-mis-added elements moved an 11-element project's occupancy **6.1pp against the 5.0pp default** — the same additions on the 90-element fdsdk closure move it <1pp and pass. The build owner's rule ("adding work is fine, adding it inefficiently is not") needs a **marginal** gate on the diff's own elements | High | UX-39, UX-74 | 🟢 Done — a **marginal** gate: `--fail-on-inefficient-additions` scores what share of the work a change added landed on the critical path. Measured at two scales, the same badly-added pair moves whole-build occupancy **−14.6pp at 11 elements (gate fails) and −0.5pp at 1201 (gate passes, blind)** while stretch is **1.00 in both** — both rows asserted as tests. `element_diff` + `marginal_efficiency` published; needed a new `signals.element_durations`, since nothing published a duration for an element *off* the path and a well-added one is off it by construction | [UX-79](UX-0079-efficiency-gate-dilutes-with-project-size.md) |
| UX-80 | The documented capture command cannot produce the documented join: the sandbox→element attribution needs `--invocation-log`, which appears **zero times** in README/cli.md/guide — only CI passes it. A user copy-pasting the guide on a non-default `build-root` project (fdsdk itself) gets the UX-56 collapse the docs present as solved | High | UX-56, UX-64 | 🟢 Done — `--wrapped-log` implies the invocation record (`--no-invocation-log` opts out), and round 12 discharged the acceptance: `tests/fixtures/bst_build_root_override/` overrides `build-root`, the README's three commands run against it unmodified, and the join lands by UID (`certain: 1`, `attributed_share: 1.0`). Paired with a `--no-invocation-log` control that collapses to one `buildstream-build` bucket, so a pass is the mechanism working rather than a kind fixture | [UX-80](UX-0080-documented-capture-cannot-produce-the-documented-join.md) |
| UX-81 | `captures/fdsdk-latest` is force-pushed — every publish destroys its predecessor — while the tool's own CI advice requires a **≥3-run baseline set** (measured 2.9% noise vs the 1% rule). No history, no schedule trigger, tarball-only, thin overwrite guard: the infrastructure cannot supply the input the gate requires | High | — | 🟢 Done — per-run refs (`captures/fdsdk/<ref>-b4j4-<run-id>`), never force-pushed, with the comparability tuple in the ref name so a baseline set is one `git ls-remote`; `-latest` stays a moving pointer and no longer moves to a failed build; weekly schedule; `run/` committed uncompressed so a checkout is analysable with no untar. Plus the thing the acceptance test implied: a band that cannot be built now **names the shortfall** instead of silently falling back to the fixed rule. The two-run acceptance needs the workflow to fire twice — now scheduled | [UX-81](UX-0081-captures-branch-keeps-no-history.md) |
| UX-82 | The tool measures every fact of `examples/06`'s macro fix — six chain links on the critical path, **each chain edge individually reported never-read** — and never synthesizes "unchain them": the walkthrough's single biggest structural win (**27.9s → 25.0s** measured) is left for the user to invent from five hedged rows | High | UX-46, UX-74 | 🟢 Done — `correlate` now names the wall, not five bricks: a group of never-read edges that chains elements along the critical path becomes **one** finding, rendered above the per-element rows, with the run **replayed without those edges** (28.0s → 12.0s on the fixture). A capacity-aware replay, not a longest path - removing five chain edges makes six elements ready at once and what happens next is decided by the builder count. The `UX-68` hedge survives the synthesis, and it stays silent on the real capture, correctly | [UX-82](UX-0082-never-read-chain-edges-never-become-a-restructuring-finding.md) |
| UX-83 | The two planes give contradictory advice on the same capture and nothing arbitrates: Plane 1's headline says raise `--capacity`, `sweep` names knee **5 on a 4-core host**, while `correlate` (same run) correctly says remove `core.bst`'s `notparallel` (verified **−32.4%**). When Plane 2 data exists, Plane 1's capacity hints should consult it | Medium | UX-51, UX-45 | 🟢 Done — `analyze --plane2` / `sweep --plane2`. On the real capture the knee line now reads *\"Plane 2 measured 3.25 of 4 cores busy - the host was already CPU-saturated\"* and names `gperf.bst`'s `-j1` as free capacity, instead of steering toward a fifth builder. A saturated host is told not to raise capacity; a pinned element is named first. Only the RESOURCE WAIT hint is conditioned, and without `--plane2` every line is unchanged | [UX-83](UX-0083-the-two-planes-give-contradictory-advice-and-nothing-arbitrates.md) |
| UX-84 | With a live bst 2.7 installed, `make test` fails: **4 bst-gated tests**, including per-element `max-jobs: 16` override extraction returning **4 (the host core count)** — and no CI job runs pytest with bst present, so the gated tier can rot green | Medium | UX-22 | 🟢 Done — the four failures were one real assertion (`cpu_accounting_available`, superseded by UX-17) and nine harness artefacts of a `pip install --user` layout; the max-jobs "regression" is the correct answer post-UX-31 and was re-baselined against `notparallel`, the control BuildStream actually honours. New `bst-tests` CI job runs the tier and pins its count at 14 so a skip cannot read as a pass | [UX-84](UX-0084-bst-gated-tests-fail-against-a-live-bst.md) |
| UX-85 | `bga/report/text.py` still carries full shadow copies of `findings.py`'s thresholds and helpers, and the UX-71 guard tests import the **shadow** — the two judgement layers can drift with the guards green, the exact failure mode UX-75 shipped to eliminate | Medium | UX-75 | 🟢 Done — five shadow definitions deleted (the audit listed four; an AST guard found `_structural_kind_tag` too), tests re-pointed at `bga/findings.py`, and two guards added: an `is`-identity check and a no-name-bound-twice scan. Text output byte-identical across all three fixtures | [UX-85](UX-0085-report-text-shadows-the-findings-logic-and-tests-bind-to-the-copy.md) |
| UX-86 | The caches-off scenario has never been captured (round 9's standing gap): every real capture is incremental, so coverage, floors and both efficiency signals are untested against a real cold build — add a `capture_mode: cold` to the workflow, bounded to the existing cut set | Medium | UX-81, UX-55 | 🟢 Done — the first caches-off capture in the project's history: `bootstrap/build/gcc-stage1.bst`'s whole 18-element closure built from source, **0 cached**, 34.2 min against a 250-min budget, confidence 1.00, no incremental caveat. The target was chosen by measuring closure sizes, not guessed: every `components/*` target roots in a 64-element bootstrap, but `bootstrap/base-sdk/*` is rooted at a 2-element binary seed. Cold-vs-incremental `compare` refuses on both checks — UX-78's first real exercise. It also falsified UX-92's hit-ratio finding, shipped hours earlier, which called a caches-off run "barely incremental" | [UX-86](UX-0086-caches-off-capture-has-never-been-performed.md) |
| UX-87 | Both efficiency gates silently return "pass" when either run lacks `occupancy_ratio` — a pipeline that believes it is gating is not, with nothing printed; the identical UX-40 failure mode, one field over | Medium | UX-39, UX-40 | 🟢 Done — fail-open kept, silence removed: stderr names the gate and the run, `--format json` publishes `efficiency_gate_evaluated` (true/false/null) and `efficiency_gate_signal`, and `--require-efficiency-signal` exits **7** for pipelines that would rather break than not gate. The two gates report separately, because `--min-efficiency` never needed the baseline | [UX-87](UX-0087-efficiency-gates-silently-no-op-when-occupancy-is-missing.md) |
| UX-88 | Documentation-drift sweep from the round-10 claims audit: the README quick start shows another command's output, cli.md's Efficiency Score formula is wrong (`LB/total` vs `LB/horizon`), exit-4 semantics are under-documented, the band-mode example is not a runnable invocation, `findings[].id` values are published nowhere, + six smaller verified items | Medium | UX-78 | 🟢 Done — all ten items fixed and each re-verified by running the quoted command. The two that could mislead: exit 4 is three different verdicts, not one, and the Efficiency Score is `LB/horizon` not `LB/total` (1.00 vs 0.875 on the golden fixture). `findings[].id` published in full for the first time — 15 + 10 | [UX-88](UX-0088-docs-promise-what-the-code-does-not-do.md) |
| UX-89 | `correlate` prints seven near-identical blocks for `examples/06`'s interchangeable libs (40 lines for two facts), and flags 0.1s `ranlib` single-processes as "serialization points" inside 2s elements — group identical findings, apply the UX-72 materiality bar to the single-process rule | Low | UX-72 | 🟢 Done — grouped on the finding-id signature: 48 lines to 21 on a real capture taken for this task, with `core.bst` still leading alone. The single-process rule gained the materiality bar every other Plane 2 rule had, so a 0.2s `ranlib` is silent and a 12s `ld` is not. Grouping touched the JSON not at all | [UX-89](UX-0089-correlate-repeats-one-finding-seven-times.md) |
| UX-90 | **17 of 24** capture-workflow runs were push-triggered cancellations (several after 25–57 runner-minutes; five in one day, zero publishes) — the push trigger + `cancel-in-progress` burns runner-hours without producing data | Low | UX-81 | 🟢 Done — the `push` trigger is removed, not smoke-shrunk: the need it served is **already met on every push** by CI's `bst-examples` job, which traces a real sandboxed build of `examples/06`, and by `test_rebuild_set.py`. `UX-81` made that safe by moving data production to `schedule` + `workflow_dispatch`. `cancel-in-progress` is kept for racing dispatches. Reproduced once more while fixing it: pushing the `UX-86` commit fired a fresh ~65-minute capture nobody asked for | [UX-90](UX-0090-push-triggered-captures-burn-runner-hours-without-publishing.md) |
| UX-91 | BuildStream's own persisted per-element logs (`~/.cache/buildstream/logs/`, `bst artifact log`) are an uningested **third data plane**: per-phase (configure/compile/install) breakdown with zero capture overhead, cross-build frequency analysis of repeated operations, and retrospective timelines for builds nobody wrapped | Medium | — | 🟢 Done — `bga cache-logs` ships (1) and (2); (3) declined on a measurement (one-second resolution cannot reconstruct overlap honestly). The fdsdk half is now met against a published 178-log tree: `cmake-stage1.bst`'s self-reported configure split renders with no Plane 2 artifact, and the repeated-operation report finds the autotools build/install idiom across 10 elements. The real tree exposed one defect — an empty command block counted as an operation shared by eight elements | [UX-91](UX-0091-mine-the-build-cache-logs-as-a-third-data-plane.md) |
| UX-93 | UX-92's churn detector indicts rebuilds the cache was never given a chance to serve: a cold pair reads as "36.5s bought nothing", and the fdsdk warm/cut captures read as **"4604.2s bought nothing" in every band comparison the weekly schedule will ever produce** — the deliberate cut reported as waste. The identical run-mode blindness was already fixed once this round, in `analyze`'s hit-ratio finding; `compare`'s churn didn't get the same treatment. On a genuine incremental pair the detector is exactly right (verified live: invalidation root named, zero false churn) | High | UX-92, UX-55 | 🟢 Done — churn now takes both runs' `run_mode` and the baseline's built set, and refuses to judge without them (three named reasons in the payload). An unchanged-key rebuild the baseline also made is reported as a cache-**retention** question, not as waste. All four acceptance cases measured: cold pair silent, the real fdsdk pair's 25-element cut reframed (4879.9s, `churned_count: 0`), the true-positive protocol unchanged (`core.bst` root, 25.7s), and a genuinely-cached baseline still accused | [UX-93](UX-0093-churn-indicts-rebuilds-the-cache-was-never-given-a-chance-to-serve.md) |
| UX-94 | UX-77's fix ships a **top-level `tools` package** in the wheel — the most generic importable name in Python, now squatted in every consumer's site-packages, with silent pip file-interleaving against any other distribution shipping `tools/`. The task file's own preferred fix (move under `bga.`) was the right one | Medium | UX-77 | 🟢 Done — the wheel now owns exactly one top-level name. Fixed without moving anything: `package-dir = {"bga._tools" = "tools"}` keeps the directory at `tools/` in the checkout and installs it as `bga._tools`, so 55 test imports, 34 CI invocations and 263 documented paths are untouched and no shims exist to drift. Needed the modules' imports of each other to become relative, which is an improvement anyway. Asserted on the built artifact in CI, not just the config | [UX-94](UX-0094-the-wheel-squats-the-tools-namespace.md) |
| UX-95 | The reports' `Run:` header prints the (deliberately stable) identity hash as the only identifier, so two different fdsdk captures 100 minutes apart display the **same digest** — and UX-81's history makes same-config runs the common case. The instance facts sit unprinted in `run-context` | Low | UX-07 | 🟢 Done — an `Instance:` line (capture wall clock in UTC, plus the run directory) beside `Run:`, and `run_instance` beside `run_id` in JSON. Verified on two real per-run refs: identical identity hashes, two distinct instance lines. Absent fields are omitted rather than filled with placeholders, and a zero wall clock counts as absent so fixtures do not print 1970 | [UX-95](UX-0095-the-run-header-names-the-identity-not-the-run.md) |
| UX-97 | The fix round broke five of its own claims in flight: the published findings-id table went stale within the same commit range (15 vs 17), UX-81's advertised ref glob matches nothing after UX-86 renamed the refs, the bst tier count reads 14 in four places and 15 in the gate, the bst CI step can go green on a failing pytest (no `pipefail`, `15 passed, 1 error` passes the count grep), and four bare paths survived the reorg — including the link-rule's own assertion message pointing at a file that no longer exists | Medium | UX-84, UX-86, UX-88 | 🟢 Done — all five confirmed and fixed. The two counts that drifted are now tests verified by mutation (finding-id table vs code; CI's tier pin vs `@pytest.mark.bst`), so the number is written down in exactly one place. The sharpest item was real: `pytest \| tee` made the bst step's exit status `tee`'s, so `15 passed, 1 error` greened the job — fixed with `set -o pipefail`, demonstrated in the workflow comment | [UX-97](UX-0097-the-fix-round-broke-five-of-its-own-claims.md) |
| UX-98 | Markdown correctness is a prose rule, not a lint: the round-11 status table rendered broken on GitHub (unescaped jq pipes split a 6-column row into 8 cells), and a sweep found **five malformed rows across three files**, one broken since the P-task era. Style-guide rule 8 is written; this integrates the linter (PyMarkdown vs markdownlint-cli2, decided by which catches all five real defects) as `make lint-docs` + CI | Medium | — | 🟢 Done — but not as specified, and the measurement is why: PyMarkdown catches **none** of the five defects (it implements MD001-MD048; the table rules are markdownlint v0.34+ with no equivalent), and the fallback needs Node. So the table check is ours, ~40 lines in the docs test, understanding `\|` escapes and fenced blocks; PyMarkdown is wired as `make lint-docs` for the rest of the correctness class (11 real findings fixed). It found a **sixth** defect on its first run — in UX-97's own status row, written an hour earlier | [UX-98](UX-0098-markdown-correctness-is-a-prose-rule-not-a-lint.md) |
| UX-99 | The sandbox tax is paid by every element and attributed to nothing: staging/integration/caching are timed in every persisted element log (verified) and no report splits toll from work — on a project staging a multi-hundred-MB sysroot into 90 sandboxes, that toll is real and invisible. Direction 3.1a: Plane 3 measures it per element and project-wide | Medium | UX-91 | 🟢 Done — `sandbox_tax` in `bga cache-logs`: work / toll / unaccounted, per element and project-wide, ranked by toll seconds. Measured on the real freedesktop-sdk log tree: **12.0s of 3630.0s, 0.3%**, top payer `components/libffi.bst` at 10% of its own time — the direction's premise (a sysroot staged into every sandbox) is real and its cost is not, because BuildStream stages from CAS by hardlink. On `examples/06` every overhead phase rounds to zero at the one-second resolution, so the figure is published as a floor | [UX-99](UX-0099-the-sandbox-tax-is-paid-by-every-element-and-attributed-to-nothing.md) |
| UX-100 | Nothing answers whether the elements are the right size — both granularity failure modes are measurable from data in hand (toll share for too-fine; critical-path share + internal parallelism + invalidation blast for too-coarse) and no conclusion is drawn. Direction 3.1b: merge candidates with replayed projections, split candidates named with evidence, thresholds derived not guessed | Medium | UX-99, UX-82 | 🟢 Done — closed by UX-120: the positive merge case ran on a real fixture with a real merged rebuild; the projection ships as an explicit floor | [UX-100](UX-0100-nothing-answers-whether-the-elements-are-the-right-size.md) |
| UX-101 | Nothing ranks what makes the project slow **across builds**: every ranking is single-run, but the team question is longitudinal — rebuild frequency × rebuild cost over the log tree's history, with UX-92's roots explaining *why* each element rebuilds. One volatile key near the root is the top developer tax, and this is the number that proves it. Direction 3.2 | High | UX-91, UX-92 | 🟢 Done — `developer_tax` ranks elements by total build seconds across the whole tree, with per-rebuild causes (unchanged key / own change / rooted upstream, the last needing `--graph`). The logs carry **no session id**, so the build count is published as a lower bound rather than clustered into a guess — which is what keeps it honest on the fdsdk tree, where one build of each element gives an empty cause column and `at least 1 build(s)` rather than a confident number over a 45-minute window. Verified on a real six-build tree too: `core.bst` tops it, and `lib-a.bst` roots at `codegen.bst` and `core.bst` exactly as the experiment did them | [UX-101](UX-0101-nothing-ranks-what-makes-the-project-slow-across-builds.md) |
| UX-102 | The configure tax is measured twice and totaled never: Plane 3 has per-element `Configuring` time, Plane 2 traces the probe processes (the 9× repeated compiler-id compiles), and neither sums to "N% of this project is configure, these elements pay the most". Direction 3.3; the two planes' agreement is a free cross-check | Medium | UX-91, UX-45 | 🟢 Done — Plane 3 totals the tool's self-report; Plane 2 classifies traced processes by **parentage** under a configure entry point. On freedesktop-sdk the two-measurement cross-check immediately caught a bug in the second: matching the pattern anywhere on the command line read `-L…/Bootstrap.cmk/cmake` on a linker as a configure root and mis-filed its whole subtree — 1329 CPU s, 34% of `cmake-stage1.bst`, against 30.50s of self-reported wall. Matched against the executable it reads 25.79s, and project-wide 75.8 CPU s (0.9%). By share the autotools elements lead as predicted (gperf 38.9%, bison 18.7%) and 5 have no self-report at all | [UX-102](UX-0102-the-configure-tax-is-measured-twice-and-totaled-never.md) |
| UX-103 | Cache health is a point reading with no trend: hit ratio, churn, and transfer cost exist per run while the CI question is "is the cache getting worse?" — a degrading remote slows every build in the org and no single run shows it. Direction 3.4, and the trend stage UX-92 deferred: `bga cache-trend` over the retained per-run refs, reusing the UX-59 band | Medium | UX-92, UX-96, UX-93 | 🟢 Done — `bga cache-trend` reads a series and judges only its newest member, reusing `compare`'s band (and its widening rule: a 0.03% spread produced a 2.2s band on a 4740s median and called a 6% rise a regression). Churn is pairwise with `UX-93`'s labels. Verified on the three real fdsdk refs and on a synthetically degraded fourth (3.2x, finding fires). Wired into the capture workflow, `continue-on-error`. Deviation recorded: three refs is a *two*-run window, so it renders rows and no verdict rather than rows and no finding | [UX-103](UX-0103-cache-health-is-a-point-reading-with-no-trend.md) |
| UX-104 | The memory half of capacity advice is still an exercise for the reader: "multiply 1902 MB by however many elements build concurrently" is the tool's job — compute the builders memory envelope from measured peaks, make UX-83's arbitration memory-aware, and note envelope growth in compare. Direction 3.5 | Medium | UX-63, UX-21, UX-83 | 🟢 Done — `host_memory_mb` is now recorded at capture time (the reason `UX-21` scoped detection out stopped holding when `UX-63` measured peaks), and the envelope sums the N largest measured peaks against it: an upper bound, no invented reserve, projected only as far as the measured population. Renders in `analyze` (`memory-envelope`), `correlate` and the sweep knee; the RESOURCE WAIT hint refuses a raise that would swap; `compare --baseline-plane2/--candidate-plane2` notes envelope growth. fdsdk's retained captures predate the field | [UX-104](UX-0104-the-memory-half-of-capacity-advice-is-still-an-exercise-for-the-reader.md) |
| UX-105 | The hook's static-binary blind spot is itself unmeasured: the disclaimer fires identically on a capture that missed nothing and one that missed everything (`examples/01`'s busybox elements have an **empty Plane 2 capture today** and nothing says why). Static-ness is knowable pre-build from the staged roots (`PT_INTERP` absent) — Direction 4's census, and the ground truth `UX-106` verifies against | Medium | — | 🟢 Done — `bga capture census`, and the disclaimer is now data-driven: named where there is something to name, silent where there is not. `examples/01` reports its 5 static busybox applets reaching all 10 elements, which is why its Plane 2 capture is empty; `examples/06` reports zero. Implementing `PT_INTERP` alone as specified was wrong — shared objects have none either, and the first real run called `ld-linux.so` and three `liblto_plugin.so` static — so `e_type` decides too, with static-PIE under-reported rather than guessed | [UX-105](UX-0105-the-blind-spot-is-itself-unmeasured.md) |
| UX-106 | A process spine the linker cannot hide from: a static tracer injected by the existing shim chain, ptrace **process events only** (fork/exec/exit — never per-syscall), argv at exec-stop, CPU/RSS at exit-stop, same log and same monotonic timeline as the hook, init duties handled, fail-open structurally. The hook stays for opens and enrichment; alternatives (acct, CN_PROC, eBPF, polling, fanotify) weighed and rejected in Direction 4 | High | UX-105 | 🟢 Done — reopened by round 12: the fail-open acceptance clause was **measured failing** and shipped under 🟢 attributed to ptrace at large; the code review found implementation causes, filed as `UX-117` (degrade path strands tracees), `UX-118` (attach-SIGSTOP re-injected — the likely mechanism of the failed clause), `UX-119` (pid-1 signal model built for a configuration BuildStream never runs). The value case is verified live (round 12: 24 static busybox processes, `sleep 3` = 3.0016s wall / 0 CPU). **Round-13 note:** UX-117/118/119 all landed and the kill-the-tracer clause now passes 3/3 — as a plain subprocess; the file's own return condition ("passes in a real sandbox") was the last open inch. **Closed by `UX-144`:** run in `bwrap --dev-bind / / --unshare-pid`, the spine SIGKILLed at 0.6s, the build's own work still completed |  [UX-106](UX-0106-a-process-spine-that-the-linker-cannot-hide-from.md) |
| UX-107 | Two record streams, one process list: without join+dedupe the spine double-counts every dynamic process's CPU; with it, each process carries provenance (`spine+hook` / `spine-only`), opens-dependent findings state their real coverage, the global disclaimer becomes a measured number, and old captures parse unchanged. Self+children CPU recorded twice becomes a free cross-check (the UX-53 pattern) | High | UX-106, UX-105 | 🟢 Done — `examples/06` merges **1644 records into 822 processes**, CPU **112.61s naive → 58.47s**, inside the range two hook-only builds produce. Two corrections the measurements forced: `pair_events` was crossing the streams (a `src=spine` record carrying `utime=0.013204`, a resolution /proc cannot produce) while every count still looked right, and the hook's µs `getrusage` — not the spine's tick-truncated `/proc` figure, **-53.8%** on a short-process build — is the CPU the merged model uses. `examples/01` now reports its 8 elements UNCOVERED with counted reasons instead of skipping them; the fdsdk capture re-renders byte-identically | [UX-107](UX-0107-two-record-streams-one-process-list.md) |
| UX-108 | The spine proves itself on both build classes: `examples/01`'s busybox elements gain their first Plane 2 records ever (with `sleep 3` ≈ 0 CPU / 3s wall as a known answer), fdsdk confirms nothing regresses at 127k processes, and the measured overhead (<2% budget on a configure-heavy fixture) decides the default — on with the hook, or opt-in | Medium | UX-106, UX-107 | 🟢 Done — `examples/01` measures its known answer (**0.00s CPU over 3.01s wall**, eight times, ±2ms). Overhead at n=10: **+2.7%** on `examples/06`, **+13.5%** on the new `examples/08-process-storm` (575 proc/s, built because no project was configure-heavy) — both past the 2% budget, so **`--trace-spine` stays opt-in** by the rule stated before the measurement, and `UX-106`'s n=2 6.9% was an over-estimate. fdsdk at 127,632 processes: all `spine+hook`, **+734 processes gained an observed exit**, 40 CPU disagreements in 119,497 (0.03%), raw trace +32.7%. The ground truth also exposed a Plane 1 defect — two of eight identical `sleep 3` elements reported at 2.686s (`UX-110`) | [UX-108](UX-0108-the-spine-proves-itself-on-the-builds-that-need-it-and-the-ones-that-do-not.md) |
| UX-109 | `make lint-docs` runs PyMarkdown without `-r`, so the docs gate scans exactly two files — `README.md` and `docs/README.md` — while naming `docs/`. The other ~150 documents are unlinted; with `-r` the same config reports **1300 violations**. Same shape as `UX-84` and `UX-97`: a gate that cannot fail, in a repository written as though it holds | Medium | UX-98 | 🟢 Done — `scan -r`, and the tree it now scans is clean: 1287 of the 1300 fixed (blank lines around headings/lists/fences, fence languages — the class that changes rendering), 13 disabled across four rules that argue with correct content, each with its reason. PyMarkdown's own `fix` mode crashed on one file and left three rules untouched. Guarded by a test on the flag, verified by mutation both ways, and the lint verified by breaking a document it previously could not see | [UX-109](UX-0109-the-docs-lint-scans-two-files-not-the-docs-tree.md) |
| UX-110 | Plane 1 under-reports a task whose log lines the wrapper flushed late: on eight identical `sleep 3` elements it spreads **0.319s** where Plane 2 spreads 0.002s, and reports two of them at **2.686s** for a three-second sleep — an impossible duration, not merely an imprecise one. A wrapped line is stamped when the wrapper *reads* it; BuildStream's own log file name says the task started 1.2s earlier. Every Plane 1 signal is built on these spans | Medium | — | 🟢 Done — the same duration was already in the log twice (the wrapper's stamps against BuildStream's own elapsed prefix) and nothing compared them. Measured on three builds from 12s to 3261s: the lag envelope is **-0.6s to +1.5s and does not grow with the task**, so it is 0.03% of a 1415s element and 11% of a `sleep 3`. Reported rather than corrected, with the reason recorded: the elapsed prefix is a second-resolution lower bound, and moving a span's endpoint to satisfy it manufactures overlap the capacity model would report as a violation | [UX-110](UX-0110-plane-1-under-reports-a-task-it-flushed-late.md) |
| UX-111 | The six report surfaces were each checked alone and never against each other. Rendered side by side on one real capture they do not read as one tool — a label naming microseconds beside a value in seconds, slot-seconds printed as wall-clock `s`, a missing-file hint whose commands are not executable, no run identity outside Plane 1, no banner at all on Plane 2, and one 330-character caveat printed four times | Medium | — | 🟢 Done — ten fixes, all found by rendering the real thing: `Execution On Chain Us` → `Execution On Chain`, occupancy buckets in `slot-s` with the arithmetic stated, the stuck-user hint rewritten to `bga` aliases (and its test now asserts against the alias table, not a string), `correlate` and the Plane 2 report given the identity and banner discipline `UX-95` gave Plane 1, four split candidates grouped behind one shared rationale with the JSON unchanged, one banner width, `<1%` instead of `0%`, element names elided from the head so the identifying tail survives, and legends for the two columns a reader could not decode. **Follow-through:** item 3 ("every report says which run it is") had stopped at two of them — `bga cache-trend` trended three freedesktop-sdk runs with one `examples/06` run among them and reported "every trended metric sits inside the band". It now refuses the verdict with exit 6, keeps the per-run rows, and keys on project+targets rather than the run-identity hash the siblings use, because that hash includes the commit and a cache trend across commits is the only kind there is. **Round-12 exception:** item 1 is unfixed on one of the six surfaces — `bga compare` still prints `Execution On Chain Us` beside seconds, and the guard test asserts the helper, not the rendered surfaces (`UX-121`) | [UX-111](UX-0111-the-reports-were-each-checked-alone-never-against-each-other.md) |
| UX-112 | The spine's price is quoted for a configuration the workflow never runs: UX-108 measured spine-vs-off (+2.7%) but never the **spine × opens combination** — measured in round 12 at **+31-44%** on `examples/06` (41s / 41-45s alone, 59s together) — and `trace_opens=true` is the capture workflow's default, so the one real fdsdk spine capture ran exactly the unmeasured pairing | High | UX-108 | 🟢 Done — and **the interaction it was filed for is not there**. A full {spine} × {opens} factorial, five repeats a cell: on `examples/08` the spine costs +2.29s without opens and **+1.50s with**, so the combination is *cheaper* in relative terms; on `examples/06` every cell overlaps every other. What is real is **about a millisecond per process** — invisible at a 36ms mean process lifetime, dominant at 2ms — which reconciles the same cell being quoted at +13.5%, +13.2% and +55.7% while the absolute cost barely moved. The docs now quote the per-process price, because a ratio is a fact about the fixture's baseline | [UX-112](UX-0112-the-spines-price-is-quoted-for-a-configuration-the-workflow-never-runs.md) |
| UX-113 | The spine and the census were never introduced: the spine is all-or-nothing while the census knows **per element, pre-build**, where the hook is blind. `--trace-spine=auto` traces only elements whose staged root contains a static executable — universal coverage at hook-only cost on the 95% that don't need it, instead of an opt-in that quietly reopens Direction 4's blind spot | Medium | UX-105, UX-106, UX-112 | 🟢 Done — `--trace-spine=off\|on\|auto`; the shim consults the pre-build census per sandbox. `examples/01`: auto traces 8 of 8 and its per-element records are **identical** to a full `on` capture. `examples/06`: **0 of 9**, zero spine records, hook-only cost. A mixed fixture flags exactly 2 of 13. Fails towards coverage — an unassessed element, an unrecoverable name (every element under a build-root override) or an unreadable census all get traced. The decision is recorded per sandbox rather than inferred, since a skipped element and an element that ran nothing look identical in the trace | [UX-113](UX-0113-turn-the-spine-on-only-where-the-census-says-the-hook-is-blind.md) |
| UX-114 | The baseline set's edges, each hit live in round 12: the homogeneity check skips absent fields, so the **spine capture joined a five-run hook-only band with no warning**; `fdsdk-latest` was moved to that differently-instrumented capture by a branch dispatch; and a cross-mode candidate surfaces as exit 2 instead of the documented 6 | Medium | UX-96, UX-108 | 🟢 Done — all three clauses run on live refs with before/after pasted; the publish decision extracted from the YAML into `publish_decision()` and falsification-tested; cross-mode candidate now exits 6 from `bga compare` itself | [UX-114](UX-0114-the-baseline-sets-edges-three-small-holes-round-12-walked-into.md) |
| UX-115 | The CI comment exists in a design doc and nowhere else: every ingredient is verified and shipping (band verdict, both gates, marginal element table, churn with honest labels, findings ids) and nothing renders them where a reviewer looks. A gate that fails as JSON gets its threshold loosened; one that fails as a named element with its cost gets the element fixed. Render-only by design | High | UX-79, UX-96, UX-75 | 🟢 Done — `--format ci-comment` renders the design-doc sketch from the same predicates the exit codes use (verdict + band, gate table with one-sentence reasons, added-elements table, honest not-requested/not-measured cells, instance stamps, update-in-place marker); all three named retained pairs pasted, re-verified live in round 13 on the grow pair | [UX-115](UX-0115-the-ci-comment-exists-in-a-design-doc-and-nowhere-else.md) |
| UX-116 | UX-09's founding question — what should `builders × max-jobs` *be* — now has every constraint measured in one capture (scheduling knee, cores-busy, memory envelope, pinning) and no block intersects them; the report still calls the CPU axis "currently unmodeled" even where it no longer is | Medium | UX-83, UX-104, UX-14, UX-31 | 🟢 Done — the capacity block intersects knee/cores-busy/memory/pinning and names the binding constraint; the round-10 clause produced the *opposite* of the predicted answer and a real 4×3 timing table settled it — "room for 2 more builders" was not realizable, and the claim was reworded rather than the test | [UX-116](UX-0116-the-tools-founding-question-is-now-answerable-and-unanswered.md) |
| UX-117 | The spine's **degrade path hangs the build it promised never to break**: on `cont-failed` only the offending pid is detached; every other tracee's next event-stop is popped and never restarted, and the loop waits for an `ECHILD` that cannot arrive. Inverts UX-106's own "detach everything" clause. Opt-in default bounds the blast radius — a filing, not a stop-ship | High | UX-106 | 🟢 Done — the deadlock demonstrated before it was fixed: five concurrent children and a degrade forced at 2, 4 and 8 events **hung 3 of 3** on the shipped logic, exit 4 in 0.7s after. Detach-on-stop rather than the tracked set the task suggests, because every tracee reaches that branch on its own (`PTRACE_O_TRACEEXIT`). The first attempt hung on its own test — it detached tracees *with* their pending attach-SIGSTOP — which is what the injection seam is for | [UX-117](UX-0117-the-spines-degrade-path-hangs-the-build-it-promised-never-to-break.md) |
| UX-118 | Every auto-attached child's attach-SIGSTOP is **re-injected** (only SIGTRAP is zeroed), converting the kernel's attach-stop into a real group stop per process — extra cost per process, a `CLD_STOPPED` the untraced build never produces, and via `__ptrace_unlink`'s group-stop reinstatement, the likely mechanism of UX-106's measured-failing kill-the-tracer clause | High | UX-106 | 🟢 Done — **it corrected `UX-106`**: that task recorded its kill-the-tracer clause as measured-failing and blamed ptrace at large; re-run 3× per binary, the traced tree completed 0/3 before and 3/3 after. The mechanism was ours — restarting a tracee with the kernel's attach-SIGSTOP makes a real group stop, which `__ptrace_unlink` re-instates when the tracer dies. The predicted overhead win did **not** appear: `examples/08` re-measured at +13.2% median against `UX-108`'s +13.5%, unchanged within the spread, recorded either way | [UX-118](UX-0118-every-new-tracee-gets-its-attach-sigstop-thrown-back-at-it.md) |
| UX-119 | The spine's pid-1 story is untested in the shape it ships in: BuildStream's real bwrap argv carries no `--as-pid-1`, so bwrap's reaper is pid 1 and the spine is pid 2 — where its fatal-signal handlers *replace* the default terminate disposition (the hang the comment claims to prevent), while the pid-1 branch it was written for would discard `raise(signo)` under `SIGNAL_UNKILLABLE`. Tests run it as a plain subprocess, never in a sandbox | Medium | UX-106 | 🟢 Done — premise corrected (`bwrap --unshare-pid` puts the command at **pid 2**; `--as-pid-1` at 1), and the alternative measured and rejected: bare bwrap with `--as-pid-1` surfaces **0** for a signal-killed command against 143 without it, so passing the flag would change what BuildStream observes about its own builds. The predicted cancellation hang did **not** reproduce — the handler forwards, the command dies, and the re-raise makes the status right — so the dispositions stay with their rationale replaced. The `setpgid` race was real and is closed. Status parity now tested inside a real sandbox against bare bwrap as the control, not against an expected number | [UX-119](UX-0119-the-spines-pid-1-story-is-untested-in-the-shape-it-ships-in.md) |
| UX-120 | UX-100's merge candidate has never fired on real data: the acceptance's positive case (fine-grained fixture, fired candidate, projection vs a real merged rebuild) was never run, and unlike the task's two recorded deviations this one was unrecorded. Real captures have only ever produced the negative answer, which cannot distinguish a working detector from an inert one | Medium | UX-100 | 🟢 Done — the merge candidate fired on real data for the first time (`examples/09-fine-grained-siblings`); projection 1.00s vs measured median 2.70s → re-hedged to a floor (`projection_is_a_floor`), with the never-documented "band" clause refuted in place rather than invented | [UX-120](UX-0120-the-merge-candidate-has-never-fired-on-real-data.md) |
| UX-121 | `bga compare` still prints `Execution On Chain Us` beside a value in seconds — UX-111's own item 1, unfixed on one of the six surfaces it audited — and the consistency test asserts the *helper*, not the rendered surfaces, so it passed anyway (the UX-85 pattern recurring) | Medium | UX-111 | 🟢 Done — one line of renderer, and the test re-pointed at **rendered surfaces** rather than the helper it was asserting — which is why it passed while `compare` printed `Execution On Chain Us` through the whole of `UX-111`. Verified by reverting the fix and watching the new test fail | [UX-121](UX-0121-compare-still-says-us-and-the-consistency-test-cannot-see-it.md) |
| UX-122 | The capture guide's ref globs are stale **again** — a documented fetch command matches no existing ref (the mode segment is missing, UX-97 item 2 recurring in the file it was fixed in), the manual band-assembly block UX-96 abolished still stands, and `bga baseline` is missing from cli.md's exit-6 list. Fix + a docs test validating every documented `captures/…` pattern against the workflow's ref-name expression | Medium | UX-97, UX-96 | 🟢 Done — both globs fixed, the manual band assembly replaced by the `bga baseline` one-liner it was superseded by, and `bga baseline` added to the exit-6 list. The recurrence-stopper reads the workflow's own `RUN_REF=` assignment and checks every `captures/…` token in the **guides** against the shape it generates — verified by putting the bug back and watching it fail. Scoped to the documents that tell a reader what to type: `docs/backlog/` quotes ref shapes as history, and this task's own file quotes the broken glob as the defect | [UX-122](UX-0122-the-capture-guides-ref-globs-are-stale-again-and-nothing-can-catch-them.md) |
| UX-123 | Spine record hygiene: exit records emitted for fork-without-exec children (discarded uncounted, wearing the parent's cmdline), exec chains billing CPU/RSS/exit to the **pre-exec** image with the surplus START counted as "no observed exit" (plausibly part of fdsdk's 8,135), and a stream join that takes the *first* hook record within 1.0s rather than the nearest — under pid reuse the repo's own tests document | Medium | UX-106, UX-107 | 🟢 Done — the exec-chain defect was the big one: **7,384 fdsdk records** billed a whole pid's CPU to the pre-exec image, which put **`sh` third in "where did the CPU go"** at 2.6s; after the fix that slot is `python` and `sh` leaves the top five. Chains collapse into one record rather than re-pair, because /proc and getrusage are per-pid and cumulative across execs. Unobserved exits 37 → 16 on the fdsdk head and 9 → 0 on `examples/06`; 552 fork-only exits now counted rather than silently dropped; the join takes the nearest candidate, not the first | [UX-123](UX-0123-spine-record-hygiene-ends-without-starts-exec-chains-and-the-nearest-join.md) |
| UX-124 | UX-104's fdsdk clause was recorded unattainable — and a capture that satisfies it (32223468993, carrying `host_memory_mb`) shipped the next day, unused. Round 12 ran it: *"4 builders … ~4.0 GB of 15.6 GB (25%); 11 would still fit"*. Close the clause, and replace README's hand multiplication with the tool's own envelope line | Low | UX-104, UX-108 | 🟢 Done — the clause was always satisfiable, by a capture that did not exist when it was written and did by the next morning; re-run live against run `32223468993` and pasted into `UX-104`'s log, and the README's hand multiplication replaced by the tool's own envelope line. No logic changed: the fallback already fires only where a capture records no host memory. "Unattainable" is a claim about the data on hand, and it expires | [UX-124](UX-0124-close-ux-104s-fdsdk-clause-with-the-capture-that-can.md) |
| UX-125 | `bga doctor` — every capture environment this project ever stood up was assembled by failure (venv-vs-setuptools, missing plugins, bwrap loopback sysctl, no compiler, unstaged runtimes); every check already exists somewhere in CI comments and script headers, and only the *sequence* is the user's problem. Post-MVP polish: the setup half-day becomes a minute | High | UX-105 | 🟢 Done | [UX-125](UX-0125-bga-doctor-the-setup-half-day-becomes-a-minute.md) |
| UX-126 | The local loop is three commands and five user-invented paths, with the project repeated — everything clerical, and clerical is what fails at 6pm. `bga snapshot` + a project-local run store (`.bga/runs/`, `@last`/`@prev` resolution, sticky capture flags): the loop becomes **one command, run twice**, with the second printing the compare verdict automatically | High | UX-95, UX-78, UX-115 | 🟢 Done | [UX-126](UX-0126-the-loop-should-be-one-command-run-twice.md) |
| UX-127 | Plane 3's front door undercuts its "costs nothing" pitch: it wants the project's log-tree *name*, discoverable only by `ls ~/.cache/buildstream/logs`, and handing it the obvious argument — the project directory — yields a confidently wrong "nothing to report" (reproduced live). Take the project you have; list what the tree holds | Medium | UX-91 | 🟢 Done | [UX-127](UX-0127-cache-logs-should-take-the-project-you-have-not-the-name-it-hides.md) |
| UX-128 | UX-117 guarded one `PTRACE_CONT` site and wrote "no other path can strand a tracee" — **three sibling restart sites discard the CONT return value** (exec-stop, exit-stop, fork-stop, + the initial one), each able to reopen the identical hang; and three filed in-a-real-sandbox clauses (UX-117/118/119) landed as plain subprocess tests | High | UX-117 | 🟢 Done | [UX-128](UX-0128-three-more-unguarded-cont-sites-reopen-the-hang-one-branch-over.md) |
| UX-129 | UX-112's replacement claim overshoots its own data: the published figures span **0.32-1.14 ms/process** (not "roughly a millisecond", not "+1.5 to +2.3s absolute"), the cited `matrix.json` does not exist, run order is unstated on a machine the file says drifted — and the only real-scale observation (fdsdk **+11 min**) contradicts the "budget two minutes" extrapolation by ~5×, unremarked in four doc sites | High | UX-112 | 🟢 Done | [UX-129](UX-0129-the-millisecond-does-not-reconcile-the-figures-it-claims-to.md) |
| UX-130 | UX-118 fixed the re-injected attach-SIGSTOP by *guessing* which stop is the attach one, and the guess is wrong at both ends: the direct child's own SIGSTOP is eaten (and the pass-through test passes either way), `forget_pid` zeroes probe chains that only collide at fdsdk's pid scale, and classic ptrace cannot see group-stops at all. `PTRACE_SEIZE` + `PTRACE_EVENT_STOP` deletes all three | Medium | UX-118 | 🟢 Done | [UX-130](UX-0130-seize-dont-traceme-the-attach-stop-should-be-identifiable-not-guessed.md) |
| UX-131 | Third recurrence of the two-copies defect: **five** status-table rows contradicted their task files this round (UX-114/115/116/120 shown 🔴 with 🟢 files; UX-100 stale). The emoji and the file's line-3 status are mechanically comparable — make a test compare them | Medium | — | 🟢 Done | [UX-131](UX-0131-the-status-table-and-the-task-files-can-disagree-silently.md) |
| UX-132 | UX-123 changed 822→813 and 1833→1812, and UX-107/UX-108 still state the old figures unannotated — while UX-118, in the same range, went back and annotated UX-106 properly. The annotate-what-you-invalidate convention exists only where one author remembered it; write it into the fixing guide and apply it to the three sites | Medium | UX-123 | 🟢 Done | [UX-132](UX-0132-figures-a-later-fix-invalidated-need-a-convention-not-luck.md) |
| UX-133 | Spine/parser hygiene round two: pairing fabricates a merged record under pid reuse when an END went missing, the fork-only counter under-counts and mislabels hook-stream orphan ENDs, and the spine waits for *every* descendant so a build step leaving a daemon behaves differently traced vs untraced | Low | UX-123, UX-106 | 🟢 Done | [UX-133](UX-0133-spine-parser-hygiene-round-two.md) |
| UX-134 | `UX-126` gave every run directory a name (`@last`/`@prev`), but the Plane 2 report sitting beside it in the same snapshot has none — so the two-plane join, the one command needing both at once, is half alias and half hand-copied path | Low | UX-126, UX-51 | 🟢 Done | [UX-134](UX-0134-the-store-names-the-run-but-not-its-plane-2-report.md) |
| UX-135 | The README makes a new user read **~2,100 words / 196 lines** before their first command on their own project: three fixture demos, a 60-line showcase of someone else's build and a report-literacy course all precede "Use it on your real project", where the two-command flow sits in fifth position. Reorder and cut: 420 → ≤250 lines, doctor+snapshot immediately after install | High | UX-125, UX-126 | 🟢 Done | [UX-135](UX-0135-the-readme-makes-a-new-user-read-2100-words-before-their-first-command.md) |
| UX-136 | The two most-read docs teach only the superseded flows: `bga baseline` appears **zero times** in README and real-project.md while both teach the manual three-`--baseline-run` assembly; ci-comment's main YAML teaches wrap+extract with `--run-dir` as a retrofit; and two guides still print correlate output the tool no longer produces ("multiply by however many…", replaced by the memory envelope real-project.md never mentions) | High | UX-96, UX-115, UX-124, UX-126 | 🟢 Done | [UX-136](UX-0136-the-two-most-read-docs-teach-only-the-superseded-flows.md) |
| UX-137 | Eleven duplicate clusters across the 3,094-line user corpus — the gate table pasted three times, the cmake-stage1 block in three *drifted* copies, the noise-band lesson taught with **two different noise figures** for the same project — each a future drift finding waiting for an audit. One canonical home per cluster, everything else one sentence + a link | Medium | UX-135, UX-136 | 🟢 Done | [UX-137](UX-0137-eleven-things-the-docs-say-twice-and-one-they-say-three-ways.md) |
| UX-138 | One concept, one name: "sandbox tax" vs "toll" alternating mid-paragraph (the CLI itself prints both), two unrelated meanings of "cold" in one reference doc, "task" where user docs mean "element", set-vs-band, three spellings of run directory. The variants table is the worklist; a five-line glossary pins capture vs snapshot | Medium | — | 🟢 Done | [UX-138](UX-0138-one-concept-one-name.md) |
| UX-139 | Journey B (the CI owner) has no page — gates, comment, baseline mechanics and a README summary live in four documents — and two files listed as "guides that tell you what to type" are a retired-era transcript and a case-study-by-its-own-framing. Make ci-comment.md the journey-B page; retire one walkthrough, reclassify the other; split the capture guide's ops history out of guides/ | Medium | UX-136 | 🟢 Done | [UX-139](UX-0139-journey-b-has-no-page-and-two-guides-are-not-guides.md) |
| UX-140 | When SEIZE is unavailable the spine must **exec, not wrap**: the fallback now survives as a wrapper returning `128+WTERMSIG` — the exact `WIFSIGNALED`-vs-`WIFEXITED` confusion the same file's own UX-106 correction documents as wrong, with BuildStream as the parent reading it; no seam, no test (`grep -rn seize tests/` is empty) | High | UX-130 | 🟢 Done | [UX-140](UX-0140-when-seize-is-unavailable-the-spine-must-exec-not-wrap.md) |
| UX-141 | The failure-injection lists still name the `initial` site UX-130 deleted — both `[initial]` tests pass **vacuously**, one inflating the bst tier pin — while the new `attach` site (the restart that runs once per process, ~127k times on fdsdk) has no coverage in either tier | High | UX-128, UX-130 | 🟢 Done | [UX-141](UX-0141-the-guard-list-names-a-site-that-is-gone-and-misses-the-one-that-runs-most.md) |
| UX-142 | `bga doctor` hardcodes `all.bst`: any project without one — essentially every real project, freedesktop-sdk included — gets a false `[FAIL] project-loads` and exit 1 from the exact command the walkthrough teaches first. It passes CI only because all nine examples ship an `all.bst`: a fixture convention read back as a world fact | High | UX-125 | 🟢 Done | [UX-142](UX-0142-doctor-fails-every-project-not-named-all-bst.md) |
| UX-143 | Spine degrade/drain edges: a group-stopped tracee reaching the degrade branch is detached with signal 0 and thereby *resumed* (untraced it would have stayed stopped); the drain-cap break strands its popped tracee to kernel auto-detach while the comment claims a release; and two guards assert less than their prose (the seam test covers one seam of two; doctor's compiler test restates the implementation) | Medium | UX-130, UX-133 | 🟢 Done | [UX-143](UX-0143-spine-degrade-and-drain-edges-and-two-guards-that-assert-less-than-they-say.md) |
| UX-144 | UX-130 deleted UX-118's entire mechanism and UX-128's `initial` site and annotated neither — in the same range that wrote the annotate-what-you-invalidate convention, whose scope ("a number") proved too narrow for its own worked example. Widen to mechanisms; annotate the two files; fix two stale counts; reconcile UX-106's 🟡 against its now-met return condition | Medium | UX-132, UX-130 | 🟢 Done | [UX-144](UX-0144-ux-130-invalidated-a-mechanism-and-a-guard-table-and-annotated-neither.md) |
| UX-145 | Snapshot's sticky flags apply silently (set `--trace-spine=off` once, get an unexplained blind spot three weeks later — what-ran is recorded, which is not the same as not-surprising); `bga baseline --candidate` is the one run-dir argument without `@last`; and the compare note renders "Memory envelope grew" on a **+0.0 GB** delta (observed live) | Low | UX-126 | 🟢 Done | [UX-145](UX-0145-snapshot-should-say-which-sticky-flags-it-applied.md) |
| UX-146 | Filed from a real user report, not an audit: `bst build` succeeds, `bga snapshot` fails with `buildbox-run failed with returncode 1`, and turning off both optional mechanisms changes nothing — three unrelated causes produce that sentence and none is visible from outside. `--diagnose` records what the shim received and exec'd; `--no-inject` bisects the rewrite against the shadowing | High | UX-11, UX-125 | 🟢 Done | [UX-146](UX-0146-a-capture-that-fails-tells-the-user-nothing-about-why.md) |
| UX-147 | Zero shim invocations has **three** causes — all cache hits, shim never resolved (absolute-path bwrap, or a `buildbox-casd` started before the capture whose PATH predates the shim), shim resolved but exec failed (env-python3 shebang under a noexec/AppArmor temp dir) — and UX-146's summary asserts the benign one. Self-probe exec at capture start failing fast with the errno; absolute `sys.executable` shebang at install; stale-casd detection; a summary that names all three with its evidence | High | UX-146 | 🟢 Done | [UX-147](UX-0147-zero-shim-invocations-has-three-causes-and-diagnose-asserts-the-benign-one.md) |
| UX-148 | The diagnostics record proves the rewrite happened; nothing preserves what the rewritten sandbox *said* when it died (`buildbox-run` reports only a return code on at least one real stack) and nothing can re-run it. Under `--diagnose` the shim tees the real bwrap's stderr per invocation (signal semantics kept — the UX-140 contract); a failed build's summary quotes the stderr tail; `bga capture replay-sandbox` re-execs a recorded argv directly | High | UX-146 | 🟢 Done | [UX-148](UX-0148-a-failed-sandbox-should-leave-its-argv-and-its-stderr-behind.md) |
| UX-149 | `bga doctor` proves the parts with bga's own arguments and `--diagnose` instruments the user's real (expensive, already-failing) build; nothing runs the actual chain — bst → buildbox-run → PATH shim → rewritten argv → hook — on a canned ten-second probe with every recorder on. `bga doctor --capture`: per-link ok/FAIL/skip in chain order, classifying exactly UX-147's causes plus "hook produced no records" | High | UX-125, UX-146, UX-147, UX-148 | 🟢 Done | [UX-149](UX-0149-doctor-checks-the-parts-and-nothing-checks-the-chain.md) |
| UX-150 | The one configuration users actually deploy — wheel from a checkout in one dir, installed into another project's venv, run from a third cwd — is the one configuration no CI capture runs; round 15 proved it works by hand and nothing keeps it working. The packaging job gains a real installed-wheel end-to-end capture; README Install documents the deployed form first, `-e .` as contributor mode | Medium | UX-77 | 🟢 Done | [UX-150](UX-0150-the-shape-real-users-install-in-has-zero-capture-coverage.md) |
| UX-151 | The shim's arity table (validated on bubblewrap 0.9.0, unknown `--flag` = arity 0) mis-splits at any newer flag's *operand* — producing exactly the field sentence, unchanged by turning mechanisms off — while the mis-split detector only checks `command[0].startswith("-")` (the real mis-splits put a numeric operand there) and the record omits every version fact its own motivation blames. Widen the table; make unknown flags a recorded condition; detect numeric `command[0]`; fingerprint bwrap/bst versions | High | UX-146, UX-11 | 🟢 Done | [UX-151](UX-0151-the-arity-table-is-the-likeliest-field-failure-and-nothing-records-the-version-that-breaks-it.md) |
| UX-152 | UX-143's `detach_signal` tests `event != 0` before consulting `is_group_stop_signal` — under SEIZE a group-stop **is** an event-stop, so it returns 0 for exactly the case it was written for; the degrade branch it was filed against still uses `pass_through` untouched; net: a group-stopped tracee is resumed on all three detach paths, and the acceptance's state-`T` probe was never written | High | UX-143, UX-130 | 🟢 Done | [UX-152](UX-0152-ux-143s-group-stop-fix-has-the-bug-it-was-filed-against.md) |
| UX-153 | UX-142's probe principle, half-applied: `check_staged_sources` and the tracer's census (seven sites) still hardcode `elements/` — on a nonstandard layout the census comes back empty and `--trace-spine=auto` silently traces everything at full price; the "wire doctor into the real-project workflow" acceptance clause was dropped unrecorded; `check_compiler` tests presence while the spine needs `-static` | Medium | UX-142, UX-125 | 🟢 Done | [UX-153](UX-0153-ux-142-fixed-the-headline-and-left-its-principle-half-applied.md) |
| UX-154 | Four round-14 claims outran their commits: `real-project.md` has zero `bga baseline` and still teaches the superseded three-flag assembly UX-136 was filed against; UX-138's `toll` sweep stopped one file short of `bga correlate` (which still alternates tax/toll in one sentence, pinned by a test); README's 430 was 420; UX-141 cross-references the wrong finding | Low | UX-136, UX-138, UX-141 | 🟢 Done | [UX-154](UX-0154-four-claims-that-outran-their-commits.md) |
| UX-155 | bga's scratch (shim, `hook.so`, spine, `run`'s unnamed intermediates) lived in `TMPDIR`, so a `noexec` temp mount failed the capture inside `buildbox-run` where the error is swallowed — and `probe_bwrap_shim`'s remedy, "set `TMPDIR`", led a real user to a relative path that killed `buildbox-casd` with `mkdtemp, errno: no such file or directory` | High | UX-147, UX-11 | 🟢 Done | [UX-155](UX-0155-bgas-scratch-lives-in-tmpdir-and-the-advice-for-fixing-that-breaks-the-build.md) |
| UX-156 | A snapshot of a build that **failed** ends with `Verdict: IMPROVED (-65.6%)` — four elements never built, of course it was faster — while `run-context.json` records the failed elements and nothing consults it; the failed run then silently becomes `@prev`, so the next healthy compare measures against wreckage. The verdict must lead with NOT COMPARABLE (UX-78 refusal, exit 6 under gates); auto-compare skips failed baselines and says so | High | UX-126, UX-78 | 🟢 Done | [UX-156](UX-0156-a-build-that-did-not-finish-must-not-verdict-as-if-it-did.md) |
| UX-157 | Ctrl-C on an hours-long capture: raw `KeyboardInterrupt` traceback, and the snapshot holds only `build.log` — every Plane 2 artifact sits in scratch until a post-return copy (`tracer:548-562`) that an interrupt skips, and the `finally` rmtree then deletes hours of trace already on disk; the completed elements are never extracted, and the orphaned `bst` can keep building. Interrupt means salvage: forward, wait, copy in a `finally`, extract, exit 130 | High | UX-155, UX-126 | 🟢 Done | [UX-157](UX-0157-ctrl-c-on-an-hours-long-capture-destroys-the-trace-it-already-has.md) |
| UX-158 | The docs were cut 3,128 → 2,203 for concision and `--help` was never audited: `compare` 143 lines, `cache-logs` 88, `capture run` 82 — module docstrings full of design history are fed to argparse verbatim, so "what do I type" is three screens of provenance. Short `description=` per command, history stays in files, a guard caps rendered help at ~40 lines | Medium | UX-135, UX-137 | 🟢 Done | [UX-158](UX-0158-help-is-a-design-history-lecture.md) |
| UX-159 | On a big project the small-project comforts stop holding: bga's own phases (hook compile, census walk, `bst show` extraction, analyze) are silent for however long they take, `--list` shows no sizes, and store management is a 2 GB note advising hand-deletion. One announced line per >5s phase; sizes in `--list`; `bga snapshot prune --keep N / --older-than D` that refuses `@last`/`@prev` | Medium | UX-126, UX-113, UX-155 | 🟢 Done | [UX-159](UX-0159-the-quiet-minutes-and-growing-gigabytes-of-a-big-project-snapshot.md) |
| UX-160 | Element discovery is non-recursive `os.listdir` in all three census sites — every example keeps elements top-level, essentially every real project nests them — so on real layouts the census assesses nothing, and UX-113's correct fail-safe quietly turns `--trace-spine=auto` (snapshot's default) into full-build ptrace at a cost UX-108 never measured, with no output saying so. UX-142's lesson one directory deeper | High | UX-113, UX-153, UX-108 | 🟢 Done | [UX-160](UX-0160-the-census-reads-only-the-top-of-the-element-tree-and-auto-spine-bills-the-difference.md) |
| UX-161 | UX-147 deferred its stale-`buildbox-casd` detection and the review sharpened the cost: the summary names the cause it cannot confirm, `doctor --capture` isolates `HOME` so it *structurally cannot* see the daemon (fresh casd every probe), and any plain `bst` command before a snapshot plants one. The question is answerable from `/proc` before the build — check, warn with the ten-second remedy, record in the fingerprint, upgrade the summary from guess to verdict | High | UX-147, UX-149 | 🟢 Done | [UX-161](UX-0161-the-stale-casd-everyone-suspects-is-checkable-before-the-build-and-nobody-looks.md) |
| UX-162 | Small debts the diagnosability round still owes: `buildbox_run_path` null on every standard install (`which` misses the vendored binary — verified live), "Record: (empty)" beside a fingerprint-bearing file, dead `doctor_exit` in the workflow, `element_path()`'s naive parse, four acceptance-named claims with no test, UX-152's "impossible" probe that is merely unwritten, and the one-sentence `.bga`-staging caveat real projects need | Medium | UX-151, UX-152, UX-153, UX-149, UX-155 | 🟢 Done | [UX-162](UX-0162-small-debts-of-the-diagnosability-round.md) |
| UX-163 | UX-157's salvage protects only interrupts that land while `bst` runs — round 17 hit the other window live: SIGINT during `Extracting run data...` is a raw traceback and a snapshot without `run/`, though `build.log` is complete and extraction re-runnable (nothing says how). The long unprotected windows are exactly the phases UX-159 flagged as slow; plus the 120s SIGINT grace whose escalation costs the biggest builds their `queue_summary` | High | UX-157, UX-159 | 🟢 Done | [UX-163](UX-0163-the-interrupt-contract-covers-the-build-and-not-the-minutes-around-it.md) |
| UX-164 | After a walk-back the printed hint `$ bga compare @prev @last` resolves to the *skipped* wreckage — pasting it reproduces the comparison UX-156 exists to prevent (observed live); "…: \<stamp\> record build(s)" breaks number agreement; and "0 of 7 scheduled elements built" counts six cache hits as casualties (queue was 0 built / 6 cached / 1 failed) | High | UX-156 | 🟢 Done | [UX-164](UX-0164-the-walk-backs-replay-hint-reproduces-the-comparison-it-just-refused.md) |
| UX-165 | UX-158's cut deleted continuation *lines* and **ten** flag helps lost their sentence's back half (the file first said seven; corrected by UX-176) — `correlate --help` ends "for the same", `graph --help` "grouped by BuildStream", plus five more incl. an unbalanced paren. The line-count guard rewards truncation; add a fragment check, restore the sentences, annotate the log's stale table | Medium | UX-158 | 🟢 Done | [UX-165](UX-0165-seven-help-strings-end-mid-sentence.md) |
| UX-166 | UX-161's cache-dir answer diverges from bst's: it reads only `buildstream.conf` while bst 2.7 tries `buildstream2.conf` first — a `cachedir` there makes the check silently miss the real daemon or alarm on the wrong one; the `cachedir:` parse repeats the naive `startswith` UX-162 just fixed for `element-path:`; relative argv resolves against bga's cwd | Medium | UX-161 | 🟢 Done | [UX-166](UX-0166-the-casd-check-reads-a-config-bst-does-not.md) |
| UX-167 | Prune's keep-set (`list_runs()[-2:]` + a config key nothing writes) contradicts the walk-back: with failed+interrupted as the two newest runs, `prune --keep 2` protects exactly those and offers to delete the store's only healthy snapshot — the next comparison's baseline (observed live); husks with no run directory survive every criterion; the "recorded baseline" protection guards a phantom producer | Medium | UX-159, UX-156 | 🟢 Done | [UX-167](UX-0167-prune-protects-two-aliases-and-not-the-baseline-the-walk-back-needs.md) |
| UX-168 | Big-project capacity and six one-liners: analysis slurps the whole trace into RAM (`load_and_summarize`, GB-scale RSS right after the build), the census closure is quadratic-ish and serial, the `--diagnose` tee can hang on a daemonizing sandbox descendant, a KeyError branch, a double assignment, the misplaced interrupt notice, the "0 with static binaries (spine traced)" riddle, and the per-invocation store stat-walk | Medium | UX-157, UX-148, UX-160 | 🟢 Done | [UX-168](UX-0168-analysis-holds-the-whole-trace-in-memory-and-other-round-17-leftovers.md) |
| UX-169 | UX-168 measured the memory it was filed against and found the reader was not it: `bga analyze` peaks at 635 MB either way on a 56 MB / 400k-event trace, because every traced process is materialised three times (event dict, merged record, report row) and all three are alive at once — a 16x amplification against the trace text, in the phase right after the build | Medium | UX-168 | 🟢 Done | [UX-169](UX-0169-the-report-holds-every-process-three-times-over.md) |
| UX-170 | The noise band `UX-59` added is strictly better than the fixed 1% rule and not yet enough: over **five** captures of the same fdsdk commit it absorbs the −5.8% and −9.8% pairs and still calls the widest one `IMPROVED (-25.0%)`, because 2712.4s falls below the lower edge of the band its own run helped compute (median 3405.8s ± 3×214.3s scaled MAD). A same-commit pair announced as a 25% win is the claim this tool exists not to make | Medium | UX-59, UX-96, UX-92 | 🟢 Done | [UX-170](UX-0170-the-noise-band-still-calls-a-same-commit-pair-a-25-percent-win.md) |
| UX-171 | Filed from a real user request: no analysis answers "this repo was touched — how many recipes rebuild, at what cost?" A `git` source keys on its **ref** (`directory:` narrows staging, not keying — any commit rebuilds every element on that url); a `local` source keys on **content**. Build the source inventory (element YAML, offline), the resource blast table (direct elements → closure → counts by kind → measured time), and the monorepo headline in Key Findings | High | Direction 6, UX-168 | 🟢 Done | [UX-171](UX-0171-blast-radius-by-shared-source-the-monorepo-question.md) |
| UX-172 | The developer's question points the other way: *I'm about to touch this — what does it cost?* `bga blast TARGET` auto-detects url / path / element, prints direct elements, closure, kinds, measured time from `@last`, and the keying clause; the path form answers at file level for `local` sources (which staged directories contain it). A question, not a gate — exit 0 | Medium | UX-171 | 🟢 Done | [UX-172](UX-0172-bga-blast-what-rebuilds-if-i-touch-this.md) |
| UX-173 | The user's first sentence taken literally: blast analysis ignores `element_kind` — which `graph.json` has carried all along. A blast of 84 where 39 are `stack`s and 4 `import`s is not a blast of 84 in any currency the user spends. Split counts into building vs assembling kinds everywhere blast is shown, add a cost-weighted ranking (measured durations when a run is present), and give the compare invalidation note the same split — the UX-164 honest-counting fix, one analysis over | Medium | UX-171, UX-156, UX-164 | 🟢 Done | [UX-173](UX-0173-a-blast-count-that-counts-stacks-overstates.md) |
| UX-174 | "buildstream is not the best tool for monorepo integration but there must be patterns" — both halves true, and the tool is about to measure the difference. One docs section: the ref-vs-content keying semantics (with the `directory:` caveat), the four patterns (whole-repo url / per-component refs / workspace-local / junction pinning) each with its blast shape, and the read-the-report loop — costs shown, choice left to the project | Medium | UX-171, Direction 6 | 🟢 Done | [UX-174](UX-0174-the-monorepo-patterns-page.md) |
| UX-175 | UX-163's 300s grace cannot deliver what it promises: on interrupt the read loop exits and nothing reads the child's stdout again, so bst's closing Pipeline Summary — the `queue_summary` every incompleteness sentence needs — never reaches `build.log` however fast bst complies, and a full 64KB pipe blocks the stopping bst into the escalation (demonstrated with a SIGINT-trapping fake bst). Plus: the shutdown's stopped-vs-killed answer is discarded by its only caller, and a re-extracted interrupted capture forgets it was interrupted (`bga extract` has no flag) | High | UX-163, UX-157 | 🟢 Done | [UX-175](UX-0175-the-grace-window-buys-nothing-while-the-pipe-stays-unread.md) |
| UX-176 | The clean-audit tail, one size smaller than UX-143's class: UX-164's paste-and-go hint is untested prose plus a vacuous source-grep guard; UX-165's "every string ends in a terminator" skips seven punctuation marks including the comma; UX-163's every-phase-converts guard counts `except` clauses in source; UX-169's Motivation keeps a table its own fix disproves; real-project.md calls the band's escapee the "slowest" run — it is the fastest | Medium | UX-163, UX-164, UX-165, UX-169, UX-170 | 🟢 Done | [UX-176](UX-0176-three-guards-that-assert-less-than-their-logs-say-round-three.md) |
| UX-177 | Five corners: `@stamp` exact-match loses to its own same-second sibling's prefix (pasting the printed hint raises "matches 2 snapshots" — reproduced); the casd config loop falls through on missing *key* where bst stops at existing *file*; `build_outcome`'s three-way counts have no consumer; the `.size` memo survives re-extraction wrong; `rstrip("\n")` vs CRLF in both streaming readers | Medium | UX-164, UX-166, UX-168 | 🟢 Done | [UX-177](UX-0177-five-corners-round-18-verified-its-way-into.md) |
| UX-178 | The Shared Sources table prints the normalized scheme-less identity; `bga blast`'s url detector requires a scheme — so the tool's own output, pasted as its own input, silently resolves as a *path* and answers "rebuilds nothing here" on the exact monorepo question the feature exists for (reproduced live). Plus: an existing-but-not-a-run directory is a raw traceback where the log promised exit 2, the deleted top-level file gap, and a promised sentence no code prints | High | UX-171, UX-172, UX-164 | 🟢 Done — an exact inventory match resolves before every heuristic, so the printed identity round-trips by construction (the guard renders the table, extracts the cell verbatim, and feeds it back); a not-a-run directory gets `analyze`'s sentence and **exit 2**; the no-element sentence is printed; a bare name reads as a path only when something stages the project root; "a url" | [UX-178](UX-0178-the-tools-own-printed-identity-does-not-round-trip-through-blast.md) |
| UX-179 | UX-173's acceptance named its one discriminating case (count order vs cost order disagreeing) and the review proved it was never built: reverting the sorter to count-only order left the class green — on the golden fixture both orders are identical, and the guard's closing assertion never compares them. The sorter is correct; the guard story is the exact shape UX-176 hunts, shipped beside UX-176. Plus `bga blast` outside every help guard and the untested memo-drop wiring | High | UX-173, UX-176 | 🟢 Done — the discriminating fixture exists: a stack-heavy blast outnumbering a cmake-heavy one, asserting the cost order *differs from* the count order (both printed on failure), and the count-only revert now reddens exactly it. `blast` added to the help guard, plus `test_every_subcommand_is_covered_by_this_file` so the list cannot fall behind the parser again; the memo-drop is tested through `bga extract` | [UX-179](UX-0179-the-discriminating-case-that-was-never-built.md) |
| UX-180 | UX-170's mechanism holds and its documentation trail did not keep up: real-project.md still states the fixed defect in the present tense (its own Required Fix ordered the correction), every verdict list omits the fourth verdict, the gate docstring still promises "never a second definition" — now false twice over — and the glossary has zero rows for the round's five load-bearing terms (blast, resource, keying, work, building-vs-assembling). README at 266 vs the ≤250 budget | Medium | UX-170, UX-138, UX-135 | 🟢 Done — all seven: the corrected sentence, the fourth verdict in both lists (and in `architecture.md`, which carried the stale claim *and* UX-176's fastest/slowest error), the docstring's "where the gate and the verdict now diverge", measured `directory:` provenance on BuildStream 2.7.0 (three real `%{full-key}` values), five glossary rows (five terms became ten), README **266 → 249**. Five guards, each falsified | [UX-180](UX-0180-the-docs-still-assert-what-the-disputed-region-broke.md) |
| UX-181 | `normalize_url` fails in both directions: `git+https://…` and uppercase schemes are mangled into garbage identities (the halved blast the function exists to prevent), while pip sources key on the *index* url — several pip elements report as one shared resource with "any commit rebuilds all of them" said about a package index. Per-kind identity, case-insensitive schemes, unknown schemes passed through; three one-line nits alongside | Medium | UX-171 | 🟢 Done — schemes matched case-insensitively and unknown schemes passed through untouched (`git+https://…` and `HTTPS://…` stop becoming garbage identities); pip keys on its sorted package list rather than the index url, and the keying sentence comes from the kind. The three one-liners with it: `split_by_kind` materialises its argument, `edges_outside_band` renamed to what it counts, `drain_until_exit` takes one final read after `poll()` | [UX-181](UX-0181-two-spellings-of-one-repository-and-one-spelling-of-two.md) |
| UX-182 | The inventory declares junction-prefixed elements `unreadable` — honest, and on exactly the junction-heavy shape the monorepo question arrives from (fdsdk-style), the Shared Sources answer degrades to a table of unreadable rows. Walk into checked-out junction subprojects; let `bga blast` answer the durations-free half from the inventory alone; resolve path queries against cwd first | Medium | UX-171, UX-172 | 🟢 Done — checked-out junctions are walked through the same memoised reader, content-keyed identities namespaced by the junction while repository urls stay global (so two subprojects on one monorepo group into one resource); `unreadable` survives only for junctions genuinely absent. `bga blast --no-cost` answers the structural half without the analysis: **0.102s vs 3.221s** on the 1202-element synthetic run. Path queries resolve against cwd first — but only when cwd is inside the project | [UX-182](UX-0182-the-inventory-stops-at-the-junction-boundary.md) |
| UX-183 | Field feedback: long commands need progress, and progress must not break pipes. In-phase progress on stderr only when stderr is a TTY (one self-overwriting line, no bar library); non-TTY stderr byte-identical to today; stdout untouched in both modes with a byte-identity guard on the `--format json` paths | High | UX-159, UX-168, UX-169 | 🟢 Done — one self-overwriting stderr line, drawn only when stderr is a TTY and `BGA_NO_PROGRESS` is unset (`bga snapshot --no-progress` sets it). A ticker on a non-TTY writes **zero bytes**; a redirected stderr carries UX-159's whole phase lines and no `\r`; `bga analyze --format json` produces byte-identical stdout with progress on and off, asserted through a real subprocess. Wired into the five long phases, one of which (`bst show`) became pollable — and lost a pipe-buffer deadlock — on the way | [UX-183](UX-0183-progress-you-can-see-pipes-that-stay-clean.md) |
| UX-184 | Field feedback: recipe authors put repo paths inside `kind: import`/`kind: manual`. Ground truth: the inventory reads sources on every kind, and bst rejects out-of-project local paths — but bga fed one anyway silently mangles it into a colliding identity. Named complaints for absolute/`..` paths, realpath for symlinks, and one real field stanza added verbatim to the fixtures | Medium | UX-171, UX-181 | 🟢 Done — an absolute or `..`-escaping **content-keyed** path is a named complaint instead of a silent normalisation (`/opt/monorepo` used to become `opt/monorepo` and collide with a real project-relative path of that name); content identities are normalised, so `sub/../files/src` is one identity with `files/src`; the query fallback refuses them too. A symlinked source directory resolves to one identity at extract time, so `vendor/lib -> files/lib` stops halving the blast. **The ask-the-user item is not done** — no channel to the field project from this session, recorded in the task file rather than dropped | [UX-184](UX-0184-sources-the-inventory-half-sees.md) |
| UX-185 | Field feedback: the computer sleeps mid-capture. Ground truth: hook and spine stamp `CLOCK_MONOTONIC` (stops during suspend) while Plane 1 stamps wall clock — a suspend makes the two planes disagree and nothing detects it. `--inhibit` wrapping the build in `systemd-inhibit` when present; drift detection feeding `incomplete_reason: suspended` into UX-156's refusal grammar | Medium | UX-156, UX-110 | 🟢 Done — the wrapper records both clocks at both ends, into the log so a kept log still carries the answer; wall running ahead of monotonic *is* the sleep. Threshold 5s, derived from NTP's 500ppm slew rather than tuned, and a backwards clock step is not sleep. Feeds `UX-156`'s `incomplete_reason` as a third answer, so the analyze banner, compare's refusal (**exit 6**) and the gate come for free. `--inhibit` on snapshot and capture wraps the build in systemd-inhibit (+gnome-session-inhibit), never rewriting the recorded `Executing command:` line that `UX-29` parses. Doctor warns where a sleep policy exists. The guards found `findings.py` calling an interrupted *and* a suspended capture a FAILURE — the wording UX-157 fixed in the report and missed here | [UX-185](UX-0185-a-capture-that-slept-must-know-it-slept.md) |
| UX-186 | Field feedback: "we can compare builds only built on current host — sbom capture to compare comparable." Compare performs no host check at all (verified: three comparability axes, none host-shaped) and run-context records two numbers that call a laptop and a runner the same machine. A host manifest (CPU model, kernel, distro, toolchain) in every capture; cross-host pairs caveated, confidence-capped, gates exit 6 unless `--allow-cross-host`; baseline warns on mixed sets | High | UX-78, UX-151, UX-92 | 🟢 Done — every capture records a `host/v1` manifest (CPU model from `/proc/cpuinfo`, not `platform.processor()`'s useless `x86_64`; count, memory, kernel, distro, bst/bwrap/cc versions). Compare classifies the pair: same → unchanged, **different** → caveat naming the differing values, confidence capped, and any `--fail-on-*` gate refuses with **exit 6** unless `--allow-cross-host`; **unknown** (a pre-manifest capture) → caveat only, still compares. The refusal is on the gates, not the comparison. `bga baseline` warns on a mixed-host set. Verified at the shell on all four paths; the manifest lives under `host_manifest`, since `host` is already the `--host` identifier | [UX-186](UX-0186-comparing-comparable-the-host-manifest.md) |
| UX-187 | Field feedback: check report readability on long output. The critical path still prints in full (UX-33's rule, written at ten elements), the Serialized line concatenates every chain, several sections have no top-N — while the newer sections show the house cap pattern. Render every format at 1,202 elements, cap what scrolls with named elisions and `--full-*` flags, JSON never truncates | Medium | UX-33, UX-168 | 🟢 Done — measured first: the 1,202-element synthetic run is *wide*, so its path is 14 elements and it does not exercise this at all; on a 400-layer chain the critical path rendered **405 of 498 lines (81%)** with every actionable section below it. Now **498 → 117**, folding the *middle* so both ends of the chain survive, with `--full-path` and `--full-sources` to restore. Every elision names its count and its flag — which also caught a live silent `[:5]` in the Serialized-chains line. JSON never truncates, and the flags do not change one byte of it. The guards caught two defects in the fix itself | [UX-187](UX-0187-a-report-you-can-read-at-four-thousand-elements.md) |
| UX-188 | Field feedback: chrome tracing for Plane 2 + a Plane 1+2 merge. Ground truth: the pieces exist and work (`log-to-chrome` live-verified; `native-to-chrome combined` is the merge) but snapshots don't retain the raw log it needs, wrong input succeeds silently with 0 events, and reaching it takes three commands with invented paths. Retain the raw log (gzip), `bga timeline @last`, refuse empty conversions, converter status to stderr | Medium | UX-51, UX-126 | 🟢 Done — all four, none of them the merge itself. Snapshots keep the raw Plane 2 log gzipped (**8.0%** and **8.6%** of two real captures, 12% on top of the processed report — so default-on, `--no-keep-raw` declines); `bga timeline [RUN]` composes the two existing converters rather than reimplementing them, verified on a real 24-process capture (77 events, 8 `native:` lane groups, aligned on the longest-traced element); a non-empty file with no parseable trace lines exits 2 naming the `plane2.json` mistake, while an empty one still passes; every converter's status line moved to stderr — which also stopped `timeline` telling the user to open a scratch path | [UX-188](UX-0188-one-timeline-both-planes.md) |
| UX-189 | Field feedback: cloning fetches the capture archive. Measured: eight `captures/*` refs, `.git` 50M against 5.3M with `--single-branch`. Both front doors now document the narrow clone and why; `bga baseline` verified still working from one (it reads through `FETCH_HEAD`, not the refspec). Found and fixed en route: `git show origin/captures/…` in the workflow doc is `fatal: invalid object name` under that clone | Low | UX-77 | 🟢 Done | [UX-189](UX-0189-a-clone-should-not-ship-the-capture-archive.md) |
| UX-190 | Field feedback: schemas evolved; add a schema switch. Ground truth: input formats are spec'd, output JSON (analyze/compare/blast) has no schema, no version field, and this very range renamed a published compare field with nothing to signal it. Self-declared `schema: analyze/v1` first key, `--schema` printing JSON Schema from the renderer's own source of truth, a validate-the-golden-run guard, Part 32 gains the output schemas | Medium | UX-75, UX-171 | 🟢 Done — `analyze/v1`, `compare/v1` and `blast/v1` self-declare with `schema` as the **first** key; `bga <cmd> --schema` prints the JSON Schema from the one module the renderers are built against, and needs no run directory. A round-trip guard validates the golden run's real output against the schema the CLI really prints (jsonschema, dev-only). The versioning rule — rename or removal bumps, addition does not — is in the fixing guide's checklist and the spec's new Part 32.5. A mutation found the missing direction: deleting a `required` entry reddened nothing, so a mirror guard now asserts every key the payload emits is required or named conditional | [UX-190](UX-0190-outputs-that-say-what-shape-they-are.md) |
| UX-191 | Field feedback: shell autocompletion, possibly via a click migration. Decision argued in the file: `argcomplete` completes argparse programs as they stand (one marker, one call, an optional extra) — subcommands and flags free, custom completers for `@`-aliases, element names and choices; a click rewrite would touch every subcommand and re-litigate UX-158 for no additional user value. Click recorded as considered and declined. Shipped: two-line integration, 22 completable run arguments, subcommands *and* all 17 UX-67 aliases | Medium | UX-158, UX-126 | 🟢 Done | [UX-191](UX-0191-tab-completion-without-a-rewrite.md) |
| UX-192 | The round-20 review's own findings: the table's 43-char elision breaks UX-178's round-trip for exactly the long-url projects the axis targets (67-char identity → "rebuilds nothing here", reproduced live — the passes-on-its-own-fixture shape one round later); blast's keying clause feeds `resolved_as` as the kind, so a pip resource says "any commit rebuilds all of them" — UX-181's sentence resurrected one surface over; plus the kind-pairing, junction-path, path-colon, pip-index, `git+http` and alias-help seams | High | UX-178, UX-181 | 🟢 Done — the table stops eliding (an identity wider than the column takes its own line; a key you cannot paste is not a key) and the round-trip fixture now carries a **68-char** identity; blast carries the resource's *kind* into `keying_clause`, so a pip resource prints the pinned-version sentence. The small list closed with it: `(kind, identity)` grouping, junctioned paths, the scp rewrite scoped to scheme-less forms (UX-181's log annotated - it claimed this already), the pip index back in the identity, `git+http`, `--no-cost` documented, and the every alias command under the help guard (five were over the cap: 75/67/67/55/55 lines) | [UX-192](UX-0192-the-elision-that-reopened-the-round-trip.md) |
| UX-193 | The user's call, round 21: the reports have outgrown the terminal. `bga view [RUN]`: stdlib `http.server` on `127.0.0.1`, ephemeral port, `webbrowser.open`; a vanilla-ES-module page with **no node toolchain and no build step** that renders the published schemas generically — the one rule that keeps it thin is that the JSON is the entire interface, so a new report field renders with zero viewer changes. View-hints v1 (`bga:quantity`, `bga:severity`, column order) join the schemas as the door for any external TS library | High | Direction 7, UX-190 | 🟢 Done — `http.server` on 127.0.0.1, an ephemeral port, an allowlist of four documents plus three assets and nothing else (no listing, no write method, no filesystem fall-through). The page renders the *schema*: `app.js` holds no list of the report's fields, and the discriminating guard adds a field that did not exist when it was written, asserts it renders, and hashes `app.js` to make "no viewer change" literal. View-hints v1 (`bga:quantity`/`severity`/`columns`/`direction`) are annotations, so UX-190's guard is unchanged. Three files, no package.json, no CDN. Run for real on a 46.1 s capture of examples/06 — which found two things no unit test could: `bga view @last` resolved to the snapshot instead of the run, and the schema did not declare `pipeline_overhead`/`timestamp_agreement`, both absent from the golden fixture UX-190's guard validates | [UX-193](UX-0193-bga-view-a-thin-window-onto-the-json.md) |
| UX-194 | No timeline of our own, ever: the documented Perfetto deep-link handshake (~30 lines, tab-to-tab, no upload — one docs sentence says so because it looks like one) feeds UX-188's combined trace to ui.perfetto.dev; `bga view --perfetto` skips the report page; a canned-PerfettoSQL page exposes the SQL engine the cheap way; the stay-on-legacy-Chrome-JSON decision recorded with its revisit trigger | Medium | UX-193, UX-188 | 🟢 Done — the documented deep-link handshake in ~90 lines: open, ping until PONG, post once, stop. A button on the report page (only when the run has a timeline) and `bga view --perfetto` for the direct route; both say plainly that nothing is uploaded, because it looks exactly like an upload. Gzip win measured on a real 871-event capture of examples/06: **272,964 B → 24,782 B, 9.1%**, served as `application/gzip` so the compressed bytes reach Perfetto unchanged, digest-identical to `bga timeline`'s output. Exit 7 when there is no timeline. Five canned PerfettoSQL questions, guarded to parse. Found en route: `bga view` leaked a scratch path (UX-197 item 2 at a second caller), and the origin-check guard asserted a property it could not test — both corrected | [UX-194](UX-0194-the-perfetto-handoff.md) |
| UX-195 | The same page as a file: `bga view --export report.html` inlines the JSON into one self-contained artifact — the CI journey's attachment beside the UX-115 comment, the "send me your report" answer, and the render a pruned snapshot leaves behind. Perfetto still works from `file://`; the size budget stated and measured | Medium | UX-193, UX-115 | 🟢 Done — one self-contained file: CSS inlined, both ES modules concatenated into one inline block (a browser refuses a relative import over `file://`), each payload in its own `<script type="application/json">`, the timeline a `data:` URL so the Perfetto button still works with no server. `load()` reads inline first and fetches second, so the export and the served page run the identical renderer. Measured: the 1,202-element export is **653,379 B with the page 6.0% of it** (Direction 7's thinness test); a real examples/06 capture with its timeline is **82 KiB**. Budgets reported, never enforced. CI docs gain the artifact step. Three defects found en route, one of them a guard that could not fail | [UX-195](UX-0195-the-report-you-can-attach.md) |
| UX-196 | The views where a drawing says what sentences strain to: the noise band as a strip with the runs as dots (UX-170's disputed region becomes one glance), the run store as a trend timeline (failed/interrupted/suspended marked, not hidden), and the blast explorer (the Shared Sources table clickable, a search box backed by the same resolution the CLI uses). Plus `--list --format json` as `store/v1` — the one payload the views need that does not exist | Medium | UX-193, UX-170, UX-171, UX-103 | 🟢 Done — `store/v1` first, with `--list`'s text rendering from the same rows. The band drawn: `UX-170`'s disputed region as geometry — band 100–110 s, baselines 95–118 s, candidate 114 s, and the marker must sit between the strip's edge and the dots' extent, asserted. The store trend marks incomplete snapshots as squares rather than dropping them (both mutations red). The blast box is pure transport: verified byte-identical to `bga blast --format json` on three targets of a real examples/06 capture. Exactly two SVGs, no library, no arithmetic — guarded. Four defects found, three of them in guards | [UX-196](UX-0196-the-views-that-make-the-numbers-self-evident.md) |
| UX-197 | The round-21 review's six seams: UX-183's byte-identity test compares progress-off with progress-off (no force-on exists); `bga timeline` still prints the doomed scratch path its log claims was fixed; a dead `RunContext.suspended` field; Ctrl-C during `bst show` now orphans the child; two stale counts; the UX-190 schema guards skip silently without dev extras | Medium | UX-183, UX-185, UX-188, UX-190 | 🟢 Done — all six, each reproduced first. The byte-identity test was worse than filed: **both** children emitted 0 stderr bytes, and routing the ticker to stdout left the original test *passing* — it now drives `bga snapshot --list` (102 stderr bytes on, 0 off) behind `BGA_FORCE_PROGRESS`, with a precondition test so it cannot go quiet again. `timeline` passes `quiet=True` and every printed path exists after exit; its two `FileNotFoundError` paths stopped printing to stdout and exiting 0. The dead field is gone (it sat one letter from the `suspension` property that works). `except BaseException: kill` restores `subprocess.run`'s contract — orphan reproduced at 120s, gone after. Both counts corrected with the annotation, one now read from `TOOL_ALIASES`. The schema module always collects; a canary fails where `BGA_EXPECT_DEV` is set (CI, workflow-wide) and skips honestly elsewhere | [UX-197](UX-0197-six-seams-round-21-verified-into.md) |
| UX-198 | Field report: Perfetto one-click broken in latest Chrome. Round 22 pinned it: the `window.open` is never synchronous with the user's gesture — the `--perfetto` page auto-runs with **no activation at all** (blocked every time), and the report button opens only after an async fetch (blocked past the ~5s activation window on any big trace). Open first, fetch after, post when both are ready; the `?url=` deep link with a Perfetto-only ACAO header as the belt to the braces | High | UX-194 | 🟢 Done — both halves. `handOff` opens the tab **before its first `await`** (activation is revoked at the first async gap, which is why it worked for a 25 KB warm-cache file and nothing else); the `--perfetto` page stopped auto-running `go()` at script load, where no activation had ever existed. `deepLink()` plus `Access-Control-Allow-Origin: https://ui.perfetto.dev` on the **trace blob only** — measured across every endpoint; never `*`, never on the report or blast, which carry element names. The link shows in served mode and hides in an export. A **gesture model** in the Node harness is what makes the ordering testable at all: the old harness could not tell the two apart | [UX-198](UX-0198-one-click-to-perfetto-in-the-browser-people-actually-run.md) |
| UX-199 | Field report: navigation in the HTML report is poor. Inventory agrees: no section `id`s, no TOC, no collapse, one long scroll in payload key order — Ctrl-F is the navigation. Anchors + sticky TOC + collapse + a jump box; and the export keeps its functionality (the SQL questions inlined rather than stripped; the dead blast search box hidden with a note) | High | UX-193, UX-195 | 🟢 Done — and it uncovered a live regression the filing did not predict: the export inlined only `perfetto.js`+`app.js` while `app.js` has imported `views.js` since UX-196, so **every `--export` since then threw a ReferenceError in `boot()` and rendered "Could not load this run"** (measured: 0 sections before, 14 after). The module list is derived from the entry point's imports now. Section ids, a generated TOC, default-open collapse with localStorage, a jump box; the questions inlined from a shared `questions.js` and the blast box hidden where its fetch cannot work | [UX-199](UX-0199-a-report-you-can-find-your-way-around.md) |
| UX-200 | Field report: `bga analyze` inside snapshot runs for minutes with no progress. Ground truth: the analyze pipeline has **zero** progress instrumentation — nothing under analyzer/correlate/ingest imports `progress`, and stderr *is* still a TTY there (in-process call, verified). Tickers per named phase incl. the quadratic attribution; plus the `[all]` extra pyproject lacks (`bst` + `completion`) | Medium | UX-183, UX-42 | 🟢 Done — six tickers through the pipeline (floors, attribution, utilisation, diagnostics, structural, confidence), measured drawing on the 1,202-element synthetic run with `attribution: 1202/1202`; the ticker sits inside each stage's own `if`, so a section that skips a stage says nothing. `progress.ticker` not `progress.phase` — the latter prints unconditionally and would have broken UX-183's piped contract (verified: 344 B on, 0 B off, stdout byte-identical). `[all]` = bst + completion, with `dev` kept out and CI asserting jsonschema does not leak in | [UX-200](UX-0200-the-minutes-inside-analyze-and-the-extra-that-installs-everything.md) |
| UX-201 | The external review's P0, verified line by line: schema hints stop at the top level and everything nested falls to name-sniffing — `peak_rss_mb: 512` renders "512 B", a 0-100 `cpu_pct` renders "4200.0%"; findings hard-coded against a schema that declares a bare array; the verdict banner string-matches prose. One contract wave: recursive hint resolution, column objects, item shapes into the schemas, `verdict_kind` enum, descriptions as popovers | High | UX-193, UX-190 | 🟢 Done — both live wrongnesses reproduced against the shipped renderer (`peak_rss_mb: 512` → "512 B"; `cpu_pct: 42` → "4200.0%") and fixed ("512.0 MiB", "42.0%"). Hints resolve recursively through `hintsOf`/`childNode`/`quantityFor`, which is the one place "declared beats guessed" lives; `megabytes` and `percent` joined the closed set. Columns are objects, so numeric-ness and sortability are declared rather than sampled. `findings[]` has a declared item shape with a severity enum. `verdict_kind` is emitted from the same branch as the sentence and defaults to `None`, never to a refusal. Descriptions are the popovers | [UX-201](UX-0201-the-schema-says-what-things-are-all-the-way-down.md) |
| UX-202 | The review's product observation, adopted whole: the viewer renders sections, not the argument. The overview waterfall (real duration → attribution gaps → certified floors, every number from published JSON, no viewer arithmetic) and the evidence header above it (confidence, coverage, incompleteness, host) — what this capture can support, before any number is believed | High | UX-193, UX-201 | 🟢 Done — every number in the overview is a published field, asserted by walking the rendered `data-field`/`data-raw` pairs back into `report.json` on the golden run *and* the real `examples/06` capture; the one division in `bar()` is a CSS width. Three fields entered `analyze/v1` additively rather than being computed on the page: `confidence.band` (a viewer asking "is 0.87 high?" is a second copy of the thresholds), `run_instance.incomplete_reason` (UX-185's one accessor), and `plane2_coverage` — the Required Fix asked for `stream_coverage`, which lives in the Plane 2 report, so `bga view` now passes the sibling `plane2.json` the store already writes (measured: 813 processes, opens coverage 1.00). The refusal banners moved into the evidence header, and an `ast` guard requires every reason `RunContext.incomplete_reason` can return to have a sentence there. Wiring it in also **reintroduced UX-199's export defect**: the two-line `import { … } from "./views.js"` matched neither half of the line-based strip, so the statement survived into the export and every report died on `ERR_INVALID_URL` again — the walker and the stripper share one statement-level expression now, guarded by a property assertion | [UX-202](UX-0202-the-page-that-answers-why-is-my-build-slow.md) |
| UX-203 | Round 22's own findings: the band view is **unreachable** (it renders only compare payloads and no CLI path ever serves one — UX-196's headline has never been seen outside its harness); the trend plots snapshot *size* where the filing promised duration/verdict/hit-rate; and the wheel guard never runs an installed `bga view` (CI's packaging loop stops at `--help`). Serve compare documents, extend `store/v1`, exercise the installed viewer in CI | High | UX-196, UX-193, UX-150 | 🟢 Done — all three, each reproduced first. `renderBand(analyze)` really did return **null** for every real report; serving a comparison alone would not have fixed it either, since a pairwise compare has no band at all (`MIN_BASELINE_RUNS` is 3) — so every earlier run in the store becomes a band sample, and a store too small now reports `{supplied: 1, required: 3}` where it used to show a blank. `store/v1` gained duration, verdict and cache-hit rate; the axis is duration and size moved to the tooltip. The old trend guards asserted a chart was drawn and never what it plotted. CI now serves the installed viewer — and the acceptance's proposed mutation (dropping the package-data glob) was **refuted**: the files ship anyway, so the step is falsified against the `ASSET_DIR` defect that really shipped | [UX-203](UX-0203-the-views-nobody-can-reach.md) |
| UX-204 | The investigation thesis: buttons that carry *why you are going* — TraceContext as a link-builder module, per-finding Investigate buttons with the matching canned query attached, the five questions grown into a categorized library findings reference by id. Rides on UX-198's transport, because context on a blocked popup is context on nothing | Medium | UX-198, UX-194, UX-201 | 🟢 Done — `trace_context.js` is a link-builder of pure functions; ten finding ids map to library queries by `id`, and the button's SQL *is* the entry it references rather than a copy. The query is deliberately **not** faked into the deep link — Perfetto has no documented way to preload the Query pane — so the floor is one paste, revealed on click whether the handoff succeeds or not. Dead-button rule kept: `boot()` passes the investigator only when the run has a timeline. The library gained ids, four categories and a sixth question, and closed a real drift — `sql.html` carried a **hand-written copy of every query** guarded only by a title comparison, so the SQL parse tests were checking the copy; the page renders the module now and may not contain a `<pre><code>` block. Coverage asserted both directions. Falsification found a live bug: `hidden` set through `el()` is a *property*, so `removeAttribute` cleared nothing and the paste would never have appeared | [UX-204](UX-0204-buttons-that-know-why-you-are-going-to-perfetto.md) |
| UX-205 | Tables sort and nothing else — no filter, no threshold, no way to reduce 1,202 rows to the twelve that matter. Per-table text filter with a count badge, numeric thresholds that parse units off UX-201's column metadata, copy cell/row; virtualization only if the 4,000-row measurement demands it, with the decision recorded | Medium | UX-199, UX-201, UX-187 | 🟢 Done — text filter with a `12 of 1,202` badge, per-column thresholds parsed against the unit `UX-201` declares (nine unit/quantity pairs pinned), and copy of a row as JSON or a cell as its `data-raw`. Every comparison runs on the published value: mutating it to the rendered string reddens three guards. An unparseable threshold is **no filter**, not a filter that hides everything. Item 4 answered **no virtualization**, with the numbers — 4,000 rows render in 146 ms and filter in 20 ms, and the caveat that this is a DOM shim rather than a browser. The tests found a real gap first: `applyFilters` walked each row's cells, so a threshold on a column a row lacked was never checked and every row passed. This is also where the export crossed the 80,000 B page ceiling for real — the inlined copy strips whole-line comments now (79,180 → 52,870 B of modules; the real capture is 62,152 B of page against 65,365 B of data) | [UX-205](UX-0205-tables-you-can-interrogate.md) |
| UX-206 | The review and Direction 7 converged independently: no general DAG viewer — the two graphs that are questions instead. The critical path as a width-proportional sequence (a list with widths, not a layout problem) and the blast tree as an indented hierarchy over data `blast/v1` already carries; both in the no-library, no-arithmetic discipline | Medium | UX-202, UX-199 | 🟢 Done — and the filing's premise for the second half was **refuted**: `blast/v1` did *not* already carry the tree. It had two flat lists and no per-element depth, kind or cost, so a viewer could only have got the shape by walking the graph in JavaScript — the second analysis the no-arithmetic rule exists to prevent. `blast_tree` entered the payload additively instead, and the first attempt at it was itself wrong: `compute_reachability` returns the *transitive* closure, so every element came out at depth 1 and the tree was flat. It walks the immediate successors now. The chain is drawn from `share_of_path`, a published field, with `UX-187`'s fold opening in place (1,193 folded of 1,202, all 1,202 shown after the click). The restraint is asserted, not promised: no `graph.js`, no runtime import, and a guard naming five layout libraries that must not appear | [UX-206](UX-0206-focused-graphs-not-a-dag-viewer.md) |
| UX-207 | The round-23 review's rule, adopted: first screen = decision, everything else = evidence. The page assembles fourteen sections at one visual level while the product's own answer — what should I fix first, what is it worth — renders mid-list, and the chain-bound diagnosis (`findings.py:974`) is published only as prose in one finding. A published `headline` block in `analyze/v1` (diagnosis enum, opportunity split, top actions), rendered as the first panel; the evidence header compressed to one status line; the refusal banner rendered once (today: twice); the overview's tail bars folded with no viewer-summed "Other" | High | UX-202, UX-204 | 🟢 Done — `headline` is published by `findings.py`: the diagnosis enum, the ratio it was decided by, the threshold, the sentence, the opportunity split and three action *references*. The decision existed since `UX-65` as a local `bool` that escaped only as a clause of one finding's title; `compute_findings` reads `diagnose()` now, and a guard pins `CHAIN_BOUND_RATIO` to one comparison. Two committed fixtures answer differently (golden `scheduler_bound` at 0.875 ranking by downstream count; `examples/06` `chain_bound` at 0.936 ranking by saving). Proving the panel *reads* needed a built case, not a mutation — recomputing the ratio reddened nothing until a payload existed where the published answer and the derived one disagree. The duplicate refusal was real (two `data-incomplete` nodes measured) and now renders once; the header is a status line over a `<details>`; the overview folds its tail with no summed row, guarded by "every bar's field resolves in the payload" | [UX-207](UX-0207-the-first-screen-is-a-decision.md) |
| UX-208 | Findings got investigate buttons (UX-204) and everything else stayed inspection-only: path boxes carry `data-share` and show nothing, table rows have no way into the workflow, the SQL is copied by hand next to a shipped `copy()` helper, "worst 10" takes the most typing, and the blast box opens empty when the payload knows the top targets. Popovers + investigate on path boxes, a declared element-column role giving rows a generic Inspect, Copy on every SQL block, a Top-N preset, blast example chips | High | UX-204, UX-205, UX-207 | 🟢 Done — and the declaration reached almost nothing until a second defect was fixed: `renderPairs` rendered a nested array of objects as a `<details><pre>` of raw JSON, so `signals.critical_path_detail` — the list of elements the whole report argues about — was a JSON dump, along with `optimization_horizon` and `latent_heavies`. They are tables now, through the same renderer, which is what makes the element role visible outside `blast/v1`. The first Inspect guard counted affordances page-wide and was **not discriminating**: with all three `signals` declarations deleted it stayed green on `headline.top_actions` alone; it asserts per table now. Twelve mutations red across both items. Two harness defects found and fixed rather than worked around — the DOM shim `append`ed instead of moving, turning a Top-10 into 20 visible rows, and its selector matcher could not see `section[data-section]`. The page-size guard did **not** hold at 80,000 B: every recoverable byte was recovered (the export now strips the stylesheet's comments, 1,239 B) and the remaining 3.6 KB is rounds 22–23's features, so the ceiling moved to 90,000 B with the arithmetic recorded and a new guard asserting the page is the checked-in modules and nothing else | [UX-208](UX-0208-everything-important-is-one-click-from-investigation.md) |
| UX-209 | The CLI frames bga around questions; the page answers in nouns ("Attribution", "Floors", "Signals"). A `bga:question` hint per section node in the schemas — one source for the page, the TOC and the text renderer, `title(key)` as the fallback; the TOC grouped into a rail of routes (decide/act/prove/investigate/raw); the question library folded to per-category `<details>`; the trend shrunk to its answer | Medium | UX-199, UX-201, UX-207 | 🟢 Done — `bga:question` and `bga:rail` declared on sixteen `analyze/v1` nodes and validated where they are written (a question that does not end in `?` is rejected; a rail outside the closed set is rejected, because an unrendered group is a section that vanished). The defect that mattered was not in the schema: `hintsOf` copied four hint names and neither new one, so every declaration was correct and none of it arrived. The TOC groups by rail, generated from what rendered, no-rail landing in `raw`; the viewer's `RAILS` and `schemas.RAILS` are asserted equal rather than eyeballed. `UX-199`'s contents-matches-document guard encoded *order* and is deliberately relaxed to set equality, recorded in the guard itself. The question library folds per category with every query's SQL asserted still in the DOM. The trend and band captions are one line each with the paragraphs behind a disclosure — and the first version of that guard scraped the source and **stayed green** while the caption was replaced by the very paragraph it retired, so both now render the node and read the caption back | [UX-209](UX-0209-sections-named-as-questions-and-a-rail-not-a-contents-page.md) |
| UX-210 | The review praised the question library without reading the SQL: four of six canned queries are track-blind. `element-time` groups all slices by name (Plane 2 command names pollute the per-element answer), `stalls` windows over every track (interleaved native slices zero the gaps it promises), `sandbox-tax` subtracts any time-nested slice regardless of track — wrong even on Plane 1 alone whenever two elements overlap — and `dependency-wait` matches names globally. Confident wrong answers on exactly the merged two-plane traces the tool is proudest of. Track-scope every query; a static guard so an unscoped question cannot ship | High | UX-204 | 🟢 Done — all six scoped by `slice.category`, a published semantic channel (measured on `examples/06`: 11 `bst-builder`, 663 `native-process`, 1 `bst-invocation`), and joined across planes by the `args.element` **both** carry. The filing understated one case: `native:` is a *process* name and those processes have no `thread_name`, so the old `track.name like 'native:%'` was very probably returning **nothing** — an empty table reading as "no process storm" — and the Required Fix's own prescription would have inherited that. Five mutations red, including both halves of the named `sandbox-tax` case, which needed a second guard because stripping the containment scoping left the word `category` elsewhere in the query. **Not run against `trace_processor_shell`** (absent here); the semantics are argued from the trace's measured structure, and the marked test now covers these six where the binary exists | [UX-210](UX-0210-questions-that-know-which-plane-they-are-asking.md) |
| UX-211 | The anchors are sold as "paste into an issue" and the promise stops there: filters, thresholds, sort, collapse and the chain fold live in DOM state and localStorage, so the pasted link opens the unfiltered wall. The review's "remember state more aggressively" is localStorage thinking — remember for *me*, on *this browser*. View state in the URL fragment instead: it survives `file://` exports where storage does not, plus a "copy link to this view" control beside the TOC | Low | UX-199, UX-205, UX-208 | 🟢 Done — `viewstate.js` captures the view off the document and restores it by *driving the controls*, not by rebuilding state, so there is no second path to disagree with the first. The anchor is not sacrificed: state follows a `~`, `#floors` still scrolls, and a stateless link comes back byte-identical to today's. The clause with the trap in it was "storage remains the default where the hash is silent" — a naive restore reads an absent `c` as "nothing is collapsed" and **expands what the reader's own storage had shut**, so `c` is authoritative only when present, and `captureView` emits it as soon as the view says anything. Both halves guarded on a page already collapsed. Cross-run keys drop in silence; the copy control yields a `file://` URL carrying the filter, which is the case the item exists for | [UX-211](UX-0211-a-link-that-shows-what-i-was-looking-at.md) |
| UX-212 | The trend's verdict dots and the band's two rectangles differ only by palette — grayscale print or color-blindness erases the one chart that answers "is this drifting". The precedent is in the same function: incomplete snapshots already got a *shape* (squares). A non-color channel per verdict kind and a border/hatch on the band; must survive `filter: grayscale` | Low | UX-203 | 🟢 Done — a shape per verdict kind (triangle down / up, circle, open circle, diamond) named in `data-marker`, and the band's two rectangles separated by outline rather than fill. **Which shape means which verdict is the schema's answer**: the obvious implementation puts the map in `views.js`, which is a second list of verdict kinds in JavaScript — the exact shape `UX-214` fixed one item earlier — and the project's own guard banning verdict words in the viewer caught the first attempt. It lives on `store/v1`'s `verdict_kind` node as `bga:markers`, validated to cover `VERDICT_KINDS` and to give no two kinds the same shape, because a map that repeats a shape is a colour-only encoding wearing a declaration. One incidental fix: `data-cy` on every marker, after the first version silently reduced what an existing y-axis guard could see from three points to two | [UX-212](UX-0212-verdicts-you-can-see-without-the-palette.md) |
| UX-213 | Round 23's verification: nine guards are pinned to an uncommitted capture (`examples/06…/.bga/runs/20260821T170127Z/run`) and skip everywhere but the sibling's container — among them the two mutation guards UX-206's acceptance names. Proven un-falsifiable on a fresh checkout: uniform `flex-grow` and a flattened `depth` both leave the whole suite green. Round 21's guards-that-cannot-fail class, wearing a skip. Assert widths and depths on committed fixtures; CI runs them unskipped | High | UX-206, UX-202 | 🟢 Done — reproduced first with the capture moved aside: both named mutations applied together left **25 passed, 10 skipped, 0 failed**; they now redden **3 committed guards** there and 6 with the capture present, where nothing skips at all. No new fixture was needed — the golden run already carries a three-element chain with distinct shares (0.43/0.36/0.21) and, from `base.bst`, a three-level blast tree — so every acceptance guard runs as `[committed]` *and* `[real-capture]`. A third pinned guard the filing did not name, `plane2_coverage`, was in the same condition and now assembles the store's shape in `tmp_path` instead. `TestTheGuardsGuardEverywhere` asserts the matrix always names a `git ls-files`-tracked run, so an acceptance can no longer live entirely behind a skip | [UX-213](UX-0213-the-guards-that-only-guard-one-machine.md) |
| UX-214 | The trend's colouring is a second verdict chain: `_mark_verdicts` classifies on band edges alone and emits `within_band` — a value outside `schemas.VERDICT_KINDS` — so a run below the band but inside the observed extent colours *improved* where `bga compare` on the same pair answers `within_observed_range`. And the published `compare/v1` schema carries no `enum` for `verdict_kind` at all — the closed set UX-201 promised external consumers exists only in Python and the viewer. One classification, shared; the set published as a real enum in `compare/v1` and `store/v1` | Medium | UX-203, UX-201, UX-190 | 🟢 Done — one chain: `classify_against_band`, called by both. The disagreement was measured first on a skewed set `[100,100,100,100,200]` giving a band of `[99,101]` with one edge outside it — a candidate of 150 coloured **regressed** on the trend where compare answers `within_observed_range`. `UX-203`'s "the colouring cannot disagree" was an over-claim; this makes it true. Direction is deliberately *not* unified — compare has a signed delta, the trend has only the set — so compare's verdicts mean what they meant. `widen_band` extracted too, since judging against raw edges is the same divergence one step earlier. Both schemas carry the enum built from `VERDICT_KINDS`, `within_band` is gone from code, schema and stylesheet, and three mutations redden | [UX-214](UX-0214-one-verdict-vocabulary.md) |
| UX-215 | `bga/correlate.py:141` assembles an `ElementJoin` per element — Plane 1's path share, saving and blast radius beside Plane 2's `cores_busy`, `cpu_coverage`, `requested_jobs`, `peak_rss_kb`, dominant and serial binaries — and then stops. Measured: `bga correlate --format json` **does** emit the join (11 rows on `examples/06`, every field) — and it is **unversioned**. No `schema` key; `correlate --schema` answers "correlate produces no versioned JSON output"; `schemas.names()` lists four documents and this is not one; `bga view`'s `payloads()` does not serve it; `analyze/v1` carries Plane 2 run-level only. The most valuable derived object the tool has is a blob nothing can consume. Publish it: `correlate/v1` — a stamp, a schema and wiring, not analysis — plus the same rows inside `analyze --plane2` | High | UX-201, UX-190, UX-51 | 🟢 Done — `correlate/v1` is the fifth published document: stamped, `--schema` answering instead of refusing, and `bga analyze --plane2` carrying the same rows as `element_join`. Nothing about what the join computes changed — the diff is a stamp, a schema and thirty lines of wiring — and the rows are asserted **field for field against `correlate()`'s own return**, so the report and the command cannot describe an element differently (UX-214's failure, closed by construction). A quantity had to be added and that is the point: `peak_rss_kb` as `bytes` would be wrong by 1024×, rendering 157,200 KB as "154 KB" — UX-201's error one order down. **The viewer needed no change at all**: measured on `examples/06`, an 11-row `element_join` table under its declared question with 11 Inspect affordances, which is UX-193's dispatch paying for itself. UX-213's rule held one round after it was written — the first draft pinned six guards to the uncommitted capture; they run on the golden fixture now (13 pass, 6 skip, mutation still red with the capture moved aside). One mutation **refused to discriminate** (deleting UX-66's `declared` gate: a Plane-2-only name never acquires a `saving_share`, so there the gate is belt over braces) and the load-bearing case was built rather than the mutation counted. Nine mutations red | [UX-215](UX-0215-publish-the-join-the-tool-already-computes.md) |
| UX-216 | **A live defect.** `UX-208` anchored every row's Inspect at `#${cssId(uid)}` and nothing ever sets that id. Rendered `examples/06` and resolved every anchor: **19 links, 11 distinct targets, 11 of 11 unresolvable** — the 21 ids in the page are all section keys. `wireJumpBox` scrolls by `[data-element]` and works; the shipped anchor matches nothing, and the guards asserted the affordance exists rather than that it arrives. Fix it by giving the element the object the anchor should land on: one section per element the report discusses, rendering UX-215's row, with every occurrence linking to it | High | UX-215, UX-208, UX-199 | 🟢 Done — measured by **resolving every anchor** rather than counting affordances: `examples/06` went from 19 links / 11 of 11 unresolvable to 55 links / **0 unresolvable**. The two spellings are one expression now (`cssId` delegates to `elementAnchor`), and the mutation that proves it is not "rename the anchor" but *re-duplicating the expression with a different character class* — exactly how the defect would recur — which reddens four guards. One `<section>` per discussed element carrying UX-215's join row, its findings, and what `entering` says joins the path; the cross-reference is read off the **rendered document**, so a view added later joins it with no edit. The drawer stayed declined and the absence is asserted (no `dialog`/`showModal`/`position: fixed`; `z-index` deliberately not banned — the sticky header has had one since UX-205, and a guard that fires on correct code teaches people to disable guards). UX-199's id==key and UX-187's elision both caught real mistakes en route, and UX-206's "every box links to `#signals`" was **deliberately changed** to link the element instead — rewritten to assert the new promise per box rather than relaxed. **The page-size ceiling failed for the third round running, so what it measures changed rather than the number**: composition + Direction 7's ratio at the scale the rule names (1,000 elements → 691,401 B data vs 97,488 B page, **7.1×**) + a loose structural backstop. Eight mutations red | [UX-216](UX-0216-every-element-is-one-object.md) |
| UX-217 | Every finding already carries a structured `evidence` dict — measured on `examples/06`: `time-concentration` → `path_us, share_of_path, chain_bound, rows`; `wait-category` → `category, category_us, share, hint`. `renderFindings` reads `id, severity, title, detail, elements` and drops `evidence` on the floor, so the page shows the conclusion and hides the numbers it was drawn from, in a tool whose whole proposition is that its conclusions are measured. Render it through the same schema machinery, so a new finding's evidence formats with no viewer change | High | UX-202, UX-207 | 🟢 Done — the evidence renders under each finding through the same schema machinery as everything else: `primary: 0.875` reads **87.5%**, `category_us: 2000` reads **2 ms**. **The units are the substance and each was checked against a rendered value rather than inferred from its key** — the vocabulary is 59 keys wide and `_us` is the only suffix in it safe to read mechanically, so `EVIDENCE_QUANTITIES` declares 39 of them, with a guard that every declared key is one a finding actually emits. Structured evidence (`rows`, `steps`, `constraints`) is left to the sections that already draw it — flattening a table into a definition list is a worse rendering, not a more complete one. The fold hides pixels and not the DOM, asserted. Seven mutations red. One collision surfaced en route: `renderEvidence` was already UX-202's evidence *header* | [UX-217](UX-0217-findings-carry-the-evidence-they-were-drawn-from.md) |
| UX-218 | The loop is where the repetition lives: after reading the decision the reader retypes `bga blast <element>`, the capture, and `bga compare @last @prev`, with the run path and element name copied by hand out of the page. The page holds all of it. But *which* step is right depends on `headline.diagnosis`, and that mapping lives in documentation prose — so it is decided in the pipeline and published as `next_steps` (reason, argv, the signal it follows from), and the terminal, CI and the page then give the same answer. A step whose precondition is absent is not rendered | High | UX-207, UX-126 | 🟢 Done — `next_steps` published in `analyze/v1` (reason, argv, the field it follows from), the text report ending with them and the decision panel rendering them with Copy — one function, so the terminal, CI and the page cannot advise differently. **The acceptance is not "a command is shown"** — `bga blast` with its arguments reversed shows just as well — so every published `argv` is *executed against the fixture* and required to exit zero. Two committed fixtures answer differently by value, and what is **absent** is asserted too: the golden run gets no `measure-again` (not in a store) and no `look-inside` (no Plane 2); `examples/06` gets no capacity sweep, because more builders is the wrong advice for a chain-bound build. `compute_next_steps` stays pure — the store-shaped steps are decided by the *shape* of the published run path, not by probing disk. The first draft rendered "0.0s beyond the critical path" on a 2 ms gap; a number that rounds away contradicts the sentence it supports. Eight mutations red | [UX-218](UX-0218-the-next-step-is-a-command-you-can-run.md) |
| UX-219 | `signals.optimization_horizon` already publishes the whole answer per step — `saving_us`, `makespan_after_us`, `cumulative_saving_us`, and `entering`, the elements that join the critical path once that step is taken. It renders as a five-column table. The most product-shaped question the tool answers (*fix the top three and this build becomes what?*) deserves better, and `entering` — the honest reason the savings stop adding — is what a table hides. Cumulative bars whose widths are published `makespan_after_us`, one CSS division, table folded beneath | Medium | UX-207, UX-208 | 🟢 Done — one row per published step plus the run as it stands; every width is one published `makespan_after_us` over one published total, read from `data-` attributes rather than computed style. **The committed golden fixture discriminates the width mutation on its own** — at every step the published makespan differs from the naive `total - cumulative_saving_us` (8000 vs 10000, 4000 vs 6000, 3000 vs 5000, 0 vs 2000) — and it carries a real `entering` step, asserted explicitly so emptying it fails rather than making the guard vacuous. **One mutation could not discriminate and was rejected rather than counted**: `cumulative_saving_us` *is* the running sum of `saving_us` by construction, so re-adding it can never differ on a real report; the property is guarded instead on a synthetic payload where the two disagree (15 vs 20). Size discipline after both of this round's drawings: page 107,533 B against the 120,000 B backstop, **12,467 B margin** — stated rather than assumed, since this round spent a real fraction of the headroom UX-218 created. Six mutations red | [UX-219](UX-0219-what-if-i-fix-these.md) |
| UX-220 | The round-24 review called metric popovers almost free "because the schema already carries descriptions". Measured, that is false exactly where it matters: `floors` has **0 of 0** children described and no description of its own; so do `capacity_verdict` and `occupancy`; `utilisation` and `signals` are 0 of 3. `certified_us` — a lower bound certified by the graph, not a prediction, and the most misreadable number published — says nothing at all. The viewer has rendered descriptions recursively since UX-201; **the descriptions are the deliverable**, plus a guard that a quantity-carrying leaf cannot arrive mute | Medium | UX-201 | 🟢 Done — the coverage table reproduced exactly on `main` before anything changed, and the renderer really was already there (`hintsOf` has copied `description` since UX-201), so this was prose and structure. **133 quantity-carrying leaves across the five published documents, 0 undescribed** — 84 of 112 were mute at the start, and `floors`/`capacity_verdict`/`occupancy` declared no members *at all*. Measured on the exported golden page: **54 → 185 embedded description keys**. Two corrections the task file needed: `floors.certified_us` **has never existed** (the certified floor is `floors.lb`), so its acceptance mutation was unrunnable as written; and `utilisation` declared `peak_rss_mb`/`cpu_pct`/`cpu_seconds` — **three names no code path emits** — which is why its twelve real members went unhinted for four rounds. UX-201's own guards were pinned to those phantoms; they were **re-pointed at published fields** (`memory_envelope.host_memory_mb`, `utilisation.useful_pct`) rather than deleted, and still redden on the original renderer bug. Clause 3 is the schema itself rather than a new constant: `schemas.description()` is what the text report and `bga floors --help` now read, and it raises on a path with no sentence rather than rendering empty. Six mutations red | [UX-220](UX-0220-the-numbers-that-need-a-sentence-have-one.md) |
| UX-221 | `bga compare` answers "this got slower"; nothing answers "because of what?". The review estimated a culprit strip as "mostly rendering, if compare already contains per-element deltas" — checked: it does not. `compare/v1` carries whole-run floor deltas and per-*category* attribution deltas, and no element appears in it anywhere. Payload item first, which is the right shape: a viewer differencing two element tables would be a second comparison, and UX-214 is this round's evidence for what that costs. Per-element deltas with **appeared/disappeared** distinguished — the cases a naive join drops and which are usually the culprit | Medium | UX-214, UX-203, UX-215 | 🟢 Done — and the check corrects the task: `element_diff` has carried **appeared and disappeared** since UX-79, so the two cases predicted as "the ones a naive join drops" were the two already present, and the elements in *both* runs — where the answer to "because of what?" nearly always is — were the ones missing. Measured: an element whose duration tripled appeared nowhere in the document. `element_diff` was also **declared by nothing**, so UX-190's contract never covered it; declared now. Each row carries two fields because they answer different questions — `presence` (a join fact) and `verdict_kind` (a judgement from UX-214's closed set) — so an element in one run only has `delta_us: null` and `not_comparable`, never a change from zero. Clause 3 was **broadened on purpose** to `no_significant_change` as well: a report that declines to call the run and then paints five elements red is arguing with itself. **A mutation this task names did not discriminate** — sorting the strip by its own computed delta passed, because the four-case fixture puts one element per group and no ordering assertion over one row can fail; it was rejected rather than counted and a three-regression fixture built, where it reddens two guards. The first `within_observed_range` guard **skipped**, which would have left the clause unchecked while looking checked; reaching the disputed region needs a tight baseline cluster plus one far outlier. 16 mutations red across 27 guards | [UX-221](UX-0221-which-elements-caused-the-regression.md) |
| UX-222 | At 1,202 elements "show me everything about `openssl.bst`" is Ctrl-F and scrolling; UX-205 narrows one table, UX-216 gives one section, neither narrows the report. Every ingredient exists — `data-element` is already on path boxes, table rows, blast rows, top actions and finding element lists — so focus is a root attribute plus a predicate. Dimmed and collapsed, never removed: the reader must see what they are not looking at, and Ctrl-F and the export stay honest. Carried in UX-211's fragment, so "the report, focused on the element I mean" is a link | Medium | UX-216, UX-211 | 🟢 Done — built with UX-225 in one module, because they are the same mechanism: a predicate over `data-element`, which this round's horizon steps and culprit rows earned for free by carrying it. Both invariants are guarded as **absences of removal** — the element count is unchanged while focused, and clearing restores the document exactly, compared as a serialized snapshot with a companion assertion that focusing changed something in between, so a no-op cannot pass the round-trip. One delegated listener at the root, so a view added later earns focus with no second handler. Escape clears the focus and only the focus: the marks are a decision, not a keystroke. Three mutations red, including both this task names | [UX-222](UX-0222-focus-one-element-and-dim-the-rest.md) |
| UX-223 | `wireJumpBox` searches sections and element uids and scrolls to the hit. By the time the reader has typed `openssl` the page can offer the six things they might do with it and offers one. Grouped results — the element with its published numbers, the actions UX-216/UX-218 register, the sections — with `↑ ↓ Enter` and an action list that omits what has no precondition (no Perfetto row on a run with no timeline). An index over affordances that already exist, which is why it is cheap and why it must not grow into a search engine | Medium | UX-199, UX-216, UX-218 | 🟢 Done — `paletteResults` is a pure function over the list the page already holds, so the guards test it directly. **Clause 3 is guarded in both directions**: a guard that only says "no Perfetto row without a timeline" passes on an implementation offering nothing, so a companion asserts the row does appear when there is one. Clause 4's fixture separates read from derived — `total_duration_us` is 100 s while `openssl.bst` alone is 672 s — so a palette computing its number cannot match. **Two collisions the flattened export found**: the first draft declared `elementFacts` (views.js has had one since UX-216) and `cssId` (app.js declares one), each a `SyntaxError` in the shipped page — UX-199's defect by a new route. Renamed one; the other was fixed by *not duplicating*, importing `elementAnchor` from views.js so the palette's link and its target stay one expression. A general guard came out of it: **no two modules may export the same top-level name**. Deviation recorded: no global open-key. Six mutations red | [UX-223](UX-0223-the-jump-box-becomes-a-command-palette.md) |
| UX-224 | The report ends its life in a pull request, a chat message or a ticket, and getting it there is manual: select, lose the evidence, retype the numbers. UX-115 already renders a CI comment from a payload and UX-208 already ships a clipboard helper. A Copy per finding emitting title, evidence in declared units, elements, the published next step **and the run identity** — a pasted finding without it is an assertion nobody can check (UX-178). One renderer shared with `--format ci-comment`, asserted by reading the source rather than by the two agreeing today | Low | UX-217, UX-115 | 🟢 Done — the CI comment is Python and the viewer is JavaScript, so "one renderer" across that boundary is only honest one way: the text is rendered in the pipeline and **published as `findings[].copy_text`**, and the page copies a string rather than wording one. That makes clause 2 checkable by reading the source, as the acceptance asks — `app.js` may contain `finding.copy_text` and no viewer module may contain `BGA finding:`. **The golden fixture showed two defects immediately**: a nested `blast_radius` dict rendered as 400 characters of `repr` into the middle of a paste, and `category` and `category_us` — two different values — both reduced to the label `category`. Non-scalar evidence is left out and the label is the published key verbatim. Units come from UX-217's `EVIDENCE_QUANTITIES`, not a second table. Deviation recorded: plain text only, no markdown detection — a button claiming to know what a paste target accepts would be guessing. Five mutations red | [UX-224](UX-0224-copy-a-finding-as-something-you-can-paste.md) |
| UX-225 | UX-211 put the *view* in the link; the *decision* is nowhere. "I am working on `core.bst`, I already rejected `lib-b.bst`" is state the reader rebuilds every round and cannot hand to anyone — so round three of an optimization reads exactly like round one. The fragment is the right channel: not `localStorage` (remembers for me, on this browser, and a `file://` export may get none), not the store (the viewer has no write method and must not grow one). Marks travel in the link; a marked-done element stays ranked and annotated, because a ranking that hides what was dismissed cannot be checked | High | UX-211, UX-216 | 🟢 Done — the channel decision is the item and it is asserted, not described: no `localStorage`, `sessionStorage` or `indexedDB` in `focus.js`, checked with comments stripped first, because the module's own header explains at length why storage is wrong here and a guard firing on that explanation would reward silence about the decision. Round-trip guarded in a genuinely fresh context with nothing remembered (`mk=app.bst:working,core.bst:done` → "1 working · 1 done"). Clause 3 is guarded as an absence too — marking changes no element count and a marked element is still present, because a ranking that quietly hides what the reader dismissed is one they cannot check. `views.js` imports nothing by design so it spells the vocabulary out; that duplication is guarded and reddens when either side drifts. Three mutations red, including both this task names | [UX-225](UX-0225-the-working-set-travels-in-the-link.md) |
| UX-226 | The loop ends on a question the tool does not answer: *I spent an afternoon on `core.bst` — did it work?* The store holds every snapshot, compare judges a pair, the trend draws the set, and all three answer for the **whole run**. Measured: `store/v1` rows carry `total_duration_us`, `cache_hit_rate`, `bytes`, `verdict_kind` and nothing per element. A bounded per-element slice per snapshot (path and top-action elements only — a history, not an archive), a sparkline on UX-216's section, and one sentence from published values. No per-element verdict: UX-170 already found the statistics hard at n=5 | Medium | UX-215, UX-221, UX-203 | 🟢 Done — the decision the item turns on is **written at capture time, not derived at read time**: UX-203 established that the store is rebuilt on every `bga view` for every snapshot, so a row needing an analysis would put N analyses in front of a page load. `bga snapshot` already analyses the run it captured, so the slice is one small file, and `read_element_slice` is guarded against ever mentioning `analyze(` or `BuildEfficiencyAnalyzer`. Two absences kept apart and both stated, never drawn: `elements: null` (captured before this) and `elements: []` (analysed, element not worth watching) get different sentences and no sparkline at all — a point at zero is not an absence. **The first draft broke UX-212**, reaching for a JavaScript `VERDICT_MARKERS` map; the shape comes from the schema, and the guard now asserts the drawn shapes against `schemas.VERDICT_MARKERS` rather than literals, with a plain circle where there is no contract to read. The sentence states first and last and no rate — two numbers from two builds are not a trend, and this item declines to judge. Six mutations red, including both this task names | [UX-226](UX-0226-what-happened-to-this-element-since-last-time.md) |
| UX-227 | The page can say an element is worth fixing, its path share, its downstream count and that it moved since last time - in five different sections. What it cannot do is say them together, as the reason | Medium | UX-207, UX-215, UX-216 | 🟢 Done — a fold under each top action: the rule that ranked it (from `UX-229`'s record, followed through the action's `see` pointer), what the run measured, the findings that name it, and its store history. Every value carries the path it was read from, and the guard walks that path through **both** resolvers - the page's and `bga/provenance.py`'s - so two implementations of one grammar cannot drift quietly. **That comparison found a bug in UX-229, one commit old**: `_segments` split paths on `.`, so `[element_uid=core.bst]` was nonsense; it never bit because no claim's path had a dotted selector value and this item emits nothing else. Both scan now. Six mutations red | [UX-227](UX-0227-why-is-this-ranked-first.md) |
| UX-228 | `UX-222` built focus as visual state - one element held, the rest dimmed. That mental model is "dim everything else", and the reader who focuses an element wants "show me the evidence about *this*": today they focus and still scroll four places | Medium | UX-222, UX-216, UX-227 | 🟢 Done — focusing prepends an investigation section: why it matters, what evidence exists (per published document, **present or absent, both stated**), what it is connected to (chain neighbours by published order, rebuild count), and what to do. **Unfocus restores the document exactly**, asserted by serialisation compare with a during-state to prove the compare is not vacuous. Presence rows carry `data-source`/`data-present` rather than `data-field`/`data-raw`: the first draft put a uid in `data-raw` under a path resolving to an object, which is a traceability claim the row cannot honour. The path grammar gained a literal-key bracket in both resolvers, because `signals.blast_radius` is keyed by uids and a uid contains dots. Five mutations red | [UX-228](UX-0228-focus-is-an-investigation-not-a-dimmer.md) |
| UX-229 | The headline claims `execution-bound`; the attribution fields that fired it, the threshold they crossed and the query that would prove it deeper are all real and unpublished as a chain. Findings assert, their `evidence` dicts are ad-hoc, the viewer composes fragments and the CI comment states verdicts without grounds | High | UX-207, UX-190, UX-215 | 🟢 Done — `bga/provenance.py`: every claim carries `claim → evidence (field refs) → rule → trace query`, 8 claims on the golden run and 10 on the 1,202-element synthetic, no dangling reference on either, and every quoted value re-resolved and compared. `--explain` prints it, the page draws it, `compare/v1` carries the candidate's chain and the CI comment cites it. **Two things the item did not know it was about**: the mesh threshold was a bare `>= 0.5` with no name (a rule whose threshold has no name cannot be published as one), and the finding→query table lived in `trace_context.js`, which made the page the only surface that knew it. `capacity_recommendation` and `memory_envelope` are named in `unpublished_inputs` rather than silently omitted. My first draft copied the record into each top action where `_top_actions` references by construction; it is a `see:` path now, which is also how the export stayed under its backstop. Ten mutations red — two rejected first for not discriminating | [UX-229](UX-0229-publish-why-bga-believes-what-it-believes.md) |
| UX-230 | `UX-219` draws the published plan as the fixed sequence the pipeline projects. The review's sketch - checkboxes, pick your subset, see the projected build - is what R8 brings to a prioritisation meeting, and its own warning is the constraint: a page that sums per-element savings is wrong the moment two fixes share a chain | Medium | UX-219, UX-229 | 🟢 Done — `whatif/v1` and `bga whatif`, answered by one function the CLI and the page's transport both call. **The golden fixture is the argument**: two elements on one chain are worth 10,000 µs together against 11,000 µs added up, and the document publishes the wrong number beside the right one so a reader sees the gap. Three paths and no fourth: a *prefix* of the published plan is read (the horizon is greedy, so a skipped step is a different question), any other subset is asked of the server - asserted byte-identical to the CLI's answer - and offline the command is shown instead of a dead control. Three refusals by name, each exiting 0. Seven mutations red; the revert of one used `git checkout` and deleted this item's own transport, which is recorded because a bad revert and a good one look identical in a terminal | [UX-230](UX-0230-what-if-you-could-choose-the-fixes.md) |
| UX-238 | The full suite is slow enough to distort how work gets done. Measured: `pytest tests/` is 373s, and split by per-file total, 160 files cost 18.2s between them while 7 cost 159s - an inner loop that runs everything spends six minutes to learn what twenty seconds would say | High | — | 🟢 Done — four tiers assigned from the measurement in `tests/tiers.py` and applied by a collection hook, so no file carries a marker by hand and a new one inherits `small` free. **`make test-small`: 1985 tests in 22.07s against 3112 in 319.29s** - 64% of the tests in 7% of the time. The lists are the exceptions, not the taxonomy, which is also the weakness: a slow file joins the default tier silently, so the budget is a `timeout 90` in CI with the number pinned to `SMALL_TIER_BUDGET_S` by a guard. **One of my own guards did not discriminate**: the census-gate test passed with the gate deleted, because the census is empty inside a one-file run; it plants a complaining tally now and checks both directions, since "the gate works" and "the gate is an off switch" look identical from one side. Six mutations red | [UX-238](UX-0238-the-suite-is-one-six-minute-block.md) |
| UX-236 | `UX-233` pinned the architecture document and the spec's contract table to what the code publishes, and left the two documents a reader actually arrives at. Measured against the door as it stood: seven of twelve subcommands (`whatif`, `cache-trend`, `graph`, `replay`, `sweep`, `utilisation`, `diagnostics`) named in neither `README.md` nor `docs/README.md`, and 0 of the 8 published schema ids named in the docs index | High | UX-233 | 🟢 Done — `docs/README.md` gained a **What it emits** section (the eight ids, what writes each, and `bga --schema <id>` to read one) and a paragraph placing `bga whatif`, `bga analyze --explain` and `bga snapshot --aggregate`; `README.md` gained the same three where a reader is already asking for them. The budget was the constraint, not the length: 250 measured lines, the additions took it to 252, and two came back by reflowing two paragraphs and deleting the seventh of eight pointers to `real-project.md` — it ends at 249. Ten `tools/` aliases stay off the door with a reason each, and the exemption costs something: every exempt alias must still be reachable from `cli.md`, and an exemption for an alias that no longer exists fails on its own. Four figures that had already drifted within a round were refreshed or removed. Four mutations red, plus three reddenings on the real pre-change documents | [UX-236](UX-0236-the-front-door-is-a-round-behind.md) |
| UX-237 | Documentation a change needs and does not get had nowhere to go. Two escape valves existed — a bug you notice becomes a tracker row, a doc your change made wrong is fixed in the same commit — and none for the commonest shape: *this needs a proper explanation and writing it well is half a session's work*. Round 28 produced three instances and all three survived only because someone said so out loud | Medium | UX-233 | 🟢 Done — Definition of Done item 11 and style-guide rule 14 are the third door: documentation a change needs is filed before the commit lands, and a stated decline is a decision where silence is not. The three that motivated it are rows (`UX-242`..`UX-244`), each measured first: `capacity_recommendation` in 3 backlog files and no guide, `memory_envelope` in 4, what-if's convention in none. **Two of my own guards did not discriminate**, both the same shape: the deferral check read whole filings and matched this filing's own Required Fix, the sentence listing the phrases it looks for; and the round-28 check searched every filing for each mechanism, so deleting a filing left it green twice over — the name is in this filing's Motivation as evidence and in a sibling's Out of Scope. Fourth and fifth instance of that shape in one round. Also found: the Definition of Done carried two items numbered 4, so every in-text cross-reference below it pointed one item wrong. Six mutations red | [UX-237](UX-0237-documentation-debt-has-no-way-into-the-backlog.md) |
| UX-240 | Every session begins by re-reading the same guides and then re-deriving the same procedures - how to falsify a guard, how to regenerate the golden snapshot, which budget guards exist. Round 28 rediscovered the golden recipe from a docstring and rediscovered the falsification discipline by getting it wrong four times | Medium | UX-238, UX-239 | 🟢 Done — three skills under `.claude/skills/`, scoped to procedures that are mechanical and repeated: `verify` (the Definition of Done as a sequence), `falsify` (the mutation discipline, carrying the three failure modes that have cost something), `measure` (the golden snapshot, the scale run, the export split, re-timing the tiers). Each names the guide that owns its rule and `verify` says which document wins. Every command run as written: the golden recipe regenerates byte-identically, `bga gen-synthetic --seed 1` hashes the same across two invocations at 1,202 elements, and the export splits **833,552 B total = 141,603 B page + 691,949 B data, 4.9x**. **The guard caught a defect in its own first run**: it reported a `make` target called `a`, out of the sentence "make a document wrong" - it reads only fenced blocks and inline code spans now, the same subject-versus-argument separation `UX-239` made one document over. Nine mutations red | [UX-240](UX-0240-a-session-has-no-cheap-entry-point.md) |
| UX-241 | `UX-233` fixed the architecture drift once and guarded the mechanical half, and could not catch the half that matters most: a chapter describing a mechanism the code no longer has. The document went a whole axis out of date - the viewer ran from `UX-193` to `UX-226`, 34 closed rows - before anyone noticed. Feature audits have a cadence; documentation review did not, and the asymmetry is the finding | Medium | UX-233, UX-237 | 🟢 Done — review is a seventh stream in `§6a`, with `docs/audits/architecture-review.md` carrying its input, output, checklist and log. The trigger is measured, not remembered: `test_the_review_has_a_cadence.py` reddens past **25 closed rows** since the last logged review, counted in the tree so it needs no git and answers the same on every machine, and argued against the 34-row drift that was actually missed. A review produces no code, by rule - one that fixes what it finds stops being able to see the next thing. **The first review ran and found three things**, filed as `UX-245`..`UX-247`: `blast` and `whatif` absent from the chapter titled *Real current CLI surface*, `bga whatif` absent from the end-to-end guide whose act step it prices, and the Verification Log claiming 2026-08-18 against a 2026-08-23 commit. Seven mutations red | [UX-241](UX-0241-architecture-review-has-no-cycle.md) |
| UX-239 | The fixing guide's context map — the section that tells a low-context session *not* to re-derive where things live — described a tree from the repository's first week: `tests/test_e2e.py   only existing test file` when `tests/unit/` alone held 218, and no mention of `schemas.py`, `compare.py`, `blast.py`, `correlate.py`, `provenance.py`, `whatif.py`, `store_aggregate.py`, `bga/report/`, `bga/viewer/` or `tools/`. Confidently wrong exactly where confidence had been requested | High | — | 🟢 Done — the map is regenerated from the tree and a guard holds it in both directions (16 absent subjects before, 0 absent and 0 stale after). `§6a` now names the six streams this repository actually runs — design, audit, feature, fix, documentation, refactor — with `starts from · produces · done when` for each, and `§1` branches on the stream before it picks a row, because an audit session was being told to find the highest-priority Not-Started row that its own output will create. **The third self-matching guard of the round**: `_map_text()` first read the whole of §6, including the paragraph that quotes the old bad map as evidence — so two guards would have passed with the map deleted. It reads only the fenced blocks now. The rule: when a guard reads prose, say which part is the subject and which part is the argument. Seven mutations red | [UX-239](UX-0239-the-context-map-is-from-the-first-week.md) |
| UX-231 | Round 27 wrote the role model (`docs/design/roles.md`): eight roles, four served thoroughly, four barely — invisibly, because nothing names whose problem a direction solves. `Serves:` lines on directions and new filings, guides state their role, the fixing guide keeps `roles.md` current, a guard holds it | Medium | roles.md | 🟢 Done — Directions 1–7 tagged from what each *argues* rather than its title (Direction 4 reads like a capture item and is R2's, because a blind spot hides exactly the element cost R2 owns); both guides name their role; the fixing guide carries the question; the style guide documents the field. **The guard passed for the wrong reason first**: written as the acceptance asks, `grep -l "Serves:.*R6"` matched this task file's own worked example of that query. Corrected to read only the `Serves:` field, it tells the useful truth — R1:5 R2:1 R3:0 R4:1 R5:1 R6:0 R7:1 R8:4. **No filing serves R6 or R3**; round 27 opened Direction 9 for R5–R8 and filed one anchor serving R5/R7. Not a defect — the gap analysis working — and the set is asserted so the day someone files for R6 the guard says the map moved. Six mutations red | [UX-231](UX-0231-every-direction-names-its-reader.md) |
| UX-232 | The scenarios README was 936 lines carrying 234 rows at 848 characters each, open items interleaved with five months of closed history, and `Out of Scope` ideas already lost and re-dug-out once. Split by liveness, rows shrink to their index job, a closed topic taxonomy, out-of-scope mining once and then by rule | High | UX-231 | 🟢 Done — README 936→192 lines / 9 rows / 142 chars per row, `closed.md` 776 lines / 225 rows kept verbatim; conservation checked as `{id: status cell}` maps, union equal and overlap empty. **The retargeted guards are the finding**: `_table_statuses()` read a fixed column index, which the two deliberately different table shapes break — it now scans for the cell that starts with a marker, and reads both files, where the old one would have gone quiet for 225 of 234 rows and stayed green. A claim of mine was false and is recorded: five rows I reported as carrying an unescaped `\|` were escaped all along, and the naive splitter was mine. 20 out-of-scope entries mined, 3 bare ones given reasons; style guide rules 11 and 12. Five mutations red | [UX-232](UX-0232-a-backlog-you-can-navigate-at-234-items.md) |
| UX-234 | Every question R5 and R7 ask begins with a distribution bga already holds the samples for and never aggregates: a store of captures is a measured service-time distribution with host manifests, hit rates and resource profiles attached, and its only cross-run reading was a trend line of medians | Medium | UX-203, UX-186, Direction 9 | 🟢 Done — `store-aggregate/v1`: min/median/p95/max/MAD per host class, nearest-rank percentiles checked against hand-computed values, read by `bga snapshot --aggregate` and drawn as the trend's median-p95 band. **Three refusals as behaviour rather than prose**: an unfinished capture is excluded *and counted*; a mix of machines exits 6 (compare's own cross-host code) unless `--blend` takes the claim; below `MIN_BASELINE_RUNS` a class publishes a shortfall, not a p95 of two samples. Grouping walks `hostinfo.COMPARED_FIELDS` so it cannot drift from compare's. The trend draws **no** band over a mix — the mutation that matters is falling back to the first class, and it reddens. The export backstop moved a fifth time (198,756 → 209,867 B, 9,137 of it the new schema) and gained the guard that makes the number a measurement: every embedded block parses as one of the documents the page renders. `bga snapshot --help` came back from 53 to 44 lines by deleting design history from argparse. Nine mutations red — one rejected first for not discriminating | [UX-234](UX-0234-the-store-speaks-for-more-than-one-build.md) |
| UX-233 | *We frequently forget to update architecture and specification documentation, which later increases the cost of big refactoring.* Measured: `architecture.md` still described three analysis planes and stopped at round 20, before the whole viewer axis and the contract wave | Medium | UX-190, UX-232 | 🟢 Done — the guard was written first and was **red on the tree it was written against**: five of eight published schemas were in no spec table and the architecture document named none of them. Two chapters *added* (nothing rewritten): the viewer axis at the document's altitude, and an eight-row contract inventory that points at `--schema` rather than copying it. Part 32.5 went from three rows to eight. Fixing-guide item 9 asks whether a change makes either document wrong. **Two of my own guards passed for the wrong reason and were found by falsifying**: the spec check matched anywhere in the file, so deleting a 32.5 row stayed green; the points-at-`--schema` check matched a string every table row contains. Both repaired, five mutations red | [UX-233](UX-0233-the-architecture-document-meets-the-viewer-axis.md) |
| UX-235 | Round 27's two green mutations plus a skip seam: the decision panel's "DOM order asserted" guard hardcodes its expected order and stays green under `prepend`→`append` (UX-221's culprits-above-band shares the class, page-side); the anchor probe set carries no underscore, so a `cssId` re-duplicated as `[^A-Za-z0-9-]+` survives while `my_lib.bst` misses its target; and jsonschema-guarded test files module-skip on a plain editable install. Real order assertions, an underscore probe, a skip canary | Medium | UX-207, UX-216, UX-221 | 🟢 Done — all three seams reproduced first, and the reproduction found two more. **A fourth, unfiled, is why the first three survived**: the only harness that boots the real exported page implemented `prepend` as `append`, so everything the page puts first landed last and the real order read as the exact reverse of UX-207's promise. The page was never wrong; the instrument was. **A fifth is mine** — `test_publish_the_join.py:27` carried a module-scope `importorskip` from round 25, hiding 21 guards behind one import, which round 21's seam-6 guard exists to ban and missed because it named *one file*. It reads every test file now. The skip census's own first draft gated on `config.option.file_or_dir`, which does not exist on pytest 9 — it raised, never ran, and reported nothing on a run with 81 skips: written, looked right, measured nothing, the same shape this item repairs. Fixed and verified against a real absence (31 skips for one reason → red). Six mutations red, including all four the acceptance names | [UX-235](UX-0235-the-order-the-page-asserts-and-the-order-it-has.md) |
| UX-248 | `schemas.names()` answers a narrower question than it looks like it does - the documents `bga --schema` can print, not the documents `bga` writes. Measured: 9 contracts stamped in the source, 7 in the registry, and `sources/v1` - written to `sources.json` in every run directory and read back by `load_inventory` - in no registry, no guard and no document | High | — | 🟢 Done — `bga/contracts.py` derives the set by walking the package for module-level `SCHEMA` declarations, so the next contract is inventoried by existing rather than by someone remembering. Pointing the two `UX-233` drift guards at it **immediately reddened three checks on `sources/v1`**, which is the measurement: spec Part 32.5, the architecture inventory and `docs/README.md` all gained it. A second guard derives the set independently by scanning source text and asserts the two agree - and a planted module proves the derivation is a derivation, joining the inventory without being listed. Four mutations red, one rejected as non-discriminating and redone | [UX-248](UX-0248-there-is-no-authoritative-contract-inventory.md) |
| UX-249 | `bga` reads its own past output as input, and nothing an artifact recorded said which build wrote it. Measured: `__version__` was read in exactly two places, both the `--version` string, and written into nothing - a `run-context.json` from round 3 and one from round 29 were indistinguishable to the tool reading them both | High | UX-248 | 🟢 Done — a producer stamp (`tool`, `version`, and the whole contract set) written into every run directory and every published `analyze/v1` document, on the same path and the same best-effort rule as `UX-186`'s host manifest. The **whole** set rather than this artifact's dependencies, because a writer that guesses its own dependencies freezes the guess where it cannot be corrected - the reader computes the subset its question needs. Recording only: no refusal, so the record lands before the policy that reads it. An unstamped artifact reads as an explicit `unstamped`, and `None` (no stamp) stays distinct from `[]` (a stamp whose enumeration failed). The golden snapshot drops it for the same reason it drops `run_instance`, and that drop is itself guarded. Nine mutations red, one rejected as a mutation that never landed | [UX-249](UX-0249-nothing-an-artifact-records-says-which-bga-wrote-it.md) |
| UX-250 | `bga compare` refuses on host and on cache mode, with an exit code of its own, and had nothing to say about the two runs having been measured by different builds of the tool | Medium | UX-249 | 🟢 Done — the refusal is on **contract movement**, never on the version number: two runs from `0.1.0` and `0.9.0` compare when every contract a comparison reads is unchanged, and two runs one patch apart refuse when one is not. Refusing on the version would fire on every upgrade, including the twenty-eight rounds that moved no contract at all, and a refusal that fires constantly gets switched off. `COMPARISON_CONTRACTS` names what a comparison actually reads, so `whatif/v1` moving refuses nothing. A missing stamp is named, never refused - every artifact predating `UX-249` lacks one, and refusing them would make the stamp's arrival delete the history it protects. Verified end to end: exit 6, the moved contract named, and `--allow-mismatch` still opts back in. Six mutations red | [UX-250](UX-0250-comparison-refuses-on-host-and-mode-but-not-on-contract-movement.md) |
| UX-251 | `bga --version` said `0.1.0`, unmoved across 29 rounds and 247 scenarios; `git tag` returned nothing; there was no changelog. Nothing answered "what changed between the bga I installed and the one I have now" | High | UX-248, UX-241 | 🟢 Done — `CHANGELOG.md` is the ledger: each release records its contract set and command surface in a fenced `state` block, and the version is **derived** from the delta rather than chosen (a bumped contract or a removed command is `breaking`, a new one `extending`, neither `patch`). Pre-1.0 both move MINOR, so the row records the kind too. A release **consumes** `UX-241`'s review rather than adding a second doc-sweep trigger - guarded, so a release cut without a review at or after the previous marker fails. The derivation is exercised on seven synthetic pairs as well as the real ledger, because a rule first run on the day a contract breaks is a rule nobody has seen work. Twelve mutations red | [UX-251](UX-0251-a-release-is-a-contract-state-not-a-date.md) |
| UX-252 | Hand-writing release notes would make a third copy of facts that already live in the task file's Outcome and the closed row - and two hand-maintained copies of one fact drifting is this repository's most-repeated defect | Medium | UX-251 | 🟢 Done — `bga release-notes --from N --to M` emits the closed rows between two markers, grouped by the topic each task file declares, with `contracts` and `cli` before `docs` because a reader scanning for "what changed for me" should not have to pass the process news first. The head stays written: theme, contract delta, upgrade note. A guard regenerates every committed block and fails on a diff, the way `test_golden.py` holds the golden snapshot, with the marker range carried in the block's own comment so nothing keeps a second copy of it | [UX-252](UX-0252-the-release-notes-should-be-generated-from-the-closed-rows.md) |
| UX-254 | Reported from a real run: the contents occupy most of the first screen and read as content. Measured on `main` at 0.2.0 in Chromium, on a 1,202-element run: `.toc` is 573px - **71.6% of a 1280x800 viewport** - and the first content sits at y=701, 87.6% down. Three things compound: `position: sticky` *inside* the reading column costs the 573px twice, the `investigate` group inlines one link per focused element, and `insertBefore(contents, body.firstChild)` puts all of it above the run's own name | High | — | 🟢 Done — the rail is its own grid column and scrolls on its own axis, one link per line, with the group that grows with the run bounded. First content moves to **y=132 at every viewport measured** (12.2%-17.2%), and the rail takes 12.5%-23.4% of the width instead of 64%-72% of the height. Below 60rem the page returns to one column and the rail folds. **The browser found two defects nobody reported**: at 390px the page scrolled sideways (scrollWidth 436 against 390) because the table filter's flat `min-width: 12rem` would not shrink and tables were unclipped - fixed at the source. A sticky heading means content passes beneath it, but a *jump* must not land under it: the heading's height is named once and both the rail's offset and every anchor's `scroll-margin-top` derive from it, measured at **0px hidden**. Nine mutations red | [UX-254](UX-0254-the-contents-take-two-thirds-of-the-first-screen.md) |
| UX-255 | The heading arrived at y=630, *after* the navigation, and carried less than the footer did - two lines of identity and nothing that qualifies the run | Medium | UX-254 | 🟢 Done — the heading is first in the DOM and on the screen, and carries `UX-249`'s producer stamp: *"measured by bga 0.2.0"*, with an unstamped run saying so rather than being rendered as a version. DOM order was fixed as well as visual order, deliberately - grid areas reposition boxes and leave the document sequence alone, and that sequence is what a screen reader reads and `Tab` follows, so a layout-only fix would have left navigation first for the readers least able to skip it. **Deviation recorded**: the verdict is *not* repeated in the heading, because the decision block is fully visible at y=132 on every viewport measured, so it would buy no scrolling and cost a second copy of one fact | [UX-255](UX-0255-the-heading-is-below-the-navigation.md) |
| UX-256 | "A checker if everything is really collapsed by default". Measured: it is not, deliberately - 3 of 49 `<details>` open and all 12 sections open, both with reasons already written in `nav.js` and `views.js`. Nothing asserted either, so a change opening forty of the forty-nine, or closing the one that should be open, would have shipped silently | Medium | — | 🟢 Done — a census with named, reasoned exceptions, `UX-235`'s skip census in a second place. `OPEN_BY_DEFAULT` names the one site that opens anything and why; a second site reddens it naming the function, and an entry for a function that no longer exists reddens it too. For sections it pins `UX-199`'s reasoning where the next person who thinks collapsing them would be tidier will meet it, and checks the page never collapses something a reader did not. **The sixth instance of the self-matching-guard failure**, and the first in JavaScript rather than prose: `UX-254`'s mount check matched the *comment* quoting the old `insertBefore` call as evidence the call was still there. Both files strip comments before reading now. Five mutations red, one rejected and redone | [UX-256](UX-0256-the-default-open-state-is-a-policy-nobody-checks.md) |
| UX-258 | The blast ranking put `toolchain.bst` first — an `import` element with 1,201 dependents, `is_structural_kind: true` on the very entry the ranking ordered. The advice was true and useless: a base image has a thousand dependents on purpose | High | — | 🟢 Done — structural kinds are **reported, not ranked**. `_criticality_findings` has excluded them since round 12 citing `UX-76`; the blast ranking simply never got the rule, and applying it moved `next_steps[0]` from *"toolchain.bst is the first thing to fix"* to *"layer00/mod037.bst is the first thing to fix"* with no second change. Hiding the element was refused — `UX-203` was filed over unreachable views — so a second `info` finding `blast-radius-structural` carries element, count and kind with its own provenance entry and `cli.md` row, and a graph that is entirely structural still emits it rather than reading as "nothing to report". One guard pins the `_criticality_findings` premise so this stops being an existing rule loudly if that rule ever moves | [UX-258](UX-0258-the-blast-ranking-tells-you-to-optimize-the-base-image.md) |
| UX-259 | `753 downstream` is p99.9 in a 1,202-element graph and unremarkable in a graph of forty thousand — and the number is what travels into a ticket while the rank stays behind. Positions 2–12 were 753..697, an 8% spread presented as an ordered list of what to do first | High | UX-258 | 🟢 Done — `signals.blast_radius_distribution` publishes deciles plus p95/p99 over every element's downstream count, and each ranked count carries its position: *"753 downstream elements, at or above p99"*. A spread under 10% across what is shown says so — *"not a difference worth acting on"* — and a flat graph gets no percentile theatre. The shape is one sentence, not a chart (`UX-196`). Under ten elements it returns `None` rather than dressing four numbers in ten labels (`UX-234`'s precedent), and the key is then **absent, not null**, which kept the golden byte-identical. One percentile function in the codebase: `store_aggregate.percentile`, asserted by a guard and re-derived independently in a second | [UX-259](UX-0259-a-blast-number-has-no-scale.md) |
| UX-262 | `UX-187` capped the tables that grow with element count; a table that grows with **critical-path depth** was not capped and nobody had a run deep enough to notice. A 482-element run with a 122-deep path grew the `signals` section to 6.2 screens against the 1,202-element run's 2.1 | Medium | — | 🟢 Done — the critical-path table opens on its top 25 above `TABLE_OPENS_BOUNDED_ABOVE = 40` rows, with `All rows` the same one click `UX-187` chose. Measured after: the section falls 5539px → 2292px (6.2 → 2.5 screens) and the document 24.7 → 21.1, with the badge reading `25 of 122` so the reader can see what they are not seeing (`UX-208`). The bound clears the ordinary case deliberately — the 1,202-element run's widest table is 26 rows — and the guard pins both ends rather than the number. The work exposed a shim defect: `applyTopN` reorders by re-appending, a real DOM *moves* an already-parented node and the shim *copied* it, so a 4,000-row table read as 8,000 — `UX-235`'s "the page was never wrong; the instrument was" in a second place. Pixels stay unassertable; that is `UX-257` | [UX-262](UX-0262-a-long-critical-path-grows-a-section-without-bound.md) |
| UX-263 | Reported from a real project: Chrome logs "Refused to apply inline style ... default-src 'self'" against `views.js` on every page load. A style *attribute* is inline style, and `bga view` sends that policy itself | High | — | 🟢 Done — the symptom was console noise; the defect was that **four visual encodings were dead** — the waterfall's width, the critical path's share, the blast tree's indentation and the horizon's bar were all set on the element and none of them applied. Measured in Chrome 141 on the golden run, each in a fresh profile: 15 violations and 1 distinct value per channel before, 0 violations and 4/3/5 distinct after — the `distinct` half matters, since a page that drew nothing would also log zero. Fixed with CSSOM, which is not inline style; the CSP was **not** relaxed to `'unsafe-inline'`, and a guard fails if it ever is. The shim that could not see it now reflects `.style` into the attribute and serialises it as Chrome does, measured first. Five mutations red, including deleting a drawing outright so the ban cannot be satisfied by not drawing | [UX-263](UX-0263-the-pages-own-policy-refuses-its-drawings.md) |
| UX-265 | Reported from a real project: the Perfetto hand-off stopped working in latest Chrome, with "blocked by cors policy, no access control allow origin header is present on the requested resource" | High | — | 🟢 Done — `UX-198`'s grant answered the **read** and nothing answered the **pre-flight**: `BaseHTTPRequestHandler` replies `501 Unsupported method ('OPTIONS')`, a 501 carries no allow header, and a browser reports that as the header being absent from the resource. A *simple* cross-origin GET is never pre-flighted, which is why this sat unnoticed until something made the read pre-flight — Private Network Access is the documented reason a public origin reading `127.0.0.1` starts to, and it needs no change on either side. `do_OPTIONS` now answers, scoped identically to the GET grant (blob only, Perfetto only, GET/HEAD only), with `Allow-Private-Network`, the asked-for headers echoed rather than a fixed list that would break on the next one, and `Vary: Origin`. Reproduced in Chrome 141 against two servers differing only in `do_OPTIONS`; the real ui.perfetto.dev is unreachable from this sandbox and that is stated rather than papered over. Six mutations red | [UX-265](UX-0265-the-handoff-answers-the-read-but-not-the-preflight.md) |
| UX-253 | `UX-250` settled the two-run rule and its clause 2 asked for the many-run case, which was deliberately not implemented: with thirty runs there can be three contract sets and the questions that follow have no default answer | Medium | UX-250 | 🟢 Done — decided, and the decision is the argument. A **host class** partitions runs into populations that must not be pooled (a fast machine's durations and a slow one's are incomparable); a **moved read-contract** makes a run's fields absent or differently defined, so the run cannot be *read* rather than meaning something else. That is an exclusion, not an incomparability - so the many-run rule is `UX-250`'s rule applied to a set, not `UX-234`'s refusal. `store-aggregate/v1` now carries `contract_composition`: each set with its run count commonest-first, unstamped runs as an explicit unknown (never as agreement), and **which contracts this document reads**, since `whatif/v1` moving changes nothing about a duration distribution. The discriminating nine-run case with a two-run minority is built rather than assumed. Guards hold the rule itself: one fails if the composition ever reads the producer's *version*. `cache-trend` is named as remaining | [UX-253](UX-0253-the-aggregate-mixes-contract-sets-without-saying-so.md) |
| UX-257 | Every geometric claim about the viewer - "nothing overlaps", "the first content is above the fold" - was measured by hand and then held by nothing, because the shim the guards run on has no layout engine at all | Medium | UX-254 | 🟢 Done — **a real browser, and it costs nothing to add.** The trade assumed Playwright; measured, that premise is false: node 22's built-in `WebSocket` drives Chrome over CDP in forty lines, and `node` was already required. Where there is no Chrome these skip, and the reason is declared in the census so "no browser here" is reported rather than silent. It found two measurement errors on its first run - `getBoundingClientRect` returns the *union* of a wrapped inline's per-line boxes, and out-of-flow boxes overlap on purpose - then zero overlaps at 1440x900, 1280x800 and 390x844, with the viewport set part of the contract. Four mutations red: a negative margin, a collapsed grid, a too-wide table, a dropped anchor offset. What it cannot see is named in the file: machines without Chrome, font differences (hence thresholds, not screenshots), and the served page's dynamic halves | [UX-257](UX-0257-nothing-reads-the-pages-geometry.md) |
| UX-260 | Where else a percentile belongs, argued per quantity rather than applied everywhere - `UX-259` gave blast radius a scale and the same question stood for duration, sandbox tax and process count | Medium | UX-259 | 🟢 Done — the three `yes` quantities publish `distribution()` beside their rankings: element duration in `analyze/v1`, sandbox tax and per-element process count in `correlate/v1`, each only where its plane was captured and only where the population is large enough. Measured on a 44-element run spanning three orders of magnitude, every decile agreeing with a nearest-rank computation done independently in the guard; the 4-element golden run publishes none at all. The split is a decision: `DISTRIBUTED_QUANTITIES` and `UNDISTRIBUTED_QUANTITIES` carry their arguments and guards hold **both** directions, since a `no` with no argument is what invites the next round to add one. One statistic throughout - `blast_radius_distribution` survives as a wrapper and `UX-259`'s guard was moved to follow it | [UX-260](UX-0260-the-other-quantities-that-need-a-scale.md) |
| UX-261 | The first screen met the reader with eleven near-identical blast counts; what they need first is what the build is *waiting for*, then what shape the graph has | Medium | UX-258, UX-259 | 🟢 Done — `next_steps[0]` is now the longest element on the critical path, read from `critical_path_detail` which already published it, and the blast step follows. Nothing new is computed and the biggest entry is taken rather than the first, since the path is in order and its first element is rarely its largest. `headline.graph_shape` states the density in one sentence from `UX-259`'s distribution - never a chart (`UX-196`), with a guard on that. My first concentration rule compared the top decile to the median, which in a star-shaped graph are both zero, and called the most concentrated shape there is "spread"; that case is now a guard. Marking structural and tied entries *inline in the table* is named as not done | [UX-261](UX-0261-the-first-view-ranks-what-is-big.md) |
| UX-264 | The DOM shim every viewer guard runs on was written inline **25 times**, so each of three fidelity defects had to be found in the page and then fixed twenty-five times - and `UX-263`'s seven-file fix left the eighteen untouched copies more different than they started | Medium | UX-263 | 🟢 Done — one `tests/dom_shim.mjs`, imported through an absolute file URL published in `conftest.py` (several harnesses run node from a `tmp_path`, and the first migration failed exactly there). The acceptance mutation - `append` copying instead of moving - now reddens guards in three files. Consolidating found four disagreements with a browser: `textContent` was a plain string where a real DOM concatenates descendants (measured in Chrome: no separator), `href` did not reflect into the property, parent links were declared and never set so one probe snapshotted a forest rather than a cyclic document, and **no shim implemented `after`** - so every guard had only ever exercised `app.js`'s fallback mount path. The shim now refuses selectors it cannot parse rather than matching nothing, and still says it has no layout engine | [UX-264](UX-0264-the-dom-shim-is-copied-twenty-five-times.md) |
| UX-266 | Reported from a real run: a CSP problem on `sql.html`. Checked across every served page: **two of three ran nothing** - `default-src 'self'` refuses inline *script* exactly as it refuses inline style, and `sql.html` and `perfetto.html` each carried one | High | UX-263 | 🟢 Done — `sql.html` rendered **zero** children, so the questions list never existed; `perfetto.html` is quieter and worse - the page renders, the "Open in Perfetto" button is there, and nothing was listening, so `bga view --perfetto` lands on a dead button. Both scripts are files now, both in `ASSETS`, and the guard sweeps **every** served page for inline script *and* inline style rather than one page for one of them - which is why `UX-263` missed this. The other direction is guarded too: each page must name the module that drives it, since a page with no script would also pass a ban on inline ones. Re-measured in Chrome 141: 0 violations across all three, `sql.html` back to 4 questions. Three mutations red | [UX-266](UX-0266-two-of-three-pages-run-nothing.md) |
| UX-267 | Every object *and every array* rendered as `<details><summary>object</summary><pre>{raw JSON}</pre>` - 34 such cells and 32,393 characters of `<pre>` on a 44-element run, the largest 8,191 and scaling to ~224,000 at 1,202 elements | High | — | 🟢 Done — the whole fix was one function: `renderTable` returns a `<section data-section=…>`, right for a view and wrong for a **cell**, so a first spike put 22 sections inside table cells and the contents listed `summary` twice. `buildTable` is the same builder without the wrapper. Small objects now render inline (`Average depth 0 · Peak depth 0`), short arrays as a comma list, wide maps as searchable bounded tables, and whatever still folds says `Downstream count · 44 entries` rather than `object`. Zero opaque cells, zero raw JSON, and the document is **shorter** than before (13.8 → 13.6 screens) because the fold was kept and merely labelled - a spike that replaced folds with open tables measured 35.5. One guard did not discriminate (restoring the defect at the call site left every test green, since they drove the function directly) and was fixed rather than counted | [UX-267](UX-0267-every-object-is-a-details-called-object.md) |
| UX-268 | Six of the seven wide `signals` maps are the same element list rendered six times; the seventh is keyed by task and shares **zero** keys with them, with nothing on the page saying so | High | UX-267 | 🟢 Done — one 44-row, 13-column element table joined from six signals, with the sentence saying so; tables on the page went 28 → 23 and none of the six appears as its own fold any more. `wall_clock_share` stays separate and is labelled "keyed by task, not by element". Membership is **declared** - `ELEMENT_KEYED_SIGNALS` and `NOT_ELEMENT_KEYED`, the latter carrying the argument - so a new element-keyed signal joins a list or is argued about rather than quietly becoming a seventh fold | [UX-268](UX-0268-six-maps-are-one-table.md) |
| UX-269 | Field contents measured per field: 678 chars of `copy_text`, 572 of `capacity_model_note`, 293 of `attribution_hints.resource_wait_us` - all shown in full, always | Medium | UX-267 | 🟢 Done — and the measurement settled the rule: a flat cap is wrong, because a paragraph meant to be copied (`UX-224`) and a **caveat on a number** want opposite treatment. `CELL_TEXT_CAP = 160` truncates values with the first whole words, a `…`, the character count and the full text kept on `data-raw` so it stays selectable; declared explanations and any `*_note`/`*_sentence` are exempt, because hiding a caveat by default is how a reader stops seeing it. Exemptions are guarded for carrying reasons. Four mutations red | [UX-269](UX-0269-a-long-field-shows-all-of-itself.md) |
| UX-270 | The critical path - the run's most important list - was a row inside a section named after a schema key | Medium | — | 🟢 Done — lifted to its own top-level section immediately after `signals`, keeping `UX-262`'s row bound and `UX-208`'s badge. It was also the **last of `UX-267`'s nested sections**: the array-of-objects cell branch called `renderTable`, so the critical path and two others had been rendering whole `<section>`s into `<dd>` elements all along - which is why it looked like it had its own heading and read as "quite good". All three use `buildTable` now; sections inside cells went 3 → 0 | [UX-270](UX-0270-the-critical-path-is-its-own-section.md) |
| UX-271 | The rail is one flat list and the report renders 30+ sections; a third navigation column carrying the JSON structure was proposed | Medium | UX-254 | 🟢 Done — **nested instead of a third column**, and the refusal is argued rather than asserted: a structural tree makes the document's shape the organising principle, which `UX-207` and `UX-199` moved away from, and a third column leaves under 900px of reading width at 1440 where `UX-254` measured the split. 31 top-level entries plus 12 nested, bounded at 8 per section with the remainder counted (`UX-208`) so a section with one entry per element cannot put the run's size back in the rail. The declined alternative lives in `nav.js` beside the code that answers the need, guarded. A guard that checked the function existed rather than that the rail called it did not discriminate and was fixed | [UX-271](UX-0271-the-rail-is-flat.md) |
| UX-272 | The header stacks four block elements and was reported as too long | Low | UX-254 | 🟢 Done as requested, **and the size claim corrected on the record**: measured at 92px (1440) and 134px (390), which is 0.1-0.2 screens of a 13.6-screen document. It is worth tidying because the header is *sticky* - every pixel is paid on every screen - not because it is where the length lives; `UX-267` and `UX-268` were. Identity left, actions right, stacked again below `UX-254`'s breakpoint. The measurement also exposed a real defect: `--head` was one `5.5rem` for both widths and is what every anchor's `scroll-margin-top` reads, so a jump would land **46px under the heading** at 390px. Two mutations initially passed - a CSS slice that read past the block, and a comment containing the string it grepped for (eleventh instance) - and were fixed rather than counted | [UX-272](UX-0272-the-header-is-four-stacked-paragraphs.md) |
| UX-242 | `bga analyze` computes `capacity_recommendation` and no instructional document named it; three constraints, the smallest binds, and five distinct reasons it declines | Medium | — | 🟢 Done — `cli.md` gained "How many builders, and what stops you"; clause 2's premise was false (it is not an `analyze/v1` key) and the contract gap is `UX-275` | [UX-242](UX-0242-the-capacity-recommendation-is-documented-nowhere.md) |
| UX-243 | `memory_envelope` decides whether `--builders` can go up and reached no reader; the README sentence about peak memory named no field | Medium | — | 🟢 Done — named with its unit and its Plane 2 condition, concurrent peak rather than a sum, and the README points at it | [UX-243](UX-0243-the-memory-envelope-reaches-no-reader.md) |
| UX-244 | `bga whatif` publishes a projected makespan and what "fixed" means lived only in `whatif.py`'s `CONVENTION` | Medium | UX-230 | 🟢 Done — the guide already carried it (the filing's `git grep` was a false negative on a wrapped line); the architecture gained *why* it is a bound, and summing errs in **both** directions | [UX-244](UX-0244-whatifs-convention-lives-in-its-own-docstring.md) |
| UX-245 | the chapter titled "Real current CLI surface" was missing `bga blast` (ten rounds shipped) and `bga whatif`, and named `--explain` nowhere | Medium | — | 🟢 Done — two rows, the `--explain` sentence beside the provenance it prints, and a guard reading the table's first column only | [UX-245](UX-0245-the-architectures-cli-table-is-two-commands-behind.md) |
| UX-246 | the end-to-end journey walks capture → read → go inside → join → act → gate and named `bga whatif` nowhere in the act step | Medium | UX-230 | 🟢 Done — Step 7 opens by pricing the change, on a committed run where `codegen.bst` is worth 0.000s alone and 7s beside `core.bst`; pasted figures are recomputed by the guard | [UX-246](UX-0246-the-journey-guide-never-reaches-whatif.md) |
| UX-273 | the width-not-depth rule governs every nested value in the report and `git grep` found it in exactly one task file | Medium | UX-267 | 🟢 Done — the viewer chapter states it beside the hint rule, naming the exported thresholds rather than copying their numbers | [UX-273](UX-0273-the-rule-that-draws-a-nested-value-lives-in-one-task-file.md) |
| UX-274 | the context map's guard globbed `bga/` and `tools/` only, so the `tests/` half had drifted to 5 of 12 entries with every figure stale | Medium | UX-239 | 🟢 Done — every entry named including both harnesses this axis built, the guard covers `tests/` both directions, and counted figures are gone | [UX-274](UX-0274-the-context-map-is-guarded-on-one-half-of-the-tree.md) |
| UX-276 | round 37's two new guards rested on a `bga snapshot` store that is ignored by design, so they passed on one machine and failed CI on all four Python versions before an assertion ran | High | UX-213 | 🟢 Done — the run is committed as a 72 KB fixture, and `UX-213`'s rule is mechanical: a test may not rest *only* on a path git does not track | [UX-276](UX-0276-a-guard-can-rest-on-a-path-no-clone-has.md) |
| UX-277 | `UX-267`'s width-not-depth rule was wired into `renderPairs` (which draws `<dd>` cells) and never into `buildTable` (which draws every `<td>`), so 6 cells rendered raw JSON, 11 joined arrays, one `[object Object]`, and the widest held 14,300 characters | High | UX-267 | 🟢 Done — the cell leaf calls `renderStructured`; widest *visible* cell 14,300 -> 152 characters, raw JSON and `[object Object]` to zero, document height unchanged | [UX-277](UX-0277-every-table-cell-stringifies-its-own-structure.md) |
| UX-288 | `analyze/v1` published the same leaf membership three times and the same critical path twice, each a subset of the one element table, and no guard said the copies must agree | High | UX-268 | 🟢 Done — `analyze/v2` publishes each selection once; the guard's own sweep over the committed run goes 2 clashes -> 0, and it **discovers** populations by walking the payload rather than reading a list, because the hand-listed draft stayed green when a removed field was re-added. A fourth duplicate the acceptance test found and the filing had not: `choke_points` and `choke_point_impact`, now one ordered list of records. The deferrability lists were **not** a duplicate and were nearly removed as one — they split the leaves by a rule that disagrees with `is_potentially_deferrable` 8 against 134, now published as `deferral_risk` per leaf. `test_section_stage_gating` caught the first version making the answer depend on which sections ran. Eight mutations, each asserted to land | [UX-288](UX-0288-the-contract-publishes-membership-three-ways.md) |
| UX-289 | the page drew 19 element tables over 13 populations and the one table every element is in carried 13 columns, because it served every question at once - and it had bounds and filters but **zero named presets** | High | UX-288 | 🟢 Done — five views declared in the schema (`bga:presets`), each naming one question and the five to six columns that answer it; widest table 13 -> 6 columns, tables over eight -> 0, named presets 0 -> 5. Every population is a filter over a published field: `from` reads a selection the payload publishes once *and takes its order from it*, `where` tests a column the records carry. A view the run cannot support is not offered, and a link naming one is ignored rather than landing elsewhere under that name. Two defects found while measuring and fixed here: the DOM shim reflected attributes only one way (measured in Chromium and pinned), and `serialization_point_risks` published one membership three ways at one element per risk - under `UX-288`'s floor. Thirteen tables sharing the view-state key `value` filed as `UX-292` | [UX-289](UX-0289-one-element-table-many-presets.md) |
| UX-293 | `UX-288` moved `analyze/v1` to `analyze/v2` on purpose, the suite was green at 3463 passed and `make lint` clean - and CI went red on a packaging smoke test that pinned the contract literally, in the one file the suite does not scan | High | UX-276 | 🟢 Done — the step reads the expected contract out of `bga/schemas.py` instead of carrying a copy, and keeps a non-circular assertion of its own; a guard sweeps every CI check for a contract version the tool no longer declares. `UX-276`'s shape again: a rule everybody knows with nothing mechanical behind it outside Python. The sweep reads what a runner *runs* rather than the comments explaining it - the first draft failed on the comment this fix wrote, the thirteenth self-matching guard here | [UX-293](UX-0293-a-ci-check-pins-a-contract-version.md) |
| UX-292 | `UX-211` keys a table's view state by the table's name and `renderStructured` named every nested table `value`, so thirteen tables answered to `f.value` - a filter typed into one landed, on the other side of the link, in whichever the loop reached first | Medium | UX-211 | 🟢 Done — the key is the path the table sits at plus the row it is nested in; 28 distinct keys of 40 -> 40 of 40, and 26 of 38 -> 38 of 38. Threading the path alone left three collisions, because a map table's cells are all in a column called `value` - the row's own key is what tells them apart, and is the name a reader would use. Guarded end to end: type a filter into a nested table, read the fragment back, it must name that table | [UX-292](UX-0292-thirteen-tables-share-one-view-state-key.md) |
| UX-287 | the export's byte ceiling was asserted against a **four-element** run, so it bounded the one quantity that barely varies while the content that drives the size went unwatched - the committed 11-element fixture was already 20 KB over it and nothing had ever looked | High | UX-195 | 🟢 Done — two bounds, because an export has two halves: the page is 162,909 B on every run (guarded, and a second test asserts it really is run-independent) and each committed fixture has its own stated total. The decision the item asked for: the export is *not* too big - 288 KB at 11 elements and 1.04 MB at 1,202 are well inside what an attachment takes, and the old number was the size of a four-element run rather than a judgement, which is why it had moved five times. A mutation that padded `app.js` with comments proved nothing, because the export strips them when it inlines a module | [UX-287](UX-0287-the-export-ceiling-is-measured-on-a-four-element-run.md) |
| UX-278 | a magnifier that consumes the click and does nothing: the detail cap excluded 1,178 of 1,202 elements, so the affordance was absent for 98% of the run and dead where it was present | High | UX-216 | 🟢 Done — the block is built when the anchor is followed, from the payload the page already holds; 7 dead anchors -> 0 at 1,202 elements, and an element the run measures nowhere gets a block that says so. The cap stays. The guard runs at scale and asserts that the committed fixture *cannot* see this defect | [UX-278](UX-0278-the-magnifier-opens-nothing-for-most-elements.md) |
| UX-279 | 43 copy controls, three vocabularies, `Copy` fourteen times over two different payloads, and not one `title` among them | Medium | UX-224 | 🟢 Done — the noun, not the verb: `Copy finding`, `Copy query`, `Copy command`, `Copy 12 rows` with the count following the live filter; hover text 0 of 43 -> 49 of 49, bare `Copy` 14 -> 0, and a guard that no label covers two payload kinds | [UX-279](UX-0279-forty-three-copy-controls-and-no-way-to-know-what-they-copy.md) |
| UX-280 | JSON pastes into a ticket as a code block somebody has to read | Low | UX-205 | 🟢 Done — Markdown beside it on every table, remembered in `localStorage`; a *rendering* of the rows `rowJson` already copies rather than a second selection, guarded by comparing the two row for row, with numeric columns aligned and a pipe inside a value escaped | [UX-280](UX-0280-copy-as-markdown.md) |
| UX-284 | the table tools sat below their table and scrolled away: 28 of 43 inputs started below the table they belong to, all 43 were `position: static`, and the jump box was at y=1236 on a page whose fold is 900 | Medium | UX-205 | 🟢 Done — measured in Chromium at three viewports: 0 of 24 below, 24 of 24 sticky *inside the table's own scroll box*, jump box 1236px -> 171px | [UX-284](UX-0284-the-table-tools-are-below-the-table-and-scroll-away.md) |
| UX-281 | both satellite pages were dead ends; `perfetto.html`'s only internal href was `#` | Medium | UX-199 | 🟢 Done — a link home above the heading on each, named for where it goes; the guard sweeps the server's own asset table rather than a list, so a page added later is covered by construction | [UX-281](UX-0281-the-satellite-pages-are-dead-ends.md) |
| UX-282 | *"Nothing opened? Use the direct link"* sat three paragraphs under the button it is about, read only by somebody who has just watched that button fail | Low | UX-265 | 🟢 Done — one row at wide widths, stacked at `UX-272`'s breakpoint, button first so a reader who has not pressed it is not offered two doors | [UX-282](UX-0282-the-perfetto-fallback-is-below-the-button-that-fails.md) |
| UX-283 | the bottleneck block rendered all seven of its members and carried **zero** links out of the entire `structural` section - nine choke points, none clickable | Medium | UX-277 | 🟢 Done — 0 links out -> 33 and 26 on the two runs; the choke points sort, the fan-in/fan-out pairs are tables with named columns, and one click reaches the element's detail (which needed `UX-278`, because a choke point at scale is exactly what the cap excludes) | [UX-283](UX-0283-the-bottleneck-view-names-elements-you-cannot-reach.md) |
| UX-247 | a document's claim about its own currency, false: the Verification Log said 2026-08-18 while five commits had touched the file since | Low | — | 🟢 Done — re-grounded rather than re-dated, and it was one axis out: the viewer section said nothing about `UX-286`'s chapters. The guard compares the date the log claims against `git log -1` for that file, holds the entry to naming what it was grounded in, and skips with a declared reason where a shallow clone has no history. The reproduction calls the guard's own comparison rather than restating it | [UX-247](UX-0247-the-architectures-verification-log-is-stale-about-itself.md) |
| UX-291 | twenty-three numbers across nine findings, ten carried a second time in `provenance.evidence[].value` and twenty a third time in `copy_text` - with no rule saying they must agree | Low | UX-288 | 🟢 Done — the finding's own `evidence` is the carrier to believe, said in the published schema where a consumer meets it. Not a projection over the citations, measured: 14 of the entries have a citation and the rest are derived ratios with no published path, so a projection would drop numbers or invent paths. The guard holds every pair equal, re-runs `UX-229`'s quotation rule over the two-plane document for the first time, and asserts the pair count so it cannot pass over an empty list | [UX-291](UX-0291-a-finding-carries-its-numbers-three-times.md) |
| UX-275 | the tool's answer to the question this backlog opened with - what should `--builders` be, and which constraint is the reason - was computed, rendered by the text report, and dropped by the JSON renderer, so a CI job had to parse prose | Medium | UX-116 | 🟢 Done — published in `analyze/v2` beside `capacity_verdict`, not in `correlate/v1` beside `memory_envelope`: that document is the per-element join and a run-level recommendation is not a row of it. The guard reads the payload, the finding's evidence and the printed text, and all three must name one binding constraint. Two things the filing did not know: `UX-229`'s chain still called these fields unpublished, and one of its citations (`occupancy.builders`) had never resolved at all - occupancy has never carried a builder count. The first fractional count also met the page, as `1.603977885512677` | [UX-275](UX-0275-the-capacity-recommendation-is-text-only.md) |
| UX-286 | forty-eight sections averaging 0.24 screens with nothing grouping them: a report read by scrolling past fragments, and a rail of thirty-one top-level entries | High | UX-285 | 🟢 Done — seven chapters, each named for a question the reader has and holding the sections whose published `bga:question` is a spelling of it. The table is in the viewer because nine of the forty-eight sections are built by the page and published by no contract; `bga:rail` is the fallback, so a payload key added later is chaptered by what it already declares. The rail lists the chapters, which also fixed a disagreement nobody had noticed - it grouped by rail, and the nine page-built sections have none, so `decision` and `findings` were listed under **raw**. The document is *shorter*: 18.51 -> 18.10 screens, headings 2.3-2.9% paid for by the space sections no longer need between them. `UX-285`'s two placement passes are deleted and its outcomes are now chapter boundaries | [UX-286](UX-0286-the-report-is-forty-eight-fragments-with-no-chapters.md) |
| UX-285 | three identity blocks answering one question, split across the page - `summary` and `run_instance` at screens 1.4 and 1.6, `producer` at 10.9 of 18.8 - and the blast control, an interactive query, as the last thing on the page at 18.3, seventeen screens from every finding | Medium | UX-207 | 🟢 Done — the identity closes the document (96% of the 1,202-element run and 94% of the golden fixture, adjacent to within 0.04 screens) and the control sits under `next_steps`, the line that names `bga blast` as the command to run - 18.27 -> 4.32 screens. Placed from `boot`, not `render`, because the element sections land after `render` returns. The pair with `resource_blast` was unfalsifiable when the item was filed - neither run had a source inventory - so the harness grew one. Two screens from `findings` holds at both desktop widths and not on a phone, where the diagnosis alone is 2.04 screens; the deviation is recorded and a width-independent clause guards it instead | [UX-285](UX-0285-the-identity-blocks-are-split-and-the-blast-box-is-last.md) |
| UX-290 | `[["app.bst", 8], …]` was described by nothing, so the page named its columns after their *position* - 78 of the report's headers named a place in a data structure rather than a measure | Medium | UX-201 | 🟢 Done — a tuple is described by naming its members in order, which `bga:columns` already does for records; positional headers in `structural` 4 kinds -> 0. Declaring them changed nothing at first: two lookup bugs made every declaration under an object-valued field unreachable, and a third was in the DOM shim, which did not reflect `title` the way a browser does | [UX-290](UX-0290-the-schema-does-not-describe-its-tuples.md) |
| UX-296 | `bga view` on a real ~2 GB dual-plane snapshot froze in parsing and died of memory near server start: `serve()` built every payload before the socket existed, running every whole-file load path in the codebase serially | High | UX-226, UX-203 | 🟢 Done — all four measured load sites left the startup path. The capture publishes `analyze.json` and the view serves it; the resource scalars join the store row as a capture-time sidecar (`UX-226`'s precedent extended) so the aggregate stops re-parsing every snapshot's monolith for two floats; the band reads `run-context.json` instead of re-analyzing each baseline; the trace leaves startup entirely, rendered on first request under a lock and streamed in 256 KB chunks. Measured on one generated fixture (247 MB report, 10^6 records) shared by both trees: the big run 17.04 s / 1232.9 MB -> 0.04 s / 39.5 MB, its 2 MB neighbour 8.79 s / 1233.5 MB -> 0.06 s / 39.8 MB, monolith opens at startup 3 and 2 -> 0. Guarded by a generated big-run fixture with an argued RSS ceiling and time ceiling, falsified by four mutations. Two defects found while measuring and fixed here: the guard read `ru_maxrss` in the child, which survives `execve` and reports the *parent's* high-water (411.4 MB against a true 10.5 MB, measured) - it now reads `VmHWM`; and the band's cheap path had no fallback for a run that recorded no wall clock, contributing `None` to `compute_band`. Deviation recorded: an unpublished run's Plane 1 analysis still runs in process (0.04 s, no O(events) work) — the refusal sentence guards the Plane 2 report above 64 MB | [UX-296](UX-0296-the-view-that-parses-nothing.md) |
| UX-297 | `summarize()` embedded the whole per-process record list in plane2.json - ~95% of a 1.5 GB monolith that no production reader consumed - and extraction then held the whole event list in RAM to produce it | High | Direction 15, UX-298, UX-215 | 🟢 Done — the record list left the report (69,641,647 B to 43,879 B on the same trace, 1,587x, and every published number is a per-element reduction computed by the same fold), the auto-compare releases before it parses, and a legacy monolith is still read by the same interface with the payload saying which shape served it. The streaming clause closed in round 43: pairing needs one *key*'s events in order, not the global order the sort was buying - `examples/06` carries 2 global inversions and 0 per-key inversions, which is the measurement that decides it - so parsing and pairing became one generator pass and `pair_events` became that pass with its input and output sorted. End to end on a 200,000-process trace: peak 288.3 MB to 259.5 MB, 8.2 s to 7.1 s, report digest identical. Stated rather than implied: this is O(processes), not O(elements) - 185.8 MB of the remaining 221.1 is the record list `merge_record_streams` joins whole, and whether that can be windowed is `UX-313`. The timeline's writer streams its events and still sorts its records, because a dual-stream process shares one track with itself and the pass yields hook-then-spine while their starts run the other way | [UX-297](UX-0297-extraction-streams-and-the-monolith-retires.md) |
| UX-308 | a slice said one thing - its name - and for Plane 2 that name is the command truncated to 120 characters, so the argv tail that tells two compiler invocations apart was not in the trace at all, while the record beside it carried cpu_us, max_rss_kb, exit_status, exec_chain and its hook-or-spine provenance | High | UX-298, UX-297 | 🟢 Done — debug annotations, from field numbers fetched and sha256-recorded rather than remembered (track_event.proto and interned_data.proto came back byte-identical to what UX-298 pinned). Ten keys, a documented contract in both directions, interned so 825 slices cost ten strings; the `failed` category earns EVENT_CATEGORY_IIDS, pinned by UX-298 as "reserved rather than used". Three things the record said that a guess would not: exit_status is a string vocabulary (`3`, `signal:9`) and the first draft's `not in (None, 0)` would have marked every process failed; an absent field is an absent key, because the hook cannot see an exit status and `0` there would claim success; and annotations ride the begin, so Plane 1's outcome is paired B-to-E before it is written. Cost on examples/06, 825 slices: 100,922 to 330,188 B uncompressed, 27,013 to 51,102 B gzipped. Recorded deviation: no trace_processor in CI, so extract_arg is unproven and UX-312 owns it | [UX-308](UX-0308-a-slice-that-says-what-bga-knows-about-it.md) |
| UX-310 | UX-298 pinned TYPE_COUNTER with the comment "reserved rather than used", and the three series the capture could fold went undrawn | Medium | UX-298, UX-297 | 🟢 Done — **one** series of the three, with the other two refused rather than omitted, and the refusals are clauses. The memory curve does not exist: max_rss_kb is a per-process *lifetime* peak, so a curve from it would sum peaks that never coexisted - exactly what compute_peak_memory refuses - and a guard asserts no memory counter is emitted. "Cores busy" and "open process count" are one question, answered by compute_max_concurrency over matched records only. The series shipped is that one, and its peak **equals** the published max_concurrency (20 both ways on examples/06), with the tie rule taken from the scalar rather than re-decided. Stride argued and measured: 1,000 windows, each contributing its maximum and closing value, so 1,626 raw endpoints become 538 samples with the peak exact; the knob is turned at 10/100/1000 and the peak holds. Cost +539 packets, +13,507 B raw, +3,411 B gzipped, asserted as packet arithmetic against the same capture with the series silenced | [UX-310](UX-0310-the-counters-the-reserved-constant-was-waiting-for.md) |
| UX-311 | a trace file leaves the machine that made it and carried no identity at all - not which run, not which host, not whether the capture was complete - while the lane order was discovery order | Medium | UX-298, UX-308 | 🟢 Done — one `bga: run` track ranked first with one annotated instant carrying the run stamp, project, targets, manifest hash, git commit, bga and bst versions, the host manifest, the builders, and the plane anchor and offset. Incompleteness is in the **track name**, not only an annotation, and all three ways of being unfinished are covered because it calls bga's own one accessor rather than re-deriving the rule. The ordering rule had to be read rather than remembered: `sibling_order_rank` is ignored on a process track unless the root descriptor (uuid = 0) sets `process_ordering = PROCESS_ORDERING_EXPLICIT`, so a rank without that one packet is a hint no UI reads. Recorded deviation: lanes are ordered heaviest-traced-first and say so in `lane_order`, because the timeline has no critical path and computing one here would be a second copy of the analyzer | [UX-311](UX-0311-a-trace-that-knows-whose-build-it-was.md) |
| UX-309 | the dependency question is the one a timeline is *for* - an element ends, another begins, and whether that adjacency is causation is exactly what graph.json knows and the trace did not say; Perfetto's flows were unused | High | UX-298, UX-308 | 🟢 Done — one flow per graph edge whose two endpoints both produced a task, and one per ppid link inside a sandbox, keyed on (invocation, element, ppid) so a pid collision between two sandboxes cannot invent causation. flow_ids = 47 and terminating_flow_ids = 48 are `repeated fixed64` - a different wire type from every other number the emitter writes, replacing deprecated varint fields, so the guard asserts the wire type and not only the value. A flow is one id on two slices and the direction comes from the begin timestamps, so an edge whose source does not begin strictly before its sink is dropped and counted - two on examples/06, where toolchain.bst is instantaneous and both dependents begin in the microsecond it does. The bound is no bound and the measurement is the argument: packets unchanged at both scales (2,335 and 62,804), 20.0 B per flow raw and 8.6 B gzipped. Recorded deviation: no trace_processor in CI, so the flow table is reconstructed by the in-repo reader | [UX-309](UX-0309-the-arrows-that-answer-why-now.md) |
| UX-298 | the timeline was legacy Chrome JSON - a shape Perfetto tolerates rather than reads - assembled whole in memory and regenerated from the raw log on every handoff; at field scale a 1.5 GB document, built entire, per conversion | High | Direction 15, UX-188, UX-297 | 🟢 Done — Perfetto's own TrackEvent, written by the stdlib. A `Trace` is `repeated TracePacket`, so the writer appends packets and gzips as it goes: measured on a 40,000-process capture, 120,026 packets, 4.83 MB uncompressed and 1.14 MB gzipped, 82.4 MB peak, and bytes on disk 10,000 slices before close - which is the streaming proof, since a ceiling cannot be one (a buffering writer would add ~10 MB here and pass). No protobuf dependency: the encoding is a page, and every field number is pinned to upstream's own `.proto` by a committed fixture carrying each file's sha256, because a wrong number is silent. `bga timeline` emits it by default, `--format chrome` keeps the legacy JSON, and `bga view`/`--export` carry the new format - `trace_file` renders straight to its destination now rather than rendering JSON and gzipping it. Defect found and fixed: the anchor was picked from Plane 2 alone, so a capture whose heaviest element Plane 1 never built refused; it is the longest element both planes know now. Two acceptance clauses could not run here and are recorded: `trace_processor` is not installed (the guard carries its own protobuf reader instead) and the trace has not yet been opened in ui.perfetto.dev | [UX-298](UX-0298-the-timeline-speaks-perfetto-natively.md) |
| UX-299 | the tab-to-tab handoff fetches the whole trace into the report page, posts it to Perfetto's window and was measured at 25 KB - and it is the same design at 1.5 GB, where the browser tab meets the memory wall the server met | Medium | UX-198, UX-298, UX-195 | 🟢 Done — one threshold with one explanation, published in the payload so the page keeps no second copy. Above 4 MiB compressed the opened tab is navigated to the `?url=` deep link and Perfetto fetches the trace from this server itself - no `arrayBuffer()`, no structured clone, no byte of the trace read by the page's own JS; the export carries `bga view <snapshot> --perfetto` instead of a `data:` URL. Below it, `UX-198`'s behaviour is untouched and its guards are unchanged and green. The size is asked for with a `HEAD` at click time because knowing it at load would mean rendering the trace, which `UX-296` moved off the startup path. Defect found by the new guard's first run: the small path opened a second tab after an `await`, which is the pop-up a browser blocks - the tab is opened once now and handed over. Range support checked and recorded: the server answers `Range` with 200 and the whole body, and nothing has been shown to need otherwise | [UX-299](UX-0299-a-handoff-that-does-not-carry-the-trace-in-its-hands.md) |
| UX-300 | one field snapshot reached ~2 GB and the store's retention thinking dated from kilobyte captures: the raw log kept by default on an 8-12% measurement, pruning that thinks in age and count, and nothing on the capture path saying what was just written | Medium | UX-167, UX-188, UX-234 | 🟢 Done — every capture states what it weighed and what the store totals; the aggregate carries a per-class `snapshot_bytes` distribution and a document-level `store_bytes` that counts the failed captures too (a byte is a byte, so it sits outside `blended` where every other cross-host figure is refused); and `prune --max-store 2G` deletes oldest-first under a budget, never `@last`/`@prev`, reporting rather than emptying when it cannot reach it. `UX-188`'s ratio re-measured at 200,000 processes: compression unchanged at 9.0%, but after `UX-297` took the records out of the report the raw log is **99.0%** of a snapshot - the default stands because that log is now the capture, and the sentence a capture prints changed to say so | [UX-300](UX-0300-what-a-two-gigabyte-snapshot-does-to-a-store.md) |
| UX-306 | round 41 wrote the web report's visual contract and left it beside the tree it governs; a style guide nobody is routed to governs nothing, and the hint vocabulary it depends on was named five-of-eleven in the architecture and trailed off in an ellipsis | Low | UX-231 | 🟢 Done — §1a is the hint vocabulary in one table (what each declares, which control reads it), and the guard holds the emitted set and the documented set equal in both directions plus every row's three cells filled. The two style guides name each other's scope in their openings, and the docs-index row and fixing-guide checklist are asserted rather than assumed - the index clause matching the link *target* after a mutation that repointed it while leaving the words passed the first draft. Six mutations, all discriminating | [UX-306](UX-0306-the-guide-joins-the-tree.md) |
| UX-305 | styleguide §4 budgets emphasis - one emphasized element per block, one accent, text in ink never in status tone - and the page had grown section by section without ever being read against it | Medium | UX-304 | 🟢 Done — the conformance pass found five: a second accent hardcoded eight times as a `var()` fallback, a colour whose only definition *was* a fallback for a token nothing declared, a fill wearing a text-grade token that the fallback hid from UX-304's guard, one class styled by two disagreeing rules, and the delta value wearing a status tone (§4.4's own example). All five fixed, and the rule they teach is guarded: a colour-valued fallback is a second palette and it hides the first from every guard that reads the stylesheet. The emphasis walk itself found nothing on either page at three viewports - the instrument is what this adds there, and it had to be taught that a `<th>` is a column label rather than an emphasis. Eight mutations, all discriminating; two were green first time and both were the mutation's fault, one of them exposing a clause matching words the fixing guide uses elsewhere | [UX-305](UX-0305-emphasis-is-a-budget.md) |
| UX-303 | styleguide §2 asks that a value which *is* a shape draws as its shape first; the viewer had one sparkline (`UX-226`'s element history) and no density strip anywhere, so a table longer than a screen gave no sense of its distribution until somebody scrolled it | Medium | UX-226, UX-205, UX-234 | 🟢 Done — `bga:series` and `bga:distribution` join the vocabulary, each carrying the reading its control needs (the unit of one step; the key holding the sample count, which is the only thing this repository's two distribution shapes disagree on). `bga/viewer/drawings.js` holds both controls, imports nothing and takes its formatter. A table past the row bound wears a strip built from its primary quantity column's own `data-raw` values under §2's boundary — **a self-built strip prints no derived number** — and clicking it (served only) sets the threshold to the nearest actual row value, never to the interpolated click position. Fourteen mutations, all discriminating. Two repairs the work turned up: `served()` threw on a missing `location` now that it is asked mid-table, and `UX-304`'s §4.3 guard caught the first draft colouring a peak and a p95 tick amber — a position is not a status. The page grew 183,006 → 196,615 B and tripped `UX-287`'s ratio at 3.90x, whose cause (the export inlines module comments; 175 KB of 196 KB is commented JavaScript) is filed as UX-307 | [UX-303](UX-0303-the-shape-before-the-rows.md) |
| UX-304 | the page was authored light-first with a dark media override and the reader it was built for reads dark; round 41 measured three of the four dark tokens above the mark-lightness band - text-grade colors doing fill work in every bar, band and dot - and reported it in prose, with neither the validator nor its numbers committed | Medium | UX-212 | 🟢 Done — `tests/palette.py` is the validator (WCAG luminance and contrast, CIE L*, ΔE2000, Viénot/Brettel/Mollon), `:root` carries the dark set with light as the override (which also matches an unset browser, so the base moved and the default did not) and `@media print` renders light on white. Tokens split into text-grade and mark-grade, validated per surface: the dark marks came down from L* 70.7/76.0/72.8 to 63.9/64.1/61.9 and the light set needed no change at all. Writing the guard found one real §4.3 violation - the unparsed-threshold input's whole signal was a red border, and it now goes dashed with `aria-invalid` and a title. Eleven mutations, all discriminating, including one that runs the band over the old values and requires exactly three to fail | [UX-304](UX-0304-dark-first-with-two-grades-of-token.md) |
| UX-302 | round 41's style guide made §1 a dispatch table on paper; the code still tested shapes inline, the deliberate raw-JSON escapes were unnamed, and a *mixed* array had no row and was improvised into `[object Object], 2` | High | UX-267, UX-277 | 🟢 Done — `bga/viewer/shapes.js` is §1 as code and both `renderStructured` and `renderSection` dispatch on `classify()`; a shape the table does not cover folds *and* warns on the console naming the payload path. `bga/viewer/rawjson.js` builds the per-section "view as JSON" toggle, §1's second deliberate site, working in the export because that is who needs it (15 of golden's 28 sections, 17 of macro_micro's 37; the rest are composed and have no payload slice). The guard boots both pages and walks every text node three times — closed, every toggle open, closed again — and resolves every viewer `JSON.stringify` to its function against a five-entry allowlist, both directions. Ten of eleven mutations redden here and the eleventh in the guard that owns ASSETS; the walk's first draft read a detached `body` and its second never opened a toggle, so both were rebuilt | [UX-302](UX-0302-the-mapping-made-law.md) |
| UX-301 | round 40 ran `UX-235`'s own acceptance mutation - `root.prepend(decision)` to `append` - and the booted page did not change: `UX-286`'s chapter pass had become the ordering authority, leaving five insertion-order calls in `boot()` that decide nothing and read as the mechanism | Low | UX-286, UX-235 | 🟢 Done — the five calls are plain appends now, and the booted section order was measured identical before and after, with and without a comparison spliced in. `boot()` names `chapters.js` and the `CHAPTERS` table, because a grep guard can only say what must not be there; the guard holds `boot()` free of `prepend` and `insertBefore` while leaving the transient focus overlays outside it alone, and asserts the table still declares an order so it cannot pass over an absence. `UX-235`'s log gained a superseded-in-part note naming the chapter-table mutation as the discriminating one | [UX-301](UX-0301-the-ordering-authority-moved-and-left-its-old-uniform.md) |

## UX-207..UX-212: the twenty-third audit round — the report learns to answer first (2026-08-22)

Round 23 verified the sibling's landing of the entire round-22 slate
(UX-198..206) and synthesized the next iteration from a second
external review — this one a Pareto argument: stop adding viewer
architecture, spend the next round on compression and actionability,
because the reader has to read too much before knowing what deserves
attention. Its diagnosis was confirmed against `boot()` (fourteen
sections at one visual level, the product's own "what should I fix
first" answer rendering mid-list) and its shortlist adopted with one
house adjustment — the decision panel's inputs enter `analyze/v1` as
a published `headline` block first, because a viewer that derives
the diagnosis is a second analyzer. Its stat-card dashboard was
declined with its own argument. What it missed, this round filed:
the canned SQL it praised is track-blind
([`UX-210`](UX-0210-questions-that-know-which-plane-they-are-asking.md)),
its remember-my-state item wants the URL rather than `localStorage`
([`UX-211`](UX-0211-a-link-that-shows-what-i-was-looking-at.md)),
and it never looked at color-only encodings
([`UX-212`](UX-0212-verdicts-you-can-see-without-the-palette.md)).
Verification added what no review sees: the landing's named mutation
guards are pinned to a capture that exists on one machine and skip
everywhere else
([`UX-213`](UX-0213-the-guards-that-only-guard-one-machine.md)), and
the trend's colouring is a second verdict chain that re-litigates
the disputed region
([`UX-214`](UX-0214-one-verdict-vocabulary.md)).
Full narrative: [`../../audits/round-23.md`](../../audits/round-23.md).

## UX-198..UX-206: the twenty-second audit round — the viewer learns what it is looking at (2026-08-21)

Round 22 verified the viewer landing — all five items hold, six
mutations re-run red, exit 7 and the export verified live — and then
synthesized the next iteration from two inputs: the user's four field
observations (each ground-truthed to its mechanism: the popup opened
outside the click's activation; the analyze pipeline with zero
tickers and a TTY nobody draws on; no `[all]` extra; a page with no
anchors) and an external review of the shipped viewer whose six code
claims round 22 confirmed line by line. The synthesis is Direction
7's second iteration: the review's thesis adopted (the opportunity
is not more generic rendering — it is the viewer understanding BGA's
relationships and using Perfetto as the microscope), its
architecture trimmed to modules, and its blind spot supplied by the
field (the handoff it praises is popup-blocked by construction).
Verification added what neither saw: two shipped views nobody can
reach ([`UX-203`](UX-0203-the-views-nobody-can-reach.md)). Full
narrative: [`../../audits/round-22.md`](../../audits/round-22.md).

**All nine landed.** The round's shape held: every item was reproduced
before it was fixed, and three of the filings turned out to be wrong
about the codebase in ways worth recording. `UX-203`'s proposed
package-data mutation was **refuted** — the files ship anyway.
`UX-206`'s "a `<details>` tree over data `blast/v1` already carries"
was **false** — the payload had two flat lists and no depth, so the
depth had to enter the JSON. And `UX-204`'s library item uncovered a
copy nobody had noticed: `sql.html` spelled out every query a second
time, guarded only by a title comparison.

Three regressions were introduced *by this round* and caught inside it,
which is the part worth reading: `UX-202`'s two-line import
reintroduced `UX-199`'s export defect verbatim (the line-based stripper
matched neither half, and every export died on `ERR_INVALID_URL`
again); `UX-204`'s reveal used `removeAttribute` against a
property-set `hidden` and would never have shown its paste; `UX-206`'s
first depth walk used the transitive closure and produced a flat tree.
Each is now a property assertion rather than a mechanism test.

Two measurements moved a rule rather than a number. The page-size guard
summed a *directory*, counting two served-only pages an export never
carries — it measures the exported file with its data blocks removed
now. And when that honest number crossed 80,000 B, the bytes came from
stripping comments out of the *inlined copy*: they are written for
someone reading the repository, and an attached report carries none of
those readers.

## UX-193..UX-197: the twenty-first audit round — the viewer argued, the field round verified (2026-08-21)

Round 21 verified all ten of round 20's landings — four mutations
re-run by the review, the pip-through-blast and 68-char paste cases
run live, the synthetic budget test timed — and every one holds; the
six seams the verification collected are
[`UX-197`](UX-0197-six-seams-round-21-verified-into.md), none a
reopening. The round's design work is
[Direction 7](../../design/directions.md): **the viewer as a thin
window onto the JSON** — the published schemas (UX-190's) are the
entire interface, the page renders the schema rather than the report,
timelines go wholesale to ui.perfetto.dev through the deep-link
handshake, and the same page ships as a served view and an attachable
file. Decomposed as
[`UX-193`](UX-0193-bga-view-a-thin-window-onto-the-json.md) (the
core), [`UX-194`](UX-0194-the-perfetto-handoff.md),
[`UX-195`](UX-0195-the-report-you-can-attach.md) and
[`UX-196`](UX-0196-the-views-that-make-the-numbers-self-evident.md);
the dependency-DAG view is deliberately deferred in the direction
itself. Full narrative:
[`../../audits/round-21.md`](../../audits/round-21.md).

## UX-183..UX-192: the twentieth audit round — the field speaks (2026-08-21)

Round 20 verified round 19's landings — all five hold, nine claimed
mutations reproduced by the review, the round-trip re-run live —
and turned the user's first sustained field feedback into ten
filings. The feedback drove the round's shape: each item was
ground-truthed before filing (the inventory does read `import`/
`manual` sources but silently mangles the paths bst would reject;
the hook stamps a clock that stops during suspend while Plane 1's
does not; compare has no host check of any kind; the chrome-trace
merge already exists and is unreachable from a snapshot; the output
JSON has no schema to drift from — this very range renamed a
published field silently). The verification also found the two
defects [`UX-192`](UX-0192-the-elision-that-reopened-the-round-trip.md)
carries: the table's elision re-opens the round-trip UX-178 closed,
for identities longer than 43 characters — real forge urls — and
blast's keying sentence undoes UX-181 one surface over. Full
narrative: [`../../audits/round-20.md`](../../audits/round-20.md).

## UX-178..UX-182: the nineteenth audit round — the source axis meets its own output (2026-08-20)

Round 19 verified all of round 18's landings live — the drain fixtures
re-run, the disputed-region band verdicts checked at n=5 *and* n=3,
and the whole source axis exercised end to end on a rewritten-fixture
monorepo: the Shared Sources table, the keying clause, the kind
split, the work-not-wall-clock note, `bga blast` in all three target
shapes, and the pre-inventory run's honest "this capture cannot answer
that". The headline finding came from pasting the tool's own output
into itself: [`UX-178`](UX-0178-the-tools-own-printed-identity-does-not-round-trip-through-blast.md)
— the table's identity string resolves as a path and answers
"rebuilds nothing here". The review's live mutation proved
[`UX-179`](UX-0179-the-discriminating-case-that-was-never-built.md)
(the cost-order guard passes with the sorter reverted), and
[`UX-180`](UX-0180-the-docs-still-assert-what-the-disputed-region-broke.md)..[`UX-182`](UX-0182-the-inventory-stops-at-the-junction-boundary.md)
carry the docs trail, the identity edges, and the junction boundary —
the last being where the user's real monorepo question most likely
lands next. Full narrative:
[`../../audits/round-19.md`](../../audits/round-19.md).

## UX-171..UX-177: the eighteenth audit round — the source axis (2026-08-20)

Round 18 verified round 17's landings — all of UX-163..UX-169 hold,
with the review re-running every measured number (the 204 MB peak, the
byte-identical digest, UX-170's −25% band escape) and reproducing each
exactly; the three live interrupt windows, the pasteable extract hint,
and the prune keep-set were exercised end to end. The verification's
own findings became [`UX-175`](UX-0175-the-grace-window-buys-nothing-while-the-pipe-stays-unread.md)
(the grace window cannot deliver its promise while the pipe stays
unread — demonstrated with a SIGINT-trapping fake bst),
[`UX-176`](UX-0176-three-guards-that-assert-less-than-their-logs-say-round-three.md)
and [`UX-177`](UX-0177-five-corners-round-18-verified-its-way-into.md).
Full verdicts in [`../../audits/round-18.md`](../../audits/round-18.md).

The round then opened the axis the user's monorepo question demands:
[Direction 6](../../design/directions.md) — **blast analysis by shared
resource**. The argument in one line: a `git` source keys on its ref,
so one url feeding N elements rebuilds all N on any commit regardless
of `directory:`, while a `local` source keys on content — the same
monorepo consumed two ways has order-of-magnitude different blast, the
`.bst` files encode which way, and everything needed to compute it is
already on disk. [`UX-171`](UX-0171-blast-radius-by-shared-source-the-monorepo-question.md)
builds the inventory and the report table,
[`UX-172`](UX-0172-bga-blast-what-rebuilds-if-i-touch-this.md) the
query command, [`UX-173`](UX-0173-a-blast-count-that-counts-stacks-overstates.md)
retrofits kind-awareness into the existing blast surfaces (the user's
literal first sentence), and
[`UX-174`](UX-0174-the-monorepo-patterns-page.md) documents the
patterns the numbers let a project choose between.

## UX-163..UX-168: the seventeenth audit round — the fixes verified where they will be lived in (2026-08-20)

Round 17 verified all eight round-16 landings live — the refusal
verdict, the walk-back, the gate's exit 6, the mid-build interrupt
salvage (exit 130, both planes kept), the recursive census joining
both planes on a real nested layout, stale-casd detection positive and
negative, the help caps, prune's alias protection, the stderr tee and
`replay-sandbox`'s polite refusal — and every verdict is **holds**,
five with caveats that became this round's filings rather than
reopenings. The two Highs are edges the verification itself walked
into: [`UX-163`](UX-0163-the-interrupt-contract-covers-the-build-and-not-the-minutes-around-it.md)
(a SIGINT during post-build extraction is still a raw traceback — the
salvage covers the build and not the slow minutes around it) and
[`UX-164`](UX-0164-the-walk-backs-replay-hint-reproduces-the-comparison-it-just-refused.md)
(the walk-back's own replay hint reproduces the wreckage comparison it
just refused). [`UX-165`](UX-0165-seven-help-strings-end-mid-sentence.md)..[`UX-168`](UX-0168-analysis-holds-the-whole-trace-in-memory-and-other-round-17-leftovers.md)
carry the truncated help sentences, the casd config divergence, the
prune-vs-walk-back contradiction, and the big-project capacity list.
Full narrative: [`../../audits/round-17.md`](../../audits/round-17.md).

## UX-156..UX-162: the sixteenth audit round — the tool meets a big project (2026-08-20)

Round 16 verified the diagnosability round's landings (UX-147,
149..155 — all hold, three with caveats now carried by `UX-161` and
`UX-162`; UX-148 deferred cleanly, nothing half-landed) and then ran
the polish lens where the user now lives: **a multi-hour capture on a
project with thousands of elements**. Live experiments on a sabotaged
`examples/06` produced the round's headline — a snapshot of a build
that *failed* ends `Verdict: IMPROVED (-65.6%)`
([`UX-156`](UX-0156-a-build-that-did-not-finish-must-not-verdict-as-if-it-did.md)),
and Ctrl-C mid-capture destroys the Plane 2 trace it already has
([`UX-157`](UX-0157-ctrl-c-on-an-hours-long-capture-destroys-the-trace-it-already-has.md)).
The review's sweep added the structural pair:
[`UX-160`](UX-0160-the-census-reads-only-the-top-of-the-element-tree-and-auto-spine-bills-the-difference.md)
— non-recursive element discovery silently turns auto-spine into
full-build ptrace on real layouts — and
[`UX-161`](UX-0161-the-stale-casd-everyone-suspects-is-checkable-before-the-build-and-nobody-looks.md)
— the field failure's live suspect is checkable before the build and
nobody looks. [`UX-158`](UX-0158-help-is-a-design-history-lecture.md)
(the `--help` surface the docs concision pass never reached),
[`UX-159`](UX-0159-the-quiet-minutes-and-growing-gigabytes-of-a-big-project-snapshot.md)
(silent phases, unsized store, no prune) and
[`UX-162`](UX-0162-small-debts-of-the-diagnosability-round.md) round
out the batch. Full narrative:
[`../../audits/round-16.md`](../../audits/round-16.md).

## UX-147..UX-154: the fifteenth audit round — a field failure the tool cannot see (2026-08-20)

Round 15 chased the first real external deployment's failure (`bst
build` works, `bga snapshot` dies with `buildbox-run failed with
returncode 1` on Ubuntu 24.04) by rebuilding the user's exact shape on
this container — wheel → fresh foreign venv with its own bst → project
copy outside the checkout → snapshot from a third directory — and it
**worked**, falsifying the packaging hypothesis: the failing link is
environment-specific and *earlier than the shim*, precisely the region
UX-146's shim-side record cannot see. The first four filings close
that region: [`UX-147`](UX-0147-zero-shim-invocations-has-three-causes-and-diagnose-asserts-the-benign-one.md)
makes the zero-invocation verdict honest (self-probe, absolute
shebang, stale-daemon check, guarded environ),
[`UX-148`](UX-0148-a-failed-sandbox-should-leave-its-argv-and-its-stderr-behind.md)
preserves the dying sandbox's stderr and argv and replays them,
[`UX-149`](UX-0149-doctor-checks-the-parts-and-nothing-checks-the-chain.md)
composes everything into a canned whole-chain probe, and
[`UX-150`](UX-0150-the-shape-real-users-install-in-has-zero-capture-coverage.md)
puts the deployed install shape under CI so it stays working.

The round's code review over `0acaff5..dcdd402` then produced the
second batch: [`UX-151`](UX-0151-the-arity-table-is-the-likeliest-field-failure-and-nothing-records-the-version-that-breaks-it.md)
— the arity table against post-0.9.0 bubblewrap, now the **strongest
remaining hypothesis** for the field failure and invisible to the
mis-split detector; [`UX-152`](UX-0152-ux-143s-group-stop-fix-has-the-bug-it-was-filed-against.md)
— UX-143's group-stop fix re-verified by hand and found to contain the
bug it was filed against, on all three detach paths;
[`UX-153`](UX-0153-ux-142-fixed-the-headline-and-left-its-principle-half-applied.md)
and [`UX-154`](UX-0154-four-claims-that-outran-their-commits.md) — the
half-applied doctor principle and four claims that outran their
commits. Everything else verified: UX-140/141/144/145/146 hold in
full, UX-135..139's corpus arithmetic checks to the line. Full
narrative: [`../../audits/round-15.md`](../../audits/round-15.md).

## UX-135..UX-145: the fourteenth audit round — docs made simple, concise, consistent (2026-08-19)

Round 14 verified round 13's polish fixes hands-on (the two-snapshot
loop ran with zero user-invented paths and auto-compared IMPROVED
−25.8%; doctor, the cache-logs front door and `@last` aliases all
behaved as filed; suite 1689 green with live bst) and ran the round's
named lens: a fresh-eyes read of the whole 3,094-line user-facing
corpus for simplicity, concision and consistency. Full narrative:
[`../../audits/round-14.md`](../../audits/round-14.md).

Two threads of filings. **Docs** (`UX-135`..`UX-139`): first-value
ordering, the superseded flows still taught in the two most-read docs,
eleven duplicate clusters, the terminology table, and journey B's
missing page. **Verification findings** (`UX-140`..`UX-145`): the
SEIZE fallback breaks the exit-status contract its own file teaches,
the failure-injection lists test a deleted site and miss the
most-executed one, doctor false-FAILs every project without an
`all.bst`, plus the degrade/drain edges and the convention that
proved too narrow for its own worked example within one range.

## UX-125..UX-133: the thirteenth audit round, and the polish direction (2026-08-19)

Round 13 re-verified round 12's thirteen fixes (full narrative:
[`../../audits/round-13.md`](../../audits/round-13.md)) and opened the
post-MVP axis the MVP verdict pointed at: **simplify the user
scenarios**. Two threads:

- **Verification**: nine of thirteen hold outright — UX-114/116/120
  are model work (UX-116 ran its acceptance, got the *opposite* answer,
  and reworded the claim rather than the test; UX-120's projection
  missed reality and ships as an explicit floor), UX-115's CI comment
  was re-verified live on the retained pairs, and UX-118's SIGSTOP
  diagnosis was confirmed 0/3 → 3/3. The failures repeat two known
  classes: a guard that covers one of four identical sites (`UX-128`),
  a headline number its own inputs don't support (`UX-129` — which
  also, honestly, records that round 12's +31-44% was a warm-up
  artifact, refuted twice and confirmed by this round's interleaved
  re-measurement at ~0.85 ms/process), and the third round running of
  status-table drift (`UX-131`).
- **Polish** (`UX-125`..`UX-127`): the MVP bar was "following only the
  documentation"; these three attack what documentation cannot smooth —
  the environment (`bga doctor`), the loop's clerical overhead (one
  command, run twice, with a project-local run store), and Plane 3's
  name-vs-path front door. Each filed from friction this audit
  personally hit, with the failing commands pasted.

## UX-105..UX-108: Direction 4 — seeing every process (2026-08-18)

The one deliberate limitation Plane 2 has carried since `UX-11` chose
`LD_PRELOAD`: a fully static executable never invokes the dynamic
linker, so it produces no record and no error — and this repo's own
`examples/01`/`02` busybox elements are invisible to Plane 2 today.
[`design/directions.md`](../../design/directions.md) Direction 4
carries the argument: the existing shim → argv-rewrite → in-sandbox
env chain is kept (it is validated and the opens-tracking genuinely
needs in-process interposition), and the complement is a **ptrace
process-event spine** — chosen over acct(2), netlink CN_PROC, eBPF,
`/proc` polling and fanotify because it alone sees statics with full
argv/CPU/RSS, needs no privileges (tracees are its own descendants),
and pays per *process*, not per syscall.

Order: `UX-105` (measure the blind spot itself, pre-build, from the
staged roots — small and independently shippable) → `UX-106` (the
tracer) → `UX-107` (join/dedupe/provenance — without it the spine
*corrupts* CPU accounting instead of completing it) → `UX-108`
(validation on both build classes decides the default by measurement).

## UX-41..UX-48: the second audit round (2026-08-16)

Filed from a round that did exactly what the first round's own
[`docs/design/directions.md`](../../design/directions.md) told it to: it took
the three "what the next audit round should probe" items that were
actionable in one session - **scale**, **a real CPU measurement**, and
**declared-vs-used dependencies** - and probed them against real data
instead of re-reading the code.

**Scale was the productive one.** Every finding in the first round came
from projects of 8-13 elements. A 1202-element run - a toolchain import
everything depends on, twelve layers of 100 modules with real fan-out and
fan-in between adjacent layers, and a stack on top, scheduled onto 16
builders by a real dependency-respecting greedy pass - broke four things
at once (`UX-41`..`UX-44`), and `UX-47` fell out of timing the same run.

Three of those four are the **same failure mode**: a placeholder that was
plausible at 11 elements and is obviously wrong at 1200. `_compute_all_slacks`
returns `duration * 0.5` with the comment *"In full implementation, would
use forward/backward pass"*; the choke-point test is `in_degree >= 2 and
out_degree >= 2` under a comment naming the dominator approach; the level
decomposition is a BFS whose first-visit-wins semantics were never the
intended ones. Each is a few lines, each ships under a name that promises
a real computation, and none of them announces itself as provisional in
any output a user sees. That pattern - **not the individual bugs** - is
this round's main finding.

So the round then grepped for the rest of the pattern rather than leaving
it to the next scale probe, and `UX-48` is what that sweep found outside
`bga/structural/`: `IDLE_UNDERPARALLEL` is declared, read, and never
assigned, so the bucket meaning "raise `--builders`" reads 0.00s on every
run and its time is booked to the bucket meaning "restructure your
graph". That one needs no scale at all - it is wrong on an eleven-element
project, and it sends a user to the wrong half of the optimization cycle.

`UX-44` is the one to fix first. It is the tool's answer to the only
question a user actually arrives with - *what should I optimize?* - and
on a real capture of `examples/06` it omits the element that is 35% of the
build and names five interchangeable ones instead.

The other two probes produced one filing each and were both worth doing
for what they *settled*: `UX-45` confirmed by reading the real hook that
per-process CPU time is two `getrusage` calls away, and `UX-46` **refuted**
the cheap approach to declared-vs-used detection with real trace data -
the hypothesis was formed from reading the hook and killed by measuring
the output, which is recorded in that doc so the next attempt does not
re-derive it. Recording the refutation is the point; had it been filed as
"match staged paths against traced cmdlines", it would have been an
implementable-looking task that could not work.

### All of the above are now implemented (2026-08-17)

Every item in this round shipped, each verified against a real capture
rather than a fixture. Three results are worth knowing without reading
eight docs:

- **`UX-44` was the highest-value fix**, as predicted. "Slack" was never
  computed - it was `duration × 0.5` - so the improvement ranking was a
  strictly *inverted* duration sort. It now names `core.bst` first at up
  to 10.00s off the finish, independently reproducing the ~10s `UX-09`
  had measured by hand.
- **`UX-42` was 30x** (95.5s → 3.2s on the 1202-element fixture), and
  `UX-47` took `bga graph` from 117s to 0.99s across both fixes.
- **`UX-46` proved `examples/06`'s own documentation wrong.** The
  detector found 24 unused declared edges and confirmed `toolchain.bst`
  as the only dependency any element actually reads - so the project's
  comment that "only `lib-f` consumes `codegen.bst`" was false, and its
  *entire* cross-element dependency structure is decorative. The element
  files now say what was measured.

Two method notes carried forward into `docs/design/directions.md`:
byte-identical output on real fixtures did not establish `UX-42`'s
correctness (an oracle test found two bugs it had hidden), and five filed
acceptance criteria across the two rounds turned out to be wrong with the
measurement right - each corrected in place in its own doc rather than
quietly dropped.

## UX-27..UX-40: the 2026-08-16 audit round

Filed together from one real, hands-on session that did two things the
earlier rounds had not: it **audited what the tool claims against what it
does**, and it **walked a genuinely mis-optimized real project through a
full macro-then-micro optimization cycle** rather than a `sleep N` proxy.
`examples/06-macro-micro-optimization` was built for that walkthrough and
is part of this round - eleven real CMake/C++ elements, deliberately
broken in three independent, one-line ways (a six-deep chain that should
be a fan-out, an over-declared build dep, and `notparallel: True` on the
heaviest element), with an `optimized/` sibling that fixes exactly those
three and nothing else.

The full transcript with every real command and output is
[`docs/audits/case-study-06-macro-micro.md`](../../audits/case-study-06-macro-micro.md);
the argument about which of these items are load-bearing and which are
polish, for each of the tool's two real usage scenarios, is
[`docs/design/directions.md`](../../design/directions.md).

The headline result, and the reason `UX-27` is the anchor of this round:
fixing all three problems made the build **30.5% faster** (39.57s ->
27.50s) while `bga`'s own `efficiency_score` moved **1.00 -> 0.83** and
its `certified_headroom` moved **0.00s -> 4.05s**. Both moved backwards
because every certified floor is derived from the run's own observed
graph - a chain-shaped graph has a critical path equal to its own total
work, so the scheduler is tautologically perfect. Nothing in `bga` asks
whether the graph was worth packing.

Two of these items revise earlier decisions, both with new evidence rather
than on re-argument. `UX-29` reopens the "`native_max_jobs` isn't
auto-extracted" claim that an earlier review round triaged as not worth a
task: that triage was about `bst show` (where it genuinely is not
exposed), and the wrapped log's own first line does carry it, with five
shipped features inert on the documented happy path as the consequence.
`UX-31` corrects `UX-22`'s identification of BuildStream's per-element
parallelism mechanism - `public: bst: max-jobs` is inert metadata in
2.7.0; `notparallel` is the real control, confirmed by a real trace
showing `core.bst` running `make -j1` while every sibling ran `make -j4`.


`UX-08` was never used - no scenario was ever filed under that number, it isn't a lost/missing file. Numbering otherwise continues sequentially from whatever the next unused ID is at filing time.

## Why these five (plus five bugs/design docs found along the way)

Grounded in a real, hands-on walkthrough of the current CLI against `tests/fixtures/synthetic_multi_subproject` (not a hypothetical brainstorm) - `bga analyze`, `bga sweep`, `bga floors --cold`, `bga graph --by-kind` were all run for real to check what already works well (the Key Findings block, blast-radius ranking, and `bga sweep`'s knee-point detection are all already genuinely useful decision support) versus what's actually missing. `UX-01`/`UX-02` are the two gaps that most directly block the specific next-session goal: iteratively optimizing example projects using `bga`'s own output to guide each step, until reaching a "good enough" bar the tool itself can name. `UX-03`/`UX-04`/`UX-05` are natural extensions once those two exist.

## Recommended implementation order (open items, assuming full correct closure)

Built by pulling each open item's real cross-dependencies - both declared "Depends on" fields and *practical* implementation-order dependencies (touching the same function/file, one task's output making another cheaper) found by re-reading every open doc together, not just each in isolation. All hard "Depends on" prerequisites for the items below (`UX-09`, `UX-12`, `UX-15`, `P1-31`/`P1-39`/`P1-30`) are already done.

**Phase A - small, foundational, unblock everything else cleanly:**

1. **`UX-16`** (max-jobs=0 truthiness fix) - small, no blockers, and the base every other capacity-check task below either extends (`UX-21`) or delegates through (`UX-17`). Do first so nothing downstream inherits a known bug.
2. **`UX-18`** (canonical run-context builder, `bst_run_context.py` parity) - no blockers. Extracting one canonical manifest/run-context-builder function here is real prep work for `UX-07` below - do this first and `UX-07` only has to touch it once instead of twice.

**Phase B - build directly on Phase A:**

3. **`UX-07`** (run-identity collision fix) - do after `UX-18` for the reason above; otherwise fully independent.
4. **`UX-17`** (`UtilizationAnalyzer` consolidation) - scope now decided (delegates to `_check_process_oversubscription`); do after `UX-16` so it delegates to an already-fixed base, not one that still needs the truthiness fix.
5. **`UX-21`** (memory/swap oversubscription guard) - hard-depends on `UX-16` (mirrors its now-fixed governing-ceiling pattern for a new resource dimension); natural next pick while that pattern is fresh.

**Phase C - independent tracks, any order, safe to parallelize across sessions/contributors:**

6. **`UX-22`** (per-element `max-jobs` + serialization-point detection) - deliberately additive/decoupled from `UX-16`/`UX-17`/`UX-21`'s aggregate checks (confirmed in its own "Out of Scope" - not a replacement), so it doesn't force rework if done before *or* after them. Larger and more novel than the others (new per-element `bst show` capture).
7. **`UX-20`** (batch/map-reduce reporting) - zero dependencies, reporting-layer only, doesn't touch the capacity-check cluster at all. Good filler for a narrower-context session, same role `UX-04` played earlier.
8. **`UX-19`** (resource/scheduler-wait re-saturation + retry-gap decomposition) - zero dependencies on anything above, deep attribution-engine work, fully self-contained. Substantial; a good pick for a session with room for real design work.

**Phase D - large, higher-risk, most speculative (do last, and do the spike before committing):**

9. **`UX-11` risk-reduction spike** (not full implementation yet) - verify the two load-bearing, currently-unverified assumptions in its own leading candidate design before committing further work: does PATH-shadowing `bwrap` actually survive BuildStream's cache-key computation unchanged (real risk of silently invalidating caches, contradicting the design's own stated constraint), and how much real coverage is lost to statically-linked binaries in a real toolchain. A small real prototype against a live BuildStream sandbox answers both cheaply, before investing in the rest.
10. **`UX-11` full implementation** - only once the spike confirms the leading candidate design is viable (or a different option is chosen instead).
11. **`UX-14` tier 2** (contention-aware duration model) - explicitly gated on `UX-11` existing first per its own doc; needs real per-task CPU-vs-wall-clock data `UX-11` would supply to have any honest grounding.

## Decisions made during housekeeping (2026-08-16)

- **`UX-17` scope**: three options were presented (minimal wiring-only fix that stays practically inert per `P1-33`'s own gating; wire `effective_cpus` from `host_cpu_count`/`cpu_budget` but keep Part 30.3's check as a genuinely separate formula; or the same wiring plus delegating fully to `UX-12`'s already-correct `_check_process_oversubscription` logic). **Decided: delegate to `UX-12`'s check** - the two checks would otherwise compare the same real inputs via two independently-derived threshold formulas, risking divergent verdicts for the same real condition. `UX-17`'s own doc updated with the resolved Required Fix.
- **`UX-22` vs. the aggregate checks (`UX-16`/`UX-17`/`UX-21`)**: no live decision was actually needed here - re-reading `UX-22`'s own filed doc found it had already committed to staying additive/decoupled (its own "Out of Scope": *"The aggregate, single-global-value oversubscription check `UX-12`/`UX-16` already implement - this task is additive... not a replacement"*), consistent with the recommended order above. Noted, not re-litigated.
- **"Does `bga`'s analysis flow try to fully utilize `builders x native_max_jobs`?"** - checked directly (zero references to `native_max_jobs` in `bga/floors/` or `bga/replay/`) and found **false**: `LB`/`efficiency_score`/replay use `builders` alone, per `UX-13`'s own deliberate Part-16 scoping. Not a gap, so not a task - if a fuller CPU-utilization-targeting mode is ever wanted, that would be a real, separate design discussion that would contradict this existing, deliberate scoping and needs to be opened explicitly, not folded into any currently-filed item.

## Filing history (why each item was filed, in the order it was filed)

1. ~~`UX-01` and `UX-02` first, in either order~~ - **both done.** `bga compare BASELINE CANDIDATE` and `efficiency_score` are now real; the next optimization-iteration work session can lean on the tool instead of manual eyeballing.
2. `UX-04` is independent and small - good filler for a narrower-context session.
3. `UX-03` is now unblocked (depends on `UX-01`/`UX-02`, both done) - a natural next pick.
4. `UX-05` is in progress, written from a real optimization transcript (`examples/04-critical-path-optimization` + its `optimized/` variant) rather than a hypothetical one.
5. `UX-06` and `UX-07` were found *while doing* `UX-05`'s real optimization work - both are correctness bugs (not UX-flow gaps) discovered via real `bst build` + `bga analyze`/`bga compare` runs, deferred to backlog per this session's scope rather than fixed inline since both touch widely-referenced code (the core extraction pipeline's timestamp reconstruction, and run-identity's manifest shape referenced across ~20 files). `UX-06` is the higher-priority pick next since it likely affects every example project's CI-reported numbers, not just the new one.
6. `UX-09`/`UX-10`/`UX-11` came from a second, more realistic round of `UX-05` (`examples/05-cmake-cpp-toolchain` - real CMake/C++ builds, not `sleep N`), directly testing the user's own hypothesis that `--builders` and native `max-jobs` compete for the same CPU cores. `UX-09` is done (confirmed with real evidence); `UX-10` is done (`total_duration_us` now prefers real wall-clock); `UX-11` is a design brainstorm for a substantial future tool, not attempted yet.
7. `UX-12`/`UX-13`/`UX-14` are a follow-up brainstorm asking, once `UX-09`'s finding was confirmed and `UX-06`/`UX-10` were fixed, "does `bga`'s own measurement/report model actually reflect the builders×max-jobs contention effect, or is it still invisible to the tool?" - answer: still invisible, on three distinct fronts. `UX-12` is the cheapest, highest-leverage pick (pure capture/instrumentation, no math change) and directly supplies the real numbers `UX-13`'s report caveat and `UX-14`'s sweep caveat would want to cite. `UX-13` is a report-honesty-only fix (no math change, small). `UX-14`'s tier-1 minimum acceptance bar (an honest caveat in the actual CLI output, not just the spec doc) is now done; its full fix (a contention-aware duration model) is real, hard design work still likely gated on `UX-11` existing first.
8. `UX-15` came from the user directly challenging `UX-12`'s design once it shipped - a real, evidenced correction (cgroup CFS CPU quotas are invisible to `os.sched_getaffinity`, `UX-12`'s own detection method) rather than a hypothetical preference, folded into the same round as `UX-14` since both touch the same capacity-checking code path.
9. `UX-16`-`UX-19` came from an external review of the merged `UX-12`-`UX-15` work, triaged by independently re-verifying each claim against the real code before filing (not taken on the review's word) - two claims turned out to already be known, previously-documented, deliberately-deferred limitations (`UX-19`, reconfirming `P1-39`'s own explicit invitation to file the deferred consolidation separately), one was a real, reproduced bug in the new code (`UX-16`, a truthiness-vs-`is None` gap on BuildStream's real `max-jobs=0` sentinel), and one was a real, previously-undiscovered bug found by verifying the review's general architectural concern against the actual codebase rather than just the two examples the review itself cited (`UX-17` - `UtilizationAnalyzer`'s own oversubscription check has been dead code all along, and is the concrete case the review's abstract "naming trap" concern warned about). `UX-18` closes a real, concrete consistency gap the review surfaced between `bga`'s two documented run-context.json producers. Several other review claims (that `native_max_jobs` isn't auto-extracted from `bst show`/the log; that a `max_jobs`-vs-`native_max_jobs` internal rename is warranted on its own; that current report wording conflates "potential" with "observed" CPU usage) were checked and found to be either already-correct, already-deliberate, or not worth a dedicated task on their own - not filed.
10. `UX-20`-`UX-22` came from a further round of user brainstorming on top of `UX-12`-`UX-19`, each independently checked against the real code (not filed on the idea's own merits alone): `UX-20` (large-graph batching) confirmed a real, existing-but-invisible `sensitivity.top_opportunities` signal and a real absence of any multi-element batch simulation; `UX-21` (memory/swap oversubscription) confirmed zero memory accounting exists anywhere in `bga` today, a genuinely new gap, not an incomplete existing feature; `UX-22` (per-element `max-jobs`/serialization-point detection) elaborates a limitation `UX-12`'s own doc had already named and declined for a first pass, with a real, compelling motivating scenario (a large single-synchronization-point element like an LLVM build). A separate claim - "does `bga`'s analysis flow try to fully utilize `builders x native_max_jobs` as its scheduling-capacity target?" - was checked directly (`bga/floors/`, `bga/replay/`: zero references to `native_max_jobs` in either) and found **false**: `LB`/`efficiency_score`/replay all use `builders` alone, per `UX-13`'s own deliberate Part-16 scoping - not filed as a task since it's a factual question, not a gap, and reopening that scoping would be a real, separate design discussion if ever wanted. A concrete external design proposal for `UX-11` (`LD_PRELOAD` + `bwrap` PATH-shadowing process-lifecycle tracing) was evaluated and incorporated into `UX-11`'s own doc as its current leading candidate design, with real open risks (static-binary coverage gaps, an unverified BuildStream cache-invalidation assumption, musl/non-glibc `LD_PRELOAD` support) flagged for whoever picks it up.

11. `UX-71`-`UX-76` came from a user brainstorm after round 9's MVP verdict, asking two things: whether the report could be **more concise, actionable, and able to enable parallel optimization workstreams**, and whether **every valuable finding actually reaches the JSON report** or only a cut of the text one. Each was checked against round 9's real published capture (`5eda28a`) rather than filed on the idea's merits: the concision question surfaced a live regression (`UX-76` - `UX-70` re-sorted a shared helper and quietly changed what "Where the time is" means) and three overlapping headline rankings; the parallelism question surfaced that the tool already owns the machinery (`compute_realizable_savings`, `batching.py`) but points it at a saturated candidate score, so it returned **0 groups on real data** while two of the build's six heaviest elements sit unmentioned two fixes away (`UX-74`); and the JSON question turned out to cut **both ways** - JSON has no conclusions, text has no `aggregating_dependencies` (`UX-75`). Investigating what the join *could* say then produced the two sharpest items: `UX-71` (the join's headline verdict is unreachable on real data because its ranking metric is a constant) and `UX-72` (three of Plane 2's findings are read, the rest are dropped), plus `UX-73`, a producer-side false-positive rate that would be inherited the moment `UX-72` wires redundancy into the join. Ordering: `UX-71` then `UX-72` (both correct the join, and `UX-73` gates the redundancy half of `UX-72`), `UX-76` is a small independent fix worth taking in the same pass, `UX-74` is the largest and most valuable, and `UX-75` is deliberately last because it should serialize whatever conclusions the others settle on.

## UX-215..UX-226: the twenty-fourth audit round — the relationship layer already exists (2026-08-22)

Round 24 evaluated a third external review of the shipped viewer, the
same way as the previous two: claim by claim, against the code, every
premise checked before any of it was agreed to. Its direction is right
and its diagnosis — *the missing thing is relationships between
existing information* — is correct. Its cost model is wrong in three
places, and once in the cheap direction.

The finding is that the relationship layer **is already computed and is
not published**. `bga/correlate.py:141` assembles an `ElementJoin` per
element — Plane 1's path share, saving and blast radius beside Plane
2's achieved parallelism, CPU coverage, peak RSS and dominant binary —
and `bga correlate --format json` emits it, correctly and
completely, as an **unversioned blob nothing can consume**: no `schema`
stamp, no view-hints, not served by `payloads()`, and
`correlate --schema` answering *"correlate produces no versioned JSON
output"*. So what the review proposed building as a viewer feature, and
separately as a new "three-plane investigation ladder", is one
already-computed join missing a contract — `UX-206`'s pattern for the
fourth time ([`UX-215`](UX-0215-publish-the-join-the-tool-already-computes.md)).

Verification added what no review sees: **round 23 shipped nineteen
links to nowhere.** `UX-208`'s Inspect anchors at `#${cssId(uid)}` and
nothing ever sets that id — measured on `examples/06`, 11 of 11
distinct targets unresolvable, while the guards asserted the affordance
existed rather than that it arrived
([`UX-216`](UX-0216-every-element-is-one-object.md)).

Three of the review's premises did not hold: the schema does **not**
already describe the metrics that need describing (`floors` is 0 of 0)
([`UX-220`](UX-0220-the-numbers-that-need-a-sentence-have-one.md));
`compare/v1` carries **no** per-element deltas, so the culprit strip is
a payload item first
([`UX-221`](UX-0221-which-elements-caused-the-regression.md)); and its
element-inspector *drawer* was declined in that shape — overlay
machinery is the one part of this page that would not survive an
export, a print, `filter: grayscale` or a pasted anchor, and a section
gets the same value while making `UX-208`'s dead anchor resolve as a
side effect. Its "resist adding more charts" was agreed and not filed:
that is Direction 7's standing position already.

What the review did not look at is the **loop** rather than the report —
every item it proposed improves one reading of one report, while the
repetition lives in capture → analyze → read → change → capture again.
Three items came from walking that instead: the next three commands are
always retyped and can be published and rendered
([`UX-218`](UX-0218-the-next-step-is-a-command-you-can-run.md)), the
investigation is not resumable because the *decision* is nowhere
([`UX-225`](UX-0225-the-working-set-travels-in-the-link.md)), and
"did my fix work?" is answered by opening two reports side by side
([`UX-226`](UX-0226-what-happened-to-this-element-since-last-time.md)).
Full narrative: [`../../audits/round-24.md`](../../audits/round-24.md).
