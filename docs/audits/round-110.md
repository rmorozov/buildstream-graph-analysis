# Round 110 — the open tasks, in parallel tracks behind verifiers

Run on 2026-09-08.

Round 109 read the ten unread tracks and worked seven open tasks the
same way. This round took the rest of the open list that fits a
track — fifteen tasks — as one survey, three batches and two serial
chains, every track an `implementer` on `sonnet` in a worktree and
every merge behind a `verifier`. Seven filings came out of the
verifiers, the review and the drift gate; five open tasks are round-sized and stay.

## What closed

- `UX-744` — the round register, closed on a verifier's reading of the tree (the HOLD answered by `UX-772`).
- `UX-758`, `UX-779`, `UX-764`, `UX-795` — the edge-mark test, the README's wall clocks, the register caps, the focus guard's settle.
- `UX-777`, `UX-789`, `UX-775`, `UX-780` — the page-built count derived (14, not 13), the baseline's stray key refused on a finding too, the last two bst files in their own home, a map row's every cited id read (slash groups included).
- `UX-741` — the spine guard reads a lower bound and the harness's enclosing span; green quiet and at load 16.
- `UX-774` — the guide's size figure written by `dev_touching.py --size --write` into both documents; `UX-607`'s band retired for tool equality.
- `UX-668` — a reader is a shape: the select in the header, promotion a 3 px left rule, zero new colors.
- `UX-690` — the suite's shape table, derived; the browser row a ratchet adopted at 50.5 %, the journey row reported at 2 of 25.
- `UX-683` — a declared foundation tier, its own row in every blast, fan-in and expected-cost ranking; discovery proposes the rest.
- `UX-671` — the jump box writes the anchor it scrolled to; the rail preset's was already carried by the href.

Filed: `UX-796` (the host sampler over-counts busy cores under load),
`UX-797` (eight identical sleeps spread past a tenth), `UX-798` and
`UX-799` (review 21), `UX-800` (the rail landing counts frames),
`UX-801` (`bst show` writes the CAS under the ambient HOME),
`UX-802` (the baseline guard files spawn pyright sixteen times).

## What the verifiers found

Fifteen verifier runs; nine returned a HOLD or a named fix, each
landed on the track's branch before the merge:

```text
UX-779   the 21s clock was the task's second, not tiers.py's; four points
UX-764   a bogus path with the word "unconventional" passed the convention guard
UX-795   scope: the settle applied to one read, not every mid-flow read
UX-777   two soft points: the fraction typed, the count word tabled
UX-789   the stray-key refusal read the document and not a finding
UX-775   the Outcome omitted that the fallocate step was substituted
UX-780   the citation regex read the first id of a slash group only
UX-690   an 11-line commit body; a missing ci_reference.json passed vacuously
UX-683   the validation test was a proxy (49 green with the clause collapsed); two decisions off the record
```

Four PASS verdicts carried a finding the track's own table could not
see: `UX-741`'s enclosing bound is loose (4.8×, a +2 s plane shift
now passes — `UX-110`'s axis); `UX-668`'s third frame is a count, not a
settle (`UX-800`); `UX-671`'s rail write was dead code, removed; the
`UX-741` file's untouched spread clause reddened once in twenty at
organic load (`UX-797`).

## Review 21

The cadence guard reddened at 30 closes since review 20. One reader
over the five document groups the two rounds touched: two findings
(`UX-798`, `UX-799`), both the shape review 20 named — a record no
guard reads beside the tree that shows it. `CLAUDE.md`'s advisory,
round 109's thirteen closes and the guide's size figure reproduced.

## Standing

The suite's shape, derived by `dev_shape_budget.py` and printed by
`dev_close_task.py --check` (`UX-690`):

```text
shape         files   CI seconds    share
unit            448       789.2s    49.3%
sweep             1         2.1s     0.1%
journey           2         0.1s     0.0%
browser          53       770.0s    48.1%
enormous         20        39.3s     2.5%
total           524      1600.8s
```

Browser holds a 48.1 % ratchet (50.5 % at `UX-690`'s merge, re-adopted
after the reference refresh below) against the 40 % the row was filed
with; journey is 2 files against 25 published contracts, reported.

## Process, measured

- The gate reddened once on a host reading (`UX-796`, load 12) and the bst tier reddened on a CAS whose default quota casd sized from a disk this container reports as full; a 3 G quota in the ambient config cleared it (`UX-801`).
- A rate limit at 13:35 cut four agents mid-run; all four resumed from their transcripts, the tracks with their edits on disk.
- Two merges conflicted on the guide's derived cost row — both sides had written it; re-derived after each merge.
- The size ledger re-adopted once after the last merge, 19 cells (round 109's §7a note, done as the last step before the gate).
- Two tracks committed with `BGA_SKIP_SELECTOR=1` on the review-cadence red, stated in the body.
- The `derive` shape held: no track edited the spec.
- PR #218's first CI run reddened the tier-drift gate on four files, confirmed on two runs: two guard files that spawn pyright per clause since `UX-697` (50.7 s and 13.2 s against 2.4 s and 0.9 s recorded — the reference was adopted at 06:18, round 109 merged at 13:54; `UX-802`), and two browser files at 1.6–1.8× that read the same on this box before and after the round (5.2 s → 5.0 s, 15.0 s → 15.1 s, two runs each). The four rows were refreshed from that run's own readings, the candidate artifact being unreachable through this container's proxy; the browser ratchet re-adopted after it.

## Agents

30 runs — 14 `implementer`, 15 `verifier`, 1 `researcher`, all on
`sonnet`. Nine tracks were judgement-shaped; the session took the
judgement in the brief and the track the work.

| | |
|---|---|
| implementer | 14 tracks, every one merged behind a verifier; median 257k tokens, 53 m |
| verifier | 15 runs; median 66k tokens, 13 m; 9 of 15 returned a HOLD or a fix |
| researcher | review 21; 73k tokens, 5 m |
