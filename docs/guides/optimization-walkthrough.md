# Walkthrough (retired)

This was a happy-path transcript of `examples/04-critical-path-optimization`
— a `sleep N` proxy graph — from the `wrap`/`extract` era. Every lesson in
it is now taught by [`real-project.md`](real-project.md) with output from a
real build, and none of its commands mention `bga snapshot`, `bga doctor`
or `@last` (`UX-139`).

- **Start here instead:** [`real-project.md`](real-project.md).
- The transcript is kept, unmodified, as a record of what that round
  measured: [`../audits/optimization-walkthrough-04.md`](../audits/optimization-walkthrough-04.md).
- The harder companion — the case where the tool *does not* guide you,
  written down with real numbers — is
  [`../audits/case-study-06-macro-micro.md`](../audits/case-study-06-macro-micro.md).
