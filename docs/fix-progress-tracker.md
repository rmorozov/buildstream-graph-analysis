# BGA Spec Compliance Fix Progress Tracker

**Last Updated:** 2026-08-13
**Overall Status:** P0 verified fixed. **Every P1 item (P1-01 through P1-22) and every P2 item (P2-01 through P2-05) is now done.** `bga/floors/`, `bga/report/`, `bga/validation/` now exist per spec Part 39's recommended architecture (`P1-15`, done incrementally with byte-identical CLI output verified after each of 3 extraction steps). Only P3 (test-coverage build-out) remains — see tables below.

> **Start here, but don't stop here.** This file is an index only. Read `docs/fixing-guide.md` once per session, then open exactly one task file under `docs/tasks/` — do not try to absorb this whole tracker or the full spec into context at once.

## Status Legend

| Status | Meaning |
|---|---|
| 🔴 Not Started | No work begun |
| 🟡 In Progress | Work underway, or a prior claim of "done" that could not be re-verified — treat as not-done |
| 🟢 Fixed & Verified | A named agent ran the task's acceptance test in-session and pasted a passing result into the task file |
| ⚪ Blocked / Out of Scope | Needs a product decision, or deliberately deferred — reason in task file |

**Rule:** a row may only show 🟢 if the linked task file's Verification Log has a real pasted command + passing output. If you find a 🟢 row without that evidence, treat it as 🟡 and re-verify before trusting it — this has already happened once (see Correction Log).

---

## Correction Log (read once, this is why re-verification is mandatory)

On 2026-08-13 the previous tracker's P1 "Attribution Categories" block (old rows 2.1–2.4) was marked 🟢 "All Fixed." Direct code inspection found:
- `classify_resource_wait()` (`bga/attribution/blame_chain.py:291-338`) is now *called* (good), but its body is still the original stub: a `for res in task.resources: pass` no-op loop, `blocking_tasks: {}` always empty, `ambiguous: False` hardcoded regardless of whether a holder was actually identified. Not real per Part 8.
- `classify_scheduler_wait()` (`bga/attribution/blame_chain.py:340-372`) still unconditionally `return False` — completely unimplemented, contradicting its "Fixed" mark.
- Running the CLI end-to-end against a minimal 3-task single-resource-pool fixture (see `docs/fixing-guide.md` §7) shows the Attribution Breakdown summing to only ~33% of the task horizon (H) — a live invariant I4 violation, not previously listed as an issue at all. Filed as `P1-03` (highest real-world priority of the open P1 items).

This is not a criticism of any particular session — it's the reason the verification rule in `docs/fixing-guide.md` §3 is mandatory going forward.

---

## P0 — The Tool Does Not Run

**Status:** 🟢 Verified fixed — confirmed by running `PYTHONPATH=. python3 tests/test_e2e.py` (7/7 passed) and `python3 -m bga.cli analyze <fixture>` end-to-end (exit 0, produced a full text report) on 2026-08-13. No task files — nothing further to do here unless a regression is found.

| # | Issue | Status |
|---|-------|--------|
| P0-1 | CLI constructor mismatch | 🟢 |
| P0-2 | `analyze()` signature mismatch | 🟢 |
| P0-3 | Output formatters referenced non-existent fields | 🟢 |
| P0-4 | Undeclared `networkx` dependency | 🟢 (now in `pyproject.toml` deps) |
| P0-5 | `DiagnosticsAnalyzer` missing graph attribute | 🟢 |
| P0-6 | Missing `deque` import | 🟢 |

---

## P1 — Silently Wrong Numbers

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P1-01 | Resource-wait holder tracking is a stub (no real occupancy-based holder set, `ambiguous` hardcoded False) | — | 🟢 Done — surfaced a related wiring gap, see `P1-20` | [P1-01](tasks/P1-01-resource-wait-holder-tracking.md) |
| P1-02 | Scheduler-wait detection unconditionally returns False | — | 🟢 Done (also fixed a tautological call-site `resource_available` check that would have made this fix unobservable) | [P1-02](tasks/P1-02-scheduler-wait-detection.md) |
| P1-03 | Attribution identity (I4) violated on resource-constrained chains — 3 root causes fixed (zero-wait walk termination, multi-task-per-element predecessor mismapping, spurious multi-terminal heuristic) | — | 🟢 Done (residual gap scoped as `P1-19`) | [P1-03](tasks/P1-03-attribution-identity-resource-chains.md) |
| P1-04 | Flattened timeline undercounts on genuinely disconnected multi-terminal graphs | — | 🟢 Done — also added interval-overlap prevention across walks and IDLE segment generation (previously nonexistent for any scenario) | [P1-04](tasks/P1-04-flattened-timeline-multi-terminal-coverage.md) |
| P1-05 | No violation raised when timeline undercounts | P1-04 | 🟢 Done — defense-in-depth (no known fixture currently triggers it, verified via monkeypatch) | [P1-05](tasks/P1-05-reconciliation-violation-reporting.md) |
| P1-06 | `T∞,cold` hardcoded None; `historical_runs` never supplied | — | 🟢 Done | [P1-06](tasks/P1-06-cold-floor-historical-wiring.md) |
| P1-07 | No `--cold`/`--allow-partial-cold` CLI flags; cold publication gate unimplemented | P1-06 | 🟢 Done — also added `--history-dir` (necessary CLI addition, spec never says where historical data comes from) | [P1-07](tasks/P1-07-cold-cli-flags-and-publication-gate.md) |
| P1-08 | Capacity lower bound only accounts for PROCESS pool; no exclusive-serialization bound | — | 🟢 Done — also fixed a newly-exposed I2 violation in `ReplayScheduler._get_task_resources` (hardcoded to PROCESS-only) | [P1-08](tasks/P1-08-capacity-lower-bound-completeness.md) |
| P1-09 | Criticality "Monte Carlo" ignores perturbed durations, returns fixed 0/1 | — | 🟢 Done — resampling itself was already fixed (stale diagnosis); real bug was a key-format mismatch collapsing every probability to 0.0 | [P1-09](tasks/P1-09-genuine-montecarlo-criticality.md) |
| P1-10 | Blast-radius weighted duration uses fake average instead of real downstream traversal | — | 🟢 Done | [P1-10](tasks/P1-10-blast-radius-real-traversal.md) |
| P1-11 | Leaf/deferrability fix (`is_required_by_target`, `reachable_from_targets`) claimed fixed — re-verify | — | 🟢 Done — `edg.py`'s reachability was fine; found `requested_targets` hardcoded `None` one layer up in `analyzer.py` | [P1-11](tasks/P1-11-verify-leaf-deferrability-fix.md) |
| P1-12 | No determinism harness (N≥100 run comparison), no `bga/validation/` package | — | 🟢 Done | [P1-12](tasks/P1-12-determinism-harness.md) |
| P1-13 | Confidence computation only checks ordering violations; other hard/soft gates never computed | P1-05 (done) | 🟢 Done | [P1-13](tasks/P1-13-confidence-reconciliation-gates.md) |
| P1-14 | Only `bga analyze` exists; spec recommends `graph/floors/replay/sweep/utilisation/diagnostics` subcommands | — | 🟢 Done — user chose the hybrid option; also fixed 3 pre-existing dead-code/NaN bugs the new subcommands exposed | [P1-14](tasks/P1-14-cli-subcommand-split.md) |
| P1-15 | Missing `bga/floors/`, `bga/report/`, `bga/validation/` packages (architecture) | P1-06..P1-13 (all now done) | 🟢 Done — extracted incrementally, one commit per package, byte-identical CLI output verified after each step | [P1-15](tasks/P1-15-package-architecture-refactor.md) |
| P1-16 | Several graph/attribution algorithms are O(N·E)/O(N²), spec wants O(N+E) | — | 🟢 Done — 2 new out-of-scope hotspots found while verifying, filed as `P1-21` | [P1-16](tasks/P1-16-performance-on-plus-e-algorithms.md) |
| P1-17 | Terminology audit against spec Part 43 avoid-list | — | 🟢 Done — zero matches, no code change needed | [P1-17](tasks/P1-17-terminology-audit.md) |
| P1-18 | `structural.metrics.max_depth` uses shortest-path not longest-path (disagrees with `signals.unweighted_depth`) | — | 🟢 Done | [P1-18](tasks/P1-18-structural-max-depth-shortest-path-bug.md) |
| P1-19 | Flattened timeline doesn't cover intra-element `TRACK→FETCH→BUILD` sequencing or off-chain parallel task time | P1-03 (done) | 🟢 Done — also resolved off-chain coverage for connected components; narrowed `P1-04`'s remaining scope | [P1-19](tasks/P1-19-flattened-timeline-residual-coverage.md) |
| P1-20 | Blame-chain walk never classifies a gap as RESOURCE_WAIT/SCHEDULER_WAIT — always defaults to DEPENDENCY_WAIT, so `P1-01`/`P1-02`'s correct classifiers never reach final output | P1-01 (done), P1-02 (done) | 🟢 Done — also fixed a dormant double-counting bug in `compute_task_attribution` found in the same investigation | [P1-20](tasks/P1-20-gap-classification-into-resource-scheduler-wait.md) |
| P1-21 | Additional O(N²)-ish hotspots found while verifying `P1-16` (not in its original scope): `compute_reachability`'s full-set materialization, `compute_dominators`'s naive dataflow, `_estimate_ready_count`'s O(N²) `any()` loop | — | 🟢 Done — 5.1x faster on the 1500-element profiling fixture (14.16s→2.79s); `compute_dominators` deliberately deferred (only consumer reads dict size, not real dominance data); found and filed `P1-22` along the way | [P1-21](tasks/P1-21-additional-performance-hotspots.md) |
| P1-22 | `is_on_critical_path`/`is_on_blame_chain` always False in leaf/blast-radius diagnostics (element-UID-vs-task-key mismatch) | — | 🟢 Done | [P1-22](tasks/P1-22-blame-chain-critical-path-membership-mismatch.md) |

---

## P2 — Robustness / "Just Works"

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P2-01 | No cycle detection; exit code 3 never produced | — | 🟢 Done (was already implemented; original diagnosis was stale) | [P2-01](tasks/P2-01-cycle-detection-exit-codes.md) |
| P2-02 | Malformed JSON / bad input unhandled | — | 🟢 Done (was partially done; fixed `load_graph` wrapping + missing-file exit code) | [P2-02](tasks/P2-02-malformed-input-error-handling.md) |
| P2-03 | No logging module wired anywhere; `--verbose` does nothing but toggle traceback printing | — | 🟢 Done — also added a `BgaError` exception hierarchy, replacing a `'cycle' in str(e)` substring hack with type-based exit-code routing | [P2-03](tasks/P2-03-logging-infrastructure.md) |
| P2-04 | Retry/rebuild detection unimplemented — utilization buckets always empty | — | 🟢 Done | [P2-04](tasks/P2-04-retry-rebuild-detection.md) |
| P2-05 | `--format json` silently omits `structural`/`utilisation`/`confidence`/`violations` (typo'd `hasattr` check + missing fields) | — | 🟢 Done | [P2-05](tasks/P2-05-cli-json-missing-fields.md) |

---

## P3 — Test Coverage Build-Out

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P3-01 | Shared synthetic topology fixture library | — | 🔴 build first, everything else reuses it | [P3-01](tasks/P3-01-topology-fixture-library.md) |
| P3-02 | CLI integration tests (`tests/test_cli.py`) | — | 🟢 Done (7/7 pass) | [P3-02](tasks/P3-02-cli-integration-tests.md) |
| P3-03 | Attribution identity tests (I4) across topologies | P3-01, P1-03 | 🔴 | [P3-03](tasks/P3-03-attribution-identity-tests.md) |
| P3-04 | Tie-break + resource-holder tests | P3-01, P1-01 | 🔴 | [P3-04](tasks/P3-04-tie-break-and-resource-holder-tests.md) |
| P3-05 | Phase overlap + occupancy edge-case tests | P3-01 | 🔴 | [P3-05](tasks/P3-05-phase-and-occupancy-edge-case-tests.md) |
| P3-06 | CPU reconciliation (I9) + cold-floor tests | P3-01, P1-06 | 🔴 | [P3-06](tasks/P3-06-cpu-reconciliation-and-cold-floor-tests.md) |
| P3-07 | Monte-Carlo criticality + determinism-harness tests | P1-09, P1-12 | 🔴 | [P3-07](tasks/P3-07-montecarlo-and-determinism-tests.md) |
| P3-08 | Golden/regression tests (full-pipeline snapshot) | P3-01 | 🟡 partially covered by P3-10's anti-drift check | [P3-08](tasks/P3-08-golden-regression-tests.md) |
| P3-09 | Per-module unit test split (normalize/occupancy/edg/blame_chain/replay/utilisation/diagnostics) | — | 🔴 | [P3-09](tasks/P3-09-per-module-unit-tests.md) |
| P3-10 | Large multi-subproject synthetic-project integration test, using the real `tools/bst_log_to_chrome_trace.py` converter | — | 🟢 Done — found `P1-18` and `P2-05`, amplified `P1-03` evidence | [P3-10](tasks/P3-10-synthetic-multi-subproject-large-test.md) |

---

## Recommended Order for a Sequence of Small-Context Sessions

**Every P1 item (P1-01 through P1-22) and every P2 item (P2-01 through P2-05) is now done.** All hard-invariant families (attribution identity I4, capacity lower bound I1/I2, confidence/reconciliation gates I11) hold exactly on every fixture tested, the CLI covers the full spec Part 37 command list, the codebase matches spec Part 39's recommended `bga/floors/`/`bga/report/`/`bga/validation/` architecture, and the `wasted_retry`/`wasted_rebuild` utilization buckets are now reachable from real data. Only P3 (test-coverage build-out) remains. Next up:

1. `P3-01` (shared synthetic topology fixture library - build first, `P3-03` through `P3-07` all reuse it)
2. Everything else in ID order within its priority tier, respecting the Depends-on column.

**Before starting any 🔴 row: re-verify it's actually still broken.** This tracker has now caught three separate cases this session (`P2-01`/`P2-02`, `P1-09`'s critical-path resampling, `P1-11`'s reachability computation) where a task's *originally diagnosed* root cause had already been fixed by an earlier, undocumented change - but a *different*, less obvious bug in the same area was still live. Re-verify by reading the current code and running a real reproduction before trusting any stale diagnosis, including this tracker's own.

## Change Log

| Date | Change |
|---|---|
| 2026-08-13 (latest) | Fixed `P2-04` (retry/rebuild detection): added `bga/utilisation/detection.py::compute_retry_tasks` (every non-final `attempt` recorded for the same `element_uid\|task_kind\|phase` is a retry, Part 5.2) and `compute_rebuild_tasks` (a BUILD task is a rebuild if its element's current `cache_key` was already built successfully in `historical_runs` - reusing the exact signal `bga.floors.cold`/`P1-06` already keys its historical lookups by, so no new ingest schema field was needed). Wired both into `bga/analyzer.py::_compute_utilization`, replacing the hardcoded empty `retry_tasks`/`rebuild_tasks` sets - confirmed the consumer side (`bga/utilisation/__init__.py`'s bucket routing) was already correct, so no scope expansion was needed. **Every P1 item (P1-01 through P1-22) and every P2 item (P2-01 through P2-05) is now done** - only P3 (test-coverage build-out) remains. Added `tests/unit/test_retry_rebuild_detection.py` (12 tests: 9 direct unit tests on the two pure functions, 3 end-to-end tests through `BuildEfficiencyAnalyzer` confirming real `wasted_retry`/`wasted_rebuild` bucket values). Full suite: 112 passed (was 100). |
| 2026-08-13 | Closed the last two open P1 items. **`P1-22`** (real bug, found while fixing `P1-21`): `BlameChainNode` has no `__str__` override, so `_compute_diagnostics` was putting Python object-repr strings (not task keys) into `blame_chain`; separately, `compute_blast_radius`/`compute_leaf_analysis` were routing critical-path membership checks through a task-key `task_map` translation even though `self.critical_path` already holds element UIDs directly - the combination made `is_on_critical_path`/`is_on_blame_chain` always `False`. Fixed both root causes (`str(t.task_key)` at the source; direct `elem_uid in self.critical_path` at the consumption site, keeping the `task_map` translation only for the now-correct `blame_chain` membership check). Rewrote the one test that had documented the bug (`test_leaf_analysis_and_blast_radius_match_pre_refactor_behavior` → `..._report_real_critical_path_membership`) to assert the real, verified values instead. **`P1-15`** (pure architecture refactor, deliberately deferred until every other P1 item was correct): extracted `bga/floors/` (`observed.py`/`capacity.py`/`serialization.py`/`cold.py`), `bga/report/` (`text.py`/`json.py`/`_shared.py`), and `bga/validation/invariants.py` (`compute_confidence`, redesigned to return `(confidence_dict, new_violations)` instead of mutating `self.violations` in place, so the extracted function is genuinely pure) out of `bga/analyzer.py`/`bga/cli.py`, one package per commit, each step verified via full-suite + byte-identical CLI-output diffing (`git stash` before/after) exactly as the task's acceptance test required. No computed value or output format changed. **Every P1 item (P1-01 through P1-22) is now done**, and the codebase matches spec Part 39's recommended package layout. Full suite: 100 passed (unchanged - pure refactor + one test rewrite). |
| 2026-08-13 | Resolved `P1-14` (user chose the hybrid CLI design): `analyze` stays the primary command; added `graph`/`floors`/`replay`/`sweep`/`utilisation`/`diagnostics` as thin aliases sharing the same pipeline, restricting output to one report section each (`sweep` is the exception - a genuine capacity sweep via `ReplayScheduler.capacity_sweep`, Part 19, previously fully implemented but completely unreachable from the CLI). Building `graph` surfaced two pre-existing dead-code bugs in `format_text` (nonexistent `result.critical_path`/`result.structural_metrics` attributes meant the Critical Path and Structural Analysis blocks had never fired for *any* input, in *any* subcommand, ever) - fixed alongside this task since the new subcommand's entire purpose depended on that content existing. Building `sweep` surfaced a `NaN` bug in `capacity_sweep`'s first-sample `normalized_improvement` (`prev_makespan` starts at `+inf`; the guard checked `> 0`, true for infinity too) - also fixed. Then closed most of `P1-21`'s scope: fixed `_estimate_ready_count`'s O(N²) scan (bisect-based, O(log N) per query, proved the `active_tasks`/`finish_us` checks the old version did are redundant), found and fixed two more O(N²) `any(...)` membership-scan hotspots in `compute_leaf_analysis`/`compute_blast_radius` (not in `P1-21`'s original three), and deduped `analyze_graph` from 3 redundant per-`analyze()` calls to 1. Combined: **14.16s → 2.79s (~5.1x faster)** on the 1500-element profiling fixture. `compute_dominators`'s algorithmic rewrite deliberately deferred (its only consumer reads dict size, not real dominance data - not worth a Lengauer-Tarjan rewrite for that). Fixing the `any(...)` hotspots surfaced a real, separate, pre-existing correctness bug (`is_on_critical_path`/`is_on_blame_chain` always `False` due to an element-UID-vs-task-key mismatch) - filed as `P1-22` rather than silently fixed in a performance-only commit. Added `tests/unit/test_cli_subcommands.py` (10 tests) and `tests/unit/test_diagnostics_performance.py` (4 tests). Full suite: 100 passed (was 96). |
| 2026-08-13 | Closed out every remaining open P1 item except `P1-14` (blocked) and `P1-15` (deliberately deferred, now unblocked): `P1-17` (terminology audit, clean, no change needed); `P1-16` (three named graph algorithms now O(N+E), not O(N·E)/O(N²) - found 2 more out-of-scope hotspots, filed `P1-21`); `P1-09`+`P1-10` (criticality-probability key-format mismatch that collapsed every probability to 0.0, despite the resampling itself already being correctly fixed in an earlier undocumented round; blast-radius now sums real downstream durations instead of a global average); `P1-12` (new `bga/validation/determinism.py` harness, Part 35/I11); `P1-05`+`P1-06`+`P1-07`+`P1-08`+`P1-11`+`P1-13` (reconciliation-violation reporting; the advisory cold structural floor implemented end to end with CLI flags; capacity LB generalized beyond PROCESS-only, which exposed and required fixing a previously-latent I2 violation in replay's hardcoded resource lookup; `P1-11`'s real bug found one layer above where it was originally diagnosed - `requested_targets` hardcoded `None`, not the reachability BFS itself; confidence now implements all 5 named coverage metrics and the full hard/soft-gate + `min()` formula). Added 9 new test files (~30 tests). Full suite: 86 passed (was 60). |
| 2026-08-13 | Fixed `P2-03` (logging infrastructure): added `bga/exceptions.py` (`BgaError`/`IngestionError`/`NormalizationError`/`AnalysisError`/`ValidationError`, each also subclassing the builtin type already used at that site so existing `except ValueError`/`except FileNotFoundError` handling keeps working) and `bga/logging_config.py::configure_logging`. Wired `logging.getLogger(__name__)` into all 9 packages named in the task with real log calls (ingestion summaries, normalization violations/clamps, occupancy/attribution/replay summaries, a CPU-reconciliation-tolerance warning, the ordering gate's pass/fail, and the structural critical-path computation's previously-silent `except Exception: return []` now logged with `exc_info=True`). `--verbose`/`-v` now genuinely enables DEBUG logging; added `-q`/`--quiet` and `--log-file PATH`. Replaced `cli.py`'s `'cycle' in str(e).lower()` substring-matching exit-code hack with real type-based routing (`AnalysisError`→3, `IngestionError`/`JSONDecodeError`→2) - behaviorally identical, confirmed against the existing exit-code test suite. Added `tests/unit/test_logging_and_exceptions.py` (6 new tests). Full suite: 60 passed (was 54). |
| 2026-08-13 | Fixed `P1-20` (gap classification wiring): added a single shared `_classify_wait_gap` helper that splits `[ready_time, start)` into non-overlapping `RESOURCE_WAIT`/`SCHEDULER_WAIT`/`DEPENDENCY_WAIT` portions (resource first via `P1-01`'s holder tracking, then scheduler via `P1-02`'s detector, remainder to dependency-wait), used by both `build_blame_chain` (now emits per-category segments instead of one hardcoded `DEPENDENCY_WAIT` segment) and `compute_task_attribution` (fixed the dormant double-counting bug where the same gap was written into both `dependency_wait_us` and `resource_wait_us`). `resource_wait_us`/`scheduler_wait_us` are no longer structurally always `0` — confirmed on `tests/fixtures/synthetic_multi_subproject/` (`resource_wait_us` now `2000000`, was `0`) with exact `Σ == H` identity preserved. Added `tests/unit/test_wait_gap_classification.py` (2 new end-to-end tests). Full suite: 54 passed. |
| 2026-08-13 | Fixed `P1-01` (real resource-wait holder tracking): `classify_resource_wait` now derives time-weighted `blocking_tasks` directly from observed overlapping same-resource task intervals, with the literal `"UNKNOWN"`/`ambiguous=True` fallback per spec Part 8.2 when no holder can be identified. While finishing this, found a deeper, precisely-specified gap: per spec Part 7, `[ready_time, start_us)` should be *classified* into `DEPENDENCY_WAIT`/`RESOURCE_WAIT`/`SCHEDULER_WAIT`, but the blame-chain walk always labels the whole gap `DEPENDENCY_WAIT` - meaning `P1-01`/`P1-02`'s now-correct classifiers are computed (in the currently-dead-code `compute_task_attribution`/`task_attributions` path) but never reach `result.attribution`. Filed precisely as `P1-20`, including a second, currently-dormant double-counting bug found in the same investigation. |
| 2026-08-13 | Fixed `P1-04` (the last piece of the attribution-identity family): identify *every* genuine terminal element (not just the single max-finish one) and walk all of them; added `covered_intervals` tracking so two genuinely independent terminals that happen to run concurrently in wall-clock time don't produce overlapping, double-counted segments; discovered and fixed that no code anywhere ever generated an `IDLE` segment (`idle_us` was structurally always `0`) - `_build_flattened_timeline` now fills genuine gaps with `IDLE`. Attribution identity now holds exactly (`Σ == H`) on every tested fixture: single connected components, disconnected multi-terminal graphs, concurrent independent terminals, and 3+-component graphs. Full suite: 45 passed, 0 xfailed. |
| 2026-08-13 | Re-verified `P2-01`/`P2-02` before starting and found both already partially/fully fixed (cycle detection + exit code 3 fully working since commit `ad8a2db`; JSON error handling and exit code 2 working for two of three loaders) - corrected the tracker's stale diagnosis and closed the two remaining real gaps (`load_graph`'s JSON wrapping for message consistency; missing-required-file now correctly exits `1` instead of falling through to `2`). Added `tests/unit/test_cli_exit_codes.py` as permanent regression coverage for the full documented exit-code contract. |
| 2026-08-13 | Fixed `P1-19` (flattened timeline residual coverage): added intra-element phase predecessor (`TRACK→FETCH→BUILD` sequencing) and extended `explicit_predecessors` to cover every task kind of a dependent element, not just `BUILD`. This achieved *exact* attribution identity on both the simple reproduction and the full `tests/fixtures/synthetic_multi_subproject/` fixture (diamond dependency, multi-task-kind elements, real resource contention) - better than originally scoped, because the existing "greatest finish time" tie-break turned out to already resolve off-chain parallel-work coverage for any connected component, with no occupancy-sweep needed. Narrowed `P1-04` to its now-precise, still-open remaining scope (genuinely disconnected components only) and added `tests/unit/test_multi_terminal_coverage.py` as a concrete, runnable reproduction of exactly that gap. |
| 2026-08-13 | Fixed `P1-02` (real scheduler-wait detection, plus a tautological call-site `resource_available` check that would have made it unobservable) and `P1-03` (attribution identity I4 — three compounding root causes: blame-chain walk stopping on exactly-zero-wait links, `explicit_predecessors` mismapping tasks on multi-task-kind elements, and a spurious multi-terminal heuristic causing triple-counting). Scoped the honest residual of `P1-03` as new task `P1-19`. Added `tests/unit/` (first module-level unit test directory) with `test_blame_chain.py` and `test_attribution_identity.py`. |
| 2026-08-13 | Added `P3-10`: a large multi-subproject synthetic BuildStream project (9 elements, 4 junctioned subprojects, diamond dependency, real resource contention), fed through the user-supplied `tools/bst_log_to_chrome_trace.py` converter end-to-end. Found two new bugs (`P1-18` structural max_depth shortest-vs-longest-path bug; `P2-05` CLI JSON output silently missing several `AnalysisResult` fields) and amplified `P1-03`'s evidence (negative/~453,000-year overflow values on a realistic graph, not just undercounting). Confirmed `P3-02` (CLI integration tests) was already done by a prior session and marked it `🟢`. Repo housekeeping: removed `bga.egg-info/`, all committed `__pycache__/*.pyc`, and a stray `=2.8` file from git tracking; added `make check-clean` and mandatory pre-commit hygiene rules to `docs/fixing-guide.md`. |
| 2026-08-13 (earlier) | Reworked tracker into index-only format; moved details into `docs/tasks/*.md`; corrected P1-01/P1-02 status after re-verification found stub code still present; added P1-03 as a newly discovered live invariant violation; added `docs/fixing-guide.md` as mandatory session entry point. |
| (prior) | Original tracker created from `docs/bga-spec-compliance-review.md`; P0 and several P1 items marked fixed (P0 confirmed correct on re-verification; some P1 marks corrected above). |
