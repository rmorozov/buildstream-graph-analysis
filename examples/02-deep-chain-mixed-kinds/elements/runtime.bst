# Same role as 01-resource-contention's runtime.bst - provides /bin/sh
# for this project's manual.bst elements. Populated by
# examples/stage_runtimes.sh at build time, not committed.
kind: import
sources:
- kind: local
  path: files/runtime
