# The structural fix from ../project.conf's opportunity #2, applied:
# base-config.bst + base-generate.bst (an unnecessary serial split) are
# merged into a single base.bst, and core.bst's own work is cut from
# `sleep 4` to `sleep 2` (still the single highest-blast-radius element -
# 5 downstream: lib-a..d + app - but no longer artificially slow).
# Opportunity #1 (the scheduling bottleneck) is unchanged here on purpose
# - it needs no project change, just a different --builders value against
# either project - see docs/guides/optimization-walkthrough.md.
#
# Same real, `kind: local`-only structure as ../project.conf; own
# files/runtime/bin populated at build time by examples/stage_runtimes.sh.
name: critical-path-optimization-example-optimized
min-version: 2.0
element-path: elements
