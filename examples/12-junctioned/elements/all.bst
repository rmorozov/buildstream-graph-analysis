# The stack on top of the junction (Decomposition's third input
# class) - `bst build all.bst` is the whole entry point, same shape as
# every other example's own all.bst.
kind: stack
depends:
- filename: junction.bst:stack.bst
