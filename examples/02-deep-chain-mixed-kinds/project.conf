# Real BuildStream project for CI-gathered corner-case data (P4-12): a
# deep dependency chain across mixed element kinds (import, manual,
# compose, stack) plus a junction, built for real rather than only shown
# via `bst show` (tests/fixtures/bst_show_project's role). Exercises
# depth/critical-path/dominator computation and element-kind report
# heuristics against a genuinely built, multi-stage pipeline. See
# examples/README.md.
name: deep-chain-mixed-kinds-example
min-version: 2.0
element-path: elements
