# UX-872: a junctioned example, so a jobserver-on CI step has a real
# junction to fail on - both UX-869 (the FIFO under bind_dst) and
# UX-871 (a junctioned element's kind) were first-day defects on the
# user's own junctioned project, with no CI step exercising either.
# `sub/` is a genuine second BuildStream project, reached only through
# `elements/junction.bst` - `bst show`'s junction-qualified name
# (`junction.bst:core.bst`) is exactly what UX-871 fixed the kinds
# read against.
name: junctioned-example
min-version: 2.0
element-path: elements
