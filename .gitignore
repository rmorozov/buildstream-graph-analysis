# Python cache and compiled files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
Pipfile.lock

# PEP 582
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker static files
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
*.log
*.tmp

# UX-358: the one wrapped BuildStream log that is a **fixture** rather
# than a build's leavings. `bga timeline` renders from it, so without a
# committed one the Perfetto handoff is exercised by nothing a fresh
# checkout has - which is the defect UX-358 was filed about, and which
# happened to UX-358's own first fix: it pointed at `examples/06`'s
# real capture, which `*.log` and `examples/06/.bga/.gitignore` both
# exclude, so the guard passed locally and failed in CI.
!tests/fixtures/**/build.log

# examples/: real BuildStream projects whose sandbox-shell binaries and
# throwaway git remote are generated at build time (examples/stage_*.sh),
# not committed - see examples/README.md.
examples/**/files/runtime/bin/*
examples/*/.generated-remote/
examples/03-project-refs-identity/elements/libbar.bst
!examples/**/files/runtime/bin/.gitkeep

# examples/05-cmake-cpp-toolchain/files/toolchain/: a full real gcc/g++/
# cmake/make/binutils sysroot staged from the HOST's own installed
# packages (examples/stage_cpp_toolchain.sh) - ~270MB, host-specific
# (absolute paths/binary content tied to the exact host distro/package
# versions), regenerated at build time, never committed - same reasoning
# as the busybox runtime staging above, scaled up. See examples/README.md.
examples/05-cmake-cpp-toolchain/files/toolchain/

# examples/06-macro-micro-optimization stages the identical sysroot (a
# hardlink clone of 05's - see examples/stage_cpp_toolchain.sh) into its
# own project and into its optimized/ variant. Same reasoning: host-
# specific, ~270MB, regenerated at build time, never committed.
examples/06-macro-micro-optimization/files/toolchain/
examples/06-macro-micro-optimization/optimized/files/toolchain/

# examples/07-declared-vs-used-dependencies stages the identical sysroot
# (another hardlink clone). Same reasoning as 05 and 06.
examples/07-declared-vs-used-dependencies/files/toolchain/
examples/08-process-storm/files/toolchain/

# tests/fixtures/bst_build_root_override/files/runtime/: a real
# dynamically-linked shell and `sleep` plus their library closure,
# staged from the host by that fixture's own stage_runtime.sh. Same rule
# as examples/: host-specific binaries are generated, never committed.
tests/fixtures/bst_build_root_override/files/runtime/

# examples/09-fine-grained-siblings stages the identical sysroot (another
# hardlink clone) and, additionally, a 60k-file `bulk/` tree generated by
# its own generate_bulk.py. Same reasoning as 05-08 for the toolchain;
# the bulk tree is 60,000 inodes whose only purpose is to make staging
# take a measurable second (UX-120), which is generated, not authored.
examples/09-fine-grained-siblings/files/toolchain/
examples/09-fine-grained-siblings/files/bulk/
examples/09-fine-grained-siblings/merged/files/toolchain/
examples/09-fine-grained-siblings/merged/files/bulk/

# Agent worktrees: whole clones the Agent tool makes under the repo (UX-509).
.claude/worktrees/
