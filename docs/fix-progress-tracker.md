# BGA Spec Compliance Fix Progress Tracker

**Last Updated:** 2026-08-15 (P1-31..P1-38 fixed; P1-39/P2-09/P2-10 filed from a second external review — one claim in that same review, checked directly, was false)
**Overall Status:** P0 verified fixed. Every *previously tracked* P1 item (P1-01 through P1-30), every *previously tracked* P2 item (P2-01 through P2-05), and every P3 item (P3-01 through P3-10) is done. An external review found 7 real, independently-verified P1-tier gaps and 3 real P2-tier gaps that were never tracked (same pattern this tracker's own "Correction" note, see Recommended Order, already warned would keep recurring), and `P4-16`'s real-build CI job independently found one more (`P1-38`, a genuine `ZeroDivisionError` crash, found via real data rather than review). **All 8 of these P1-tier items (P1-31 through P1-38) are done** as well, each with a real wiring or logic bug found and fixed along the way (see their rows below for specifics) — but see `P1-39` immediately below: a second external review, conducted specifically to check whether `P1-31`/`P1-32`'s fixes actually compose correctly, found one more real P1-tier bug in exactly that composition (the resource-wait/scheduler-wait split in `_classify_wait_gap` uses a stale point-in-time check for the remainder), plus two real P2-tier gaps (`P2-09`, `P2-10`). That same review also made one P0-severity claim that was checked directly against `main` and found to be **false** (a claim that `resource_capacity` still reads a nonexistent `run_context.builders` attribute - it doesn't; `P1-31` already fixed exactly that). The remaining known gaps are `P1-39` and 5 P2-tier items (`P2-06` through `P2-10`), all either a real-but-scoped classification bug (`P1-39`) or architectural/transparency hardening rather than observed core-attribution correctness bugs. `bga/floors/`, `bga/report/`, `bga/validation/` exist per spec Part 39's recommended architecture (`P1-15`). The `P4` tier (usability/tooling/maintenance, not spec-mandated) is underway — the full real ingestion pipeline (`P4-05`, `P4-08`, `P4-09`, `P4-10`) is done: a real BuildStream project + log produces a complete `bga`-ready run directory in one step (`tools/bst_extract_run.py`), verified end-to-end against a real, installed BuildStream 2.7.0 build with zero manual editing. `graph.json` also now captures each element's real BuildStream plugin kind (`element_kind`, e.g. `import`/`manual`/`junction`) per user request, currently inert metadata pending `P4-12`'s heuristic design. `dependency_type` (build vs. runtime) now genuinely affects gating (`P4-11`) — every ready-time/critical-path consumer respects it, and a more severe issue than originally scoped (the certified T∞ floor being inflated by non-gating edges) was found and fixed along the way. See `docs/ingestion-pipeline.md` for the full design record. The default `analyze` text report now leads with a synthesized "Key Findings" block plus a Confidence/Violations block (`P4-02`, both presentation-only - `--format json`/`csv` unchanged). Usability/tooling round-out is done: `make lint` runs `ruff` for real (`P4-04`), `make dev-run`/`tools/dev_run.sh` gives a one-command sample report (`P4-03`), `README.md`/`docs/cli.md` describe the CLI and ingestion pipeline accurately with every example verified to actually work (`P4-01`), and the original compliance review is marked as a historical snapshot (`P4-07`). `P4-14` (cache-query/pipeline overhead visibility, user-proposed) is also done: a real 2001-element BuildStream project measurement confirmed the cost is material (87% of wall time on a fully-cached rebuild), surfaced as a new, additive `pipeline_overhead` report signal (run-context/v9 extension, presentation-only) - the remote-cache per-element `Cache-query` case remains unverified/deferred (no remote-cache infrastructure available to confirm its real log format against). `P4-12` (`element_kind`-based heuristics) and `P4-15` (stack-consolidation heuristic) are now also done, implemented together: report-listing annotations + kind-aware structural flagging + a new `bga graph --by-kind` summary view, a purely structural (non-timing) stack-consolidation advisory in `bga graph`, and a new deliberately standalone tool (`tools/bst_checkout_cost.py`, kept outside `bga`'s core `analyze` pipeline since a checkout invocation shares no horizon with a build trace) for real measured checkout-cost comparisons - which surfaced an important, honest finding: consolidating under a `stack` is *not* automatically a net win, confirmed by a real 1500-element measurement where a full-project `stack` checkout cost *more* pipeline overhead than 5 narrow individual checkouts combined. Extending this work also found and fixed two real bugs in `P4-14`'s converter (see `docs/tasks/P4-14-cache-query-overhead-visibility.md`'s follow-up note). **Every `P4` item is now done**: `P4-06` (CI - `.github/workflows/ci.yml`, matrixed across Python 3.9-3.12, `make check-clean`/`make lint`/`make test` on every push/PR) and `P4-13` (`--strict` extraction-consistency mode via BuildStream's `project.refs`, user-proposed - `tools/bst_extract_run.py --strict` fails loudly for projects that can't provide the real guarantee, embeds a `project.refs` provenance hash regardless of `--strict`, verified against a real `kind: git`-sourced project's full lifecycle) closed out the tier - but see `P4-16` below, added after: three real BuildStream example projects (`examples/01-03`) plus new `bst-smoke`/`bst-examples` CI jobs (user-proposed, same trigger scope as the existing jobs, no new surface) now build real projects and gather real corner-case data (chrome traces, run directories, reports) on every push/PR - this found and fixed four real BuildStream/CI constraints along the way (a bwrap/AppArmor sandbox networking issue, a `stack`-element typed-dependency restriction, BuildStream having no free-form-string project option type, and the `git` source plugin needing the separate `buildstream-plugins` package) and surfaced a genuine, previously-unknown crash bug (`P1-38`) via real data, not a hand-built fixture. The full test suite stands at 399 passed (with `bst` + `buildstream-plugins` installed; 388 passed + 11 skipped without them). See tables below for the complete history and the "don't assume completeness" caveat below.

> **Start here, but don't stop here.** This file is an index only. Read `docs/fixing-guide.md` once per session, then open exactly one task file under `docs/tasks/` — do not try to absorb this whole tracker or the full spec into context at once.

## Status Legend

| Status | Meaning |
|---|---|
| 🔴 Not Started | No work begun |
| 🟡 In Progress | Work underway, or a prior claim of "done" that could not be re-verified — treat as not-done |
| 🟢 Fixed & Verified | A named agent ran the task's acceptance test in-session and pasted a passing result into the task file |
| ⚪ Blocked / Out of Scope | Needs a product decision, or deliberately deferred — reason in task file |

**Rule:** a row may only show 🟢 if the linked task file's Verification Log has a real pasted command + passing output. If you find a 🟢 row without that evidence, treat it as 🟡 and re-verify before trusting it — this has already happened once (see Correction Log).

---

## Correction Log (read once, this is why re-verification is mandatory)

On 2026-08-13 the previous tracker's P1 "Attribution Categories" block (old rows 2.1–2.4) was marked 🟢 "All Fixed." Direct code inspection found:
- `classify_resource_wait()` (`bga/attribution/blame_chain.py:291-338`) is now *called* (good), but its body is still the original stub: a `for res in task.resources: pass` no-op loop, `blocking_tasks: {}` always empty, `ambiguous: False` hardcoded regardless of whether a holder was actually identified. Not real per Part 8.
- `classify_scheduler_wait()` (`bga/attribution/blame_chain.py:340-372`) still unconditionally `return False` — completely unimplemented, contradicting its "Fixed" mark.
- Running the CLI end-to-end against a minimal 3-task single-resource-pool fixture (see `docs/fixing-guide.md` §7) shows the Attribution Breakdown summing to only ~33% of the task horizon (H) — a live invariant I4 violation, not previously listed as an issue at all. Filed as `P1-03` (highest real-world priority of the open P1 items).

This is not a criticism of any particular session — it's the reason the verification rule in `docs/fixing-guide.md` §3 is mandatory going forward.

On 2026-08-15, a second external review (commissioned specifically to re-check the P1-31..P1-38 round after it merged) made a P0-severity claim: that `bga/analyzer.py`'s `resource_capacity` construction still reads a nonexistent `run_context.builders` attribute, so `resource_capacity` is always `{}` on real runs. Checked directly against `main` before filing anything: **false** - that exact bug was already fixed by `P1-31` (confirmed by reading the merged code, which reads `run_context.resource_capacities`). The rule cuts both ways: a claim of "still broken" needs the same direct verification as a claim of "done" before it goes in the tracker. The same review's other three findings (`P1-39`, `P2-09`, `P2-10`) were independently verified as real by reading the actual code paths and are filed below - the review was net-useful, just not uniformly accurate, which is exactly why every claim (positive or negative) gets checked against source before being trusted either way.

---

## P0 — The Tool Does Not Run

**Status:** 🟢 Verified fixed — confirmed by running `PYTHONPATH=. python3 tests/test_e2e.py` (7/7 passed) and `python3 -m bga.cli analyze <fixture>` end-to-end (exit 0, produced a full text report) on 2026-08-13. No task files — nothing further to do here unless a regression is found.

| # | Issue | Status |
|---|-------|--------|
| P0-1 | CLI constructor mismatch | 🟢 |
| P0-2 | `analyze()` signature mismatch | 🟢 |
| P0-3 | Output formatters referenced non-existent fields | 🟢 |
| P0-4 | Undeclared `networkx` dependency | 🟢 (now in `pyproject.toml` deps) |
| P0-5 | `DiagnosticsAnalyzer` missing graph attribute | 🟢 |
| P0-6 | Missing `deque` import | 🟢 |

---

## P1 — Silently Wrong Numbers

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P1-01 | Resource-wait holder tracking is a stub (no real occupancy-based holder set, `ambiguous` hardcoded False) | — | 🟢 Done — surfaced a related wiring gap, see `P1-20` | [P1-01](tasks/P1-01-resource-wait-holder-tracking.md) |
| P1-02 | Scheduler-wait detection unconditionally returns False | — | 🟢 Done (also fixed a tautological call-site `resource_available` check that would have made this fix unobservable) | [P1-02](tasks/P1-02-scheduler-wait-detection.md) |
| P1-03 | Attribution identity (I4) violated on resource-constrained chains — 3 root causes fixed (zero-wait walk termination, multi-task-per-element predecessor mismapping, spurious multi-terminal heuristic) | — | 🟢 Done (residual gap scoped as `P1-19`) | [P1-03](tasks/P1-03-attribution-identity-resource-chains.md) |
| P1-04 | Flattened timeline undercounts on genuinely disconnected multi-terminal graphs | — | 🟢 Done — also added interval-overlap prevention across walks and IDLE segment generation (previously nonexistent for any scenario) | [P1-04](tasks/P1-04-flattened-timeline-multi-terminal-coverage.md) |
| P1-05 | No violation raised when timeline undercounts | P1-04 | 🟢 Done — defense-in-depth (no known fixture currently triggers it, verified via monkeypatch) | [P1-05](tasks/P1-05-reconciliation-violation-reporting.md) |
| P1-06 | `T∞,cold` hardcoded None; `historical_runs` never supplied | — | 🟢 Done | [P1-06](tasks/P1-06-cold-floor-historical-wiring.md) |
| P1-07 | No `--cold`/`--allow-partial-cold` CLI flags; cold publication gate unimplemented | P1-06 | 🟢 Done — also added `--history-dir` (necessary CLI addition, spec never says where historical data comes from) | [P1-07](tasks/P1-07-cold-cli-flags-and-publication-gate.md) |
| P1-08 | Capacity lower bound only accounts for PROCESS pool; no exclusive-serialization bound | — | 🟢 Done — also fixed a newly-exposed I2 violation in `ReplayScheduler._get_task_resources` (hardcoded to PROCESS-only) | [P1-08](tasks/P1-08-capacity-lower-bound-completeness.md) |
| P1-09 | Criticality "Monte Carlo" ignores perturbed durations, returns fixed 0/1 | — | 🟢 Done — resampling itself was already fixed (stale diagnosis); real bug was a key-format mismatch collapsing every probability to 0.0 | [P1-09](tasks/P1-09-genuine-montecarlo-criticality.md) |
| P1-10 | Blast-radius weighted duration uses fake average instead of real downstream traversal | — | 🟢 Done | [P1-10](tasks/P1-10-blast-radius-real-traversal.md) |
| P1-11 | Leaf/deferrability fix (`is_required_by_target`, `reachable_from_targets`) claimed fixed — re-verify | — | 🟢 Done — `edg.py`'s reachability was fine; found `requested_targets` hardcoded `None` one layer up in `analyzer.py` | [P1-11](tasks/P1-11-verify-leaf-deferrability-fix.md) |
| P1-12 | No determinism harness (N≥100 run comparison), no `bga/validation/` package | — | 🟢 Done | [P1-12](tasks/P1-12-determinism-harness.md) |
| P1-13 | Confidence computation only checks ordering violations; other hard/soft gates never computed | P1-05 (done) | 🟢 Done | [P1-13](tasks/P1-13-confidence-reconciliation-gates.md) |
| P1-14 | Only `bga analyze` exists; spec recommends `graph/floors/replay/sweep/utilisation/diagnostics` subcommands | — | 🟢 Done — user chose the hybrid option; also fixed 3 pre-existing dead-code/NaN bugs the new subcommands exposed | [P1-14](tasks/P1-14-cli-subcommand-split.md) |
| P1-15 | Missing `bga/floors/`, `bga/report/`, `bga/validation/` packages (architecture) | P1-06..P1-13 (all now done) | 🟢 Done — extracted incrementally, one commit per package, byte-identical CLI output verified after each step | [P1-15](tasks/P1-15-package-architecture-refactor.md) |
| P1-16 | Several graph/attribution algorithms are O(N·E)/O(N²), spec wants O(N+E) | — | 🟢 Done — 2 new out-of-scope hotspots found while verifying, filed as `P1-21` | [P1-16](tasks/P1-16-performance-on-plus-e-algorithms.md) |
| P1-17 | Terminology audit against spec Part 43 avoid-list | — | 🟢 Done — zero matches, no code change needed | [P1-17](tasks/P1-17-terminology-audit.md) |
| P1-18 | `structural.metrics.max_depth` uses shortest-path not longest-path (disagrees with `signals.unweighted_depth`) | — | 🟢 Done | [P1-18](tasks/P1-18-structural-max-depth-shortest-path-bug.md) |
| P1-19 | Flattened timeline doesn't cover intra-element `TRACK→FETCH→BUILD` sequencing or off-chain parallel task time | P1-03 (done) | 🟢 Done — also resolved off-chain coverage for connected components; narrowed `P1-04`'s remaining scope | [P1-19](tasks/P1-19-flattened-timeline-residual-coverage.md) |
| P1-20 | Blame-chain walk never classifies a gap as RESOURCE_WAIT/SCHEDULER_WAIT — always defaults to DEPENDENCY_WAIT, so `P1-01`/`P1-02`'s correct classifiers never reach final output | P1-01 (done), P1-02 (done) | 🟢 Done — also fixed a dormant double-counting bug in `compute_task_attribution` found in the same investigation | [P1-20](tasks/P1-20-gap-classification-into-resource-scheduler-wait.md) |
| P1-21 | Additional O(N²)-ish hotspots found while verifying `P1-16` (not in its original scope): `compute_reachability`'s full-set materialization, `compute_dominators`'s naive dataflow, `_estimate_ready_count`'s O(N²) `any()` loop | — | 🟢 Done — 5.1x faster on the 1500-element profiling fixture (14.16s→2.79s); `compute_dominators` deliberately deferred (only consumer reads dict size, not real dominance data); found and filed `P1-22` along the way | [P1-21](tasks/P1-21-additional-performance-hotspots.md) |
| P1-22 | `is_on_critical_path`/`is_on_blame_chain` always False in leaf/blast-radius diagnostics (element-UID-vs-task-key mismatch) | — | 🟢 Done | [P1-22](tasks/P1-22-blame-chain-critical-path-membership-mismatch.md) |
| P1-23 | `UNTRACKED_HEAD`/`UNTRACKED_TAIL` hardcoded to 0, breaking the full-wall-clock I4 identity; exposed a second dead bug in `attribution_score`'s denominator | — | 🟢 Done — found while building `P3-03` | [P1-23](tasks/P1-23-untracked-head-tail-computation.md) |
| P1-24 | Phase annotations silently dropped on every segment category except `EXECUTION_ON_CHAIN` (Part 10.2's own `IDLE`/`SCHEDULER_WAIT` phase-tag examples never held) | — | 🟢 Done — found while building `P3-05` | [P1-24](tasks/P1-24-phase-annotation-only-on-execution-segments.md) |
| P1-25 | CPU reconciliation `unaccounted_cpu_s` silently stayed 0 for residuals under the 2% tolerance, contradicting Part 33.3's "explicitly reported" requirement | — | 🟢 Done — found while building `P3-06` | [P1-25](tasks/P1-25-cpu-reconciliation-unaccounted-under-tolerance.md) |
| P1-26 | Replay's dependency graph gated readiness on the wrong task kind (same bug class as `P1-03`, different code path), under-scheduling `T_C` below `LB` and violating I2 | — | 🟢 Done — found while building `P3-08` | [P1-26](tasks/P1-26-replay-predecessor-task-kind-mismatch.md) |
| P1-27 | `compute_ready_times`/`validate_ordering` gated every successor task kind on the predecessor's any-kind-max finish (same bug class as `P1-03`/`P1-26`, a third independent code path) — produced 8 negative-duration tasks (I5 violation), 7 false ordering violations, confidence crashed to 0.147, on the flagship multi-subproject fixture | — | 🟢 Done — found by an independent post-hoc re-audit | [P1-27](tasks/P1-27-ready-time-task-kind-mismatch-negative-durations.md) |
| P1-28 | Monte-Carlo criticality rebuilt graph topology on every one of 200 samples instead of once, contradicting Part 41.2's explicit instruction | — | 🟢 Done — found by the same re-audit | [P1-28](tasks/P1-28-montecarlo-rebuilds-graph-topology-per-sample.md) |
| P1-29 | CLI help text + `docs/cli.md` claimed replay computes an "optimal" makespan, contradicting Part 1.2's own non-goal — missed by `P1-17`'s exact-phrase-only terminology audit | — | 🟢 Done — found by the same re-audit | [P1-29](tasks/P1-29-replay-optimality-overclaim.md) |
| P1-30 | `RETRY_WAIT` (one of the 8 canonical Part 11 attribution categories) has zero implementation anywhere — always hardcoded 0; `P2-04` implements a different, similarly-named utilisation concept, not this one | — | 🟢 Done — also found and fixed a real no-op bug in the initial `max()`-based ready-time extension | [P1-30](tasks/P1-30-retry-wait-category-unimplemented.md) |
| P1-31 | `classify_resource_wait` never checks whether the resource was actually saturated — any temporal overlap with a same-resource task classifies as RESOURCE_WAIT regardless of spare capacity, and even zero overlap still returns `is_resource_wait=True` (UNKNOWN) rather than falling through | — | 🟢 Done — also found and fixed a real wiring bug: `resource_capacity` was always `{}` on real runs (checked a non-existent `run_context.builders` field) | [P1-31](tasks/P1-31-resource-wait-ignores-capacity.md) |
| P1-32 | `classify_scheduler_wait`'s concurrency evidence (`concurrent_jobs_at_time`) counts tasks that *started* at each exact timestamp, not tasks concurrently active then — measures the wrong quantity, not just sparse sampling | P1-31 | 🟢 Done — replaced with a real critical-points concurrency sweep | [P1-32](tasks/P1-32-scheduler-wait-sparse-snapshot-evidence.md) |
| P1-33 | `cpu_accounting.effective_cpus` is populated from BuildStream's `builders` scheduling parameter, not real measured CPU capacity — Part 30.3's oversubscription check (`builders×max_jobs > effective_cpus`) becomes near-tautological as a result, and I9's reconciliation runs against synthetic (not measured) data | — | 🟢 Done — `cpu_accounting` now honestly omitted (unavailable) rather than fabricated from `builders` | [P1-33](tasks/P1-33-cpu-accounting-conflates-capacity-with-measurement.md) |
| P1-34 | Replay's `fifo` priority rule uses `hash(str(task_key))` — Python's string hash is randomized per-process, violating the determinism contract (I11); `depth` priority rule is byte-identical to `lpt`, doesn't implement depth | — | 🟢 Done — `fifo` now real lexicographic tuple tie-break, `depth` now real longest-remaining-path via Kahn's algorithm | [P1-34](tasks/P1-34-replay-fifo-nondeterministic-hash.md) |
| P1-35 | The determinism harness (`P1-12`) runs N times within one process — Python's hash seed is fixed per-process, so it structurally cannot catch `P1-34`-shaped nondeterminism no matter how large N is | P1-34 | 🟢 Done — added a genuine cross-process (`subprocess`) determinism check | [P1-35](tasks/P1-35-determinism-harness-single-process-blind-spot.md) |
| P1-36 | `clamp_task_starts` can produce a negative-duration `NormalizedTask` on a genuine ordering violation (distinct root cause from `P1-27`) — no check that `clamped_start <= clamped_finish` before construction | — | 🟢 Done — offending tasks now excluded with a `clamp_negative_duration` violation; `NormalizedTask` also guards the invariant structurally | [P1-36](tasks/P1-36-clamp-can-produce-negative-duration-tasks.md) |
| P1-37 | Run identity (I8) has no schema field anywhere and isn't enforced by `bga`'s own `load()`/`load_all()` — `P4-13`'s `--strict` only proves extraction-time cleanliness, not that the checked project state matches what the analyzed build actually ran against (a real TOCTOU gap) | P4-13 (done) | 🟢 Done — new `run_identity` manifest hash captured at extraction, cross-checked by `compute_confidence` | [P1-37](tasks/P1-37-run-identity-not-captured-or-enforced.md) |
| P1-38 | `compute_sensitivity` (structural/M6) crashes with `ZeroDivisionError` (exit 2) whenever slack is exactly -1,000,000us, and silently produces a negative sensitivity score for slack below that — found via real data, not a hand-built fixture: `examples/02-deep-chain-mixed-kinds`'s real build reproduces it deterministically | — | 🟢 Done — negative slack clamped to 0 before the decay formula | [P1-38](tasks/P1-38-structural-sensitivity-zerodivision-crash.md) |
| P1-39 | `_classify_wait_gap` composes `classify_resource_wait`/`classify_scheduler_wait` incorrectly: the scheduler-wait check for the remainder after a resource-wait prefix uses a stale point check (`_resource_available_at(task, task.ready_us)`, always evaluated at the *original* ready time, not after the prefix) — so once any resource-wait prefix exists, the remainder can never be classified SCHEDULER_WAIT even when the resource genuinely freed up and the scheduler was genuinely full | P1-31 (done), P1-32 (done) | 🔴 (second external review, conducted after P1-31/P1-32 merged, specifically checking their composition — verified real; that same review's own headline P0 claim, that `resource_capacity` still reads a nonexistent `run_context.builders`, was checked directly against `main` and found to be false/stale — already fixed by P1-31) | [P1-39](tasks/P1-39-resource-scheduler-wait-composition-stale-point-check.md) |

---

## P2 — Robustness / "Just Works"

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P2-01 | No cycle detection; exit code 3 never produced | — | 🟢 Done (was already implemented; original diagnosis was stale) | [P2-01](tasks/P2-01-cycle-detection-exit-codes.md) |
| P2-02 | Malformed JSON / bad input unhandled | — | 🟢 Done (was partially done; fixed `load_graph` wrapping + missing-file exit code) | [P2-02](tasks/P2-02-malformed-input-error-handling.md) |
| P2-03 | No logging module wired anywhere; `--verbose` does nothing but toggle traceback printing | — | 🟢 Done — also added a `BgaError` exception hierarchy, replacing a `'cycle' in str(e)` substring hack with type-based exit-code routing | [P2-03](tasks/P2-03-logging-infrastructure.md) |
| P2-04 | Retry/rebuild detection unimplemented — utilization buckets always empty | — | 🟢 Done | [P2-04](tasks/P2-04-retry-rebuild-detection.md) |
| P2-05 | `--format json` silently omits `structural`/`utilisation`/`confidence`/`violations` (typo'd `hasattr` check + missing fields) | — | 🟢 Done | [P2-05](tasks/P2-05-cli-json-missing-fields.md) |
| P2-06 | Cold-floor duration sources (`compute_cold_floor`) don't retain which priority tier (exact cache key / element-kind-phase / cohort) resolved each task's duration — only an aggregate high/low confidence label is exposed | — | 🔴 (external review, verified real — not a correctness bug, a transparency gap on top of already-correct behavior) | [P2-06](tasks/P2-06-cold-floor-match-provenance-not-exposed.md) |
| P2-07 | `quantize_timestamp` uses floating-point division (`ts_us / epsilon_us`) before `round()`, contradicting Part 3.1's "no floating-point arithmetic... for timeline accounting"; tie-breaking behavior isn't explicitly documented as Part 3.2 requires | — | 🔴 (external review, verified real — low practical risk for realistic timestamps, still worth fixing) | [P2-07](tasks/P2-07-timestamp-quantization-uses-floating-point.md) |
| P2-08 | "Measured / Certified / Advisory" separation is enforced only by naming convention (`cold_`-prefixed dict keys) and caller discipline, not by any structural/type mechanism | — | 🔴 (external review, verified real — architectural hardening, not an observed bug) | [P2-08](tasks/P2-08-certified-advisory-measured-not-structurally-separated.md) |
| P2-09 | `ReplayScheduler.__init__`'s own default-capacity fallback reads nonexistent `RunContext.builders`/`fetchers`/`pushers` attributes (the same bug shape `P1-31` fixed in `bga/analyzer.py`, but a separate, independent call site that fix didn't touch) — silently falls back to hardcoded 4/2/2 instead of the run's real `resource_capacities` for any caller that doesn't pass explicit capacities to `.replay()` | — | 🔴 (second external review, verified real — the main `bga analyze` path is unaffected since it always passes explicit capacities, so this is a real but currently-dormant API-correctness gap, not an observed wrong number in any current report) | [P2-09](tasks/P2-09-replay-scheduler-default-capacities-stale-attributes.md) |
| P2-10 | Ready queue depth (Part 21) is defined as "dependency-ready AND resource-ready AND not executing", but `_estimate_ready_count` only checks dependency-readiness — the `resource_capacities` parameter `compute_ready_queue_metrics` accepts is never actually consulted, so the metric can't distinguish "nothing was ready" from "work was ready but resource-starved", defeating the stated purpose of Part 21 | — | 🔴 (second external review, verified real) | [P2-10](tasks/P2-10-ready-queue-depth-ignores-resource-readiness.md) |

---

## P3 — Test Coverage Build-Out

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P3-01 | Shared synthetic topology fixture library | — | 🟢 Done | [P3-01](tasks/P3-01-topology-fixture-library.md) |
| P3-02 | CLI integration tests (`tests/test_cli.py`) | — | 🟢 Done (7/7 pass) | [P3-02](tasks/P3-02-cli-integration-tests.md) |
| P3-03 | Attribution identity tests (I4) across topologies | P3-01, P1-03 | 🟢 Done — found and fixed `P1-23` along the way | [P3-03](tasks/P3-03-attribution-identity-tests.md) |
| P3-04 | Tie-break + resource-holder tests | P3-01, P1-01 | 🟢 Done | [P3-04](tasks/P3-04-tie-break-and-resource-holder-tests.md) |
| P3-05 | Phase overlap + occupancy edge-case tests | P3-01 | 🟢 Done — found and fixed `P1-24` along the way | [P3-05](tasks/P3-05-phase-and-occupancy-edge-case-tests.md) |
| P3-06 | CPU reconciliation (I9) + cold-floor tests | P3-01, P1-06 | 🟢 Done — found and fixed `P1-25` along the way | [P3-06](tasks/P3-06-cpu-reconciliation-and-cold-floor-tests.md) |
| P3-07 | Monte-Carlo criticality + determinism-harness tests | P1-09, P1-12 | 🟢 Done — pre-existing coverage from P1-09/P1-12 already satisfied every requirement | [P3-07](tasks/P3-07-montecarlo-and-determinism-tests.md) |
| P3-08 | Golden/regression tests (full-pipeline snapshot) | P3-01 | 🟢 Done — found and fixed `P1-26` along the way | [P3-08](tasks/P3-08-golden-regression-tests.md) |
| P3-09 | Per-module unit test split (normalize/occupancy/edg/blame_chain/replay/utilisation/diagnostics) | — | 🟢 Done | [P3-09](tasks/P3-09-per-module-unit-tests.md) |
| P3-10 | Large multi-subproject synthetic-project integration test, using the real `tools/bst_log_to_chrome_trace.py` converter | — | 🟢 Done — found `P1-18` and `P2-05`, amplified `P1-03` evidence | [P3-10](tasks/P3-10-synthetic-multi-subproject-large-test.md) |

---

## P4 — Usability, Tooling & Maintenance

Not spec-mandated (no `docs/specification.md` Part references) — quality-of-life for maintainers and users on top of an already spec-compliant tool. Requested directly by the user after every P1/P2/P3 item closed, plus a few brainstormed while scoping those requests.

| ID | Issue | Depends on | Status | Task File |
|---|---|---|---|---|
| P4-01 | README quick-start (Pareto principle) + fix stale/broken doc examples (`docs/cli.md`'s two confirmed-broken `jq` examples, placeholder `pyproject.toml` URLs) | — | 🟢 Done — Quick Start rewritten around the checked-in fixture + the now-real `bst_extract_run.py` path; both `jq` examples fixed and verified | [P4-01](tasks/P4-01-readme-quick-start-and-docs-accuracy.md) |
| P4-02 | Refactor report output to lead with what's actionable (confidence/violations missing from text output entirely; no synthesized "what to look at first" summary) | — | 🟢 Done — added a Key Findings + Confidence/Violations block to the full text report, presentation-only | [P4-02](tasks/P4-02-actionable-report-output.md) |
| P4-03 | Convenience script for local development scenarios (one command: sample run dir → `bga analyze` → printed report) | — | 🟢 Done — `tools/dev_run.sh` + `make dev-run` | [P4-03](tasks/P4-03-local-dev-convenience-script.md) |
| P4-04 | Add basic Python linting (`make lint` is a literal no-op placeholder today) | — | 🟢 Done — `ruff` (pyflakes rule set); found and fixed 29 real findings across the codebase | [P4-04](tasks/P4-04-basic-python-linting.md) |
| P4-05 | `tools/bst_log_to_chrome_trace.py` only supports wrapper-prefixed logs — a raw BuildStream log silently produces an empty trace instead of erroring or parsing | — | 🟢 Done — also found and fixed 2 real bugs (missing `FAILURE` status word, nested-sub-phase mishandling) invisible against the synthetic fixture | [P4-05](tasks/P4-05-log-converter-support-raw-buildstream-logs.md) |
| P4-06 | No CI configured at all (`.github/` doesn't exist) — every verification this whole tracker documents has been run by hand | P4-04 | 🟢 Done — `.github/workflows/ci.yml`, matrixed across Python 3.9-3.12: `make check-clean` + `make lint` + `make test` on every push/PR to `main` | [P4-06](tasks/P4-06-add-ci-workflow.md) |
| P4-07 | `docs/bga-spec-compliance-review.md` (the original "tool doesn't run" review) has no note marking it as a historical snapshot, not current state | — | 🟢 Done — a blockquote note added immediately after the title | [P4-07](tasks/P4-07-archive-stale-compliance-review.md) |
| P4-08 | `graph.json` producer from a real BuildStream project, via `bst show` (design decisions settled by the user; see `docs/ingestion-pipeline.md`) | — | 🟢 Done — `tools/bst_show_to_graph.py` | [P4-08](tasks/P4-08-end-to-end-ingestion-pipeline-gap.md) |
| P4-09 | `run-context.json` producer (resource capacities, wall clock, `cpu_accounting`) from a real BuildStream invocation — `bst show` has no notion of this, needs a different source | — | 🟢 Done — `tools/bst_run_context.py` | [P4-09](tasks/P4-09-run-context-producer.md) |
| P4-10 | Wire trace (`P4-05`) + graph (`P4-08`) + run-context (`P4-09`) extraction into one convenience flow; derive requested targets from the real invocation, not a hardcoded umbrella-target convention | P4-05, P4-09 | 🟢 Done — `tools/bst_extract_run.py` + `tools/chrome_trace_to_bga_trace.py`; verified end-to-end against a real BuildStream build with zero manual editing | [P4-10](tasks/P4-10-ingestion-pipeline-wiring.md) |
| P4-11 | `dependency_type` (build vs. runtime) is populated correctly in `graph.json` but no consumer in `bga/` treats it as a genuine tri-state — ready-time gating over-constrains on `runtime`-only edges | P4-10 | 🟢 Done — also found and fixed a more severe issue: `compute_critical_path`'s certified T∞ floor (Part 14.1) was inflated by non-gating edges | [P4-11](tasks/P4-11-dependency-type-affects-analysis.md) |
| P4-12 | Explore element-kind-based heuristics for analysis — `element_kind` (BuildStream's own plugin type, e.g. `import`/`manual`/`junction`/`autotools`) is now captured in `graph.json` (user-requested addition) but is inert metadata, no consumer reads it | — | 🟢 Done — all 3 candidate directions implemented: report-listing annotations, kind-aware structural flagging, `bga graph --by-kind` summary | [P4-12](tasks/P4-12-element-kind-based-heuristics.md) |
| P4-13 | `--strict` extraction mode enforcing project-state consistency via BuildStream's `project.refs` (user-proposed) — hardens `P4-10`'s best-effort whole-tree git-dirty warning into a real, opt-in guarantee for projects using `ref-storage: project.refs` | P4-10 | 🟢 Done — `tools/bst_extract_run.py --strict`; fails loudly for `ref-storage: inline` projects, embeds a `project.refs` provenance hash regardless of `--strict`; verified against a real `kind: git`-sourced project | [P4-13](tasks/P4-13-strict-mode-project-refs-consistency.md) |
| P4-14 | Cache-query/CAS-check overhead is real (confirmed: BuildStream's own "Query cache" activity, plus a per-element `Cache-query` scheduler queue when a remote cache is configured) but currently invisible to `bga` — the ingestion pipeline deliberately drops it as an unrecognized `action="main"` pseudo-task | P4-10 | 🟢 Done — real 2001-element measurement confirmed it's material (87% of wall time on a fully-cached rebuild); surfaced as a new `pipeline_overhead` report signal. Remote-cache per-element case still unverified/deferred | [P4-14](tasks/P4-14-cache-query-overhead-visibility.md) |
| P4-15 | Structural consolidation heuristic: `kind: stack` genuinely batches `bst artifact checkout` sandbox/CAS access into one operation (confirmed) — explore surfacing consolidation opportunities via `element_kind` + topology; also corrects the related but unconfirmed "splitting into subprojects reduces overhead" idea | P4-12, P4-14 | 🟢 Done — structural advisory in `bga graph` + standalone `tools/bst_checkout_cost.py` for real measurement; found consolidation is *not* automatically a net win (real 1500-element measurement) | [P4-15](tasks/P4-15-stack-consolidation-heuristic.md) |
| P4-16 | Real BuildStream example projects + `bst-examples` CI job (user-proposed) — build real projects on every push/PR to gather real corner-case data (chrome traces, run directories, reports) for future tool development | P4-06, P4-13 | 🟢 Done — three real projects (`examples/01-03`), new `bst-smoke`+`bst-examples` CI jobs (same trigger scope as existing jobs, no new surface); found and fixed 4 real BuildStream/CI constraints along the way, and found a genuine, previously-unknown crash bug (`P1-38`) via real data | [P4-16](tasks/P4-16-real-build-ci-examples.md) |

---

## Recommended Order for a Sequence of Small-Context Sessions

**Correction (2026-08-14):** the previous version of this section claimed "every row in every table above is now 🟢 Done" and "there is no more next up." That claim was wrong - an independent re-audit, run specifically *because* the user asked to double-check it, found four real gaps (`P1-27` through `P1-30`) within minutes of actually re-reading the spec against the code, three now fixed. The lesson isn't "audits are good" (obviously) - it's that **"every tracked row is 🟢" is not the same claim as "the tool is fully spec-compliant."** A 🟢 row only means the row's own narrow acceptance test passed; it says nothing about spec requirements that never became a row in the first place (exactly how `P1-27`'s bug and `RETRY_WAIT`'s absence both went untracked for the tool's entire fixing history). Do not repeat the previous version's overclaim.

**Second correction (2026-08-14, later the same day):** confirmed again - an external review (not this session's own re-audit) found 10 more real gaps (`P1-31` through `P1-37`, `P2-06` through `P2-08`), every one independently verified against the actual current code before being added to this tracker, none taken on the external review's word alone. Two are worse in practice than the external review itself described (`P1-32`'s concurrency evidence measures the wrong quantity entirely, not just sparse sampling; `P1-33`'s CPU-accounting conflation makes Part 30.3's own oversubscription check near-tautological). This is the same pattern as the first correction, from a completely independent source - the lesson generalizes: **this tracker reaching "every row 🟢" has now been wrong twice in one day.** Treat it as a floor on what's known to be fixed, never a ceiling on what might still be wrong.

Next up:
1. `P1-31` through `P1-37` are the highest-priority open items - real, verified correctness/determinism/provenance gaps in the attribution and replay engine, not usability polish. Read each task file's Background section (each cites exact file:line and, where relevant, empirical confirmation) before starting; `P1-31`/`P1-32` are sequenced (fix resource-wait's capacity check before scheduler-wait can see its corrected share of the gap), `P1-34`/`P1-35` are sequenced the same way (fix the hash-based nondeterminism, then add the cross-process check that proves it stays fixed).
2. `P2-06` through `P2-08` are lower-urgency hardening/transparency items from the same review - pick up after the P1 items above, or independently if a session has narrower scope.
3. Every *previously* tracked P1/P2/P3 item is done, and every `P4` row is done (`P4-01` through `P4-15`, including `P4-06`/`P4-13`) - none of that is in question. What's in question, per both corrections above, is completeness *beyond* what's currently tracked.
4. Beyond that: don't assume completeness. Before declaring "done" again, re-derive coverage independently - re-read the relevant spec Part(s) fresh and check the code directly, rather than trusting this tracker's own table state, exactly as both corrections above describe.

**If you find a row that no longer holds:** this tracker has now caught this pattern - a task's *originally diagnosed* root cause already fixed by an earlier, undocumented change, but a *different*, less obvious bug in the same area still live - multiple times across its history (`P2-01`/`P2-02`, `P1-09`'s critical-path resampling, `P1-11`'s reachability computation, and now the `P1-27`/`P1-28`/`P1-29` re-audit findings). Re-verify by reading the current code and running a real reproduction before trusting any stale diagnosis, including this tracker's own.

## Change Log

| Date | Change |
|---|---|
| 2026-08-14 (latest) | User shared an external LLM's review of the repository (a prioritized list: 3 P0, 4 P1, 3 P2 correctness/architecture findings, plus a "don't work on now" section) and asked to review it and file agreed items to the backlog. Every claim was independently verified against the actual current code before being trusted or filed - none taken on the external review's word alone, per this tracker's own established discipline. All 10 numbered findings confirmed real; two turned out worse in practice than described. Filed as **`P1-31`** through **`P1-37`** and **`P2-06`** through **`P2-08`** (see their own task files for full detail): `P1-31` (`classify_resource_wait` never checks capacity saturation - confirmed via its own docstring: "`resource_capacity`... not used by this method" - and via `tests/unit/test_resource_wait.py` always passing `resource_capacity={}`, no negative case exists), `P1-32` (`classify_scheduler_wait`'s `concurrent_jobs_at_time` counts tasks that *started* at each exact timestamp, not tasks concurrently active then - worse than "sparse sampling," it measures the wrong quantity and would almost always read as "capacity free" in a realistic trace), `P1-33` (`cpu_accounting.effective_cpus` is populated from BuildStream's `builders` config, not real measured CPU - confirmed this makes Part 30.3's own `builders×max_jobs > effective_cpus` oversubscription check `builders×max_jobs > builders`, near-tautologically true for any `make -jN` build), `P1-34` (replay's `fifo` priority uses `hash(str(task_key))` - confirmed empirically in this environment that `hash()` differs across separate `python3` invocations, a real I11 determinism violation; `depth` priority rule is byte-identical to `lpt`, doesn't implement depth), `P1-35` (the determinism harness runs N times *within one process*, where Python's hash seed is fixed for the process lifetime - confirmed it structurally cannot catch `P1-34`'s bug no matter how large N is), `P1-36` (`clamp_task_starts` has no check that `clamped_start <= clamped_finish`, so a genuine ordering violation - distinct root cause from the already-fixed `P1-27` - can silently produce a negative-duration task), `P1-37` (I8 run identity has no schema field anywhere - confirmed zero `run_id` matches in `docs/specification.md` - and isn't enforced by `bga`'s own `load()`; also documents a real TOCTOU gap in `P4-13`'s `--strict`, which only proves extraction-time cleanliness, not build-time correspondence - a legitimate critique of this session's own recent work, not just pre-existing code), `P2-06` (cold-floor match-tier provenance not exposed per task, only an aggregate confidence label), `P2-07` (`quantize_timestamp` uses float division before `round()`, contradicting Part 3.1's "no floating-point arithmetic" - low practical risk but real), `P2-08` (measured/certified/advisory separation enforced only by naming convention, not structurally). None implemented yet - filing only, per the user's explicit request. Also independently agreed with the external review's "don't work on now" section (element_kind/stack-consolidation heuristics already shipped as advisory-only in `P4-12`/`P4-15`; remote-cache `Cache-query` already explicitly deferred in `P4-14` for the same reason the external review gives; no dashboard work in progress) - no action needed there, already the right call. Updated the Overall Status line and Recommended Order section with a second "don't assume completeness" correction, since this is the second time in one day this tracker reached "every row 🟢" and was found to be a floor, not a ceiling. No code changed; no tests added or run beyond confirming the existing suite is untouched. |
| 2026-08-14 | User asked to close out all remaining `P4` items - `P4-06` (CI) and `P4-13` (`--strict` extraction-consistency mode), the last two open rows. **`P4-06`**: `.github/workflows/ci.yml`, one job matrixed across Python 3.9-3.12 (`fail-fast: false`), each leg running `pip install -e ".[dev]"` + `make check-clean` + `make lint` + `make test`. Deliberately doesn't install the `bst` extra (needs the `bubblewrap` system package, documented as optional) - runs in the same "no `bst` on `PATH`" mode as local dev by default. Verified the exact commands end-to-end from a genuinely fresh venv (clean install, all three checks pass), and both real failure modes that should turn CI red: a deliberately broken test (`make test` exits 2) and a deliberately re-added tracked build artifact in a fresh clone (`make check-clean` exits 2) - both scratch changes discarded afterward, confirmed via `git status`/`git diff --stat` that the real repo was untouched. **`P4-13`**: `tools/bst_extract_run.py --strict` - fails loudly (never silently degrades) unless the project sets `ref-storage: project.refs`, the file actually exists, the project is a git repository, and `project.refs` itself has no uncommitted changes relative to git HEAD (a precise, single-file check, replacing the whole-tree dirty warning's imprecision for `--strict` specifically - the default, non-strict flow is completely unchanged). Regardless of `--strict`, a real `project.refs`'s SHA-256 is now embedded into `run-context.json`'s new `project_refs_provenance` field as a permanent record for later drift detection (additive, no schema collision - Part 32.1). Found and fixed a real bug while verifying against an actual non-git directory: `git diff --exit-code`'s exit code for "not a repository" is 129 (a usage error), not the 128 `git status --porcelain` returns for the same case (which the existing whole-tree check already relies on) - conflating them would have misreported "not a git repo" as "has uncommitted changes." Verified all 4 of the task's own acceptance-test scenarios against a real, freshly-installed BuildStream 2.7.0 + `buildstream-plugins` project with a real `kind: git` trackable source: clean succeeds (real provenance hash embedded), dirtying `project.refs` fails loudly naming it specifically, `tests/fixtures/bst_show_project/` (confirmed `ref-storage: inline`) fails loudly explaining why, and the default flow is unchanged. Added `pyyaml>=6.0` to `pyproject.toml`'s `bst` extra. Added `tests/unit/test_bst_extract_run_strict.py` (12 tests: 9 hermetic, 2 real `bst`-gated, 1 real `bst`+`buildstream-plugins`-gated full-lifecycle test - all 12 passed for real with both available). **Every `P4` row is now 🟢 Done.** Full suite: 399 passed with `bst`+`buildstream-plugins` available (388 passed + 11 skipped without them) - was 387/379+8. `make lint` clean, `make check-clean` OK. |
| 2026-08-14 | Implemented `P4-12` (element-kind-based heuristics) and `P4-15` (stack-consolidation heuristic) together, per the user's request to pursue both and their explicit choice of the most ambitious scope for each. User first redirected: "start with enriching ingest with CI flow" (`bst source track`/`bst source checkout`/`bst build`/`bst artifact checkout`), since `P4-15`'s checkout-batching advisory was pure topology speculation without real checkout-phase data. Researching that against a real BuildStream 2.7.0 install found **two real bugs in `P4-14`'s converter** (only running commands other than `bst build` surfaced them): (1) `bst source track` wraps its pipeline-level phases in a `"Track"` bracket, not `"Build"` - the original code only excluded `"Build"`, so `Track` would've been miscounted as real overhead; (2) `action="main"` events with a *real* element hash (`bst source checkout`'s `"Staging sources"`, `bst artifact checkout`'s `"Staging dependencies"`/`"Integrating sandbox"`/`"Checking out files in ..."`) were being swept into the blank-hash-only `pipeline_overhead` bucket instead of falling through to normal per-element handling. Both fixed, both regression-tested (4 new tests in `tests/unit/test_pipeline_overhead.py`). Given a checkout invocation shares no horizon with a build trace (I4's `Sum(attribution)==H` is scoped to one invocation), the user agreed checkout timing should NOT be folded into `bga`'s core `TaskKind`/attribution pipeline - built **`tools/bst_checkout_cost.py`** instead, a deliberately standalone tool reporting real, measured cost from checkout-command logs, individually or compared (N individual invocations vs. one consolidated `stack` invocation). Actually measuring it (not assuming) produced an important, honest finding: consolidating under a `stack` is **not automatically a net win** - a real 1500-element project measurement showed a full-project `stack` checkout costing *more* pipeline overhead (6s) than 5 narrow individual checkouts combined (~0s), because its own resolved closure was far larger than what was actually needed; a matched-closure comparison (5 individual vs. a 5-element `stack`) showed no measurable difference. The tool's own field naming was deliberately kept neutral (`savings_us`, not `payments_avoided`) to avoid implying a guaranteed benefit. Then implemented `P4-12`'s three candidate directions and `P4-15`'s remaining two: (1) `blast_radius`/`criticality_probability`/`leaf_analysis` signal entries now carry `element_kind`/`is_structural_kind` (a closed `STRUCTURAL_ELEMENT_KINDS = {junction, import, filter, compose, stack}` set - never a silent guess for unrecognized kinds), purely additive, with the text report's Key Findings block showing a `[structural: KIND, may not reflect real compute work]` tag on ranked listings without ever touching the underlying ranking/counts; (2) a new opt-in `bga graph --by-kind` view (count/total/avg observed duration per `element_kind`, `"unknown"` bucket for elements with none); (3) `bga/structural/consolidation.py::find_consolidation_candidates` - a purely structural (no timing) advisory grouping elements that share the exact same immediate-consumer set with no existing `stack` covering them, shown in `bga graph`'s Structural Analysis block, pointing at `tools/bst_checkout_cost.py` for real measurement of any flagged candidate. Extended `tests/fixtures/bst_show_project/` with `all.bst` (`kind: stack`) and `manual.bst` (`kind: manual`, a real "does real compute work" contrast kind) for real kind-diversity (now 4 real, `bst show`-confirmed kinds: import/junction/stack/manual), satisfying both tasks' Acceptance Test requirement. Added `tests/unit/test_bst_checkout_cost.py` (5 tests), `tests/unit/test_stack_consolidation.py` (8 tests), `tests/unit/test_element_kind_heuristics.py` (11 tests). Regenerated `tests/fixtures/golden/mixed_task_kinds/expected_output.json` twice (once per additive change), confirmed via diff that each was the *only* change. Full suite: 387 passed with `bst` on `PATH` (379 passed + 8 skipped without it) - was 359/354+5. `make lint` clean, `make check-clean` OK, `tests/test_e2e.py` 7/7. |
| 2026-08-14 | Implemented `P4-14` (cache-query/pipeline overhead visibility, previously filed research/design). Per the task file's own gate ("needs a real large-project measurement before committing to a direction"), first built a real, throwaway 2001-element BuildStream 2.7.0 project (2000 `kind: import` + one `kind: stack`) and measured a real cold build then a real fully-cached rebuild: on the rebuild, "Resolving elements" (5s) + "Query cache" (2s) = 7 of 8 total wall-clock seconds (87%) - settling Candidate Direction 3 ("do nothing, likely negligible") as not applicable, and revealing "Resolving elements" (not "Query cache" alone) was the larger cost, so the fix covers the whole `main:core activity` phase family, not just `Query cache` as originally scoped. Implemented Candidate Direction 2 (aggregate per-phase numbers, not a per-element breakdown): `tools/bst_log_to_chrome_trace.py`'s `WrapperTraceConverter` now tracks `action="main"` events via a small, separate stack into a new `pipeline_overhead` list (deliberately routed around the existing, tested per-element span-collapsing logic - zero risk to it), excluding the outer "Build" wrapper. `tools/bst_extract_run.py` writes this into `run-context.json`'s new `pipeline_overhead` field (additive run-context/v9 extension, no schema collision). `RunContext`/`AnalysisResult` (`bga/ingest/models.py`) gained a `pipeline_overhead` field each; `bga/analyzer.py::_compute_pipeline_overhead` computes a total and a horizon-relative fraction; `bga/report/text.py`/`bga/report/json.py` surface a new, explicitly-labeled-as-not-per-element "Pipeline Overhead" block/key, full-report only, empty (no section at all) when absent - every existing fixture/test byte-identical. Verified end-to-end against the real 2001-element fixture's fully-cached rebuild log: `bga`'s own `Total Duration` reports `0.0s` (0 element-level spans, since nothing was rebuilt) while the new Pipeline Overhead block correctly shows the real `7.00s` that would otherwise be entirely invisible - the exact scenario originally raised. The remote-cache per-element `Cache-query` scheduler-queue case (Candidate Direction 1) remains **not implemented** - no remote-cache infrastructure was available to confirm its real log line format, and this codebase's discipline is to verify before implementing, not guess; left as an explicit, documented gap in the task file rather than silently dropped. Added `tests/unit/test_pipeline_overhead.py` (11 tests) and one real, `bst`-gated end-to-end test in `tests/unit/test_bst_extract_run.py`. Full suite: 359 passed with `bst` on `PATH` (354 passed + 5 skipped without it) - was 347/343+4. `make lint` clean, `make check-clean` OK, `tests/test_e2e.py` 7/7. |
| 2026-08-14 | User brought three related brainstormed scenarios about `P4-12`'s territory (BuildStream cache/CAS/sandbox overhead) and asked to convert them into backlog tasks if they warranted real work. Researched each against a real, freshly-installed BuildStream 2.7.0 (both source-reading and a live `bst build`) rather than reasoning from memory, per this tracker's own discipline. **Scenario 1** ("cached builds still spend hidden time on cache-key/CAS checks, and BuildStream's log makes it look like zero seconds") - confirmed real: a real `bst build` at default verbosity (no `--verbose` needed) logs `START`/`SUCCESS Query cache` as one aggregate, non-element-scoped `main:core activity` line (`_stream.py::Stream.query_cache()`), which `tools/chrome_trace_to_bga_trace.py` already, deliberately, drops as an unrecognized `action="main"` pseudo-task - so this real cost has genuinely never been visible to `bga`, not just measured as negligible. A second, more granular per-element `Cache-query` scheduler queue exists for remote-cache-configured builds (confirmed real via `_scheduler/queues/cachequeryqueue.py` + `_scheduler/jobs/job.py`) but its real log line format wasn't directly observed (no remote cache available to configure in this environment) - flagged as needing verification before implementation. Filed as `P4-14`. **Scenario 2** ("`bst artifact checkout` + `kind: stack` can batch sandbox/CAS access into one operation") - confirmed real and precisely grounded: `Stream.checkout()` (`_stream.py`) calls `element._prepare_sandbox()`/`_export_artifact()` exactly once for the checkout target's full dependency scope, so checking out one `stack` element depending on N others is genuinely one sandbox-setup/export instead of N separate ones for N individual checkouts - `StackElement` itself confirmed to produce no artifact content of its own (`BST_ELEMENT_HAS_ARTIFACT = False`, constant `get_unique_key()`). **Scenario 3** ("more granular projects run slower; maybe subdividing into subprojects helps, like `stack` does") - the general "more elements = more overhead" premise is plausible but unmeasured (ties to Scenario 1); the specific "subprojects/junctions reduce it" claim was checked against BuildStream's own scheduler queues and found **not supported** - every planned element is processed identically by `CacheQueryQueue`/`BuildQueue`/etc. regardless of which `project.conf`/junction declares it, so this correction is stated plainly in the new task file rather than encoded as an endorsed heuristic. Scenarios 2 and 3 filed together as `P4-15` (the `stack`-batching mechanism is real and actionable; the subproject-reduces-overhead idea is explicitly flagged as not supported by the evidence gathered). Both new tasks are research/design (like `P4-12`, which they extend and cross-reference - `P4-15`'s Candidate Direction 2 overlaps `P4-12`'s own Direction 2 and should be resolved together), not implemented in this round. Updated `docs/ingestion-pipeline.md` with a new empirically-confirmed-facts entry (the real "Query cache" log excerpt) and a new section cross-referencing all three scenarios and their tasks. No code changed; no tests added or run beyond the existing suite (untouched, still 347/343+4). |
| 2026-08-14 | User asked to proceed with `P4-04`, `P4-03`, `P4-01`, `P4-07`, and to file a new task for a `--strict` extraction-consistency mode using BuildStream's `project.refs`. Closed all four, plus the new task filed. **`P4-04`**: added `ruff` (pyflakes rule set only, per its own scoping note) as `pyproject.toml`'s `[tool.ruff]`, wired `make lint` to run it for real. Found and fixed 29 real findings: 26 genuinely unused imports (auto-fixed), 2 deliberate public re-exports in `bga/utilisation/__init__.py` (fixed with the explicit-alias idiom, not deleted), 3 unused local variables (one genuinely dead forecasting variable removed, one vestigial test variable removed, one test call kept but its unused return-value assignment dropped). **`P4-03`**: `tools/dev_run.sh` + `make dev-run` - reuses the checked-in `tests/fixtures/golden/mixed_task_kinds/` fixture by default (small, static, no regeneration needed), `--large`/`ARGS=--large` regenerates the bigger `synthetic_multi_subproject` fixture fresh instead; runs `bga analyze --diagnostics` against it, `set -e` propagates any real failure as the script's own exit code. **`P4-01`**: rewrote `README.md`'s Quick Start around the Pareto principle (two commands against the checked-in fixture, zero BuildStream needed) plus - now that `P4-10`'s real ingestion pipeline exists - an honest second path from a real BuildStream project + log to a `bga`-ready run directory via `tools/bst_extract_run.py`; regenerated the "Example Output" block from a real run (previously hand-written prose that didn't match real output at all). Fixed both confirmed-broken `jq` examples in `docs/cli.md` (`.floors.certified_headroom_us` → `.floors.certified_headroom`; `.signals.criticality_probability | sort_by(...)` on what's actually a JSON object, not an array, → `| to_entries | sort_by(.value.probability) | ...`), verified against a real `--format json` run. Fixed `pyproject.toml`'s placeholder `your-org/bga` URLs to the real repo. Also fixed `docs/cli.md`'s "Basic Usage"/"Example Workflows" sections, which shared the same misleading implication that a raw BuildStream cache path is directly `bga`-readable. **`P4-07`**: a blockquote historical-snapshot note added immediately after `docs/bga-spec-compliance-review.md`'s title, linking to this tracker - no other content touched. **Filed `P4-13`** (`--strict` extraction-consistency mode via `project.refs`, user-proposed): researched and empirically confirmed against a real BuildStream 2.7.0 install (a from-scratch project with `ref-storage: project.refs` + a real local-git-repo `kind: git` source) that `project.refs` genuinely centralizes every trackable element's resolved source ref into one file - a real, content-addressable fingerprint stronger than the existing whole-tree git-dirty check `P4-10` already has. Also confirmed a real limitation: `ref-storage: project.refs` is opt-in (not the default), and even then only trackable-ref sources populate it - `tests/fixtures/bst_show_project/` itself uses the default `inline` storage, so `--strict` would need to fail loudly for it, not silently degrade. Added `tests/unit/test_dev_run_script.py` (5 tests), `tests/unit/test_docs_examples.py` (6 tests, including real `jq` subprocess checks against a live report). Full suite: 347 passed with `bst` on `PATH` (343 passed + 4 skipped without it) - was 336/332+4. |
| 2026-08-14 | Closed `P4-02` (report output led with a flat metric dump, no synthesized "what to look at first" summary, and `confidence`/`violations` were never printed in text output at all despite being fully populated). Added two new blocks to `bga/report/text.py`'s full `analyze` report, both presentation-only (`--format json`/`csv` untouched, byte-identical): a **Key Findings** block shown first, before Certified Floors - confidence headline (with violation count when any exist), the single largest non-`EXECUTION_ON_CHAIN` attribution category phrased as an opportunity, the top 1-3 elements by blast radius and by criticality probability (when `--diagnostics` ran; zero-probability elements excluded rather than padding to 3), and certified headroom in plain language - and a **Confidence/Violations** block (overall score + band label, failed hard gates, one-line-per-violation summary covering every real violation `type` this codebase produces). Both gated to the full report (`section is None`), matching `format_json`'s own existing gating - section subcommands unaffected. Verified against a deliberately-constructed fixture that the Key Findings block names the *correct* dominant wait category (`DEPENDENCY_WAIT`) and worst-blast-radius element (`root.bst`, 3 downstream) by name, not just that the block exists - and against the flagship `synthetic_multi_subproject` fixture for real-world readability. Added `tests/unit/test_report_key_findings.py` (12 tests). Full suite: 336 passed with `bst` on `PATH` (332 passed + 4 skipped without it) - was 324/320+4. |
| 2026-08-14 | Closed `P4-11` (`dependency_type` didn't affect analysis anywhere). Filtered every ready-time/ordering/critical-path *gating* consumer to `build`-type edges only, while every purely-structural consumer (reachability, blast radius, leaf/deferrability, depth, dominators - Part 24/25) still reads the full, unfiltered graph: `bga/normalize/timestamps.py`'s `compute_ready_times`/`validate_ordering`/`clamp_task_starts`, `bga/analyzer.py`'s `explicit_predecessors` (blame-chain predecessor selection). **Found a more severe issue than the task's original ready-time-only scope while verifying the acceptance-test fixture**: `bga/graph/edg.py::compute_critical_path` was summing a `runtime`-only edge's duration into T∞,observed as if it gated ordering - directly contradicting Part 14.1's own certification claim ("no schedule ... can complete faster than this value" - false if the value counts a non-gating edge as gating). Fixed via a new `exclude_dependency_types` parameter on `build_element_graph`/`compute_in_out_degree` (default unfiltered, zero risk to every other caller), applied to `compute_critical_path`, `compute_slack` (must stay consistent with `compute_critical_path`'s output), and the Monte-Carlo criticality-probability sampling (Part 26) that reuses the same critical-path concept under perturbation. Confirmed a genuine no-op against every existing pinned fixture (all hardcode `dependency_type: "build"`), and verified against **real typed input**: `tests/fixtures/bst_show_project/`'s own genuine `runtime`-only edge (`subproj-junction.bst:libfoo.bst -> app.bst`), run through the full real `bst_extract_run.py` → `bga analyze` pipeline with no errors or false-positive violations. Added `tests/unit/test_dependency_type_gating.py` (10 tests). Full suite: 324 passed with `bst` on `PATH` (320 passed + 4 skipped without it) - was 314/310+4. |
| 2026-08-14 | User pointed out `bst show --format` can also expose an element's own BuildStream plugin kind (`%{kind}`) and asked to propagate it into `graph.json` for future kind-based analysis heuristics, then proceed to `P4-11`. Added `%{kind}` to `tools/bst_show_to_graph.py`'s captured fields (confirmed real against BuildStream 2.7.0: `import`/`junction` values seen on the existing fixture project) as `element_kind` on `Element` (`bga/ingest/models.py`), loaded by `bga/ingest/loader.py::load_graph` - an additive extension beyond graph/v9's spec-minimal schema, same precedent as the existing `dependency_type` field. Currently inert metadata - no analysis consumer reads it yet. Filed **`P4-12`** (element-kind-based heuristics) as a research/design task per the user's own suggestion, rather than guessing at a specific heuristic - scoped several candidate directions (presentational-only annotation vs. structural leaf-analysis changes vs. a new kind-grouped summary view) and their tradeoffs, deliberately left unresolved pending real kind-diverse data and a scoping decision. Updated `tests/unit/test_bst_show_to_graph.py` (3 new tests: kind capture, empty-kind-to-null, malformed-record-count regression) and the existing real end-to-end tests to assert real kind values. Full suite: 314 passed with `bst` on `PATH` (310 passed + 4 skipped without it) - was 311/307+4. |
| 2026-08-14 | User asked to proceed with `P4-10` and its dependencies (`P4-05`, `P4-09`) "to close full real usage scenario." Closed all three, plus built one un-scoped piece needed to make them real (a general Chrome-Trace-to-trace/v9 adapter). **`P4-05`**: added `--format {auto,wrapped,raw}` + `--start-time` to `tools/bst_log_to_chrome_trace.py`. Found and fixed **two real bugs** while implementing, both verified against a real, installed BuildStream 2.7.0 (not assumed from the log shape alone) and both invisible against the synthetic fixture (which never exercises either scenario): (1) the status-word alternation had `FAIL`, but real BuildStream emits `FAILURE` - a real build failure never matched at all, in *either* mode, silently leaving tasks open forever; (2) a real task emits an *outer* START/terminal bracket plus *nested* START/terminal pairs for internal sub-phases sharing the same hash+action key - the old "new START force-closes what's open" handling would have produced 2-3 spurious spans per real task instead of one; fixed via a per-(hash,action) depth counter. Also discovered START/SUCCESS message text is a log file path, not a phase description (confirmed directly in BuildStream's own source) - invalidating message-text-based task-kind recovery for real logs, so `action`/`element` are now recorded directly in each event's `args` instead (additive, verified byte-identical on the pinned synthetic fixture). **New tool `tools/chrome_trace_to_bga_trace.py`**: a genuinely separate, general-purpose Chrome-Trace-to-trace/v9 adapter (not a refactor of `tests/fixtures/synthetic_multi_subproject/adapter.py`, which stays fixture-specific and untouched) - reads the new `action` field rather than parsing message text. **`P4-09`**: `tools/bst_run_context.py` - reuses BuildStream's own already-resolved `Maximum {Fetch,Build,Push} Tasks:` header lines (confirmed real, printed unconditionally) rather than re-deriving CLI-flag-vs-config precedence; falls back to BuildStream's real bundled defaults (`fetchers=10, builders=4, pushers=4`, confirmed in its own `userconfig.yaml`) only when absent. Confirmed `--builders`/`--fetchers`/`--pushers` (not the unrelated `--max-jobs`) are the real concurrency knobs `resource_capacities`/`max_jobs` should map to. **`P4-10`**: `tools/bst_extract_run.py` coordinates all of the above into one command; derives the target list from BuildStream's own `Targets:` header line (confirmed real, more robust than wrapper-shell-command parsing) rather than any hardcoded convention, fails loudly if absent; adds a best-effort git-dirty time-of-extraction-consistency warning. Verified **end-to-end against a real BuildStream 2.7.0 build with zero manual editing**: real project + real log → `bst_extract_run.py` → `bga analyze` produces a full report. Also improved `bga`'s own CLI error message for the "some but not all of the three input files present" case to hint at the specific missing file(s) and the tool that produces each. Filed **`P4-11`** (`dependency_type` doesn't affect analysis anywhere - discovered while researching `P4-08`, now has real typed input to test against) as a real follow-on, not implemented blind. Added `tests/unit/test_bst_log_converter.py` (23 tests), `tests/unit/test_chrome_trace_to_bga_trace.py` (8 tests), `tests/unit/test_bst_run_context.py` (7 tests), `tests/unit/test_bst_extract_run.py` (8 tests, including 2 real end-to-end tests against a live `bst` build), plus 2 new CLI hint tests. **Every `P4` item in this pipeline (`P4-05`, `P4-08`, `P4-09`, `P4-10`) is now done.** Full suite: 311 passed with `bst` on `PATH` (307 passed + 4 skipped without it) - was 263/261+2. |
| 2026-08-14 | Closed `P1-30` (`RETRY_WAIT`, the last open item from the earlier re-audit round): added `BlameChainAnalyzer._retry_predecessor` (`bga/attribution/blame_chain.py`) - for a task with `attempt > 0`, finds the immediately-preceding attempt of the same `element_uid|task_kind|phase`, a real predecessor relationship a retry attempt genuinely has that `graph.json`'s element-level dependency edges have no way to express. Wired it into `_classify_wait_gap` (P1-20's shared classification path used by both `build_blame_chain` and `compute_task_attribution`) as the fallback ahead of `DEPENDENCY_WAIT` whenever a task is identified as a retry. **Found and fixed a real bug while implementing, not just wiring a classifier in**: the first attempt (`ready_time = max(task.ready_us, retry_pred.finish_us)`) was a no-op in the common case, since `task.ready_us` (Part 7's "no predecessor" fallback) already collapses to the task's own `start_us` whenever there's no cross-element predecessor - already the ceiling, so `max()` against it can never be lifted by anything. Confirmed empirically: an end-to-end test against a real 2-attempt fixture initially produced `retry_wait_us: 0` with the gap silently landing in `idle_us` instead - exactly the bug this task exists to fix, still present despite `_retry_predecessor` correctly identifying the prior attempt. Fixed by only trusting the existing `ready_time` as a `max()` floor when it already reflects a genuine wait; otherwise using the retry predecessor's finish directly. Documented one known, honest limitation in `_classify_wait_gap`'s docstring: `classify_resource_wait` can't currently carve a sub-portion out of a pure-retry gap (its own early-return guard assumes a non-degenerate `[ready_us, start_us)` window), so a retry gap with genuine concurrent resource contention still gets fully attributed to `RETRY_WAIT` rather than split - still strictly more correct than the prior unconditional `IDLE`. Added `tests/unit/test_retry_wait_classification.py` (10 tests: 5 direct unit tests on the classifier, 3 end-to-end via `analyze_run` including the task file's own acceptance-test scenario, plus a regression test confirming non-retry runs are entirely unaffected). **Every P1 item (P1-01 through P1-30) is now done** - no open P1/P2/P3 items remain. Full suite: 263 passed with `bst` on `PATH` (261 passed + 2 skipped without it) - was 253/251+2. |
| 2026-08-14 | User asked to proceed with `P4-08` (previously ⚪ Blocked, needed a product decision on how to extract `graph.json` from a real BuildStream project) and proposed the resolving design directly: a separate `tools/` script that calls real `bst show` rather than parsing `.bst` YAML, since real projects reference other projects via junctions that `bst show` already resolves correctly. Built `tools/bst_show_to_graph.py`: runs `bst show --deps all --format ...` (using ASCII record/field-separator control characters in the format string, not newline-based parsing, since `%{build-deps}`/`%{runtime-deps}` can themselves contain embedded newlines for multi-dependency elements - confirmed by testing, a real bug a naive parser would have shipped) and emits `graph/v9` JSON. Everything was verified against a **real, installed BuildStream 2.7.0** (+ `buildstream-plugins` + bubblewrap), not assumed from documentation - an older/mirrored manpage had briefly suggested `--format` lacked per-element dependency-list symbols, which real BuildStream 2.x does have. Confirmed empirically: junction-qualified element names round-trip identically between `bst show`'s `%{name}` and a real `bst build`'s own log lines (the single highest-risk unknown for the whole design, since a mismatch would silently break the join between this new tool's `graph.json` and the existing `tools/bst_log_to_chrome_trace.py`'s `trace.json` - they agree, no changes needed to the existing converter). Added `tests/fixtures/bst_show_project/` (a small, from-scratch, `kind: local`-only real BuildStream project checked into the repo, since the existing `synthetic_multi_subproject` fixture turned out to not be valid `bst` syntax - its own `project.conf` disclaims being meant for real parsing) and `tests/unit/test_bst_show_to_graph.py` (pure-parser tests always run; two real end-to-end tests `skipif`-guarded on `bst` being on `PATH`). Added a new `bst` optional extra to `pyproject.toml` (separate from `dev`, since it needs a system `bwrap` binary too). Wrote `docs/ingestion-pipeline.md` as a permanent design record (target architecture, why `bst show` over YAML parsing, 8 empirically-confirmed facts, and the still-open follow-on question of `dependency_type` not yet affecting analysis anywhere downstream) per the user's explicit request to "fixate our design choices for future sessions." Split `P4-08`'s original out-of-scope items into two new tracked tasks: **`P4-09`** (`run-context.json` producer - resource capacities/`max_jobs`/wall-clock, none of which `bst show` can provide) and **`P4-10`** (wiring trace+graph+run-context into one convenience flow, deriving the target list from the real invocation rather than a hardcoded umbrella-target convention). Full suite: 253 passed with `bst` on `PATH` (251 passed + 2 skipped without it) - was 241. |
| 2026-08-14 | User asked to (1) re-verify the prior round's "everything done" claim and (2) file a backlog of usability/tooling improvements. Both done. **Re-verification**: an independent audit re-read the full spec against current code (not trusting the tracker) and found 5 real issues, independently re-confirmed before acting on any of them. Fixed 3 in this round: **`P1-27`** (the most severe) - `compute_ready_times`/`validate_ordering` (`bga/normalize/timestamps.py`) gated every successor task kind on the predecessor's any-task-kind-max finish, the same bug class `P1-03`/`P1-26` already fixed in two *other* code paths but left live here - produced 8 negative-duration tasks (I5 violation), 7 false ordering violations, and crashed confidence to 0.147 on the flagship multi-subproject fixture; fixed by scoping cross-element gating to BUILD-to-BUILD only, sourced from a shared `_element_build_finish` helper. Also surfaced and fixed a masked, unrelated gap in that same fixture's `run_context` (missing `cpu_accounting.effective_cpus`, previously hidden by the negative-duration bug coincidentally deflating accounted CPU-us into false alignment). **`P1-28`** - Monte-Carlo criticality rebuilt graph topology on all 200 samples instead of once (Part 41.2 violation, not asymptotically wrong, just wasteful) - hoisted the topology build out of the per-sample function. **`P1-29`** - CLI `--help` and `docs/cli.md` claimed replay computes an "optimal" makespan, directly contradicting Part 1.2's own non-goal - missed by `P1-17`'s exact-phrase-only audit; reworded to describe `T_C` as a feasible makespan under a heuristic, not an optimality claim. Filed the 4th finding as **`P1-30`** (`RETRY_WAIT`, one of the 8 canonical Part 11 categories, has zero implementation anywhere and was never targeted by any tracker task) rather than implementing blind - it needs real design work (what "retry sequencing delay" concretely means against real trace data), comparable in scope to `P1-01`/`P1-02`/`P1-20`'s original work. The 5th finding (no test asserted I5 non-negativity or checked ordering/confidence values on the flagship fixture, which is *why* `P1-27` went undetected) is closed by this round's own new regression tests. **Backlog**: filed 8 new `P4` (usability/tooling/maintenance) tasks per explicit user requests - README quick-start + doc-accuracy fixes (two confirmed-broken `jq` examples in `docs/cli.md`), actionable report output, a local dev convenience script, basic Python linting, and a `tools/bst_log_to_chrome_trace.py` refactor to support raw (unwrapped) BuildStream logs (confirmed: currently emits a silent empty trace for one) - plus 3 brainstormed while scoping those (no CI configured at all, the original compliance review needs a "this is historical" note, and a ⚪ Blocked item on the missing real ingestion pipeline from a live BuildStream project to a bga-ready run directory, which needs a product decision before it can be scoped as a normal task). Full suite: 241 passed (was 231). |
| 2026-08-13 | Closed every remaining P3 item (`P3-04` through `P3-09`) in one round, per the user's "fix all remaining P3 issues" request. `P3-04` (tie-break + resource-holder tests, `tests/unit/test_tie_break.py` + `tests/unit/test_resource_wait.py`, 14 tests) and `P3-09` (per-module unit test split - `test_normalize.py`/`test_edg.py`/`test_replay.py`/`test_utilisation.py`, 36 tests, with `test_edg.py`'s hand-computed diamond-graph values catching nothing wrong this time, unlike the M6 history this class of test is designed to catch) needed no bug fixes. `P3-07` needed no new files at all - `P1-09`/`P1-12`'s own rounds had already built complete Monte-Carlo and determinism-harness test coverage satisfying every bullet. The other three each surfaced a real, previously-undiscovered bug while writing the test the task itself required, fixed in the same round (own task file + commit each, not patched inline): **`P1-24`** (`P3-05`) - phase annotations (Part 10.2) were silently dropped on every segment category except `EXECUTION_ON_CHAIN`, contradicting the spec's own `IDLE`/`SCHEDULER_WAIT` phase-tag examples; **`P1-25`** (`P3-06`) - CPU reconciliation's `unaccounted_cpu_s` silently stayed 0 for residuals under the 2% tolerance instead of being reported informationally; **`P1-26`** (`P3-08`) - replay's dependency graph gated task readiness on the wrong task kind (the same bug class `P1-03` fixed elsewhere, in a second, independent predecessor-construction path), under-scheduling `T_C` below `LB` and violating I2, discovered while building `P3-08`'s golden fixture. `P3-08` also added `tests/fixtures/golden/mixed_task_kinds/` and `tests/test_golden.py`, verified to actually catch regressions (temporarily corrupted the snapshot, confirmed a clear failing diff, reverted). **Every P1 item (P1-01 through P1-26), every P2 item, and every P3 item is now done** - there is no remaining tracked work. Full suite: 231 passed (was 162). |
| 2026-08-13 | Built `P3-03` (attribution identity tests across topologies): added `tests/unit/test_attribution_identity_across_topologies.py`, parametrized over all 10 `P3-01` fixture variants, checking both the task-horizon and full-wall-clock I4 identities exactly. Building the full-wall-clock check surfaced a real, previously-undiscovered bug - filed and fixed as `P1-23`: `UNTRACKED_HEAD`/`UNTRACKED_TAIL` (Part 11) were hardcoded to 0 regardless of any actual gap between wall-clock bounds and task activity (confirmed on the synthetic multi-subproject fixture's real ~1s post-build gap), silently violating Part 12.1's full-wall-clock identity. Fixing that exposed a second, previously-dead bug: `attribution_score`'s denominator (`bga/validation/invariants.py`) divided a penalty term that includes untracked time (which lives outside the task horizon) by the task horizon alone rather than the full wall-clock horizon - unreachable while untracked was always 0, but able to crater confidence to 0.0 the moment it wasn't. Two existing tests that had silently encoded the old always-0 assumption were corrected (`tests/test_synthetic_multi_subproject.py::test_attribution_identity_exact` split into task-horizon-only + a new full-wall-clock variant; `test_confidence_gates.py`'s "perfect coverage" fixture given a genuinely gapless wall clock, since that's what its own docstring already claimed). **Every P1 item (P1-01 through P1-23) is now done.** Added `tests/unit/test_untracked_head_tail.py` (5 tests, `P1-23`) and `tests/unit/test_attribution_identity_across_topologies.py` (30 tests, `P3-03`). Full suite: 162 passed (was 126). |
| 2026-08-13 | Built `P3-01` (shared synthetic topology fixture library): added `tests/fixtures/topologies.py` with one factory per Part 36.1 topology (`linear_chain`, `diamond`, `fan_in`, `fan_out`, `multiple_equal_predecessors`, `deep_unequal_predecessors`, `independent_branches`, `graph_with_terminal_and_nonterminal_tasks`), each returning a `(run_context, graph, trace)` tuple in the canonical graph/v9+trace/v9 shape already standardized on across the P1/P2 fixture-writing tests (not the older, narrower ad-hoc shape in `tests/test_e2e.py`), plus `write_run_dir`/`build_analyzer` helpers to remove the write-to-disk boilerplate duplicated across a dozen existing test files. `multiple_equal_predecessors` produces a genuine finish-time tie between differently-deep predecessors (feeds `P3-04`); `graph_with_terminal_and_nonterminal_tasks` produces both a requested-target-reachable dependency and a fully unreachable orphan branch (feeds `P1-11`/leaf-deferrability tests). Added `tests/unit/test_topology_fixtures.py` (14 tests) as the task's own acceptance test: a smoke test parametrized over all 9 factories plus fixture-shape assertions. This unblocks `P3-03` through `P3-07`. Full suite: 126 passed (was 112). |
| 2026-08-13 | Fixed `P2-04` (retry/rebuild detection): added `bga/utilisation/detection.py::compute_retry_tasks` (every non-final `attempt` recorded for the same `element_uid\|task_kind\|phase` is a retry, Part 5.2) and `compute_rebuild_tasks` (a BUILD task is a rebuild if its element's current `cache_key` was already built successfully in `historical_runs` - reusing the exact signal `bga.floors.cold`/`P1-06` already keys its historical lookups by, so no new ingest schema field was needed). Wired both into `bga/analyzer.py::_compute_utilization`, replacing the hardcoded empty `retry_tasks`/`rebuild_tasks` sets - confirmed the consumer side (`bga/utilisation/__init__.py`'s bucket routing) was already correct, so no scope expansion was needed. **Every P1 item (P1-01 through P1-22) and every P2 item (P2-01 through P2-05) is now done** - only P3 (test-coverage build-out) remains. Added `tests/unit/test_retry_rebuild_detection.py` (12 tests: 9 direct unit tests on the two pure functions, 3 end-to-end tests through `BuildEfficiencyAnalyzer` confirming real `wasted_retry`/`wasted_rebuild` bucket values). Full suite: 112 passed (was 100). |
| 2026-08-13 | Closed the last two open P1 items. **`P1-22`** (real bug, found while fixing `P1-21`): `BlameChainNode` has no `__str__` override, so `_compute_diagnostics` was putting Python object-repr strings (not task keys) into `blame_chain`; separately, `compute_blast_radius`/`compute_leaf_analysis` were routing critical-path membership checks through a task-key `task_map` translation even though `self.critical_path` already holds element UIDs directly - the combination made `is_on_critical_path`/`is_on_blame_chain` always `False`. Fixed both root causes (`str(t.task_key)` at the source; direct `elem_uid in self.critical_path` at the consumption site, keeping the `task_map` translation only for the now-correct `blame_chain` membership check). Rewrote the one test that had documented the bug (`test_leaf_analysis_and_blast_radius_match_pre_refactor_behavior` → `..._report_real_critical_path_membership`) to assert the real, verified values instead. **`P1-15`** (pure architecture refactor, deliberately deferred until every other P1 item was correct): extracted `bga/floors/` (`observed.py`/`capacity.py`/`serialization.py`/`cold.py`), `bga/report/` (`text.py`/`json.py`/`_shared.py`), and `bga/validation/invariants.py` (`compute_confidence`, redesigned to return `(confidence_dict, new_violations)` instead of mutating `self.violations` in place, so the extracted function is genuinely pure) out of `bga/analyzer.py`/`bga/cli.py`, one package per commit, each step verified via full-suite + byte-identical CLI-output diffing (`git stash` before/after) exactly as the task's acceptance test required. No computed value or output format changed. **Every P1 item (P1-01 through P1-22) is now done**, and the codebase matches spec Part 39's recommended package layout. Full suite: 100 passed (unchanged - pure refactor + one test rewrite). |
| 2026-08-13 | Resolved `P1-14` (user chose the hybrid CLI design): `analyze` stays the primary command; added `graph`/`floors`/`replay`/`sweep`/`utilisation`/`diagnostics` as thin aliases sharing the same pipeline, restricting output to one report section each (`sweep` is the exception - a genuine capacity sweep via `ReplayScheduler.capacity_sweep`, Part 19, previously fully implemented but completely unreachable from the CLI). Building `graph` surfaced two pre-existing dead-code bugs in `format_text` (nonexistent `result.critical_path`/`result.structural_metrics` attributes meant the Critical Path and Structural Analysis blocks had never fired for *any* input, in *any* subcommand, ever) - fixed alongside this task since the new subcommand's entire purpose depended on that content existing. Building `sweep` surfaced a `NaN` bug in `capacity_sweep`'s first-sample `normalized_improvement` (`prev_makespan` starts at `+inf`; the guard checked `> 0`, true for infinity too) - also fixed. Then closed most of `P1-21`'s scope: fixed `_estimate_ready_count`'s O(N²) scan (bisect-based, O(log N) per query, proved the `active_tasks`/`finish_us` checks the old version did are redundant), found and fixed two more O(N²) `any(...)` membership-scan hotspots in `compute_leaf_analysis`/`compute_blast_radius` (not in `P1-21`'s original three), and deduped `analyze_graph` from 3 redundant per-`analyze()` calls to 1. Combined: **14.16s → 2.79s (~5.1x faster)** on the 1500-element profiling fixture. `compute_dominators`'s algorithmic rewrite deliberately deferred (its only consumer reads dict size, not real dominance data - not worth a Lengauer-Tarjan rewrite for that). Fixing the `any(...)` hotspots surfaced a real, separate, pre-existing correctness bug (`is_on_critical_path`/`is_on_blame_chain` always `False` due to an element-UID-vs-task-key mismatch) - filed as `P1-22` rather than silently fixed in a performance-only commit. Added `tests/unit/test_cli_subcommands.py` (10 tests) and `tests/unit/test_diagnostics_performance.py` (4 tests). Full suite: 100 passed (was 96). |
| 2026-08-13 | Closed out every remaining open P1 item except `P1-14` (blocked) and `P1-15` (deliberately deferred, now unblocked): `P1-17` (terminology audit, clean, no change needed); `P1-16` (three named graph algorithms now O(N+E), not O(N·E)/O(N²) - found 2 more out-of-scope hotspots, filed `P1-21`); `P1-09`+`P1-10` (criticality-probability key-format mismatch that collapsed every probability to 0.0, despite the resampling itself already being correctly fixed in an earlier undocumented round; blast-radius now sums real downstream durations instead of a global average); `P1-12` (new `bga/validation/determinism.py` harness, Part 35/I11); `P1-05`+`P1-06`+`P1-07`+`P1-08`+`P1-11`+`P1-13` (reconciliation-violation reporting; the advisory cold structural floor implemented end to end with CLI flags; capacity LB generalized beyond PROCESS-only, which exposed and required fixing a previously-latent I2 violation in replay's hardcoded resource lookup; `P1-11`'s real bug found one layer above where it was originally diagnosed - `requested_targets` hardcoded `None`, not the reachability BFS itself; confidence now implements all 5 named coverage metrics and the full hard/soft-gate + `min()` formula). Added 9 new test files (~30 tests). Full suite: 86 passed (was 60). |
| 2026-08-13 | Fixed `P2-03` (logging infrastructure): added `bga/exceptions.py` (`BgaError`/`IngestionError`/`NormalizationError`/`AnalysisError`/`ValidationError`, each also subclassing the builtin type already used at that site so existing `except ValueError`/`except FileNotFoundError` handling keeps working) and `bga/logging_config.py::configure_logging`. Wired `logging.getLogger(__name__)` into all 9 packages named in the task with real log calls (ingestion summaries, normalization violations/clamps, occupancy/attribution/replay summaries, a CPU-reconciliation-tolerance warning, the ordering gate's pass/fail, and the structural critical-path computation's previously-silent `except Exception: return []` now logged with `exc_info=True`). `--verbose`/`-v` now genuinely enables DEBUG logging; added `-q`/`--quiet` and `--log-file PATH`. Replaced `cli.py`'s `'cycle' in str(e).lower()` substring-matching exit-code hack with real type-based routing (`AnalysisError`→3, `IngestionError`/`JSONDecodeError`→2) - behaviorally identical, confirmed against the existing exit-code test suite. Added `tests/unit/test_logging_and_exceptions.py` (6 new tests). Full suite: 60 passed (was 54). |
| 2026-08-13 | Fixed `P1-20` (gap classification wiring): added a single shared `_classify_wait_gap` helper that splits `[ready_time, start)` into non-overlapping `RESOURCE_WAIT`/`SCHEDULER_WAIT`/`DEPENDENCY_WAIT` portions (resource first via `P1-01`'s holder tracking, then scheduler via `P1-02`'s detector, remainder to dependency-wait), used by both `build_blame_chain` (now emits per-category segments instead of one hardcoded `DEPENDENCY_WAIT` segment) and `compute_task_attribution` (fixed the dormant double-counting bug where the same gap was written into both `dependency_wait_us` and `resource_wait_us`). `resource_wait_us`/`scheduler_wait_us` are no longer structurally always `0` — confirmed on `tests/fixtures/synthetic_multi_subproject/` (`resource_wait_us` now `2000000`, was `0`) with exact `Σ == H` identity preserved. Added `tests/unit/test_wait_gap_classification.py` (2 new end-to-end tests). Full suite: 54 passed. |
| 2026-08-13 | Fixed `P1-01` (real resource-wait holder tracking): `classify_resource_wait` now derives time-weighted `blocking_tasks` directly from observed overlapping same-resource task intervals, with the literal `"UNKNOWN"`/`ambiguous=True` fallback per spec Part 8.2 when no holder can be identified. While finishing this, found a deeper, precisely-specified gap: per spec Part 7, `[ready_time, start_us)` should be *classified* into `DEPENDENCY_WAIT`/`RESOURCE_WAIT`/`SCHEDULER_WAIT`, but the blame-chain walk always labels the whole gap `DEPENDENCY_WAIT` - meaning `P1-01`/`P1-02`'s now-correct classifiers are computed (in the currently-dead-code `compute_task_attribution`/`task_attributions` path) but never reach `result.attribution`. Filed precisely as `P1-20`, including a second, currently-dormant double-counting bug found in the same investigation. |
| 2026-08-13 | Fixed `P1-04` (the last piece of the attribution-identity family): identify *every* genuine terminal element (not just the single max-finish one) and walk all of them; added `covered_intervals` tracking so two genuinely independent terminals that happen to run concurrently in wall-clock time don't produce overlapping, double-counted segments; discovered and fixed that no code anywhere ever generated an `IDLE` segment (`idle_us` was structurally always `0`) - `_build_flattened_timeline` now fills genuine gaps with `IDLE`. Attribution identity now holds exactly (`Σ == H`) on every tested fixture: single connected components, disconnected multi-terminal graphs, concurrent independent terminals, and 3+-component graphs. Full suite: 45 passed, 0 xfailed. |
| 2026-08-13 | Re-verified `P2-01`/`P2-02` before starting and found both already partially/fully fixed (cycle detection + exit code 3 fully working since commit `ad8a2db`; JSON error handling and exit code 2 working for two of three loaders) - corrected the tracker's stale diagnosis and closed the two remaining real gaps (`load_graph`'s JSON wrapping for message consistency; missing-required-file now correctly exits `1` instead of falling through to `2`). Added `tests/unit/test_cli_exit_codes.py` as permanent regression coverage for the full documented exit-code contract. |
| 2026-08-13 | Fixed `P1-19` (flattened timeline residual coverage): added intra-element phase predecessor (`TRACK→FETCH→BUILD` sequencing) and extended `explicit_predecessors` to cover every task kind of a dependent element, not just `BUILD`. This achieved *exact* attribution identity on both the simple reproduction and the full `tests/fixtures/synthetic_multi_subproject/` fixture (diamond dependency, multi-task-kind elements, real resource contention) - better than originally scoped, because the existing "greatest finish time" tie-break turned out to already resolve off-chain parallel-work coverage for any connected component, with no occupancy-sweep needed. Narrowed `P1-04` to its now-precise, still-open remaining scope (genuinely disconnected components only) and added `tests/unit/test_multi_terminal_coverage.py` as a concrete, runnable reproduction of exactly that gap. |
| 2026-08-13 | Fixed `P1-02` (real scheduler-wait detection, plus a tautological call-site `resource_available` check that would have made it unobservable) and `P1-03` (attribution identity I4 — three compounding root causes: blame-chain walk stopping on exactly-zero-wait links, `explicit_predecessors` mismapping tasks on multi-task-kind elements, and a spurious multi-terminal heuristic causing triple-counting). Scoped the honest residual of `P1-03` as new task `P1-19`. Added `tests/unit/` (first module-level unit test directory) with `test_blame_chain.py` and `test_attribution_identity.py`. |
| 2026-08-13 | Added `P3-10`: a large multi-subproject synthetic BuildStream project (9 elements, 4 junctioned subprojects, diamond dependency, real resource contention), fed through the user-supplied `tools/bst_log_to_chrome_trace.py` converter end-to-end. Found two new bugs (`P1-18` structural max_depth shortest-vs-longest-path bug; `P2-05` CLI JSON output silently missing several `AnalysisResult` fields) and amplified `P1-03`'s evidence (negative/~453,000-year overflow values on a realistic graph, not just undercounting). Confirmed `P3-02` (CLI integration tests) was already done by a prior session and marked it `🟢`. Repo housekeeping: removed `bga.egg-info/`, all committed `__pycache__/*.pyc`, and a stray `=2.8` file from git tracking; added `make check-clean` and mandatory pre-commit hygiene rules to `docs/fixing-guide.md`. |
| 2026-08-13 (earlier) | Reworked tracker into index-only format; moved details into `docs/tasks/*.md`; corrected P1-01/P1-02 status after re-verification found stub code still present; added P1-03 as a newly discovered live invariant violation; added `docs/fixing-guide.md` as mandatory session entry point. |
| (prior) | Original tracker created from `docs/bga-spec-compliance-review.md`; P0 and several P1 items marked fixed (P0 confirmed correct on re-verification; some P1 marks corrected above). |
