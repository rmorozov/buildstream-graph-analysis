// Deliberately trivial: this element exists to pay the sandbox toll, not
// to compile. UX-120 needs work that is small next to the staging of the
// toolchain it shares with its siblings.
int tiny_4() { return 4; }
